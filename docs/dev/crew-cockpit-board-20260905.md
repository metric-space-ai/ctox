# CREW-COCKPIT · Board (CTOX-App + Crew-Leiste + Tickets + Crew-Identität)

Stand: 2026-09-08 07:30 · Orchestrator: Fable · Implementierer: Codex-Thread `01a07107-d27c-7e80-a1dc-2311f60ad0bb` (Arbeit landet in PRs gegen `metric-space-ai/ctox` main)

**Headline (07.09. 07:20): Alles ist auf main.** `git push origin HEAD:main` → `c69119697..be4165cc4` (Fast-Forward; erster Merge `22ea45da9`, zweiter Merge des zwischenzeitlichen main-Commits `c69119697` = `be4165cc4`, konfliktfrei). Branch `crew-cockpit/optimize-ctox-app` steht auf demselben Commit. Rust auf dem finalen Baum: `cargo check` grün; Crew-/Cockpit-/Projektionstests 63/63 im ersten Gruppenlauf, im zweiten nur die bekannte Chat-Flake B7 (isoliert 3/3 grün); `canonical_queue_ack_refreshes_queue_and_command_without_repair` einmal im Gruppenlauf rot, isoliert und im Wiederholungslauf grün (SQLite-Gruppenflake, B7-Familie). Nächster Schritt: T12 (Binary aus main bauen, Screenshots mit echten Daten, Drag-Beweis), dann T11.

**Headline (07.09. 06:30, historisch): Merge mit main aufgelöst, Gates grün, Landung auf main läuft.** Der Branch `crew-cockpit/optimize-ctox-app` (9 Commits, gepusht) war 85 Commits hinter origin/main; der Merge hatte 8 Konfliktdateien (21 Hunks). Aufgelöst in `/Volumes/tmp/worktrees/ctox/merge-test` (Merge-Commit `22ea45da9`): eine Cache-Revision `20260907-shell-v2-crew-home-v340` für Shell-Einstieg, `APP_BUILD`, Loader-Buster, Bundle-URL und Multi-Tab-Epoche (mains Datenebenen-Wächter pinnt die ganze Importkette); in der CTOX-App bleibt mein Subscription-Hydrate ohne Poll-Schleife, dazu kommt mains Datenzustand (laden/offline/fehler/leer) in Fußzeile und leerem Arbeitsplatz; Lesefehler der Task-Quellen propagieren wieder (ein gescheiterter Read sah bei mir wie „keine Arbeit“ aus — mains Wächter `assert-ctox-data-state.mjs` hat das gefunden); Retry-Knopf in der Zustandszeile statt eigener Fehlerfläche; Crew-Karten-Wächter und Flow-Map-Test von main (94ee2cbce). Gates auf dem Merge-Baum: ctox 3/3, Chat 71/71, Tickets 14/14, RxDB-only, Shell-V2 37/37, Geometrie 6/6, Crew-Karte 6 Szenarien, Datenzustand 12 Szenarien (Screenshots `/Volumes/tmp/dev-artifacts/ctox/crew-cockpit/data-state/`), RxDB-Suite 116/117 (`bundle-reproducible-smoke` rot wie auf main: der Smoke baut aus anderem cwd, Kommentarpfade weichen ab), `cargo check` EXIT 0 (Log `build/check-merge.log`). Rust-Crew-Tests laufen (`build/test-merge-crew.log`); danach `git push origin HEAD:main`.

**Headline (06.09. 10:00, historisch): Grundlage vor Code.** Der Owner hat die Crew-Grundlage korrigiert: Die Crew ist ein Kollektiv in EINEM seriellen Harness; die Wesen sind Experten im MoE-Sinn, die Wissen über Tätigkeiten getrennt verwalten; ihr Gedächtnis ist das bestehende LCM (Continuity pro Wesen), die Auswahl ein Prompt über die bisherigen Erfahrungen, die Persona Teil des System-Prompt-Aufbaus. Drei Grundlagenfehler in PR-2 (#59) sind benannt (Seelen-Block in der Nutzerspur, Parallelspeicher `crew_member_learnings`, Schlagwort-Auswahl). Konzept steht in `docs/dev/crew-cockpit-vision.md` §7 und wartet auf Bestätigung des Owners; bis dahin kein weiterer Harness-Code. Die App-Optimierung (Scheiben 1–7) liegt fertig und geprüft auf Branch `crew-cockpit/optimize-ctox-app` (D10). Codex bekommt keine Aufträge mehr.

**Headline (06.09. 09:25, historisch): STOPP.** Der Neubau der CTOX-App (PR #62) wurde vom Owner abgelehnt: das Gute der App war entfernt, das Neue schlechter. Ursache: mein Brief („Neubau“, Abschnitt „Was weg muss“). PR #62 ist ungemergt.

**Headline (06.09. 06:40, historisch):** PR-1 (`b4294678f`), PR-2 (`b0e24d470`) und PR-2b (`ca1c0e362`) sind gemergt: Harness sichtbar und steuerbar, Crew als Entität mit gehärtetem Lebenszyklus. Kritischer Pfad ist jetzt PR-3 „Das Zuhause der Crew“ (Neubau `modules/ctox`), freigegeben an den Worker. Danach PR-4 Crew-Leiste, PR-5 Tickets.

## Kanban

### Done

- **D22 · Landung beta.36 auf welsch + Karussell-Fix (08.09. 09:30–12:15)** — Shell-Release beta.33/34/35 scheiterten am Release-Wächter `modules/ctox/tests/layout.browser.mjs` („Guard visible harness layout“): der Gestaltungsdurchgang hatte JEDE Kartenursache in den Tooltip verschoben, der Wächter verlangt die Ursache eines gescheiterten Tasks sichtbar (`.ctox-task-reason`, „4 Versuche · thread/start MCP handshake timeout“). Entscheidung: gestoppte Tasks (blocked/failed/cancelled) behalten EINE gedämpfte Ursachenzeile, laufende bleiben still; Meta-Trenner als `\\00B7` escaped (ohne Charset-Header wurde „·“ zu „Â·“). Commit `7b87b51fa` (v351), Tag beta.36 → Workflow grün (GitHub-Backlog ~45 min) → welsch: stage (`systemd-run --unit=ctox-crew-shell-beta36-stage-20260908`), activate, `systemctl --user restart ctox.service`, Status `current/healthy`, aktiv `0.1.46-beta.36` (vorher beta.32; die Office-Sitzung hatte beta.34 nie aktiviert, ihr Workflow scheiterte am selben Wächter). **Pane-Abnahme (Owner-Sitzung, Revision v351 geladen):** Dock-Grid korrekt (`108 42 26 1071 26 36`, Streifen 1071 px, Chips 218/269/320, 4 Mitglieder im Crew-Knopf), zwei Fenster nebeneinander (62/534 px), Flow-Karte bei gewähltem Task sichtbar, 12 Karten, 11 mit Ursache (alle gescheitert: Modell-Gateway auf welsch weiter tot), 1 mit Wesen-Portrait (einziger Task mit `crew_member_id`), Task-Drawer: Fehlerkarte mit 20-px-„Wiederholen“ und Ursachenzeile. **Neuer Befund:** bei drei Fenstern (Karussell) hingen die linken Nachbarn mit `left: -154px` aus der Bühne, weil der aktive Chip ganz links sitzt — Basisverhalten von main; Fix `alignChatWindows`: Schrittweite je Seite auf den verfügbaren Raum begrenzt (aktives Fenster bleibt unter seinem Chip, Nachbarn stapeln dichter), Szenario `carousel-fan-stays-on-stage-with-left-active-chip` (1000 px), Harness-Ladewartezeit 20 s (Fremdlast); Revision v352, Tag beta.38 (beta.37 gehörte der Office-Sitzung). Rust-Zieltests `business_os::crew_cockpit_command`/`policy` 6/6 grün (Release-Target, 3 h unter Last 136). Umgebungsfalle: Pane-Klicks auf Chat-Knöpfe kommen nicht an (Pane hidden/Emulation), Screenshots zeigen z. T. veraltete Frames — DOM-Messung per JS ist die verlässliche Abnahme.
- **D21 · Gestaltungsdurchgang nach Owner-Kritik + Wesen auf der App (08.09. 08:00–09:15)** — Owner-Befunde (Screenshots): Profil-Drawer zeigte Rohvorlagen (`# CONTINUITY ANCHORS … anchor_id:`), nackte Epoch-Zeiten und „Executionerror“, Scroll sprang; Fehlerkarte im Task-Drawer brach den „Wiederholen“-Knopf vertikal um; Flow-Karte („wo ein Wesen gerade ist, auf welcher Station“) war weg; Text „all over the place“; Task-Karten ohne Wesen; Chat-Leiste „geschrotten“ (ID-Fragmente `…c5b1b518f745`, ISO-Retry-Zeilen, doppelte Wesen, Fenster am rechten Rand übereinander). **Ursachen und Fixes (Commit `16781693b`, Revision v349, Shell-Tag beta.33):** (1) Chat-Leiste: die Pool-Zeile war ein SIEBTES Dock-Kind im Sechs-Spalten-Grid (`108px 42px 26px minmax(0,auto) 26px 36px`) → Chip-Streifen rutschte in die 26-px-Spalte (gemessen auf welsch: Strip `[1295,796,26,44]`, Chips bei 1183/1234/1285, `+`-Knopf in Zeile 2), `alignChatWindows` setzte alle Fenster an dieselbe Chip-Mitte. Pool-Zeile entfernt (der Crew-Knopf trägt die Mitglieder). (2) Vorbestehender main-Defekt: `alignChatWindows` maß `getBoundingClientRect().width` der noch mit `scale(0.8)` transformierten Karussell-Fenster → drei Fenster überlappten um 53 px, obwohl `is-side-by-side` gesetzt war (auf Basis `7089b1d2b` reproduziert); jetzt `offsetWidth`. (3) Sichtbare Task-ID im Fußzeilen-Knopf nur noch Screenreader-Text (Deep-Link + Tooltip bleiben; Crew-Map-Wächter prüft weiterhin `…567890abcdef`). (4) Harness-Zeilen `crew:…:… — ISO` werden zu „Satz · technischer Grund: … · HH:MM“. (5) CTOX-App: Crew-Zuhause nur ohne gewählten Task (Flow-Karte zurück), Karten mit Wesen-Portrait statt Pipeline-Wörtern, Grund/Quelle im Tooltip, Kopfzeile ohne `last_error`, Zeiten auch aus Millisekunden, `executionerror` → „Fehler bei der Ausführung“, Drawer rendert nur bei Signaturwechsel und behält den Scroll, Gedächtnis-Editor = Aussagen je Zeile (`anchorsDocumentFromLines` baut das Anchors-Dokument), Fehlerkarte mit `flex-wrap`. **Wächter gehärtet:** `assert-business-chat-behavior.mjs` seedet Crew-Mitglieder, verlangt einen eigenen Streifenplatz (≥120 px), keine überlappenden Side-by-side-Fenster und den Reporter-Slot am Streifenende (die alte „≤ viewport−90“-Erwartung widersprach mains Volle-Breite-Dock und war auf main rot); `assert-business-chat-layout.mjs` war auf main rot, weil sein Regex die Reporter-Slot-Regel statt der Geometrie-Regel traf (verankert). `assert-shell-chat-composition.mjs` bleibt rot (Timeout `.shell-window-control--minimize`, in CI auskommentiert). **Neu (Owner-Wunsch, Commit `cdb7cec4d`, v350, Tag beta.35): Wesen im App-Logo** — ein Mitglied mit aktivem Queue-Task (running/leased/review/drafting, Feld `module` + `crew_member_id`) erscheint als 18-px-Portrait in der Ecke des Fenster-Icons (`.shell-window-v2-icon`, Owner `module:<id>`) und auf dem Desktop-Icon (`.desktop-icon[data-target]`), Tooltip „Pico, Nia arbeiten hier“; folgt Queue, Crew, `window:opened` und Desktop-Grid; Harness-Szenario mit Shell-Attrappen + Screenshot (`output/playwright/…/crew-presence.png`), Unit-Test für die Zuordnung. Gates grün: Chat 73/73, Behavior 93 Szenarien, Layout, ctox 3/3, Tickets 14/14, Data-State 12, Crew-Map, Geometrie 6/6, Shell-V2 37/37, RxDB-only, Allowlist, Registry, Cache-Wächter. **Umgebung:** eine zweite Sitzung (Office/Documents) landet parallel auf main und setzt eigene Shell-Tags (beta.31/32/34) und aktiviert Slots auf welsch — meine Tags beta.33/35 enthalten deren Commits; wer zuletzt aktiviert, gewinnt. GitHub-Runner-Backlog: Shell-Release-Workflows seit 08:51 `queued`. Rust: gezielte Tests laufen weiter (Release-Build unter Last 136).
- **D20 · Offene Punkte abgearbeitet (08.09. 06:00–07:30)** — (a) **Live-Auftrag auf welsch** in der Owner-Sitzung („Crew-Abnahme: App-Liste“, Task `queue:system::bc906608c78fc5b1b518f745`): `crew_selected` → Pico, `crew.memory_read` „Pico liest sein Gedächtnis: 0 Wissenseinträge, 0 Erfahrungen, 1 letzte Einsätze“, `crew.learning` für Milo nach dem ersten Versuch, Stempel `last_learning_at` gesetzt — die Kette Auswahl → Lesen → Lernen läuft. **Aber:** der Router meldet ehrlich „Router nicht erreichbar (direct session timeout after 60s), Punktzahl entschied“, die Continuity-Refreshs werden übersprungen (45-s-Timeouts) und der Task selbst scheitert mit „model API temporarily unavailable“ (ExecutionError, Retry). Ursache: das Modell-Gateway auf welsch (CLIProxy `127.0.0.1:12435`, gpt-5.6-sol) antwortet auf `/v1/models`, aber Antworten (Responses-API) kommen nicht innerhalb 60 s — Provider-/Abo-Seite, nicht Cockpit. Ohne funktionierenden Provider kein Router-Urteil und kein Lernen mit Inhalt. (b) **Rolle „user“ entschieden und umgesetzt:** liest `ctox_crew_members` und neu `ctox_harness_status` (Pause, Kapazität, wer im Einsatz ist); `ctox_runs`, `ctox_harness_events`, `ctox_crew_learnings` bleiben Admin/Founder (`policy.rs::user_readable_cockpit_projection`, Test angepasst). (c) **B7 entschieden und umgesetzt:** Crew-Leiste unter den Kennzahlen, während ein Einsatz läuft (`crewStripMarkup`, Klick öffnet dasselbe Profil), Zuhause-Karten im Leerlauf. (d) **Gates:** neuer Wächter `assert-module-collection-allowlists.mjs` (Modul-Lesezugriffe gegen `SCOPED_SYSTEM_MODULE_DB_COLLECTIONS`/Companion-Allowlisten; fand sofort `ctox_harness_events` in der Chat-Leiste), in CI zusammen mit `generate-module-registry --check`; Landungs-Checkliste in `AGENTS.md`. (e) **Katalog-Trimmung:** `modules`/`allowed_module_ids` des Modul-Katalogs werden zuletzt getrimmt (Rust, `store.rs`), braucht Daemon-Upgrade. (f) Touch-Drag (`touch-action: none`). (g) **Sync-Stall an Sol:** Brief `ctox-crew-cockpit/briefs/sol-sync-stall.md`, Klon `~/.local/state/workjet-launchpads/ctox-sync-stall` (Branch `sync/native-peer-request-latency`); Start scheiterte mit `skill_runtime_unavailable` (Web-Research-Skill-Laufzeit) — Worker nicht verfügbar, kein Ersatz gestartet. (h) Rust-Vollsuite + Clippy laufen (`build/full-suite.log`). Shell-Release beta.30 (v348).
- **D19 · „Ich komme nicht zur UI“ — Ursache gefunden und behoben (07.09. 21:00–21:45)** — Owner-Sitzung im In-App-Browser auf welsch: (1) welsch lieferte trotz Upgrade die ALTE Shell (`20260906-office-page-exit`), weil der aktive Shell-Slot 0.1.46-beta.18 den `src/`-Baum übersteuert → beta.21 (v341) staged + aktiviert + `systemctl --user restart ctox.service` (Phase `restart` ohne Neustart bleibt alt). (2) Danach fehlten Crew zu Hause/Pool/Profil trotz Admin-Rolle: die Shell mischt den **eingebetteten, generierten Modul-Katalog** (`app.js` Block „offline-fallback-catalog“, `scripts/generate-module-registry.mjs`) über die native Katalogzeile; der Block war seit dem Hinzufügen der Crew-Collections nie regeneriert → `ctox_crew_members`, `ctox_crew_learnings`, `ctox_harness_events`, `ctox_harness_status` standen nicht in der Modul-Collection-Liste, wurden nie geleast, die App blieb bei „Wird geladen…“ ohne Crew. Fix: Registry regeneriert (`registry.json` + `app.js`), Revision v342, main `fea79303a`, Shell-Release-Tags beta.22/23/24 gesetzt (Workflow `business-os-shell-release.yml` baut in ~2 min). Danach beta.24 auf welsch aktivieren. Lehre: `node scripts/generate-module-registry.mjs --check` gehört in jede Gate-Liste, sobald ein `module.json` Collections ändert. **(3) Zweite Ursache, gefunden nach beta.24:** die Shell gibt Systemmodulen nur die Collections aus der harten Allowlist `SCOPED_SYSTEM_MODULE_DB_COLLECTIONS` in `app.js` (Rollen-Härtung `7c6a8d110`); für `ctox` standen dort nur fünf Collections der alten Konsole — `ctox_runs`, `ctox_crew_members`, `ctox_crew_learnings`, `ctox_harness_events`, `ctox_harness_status`, `business_chats` wurden vom Modul-DB-Handle still verweigert (`ctx.db.collection()` → null → leere Listen, auch lokal, unabhängig von der Rolle). Fix `90c00c695` (Allowlist + `docs/business-os-db-isolation-inventory.json`, Tickets bekommt Crew + Queue), Revision v343, Shell-Release beta.25 — danach zeigt welsch Crew zu Hause (Lumi, Milo, Nori, Pico) und den Profil-Drawer mit Seele/Stimme/Skizze in der Owner-Sitzung. **(4) Chat-Leiste:** die Companion-Allowlist `BUSINESS_CHAT_DB_COLLECTIONS` kannte `ctox_crew_members` nicht und die CTOX-Schemas werden erst mit dem CTOX-Fenster registriert → Pool leer; Fix: Allowlist, Registrierung vor `initBusinessChat`, Pool-Reload bei Collection-Readiness; App hydriert nach, wenn Crew/Status-Collections spät geleast werden; Fußzeile sagt nicht mehr „Wird geladen…“ über einer gefüllten Liste. Revision v344, Shell-Release beta.26. **(5)** Der In-Place-Renderpfad der Leiste rührt die Wesen nicht an → Pool erschien trotz geladener Mitglieder nicht; Signatur `crewPoolSignature` erzwingt den Vollaufbau bei Pool-Änderung; Start-Wiederholungen mit Backoff; `root.__ctoxChatState` als Sondenhaken. Revision v347, Shell-Release beta.29 (Tags beta.27/28 dazwischen). `assert-shell-chat-composition.mjs` ist auf main rot (Baseline identisch), `assert-db-isolation-inventory`/`assert-module-conformance` melden nur die vorbestehenden spreadsheets-Zeilen.
- **D18 · Beweislauf lokal mit echten Daten (07.09. 11:00–11:40)** — Release-Binary aus main (`~/.cache/ctox-opt-release-target/release/ctox`, ad-hoc signiert) gegen `~/.local/state/ctox`, Chrome per CDP. Ergebnis: CTOX-App zeigt den echten Task „Reporter Durable Intent E2E…“ (Desktop, 29.08.) mit deutschen Knoten, „keine Live-Messwerte“, „noch kein Plan“, „1 Eintrag“ (`~/.cache/ctox-crew-shots/01-ctox-app.png`). **Zwei Blocker, beide nicht im Cockpit-Code:** (1) die lokale Loopback-Sitzung ist Rolle `user` (`local-dev`, `is_admin=false`) → `ctox_crew_members`/`ctox_harness_status` sind admin-only, der Peer antwortet `UNAUTHORIZED` — Crew zu Hause, Profil, Pool und Drag bleiben lokal unsichtbar (OWNER-Karte Sichtbarkeit Rolle User); die App sagt jetzt „für deine Rolle nicht freigegeben“ statt des Rohcodes. (2) Browser-Replikation stallt gegen die 2-GB-Instanz auch mit PR #65 (`masterChangesSince`-Timeouts, Peer-Loops bis 61 s) → `docs/ctox-sync-feldbefund-20260907-crew-local.md`. Dienst und launchd wiederhergestellt, Debug-Target (41 GB) gelöscht, Release-Target (4,4 GB) bleibt für T12.
- **D17 · Kimi-UI-Review + Fixes (07.09. 09:55–10:40)** — Workjet-Run `local-2026-09-07T075446Z-ca410b17…` (Kimi · UI/UX, integrated), Bericht `/Volumes/tmp/dev-artifacts/ctox/crew-cockpit/kimi-ui-review-20260907.md`, Bilder im Klon `~/.local/state/workjet-launchpads/ctox-crew-review/src/apps/business-os/output/kimi-review/`. Urteil: nicht release-reif; 10 Befunde. Umgesetzt (Commit folgt in dieser Karte): B1 Knoten-Kicker/-Labels deutsch/englisch aus `FLOW_NODE_COPY` statt Enum-Namen; B2 Kennzahlenleiste zeigt „keine Live-Messwerte“ als eine Zeile statt 5× „nicht erfasst“, Fortschritt „—“/„noch kein Plan“; B3 ohne Daten und ohne gewählten Task kein Poster mehr, sondern Platzhalter + Zustandszeile, Quelle „nicht verbunden“ statt `unavailable`; B4 Fehler-Platzhalter mit X-Augen, technischer Grund klein/abgeschnitten mit Tooltip; B5 eigene Review-Augen (gekniffene Bögen); B6 Phase nie roh (`work` → Modellarbeit, Fallback in Worten); B8 Pool-Slots in der Leiste fokussierbar, Enter/Leertaste übergibt an das fokussierte App-Fenster; B9 Wiederholungszeit im Gescheitert-Satz; B10 Tickets-Locales (16 Schlüssel), „1 Eintrag“, leerer Zustand „Noch keine Tickets“. Nicht umgesetzt: B7 (siehe OWNER-Karte).
- **D16 · Ausdrücke „liest“/„lernt“ (07.09., `931a11820`)** — Stempel `last_memory_read_at`/`last_learning_at` an `crew_members` (gesetzt beim Gedächtnis-Lesen im Attempt und im Lern-Tick), projiziert als `…_ms`; `crewMemberExpression` in `business-chat.js` (liest 20 s im Einsatz, lernt 90 s zu Hause, Ablauf per Timer-Re-render); gesenkte Scan-Augen / weite Augen mit Nicken und Glühen; Sätze in App und Pool. welsch läuft seit 07.09. 09:00 UTC auf `branch-main-20260907T075629Z` (APP_BUILD v341, „Freigabe ohne Browser-Bestätigung“ dank `c69119697`).
- **D15 · Merge origin/main → Branch → main (07.09. 06:00–07:20)** — gelandet als `be4165cc4` auf origin/main (siehe Headline 07.09.). — siehe Headline 07.09.; Konfliktdateien: `app.js`, `index.html` (auto), `modules/ctox/index.js` (9 Hunks), `index.css` (2), `test.js` (2), `scripts/assert-ctox-crew-map.mjs` (4 → Datei von main), `shared/db.js`, `shared/rxdb-runtime.js`, `shared/sync.js`. Bundle mit esbuild 0.28.0 aus dem Merge-Baum neu gebaut, Schema-Vertrag/Hashes regeneriert (kein Diff). Merge-Commit `22ea45da9`.
- **D13 · Wesen per Drag-and-drop auf eine App (07.09., Commit auf `crew-cockpit/optimize-ctox-app`)** — Owner-Wunsch 06.09. abends: Mitglied aus der Chat-Leiste greifen, zappelt am Haken, auf eine App fallen lassen → Rechtsklick-Menü öffnet an der Stelle mit dem Wesen darin. Umgesetzt: Crew-Pool in FAB und ausgeklappter Leiste (`crewPoolSlotHtml`, `wireCrewDrag` in `shared/business-chat.js`, Pointer-Drag mit Geist `.ctox-crew-drag-ghost` + Haken-Animation), Shell-Hook `state.openCrewContextMenu` → `showGlobalCtoxContextMenu(context,x,y,crew)` (Wesen im Kopf, „<Name> übernimmt“, `payload.crew_member_id`, Chat öffnet mit Mitglied), Server `crew::assign_member_before_lease` (auch vom Assign-Kommando genutzt) im Claim-Pfad `store.rs`. Geprüft: Chat 71/71, ctox 37/37, Wesen-Guard, Shell-Vertrag, `cargo check` grün. Nicht getan: Browser-Beweis des Drags (wartet auf Instanz, T12).
- **D14 · KORREKTUR zur Chat-Zwischenstand-Flake (07.09.)** — Messung `retry_wait_delivers_interim…` auf dem Branch: 1/5, dann 2/5 grün; main laut Board 4/5 (bei geringerer Last). Analyse des Testpfads (`accept_rxdb_business_command` → Lease → Transitionen → Flow-Ereignis → `harness_cockpit_chat::project`): keine geänderte Codezeile liegt darauf (mein Zuweisungscode im Claim-Pfad ist ohne `crew_member_id` inert; `harness_cockpit_chat.rs` und `mission/channels` unverändert). Vorsorglich behoben: `project_crew` öffnete je Pump-Durchlauf die LCM-Engine (Migrations-Schreibsperre auf derselben DB) und liest das Gedächtnis jetzt über die eigene Verbindung. Verbleibende Fehlrate bewerte ich als Last-Flake (Load 40–80 auf 10 Kernen); Baseline auf main unter gleicher Last nicht gemessen (Kaltbau zu teuer). Bleibt B7.

- **D12 · Harness-Grundlage „Crew als MoE-Wissen“ committet (06.09., `7f1209e37`)** — Branch `crew-cockpit/optimize-ctox-app`. Gedächtnis pro Wesen in LCM-Continuity `crew:<id>` (`src/core/crew/memory.rs`, Migration der Learnings in Anchors), Router mit gebundenem werkzeugfreiem Urteil und ehrlichem Fallback (`router.rs`), Route vor der Schreib-Transaktion (`runtime.rs`, `CrewTurnContext`), Persona als Basis-Instruktions-Suffix im Sitzungsvertrag (`direct_session.rs`), Gedächtnis-Block in der Developer-Spur (`live_context::attach_crew_memory`, `turn_loop.rs`), Lern-Tick in der Wartungsschleife (`service.rs`, `refresh_member_memory`), Ereignisse `crew.memory_read`/`crew.learning`, Projektion `memory`/`domain`/`ticket_key`, Kommando `ctox.crew.memory.update`, `HARNESS.md` Crew-Abschnitt neu. Geprüft: `cargo check -p ctox --bins --tests` grün; `cargo test` für `crew::`, `business_os::crew`, Cockpit-Projektionen, `live_context::crew_lane`, Sitzungs-Basisinstruktionen: 55 grün, 1 rot = bekannter SQLite-Lock-Flake des Chat-Zwischenstand-Tests (B7, Wiederholung isoliert läuft). Log `/Volumes/tmp/dev-artifacts/ctox/crew-cockpit/build/test-h1c.log`, Target `~/.cache/ctox-opt-target` (löschen nach Abschluss). UI-Teil: D10-Nachtrag `6fe93432b`. Nicht getan: `cargo clippy`, Vollsuite; Live-Nachweis Router/Lernen (braucht laufende Instanz mit Provider).

- **D10 · CTOX-App optimiert, selbst gebaut (06.09. 09:00–09:50)** — Branch `crew-cockpit/optimize-ctox-app` (Worktree `/Volumes/tmp/worktrees/ctox/opt`, Basis origin/main `7089b1d2b`), sieben Commits: `861ab8ddd` Scheibe 1 Wahrheit statt Stille (Grund als Satz, Versuch, Lease, Mitglied; Zähler aus route_status) · `6fc9f0d13` Scheibe 2 Steuerung (Abbrechen/Blockieren/Freigeben/Wiederholen/Zuweisen im Drawer, Pause/Kapazität im Kopf; Permissions-Spiegel) · `799735f99` Scheibe 3 Live statt 4-s-Polling (RxDB-Subscriptions, Fluss aus `ctox_harness_events` des gewählten Tasks, Kennzahlen aus `ctox_runs`, Regler überlebt Drag, Deep-Link einmalig) · `dc3c12a5a` Scheibe 4 Wesen = Mitglieder, Crew zu Hause im Leerlauf, Profil-Drawer (Seele-Regler, Lebenslauf, Learnings, Stundenzettel) · `cd113af00` Scheibe 5 Aufräumen (Regex-Redaktion weg, Locales, echte Liste, 5. Kennzahl, Web Stack in den Drawer, 5 tote Funktionen + 36 tote Schlüssel) · `1fa1f1e8f` Scheiben 6+7 (Sync-getrennt/Laden-fehlgeschlagen ehrlich, Chat↔Task per postMessage, „Im Chat öffnen“). Geprüft: Modultests 35/35, `assert-ctox-crew-map` grün (Guard an das 04.09-Verhalten angeglichen), Shell-V2-Vertrag 37/37, Geometrie-Labor 640/1180/1440 OK (`/Volumes/tmp/dev-artifacts/ctox/crew-cockpit/geometry-opt/`), Befehlsinventar regeneriert, Data-Plane-Guard grün (Buster an APP_BUILD `20260906-shell-v2-crew-home-v339` angeglichen), RxDB-Suite 114/117 wie main (nur `bundle-reproducible-smoke` rot, Baseline). Offen: Screenshots mit echten Daten (wartet auf lokales Upgrade, Karte W6), Kimi-UI-Review, Merge nach Owner-Review. **Vorbehalt:** Scheibe 4 zeigt Learnings/Seele aus den heutigen Projektionen; nach der Harness-Grundlage (§7) wandern Learnings in LCM-Continuity, der Drawer wird dann darauf umgestellt.
- **D11 · Grundlage korrigiert (06.09. 09:55)** — `docs/dev/crew-cockpit-vision.md` §7: ein Harness, Wissen pro Wesen im LCM, Router-Prompt über Erfahrung, Persona in Basis-Instruktionen, Lernen über Continuity-Refresh. Befunde: `attach_crew_soul` → `latest_user_prompt` (turn_loop.rs:1015/1134); `crew_member_learnings` neben dem LCM (crew/mod.rs, finalization.rs, runtime.rs, harness_cockpit_projections.rs, crew_commands.rs); `select` = Punktzahl (crew/mod.rs:321). Wartet auf Bestätigung des Owners.

- **D1 · Ist-Analyse Code (4 Audits, 05.09. 12:05–12:25)** — CTOX-App (`src/apps/business-os/modules/ctox/`), Crew-Leiste (`shared/business-chat.js`), Tickets (`modules/tickets/` + `src/core/mission/tickets/`), Harness-Observability (`src/core/service/service.rs`, `store_projections.rs`, `harness_flow.rs`). Ergebnisse unten unter „Befunde“. Stichproben per grep verifiziert: `ctox_runs` hat keinen Rust-Writer; `commandBus.cancel` wird in CTOX-App und Chat 0× benutzt; `loadLocalCollection` = `find().limit(200)` → sort → `slice(0,20)` (`modules/ctox/index.js:3944-3954`); `lease_owner`/`status_note` 0× gerendert; `hold_reason`/`retry_not_before` 0× in `src/core/business_os/`; `QUEUE_PRESSURE_GUARD_THRESHOLD = 20` (`service.rs:159`).
- **D2 · Ist-Analyse live (welsch.ctox.dev, 05.09. 12:12–12:25, Rolle Admin)** — Testaufgabe über die Crew-Leiste gesendet: Command `cmd_b964f8bc-2130-42df-80f8-53e7fe9b2961`, Task `queue:system::41c33261fb7b96906277e159`. Beobachtet: Übergabe in Queue nach ~3 s; während der Arbeit nur leerer Balken, Fortschritt ausschließlich im Tooltip („0 % · 20/23 Turns · Denkblöcke 17 · Tools 6“, Prozent bleibt 0 während Turns steigen); Composer verschwindet; nach Abschluss (`execution_phase=retry_wait`, `result.user_message` vorhanden, `status=succeeded`, `attempt=1`) zeigt der Chat **keine Antwort**, die CTOX-App „Wartet“, kein Grund sichtbar. Wesen wechselte nach dem ersten Senden den Namen (Tavi → Milo), weil Identität = Hash(commandId || chatId) (`business-chat.js:1952-1978`).
- **D3 · Ist-Analyse lokal (127.0.0.1:8765)** — Instanz hing seit 04.09. 17:13 in `status=stale` eines Upgrade-Lease (`/api/business-os/ctox/maintenance`, `retry_action: ctox upgrade --dev`); Browser-Peer nie authentifiziert; CTOX-App zeigte dauerhaft „Tasks werden synchronisiert“, kein Hinweis auf die Ursache, Banner nicht schließbar.
- **D4 · Lokale Instanz repariert (05.09. 12:48–12:52)** — Ursache: Upgrade-Prozess pid 2666 starb am 04.09. nach dem Build; Release `business-os-shell-v0.1.44` war fertig, Dienst lief seit 07:12 bereits daraus (launchd `com.metric-space.ctox.service`, `releases/…/runtime` ist Symlink auf `~/.local/state/ctox`), aber `current` zeigte auf `workjet-sync-efe5ef1a7`, Manifest alt, `update_state.json` in `building`, toter `update.lock`. Kein Rebuild nötig: Lock → `update.lock.stale-20260905T-dead-pid-2666`, `current` → 0.1.44, Manifest `current_release=business-os-shell-v0.1.44` (Sicherung `backups/install_manifest.json.pre-repair-20260905`), Update-State `completed` (Sicherung `backups/update_state.json.pre-repair-20260905`). Maintenance ging sofort von `stale` auf `waiting_replication` (92 %, `service_active=true`). Der letzte Schritt (`waiting_collections` → `completed`) braucht einen Browser, der die Replikation aufbaut und `ctox.maintenance.client_ready` sendet. Aus dem Claude-In-App-Browser kommt lokal kein WebRTC-Datenkanal zustande (Signaling-Log: Offer/Answer beidseitig, Browser liefert nur einen mDNS-Host-Kandidaten, kein srflx/relay; Browser meldet `peer_connect_timeout` nach 30 s; nativer Peer `peerAuthenticated=false`). Beim Dienststart 07:12 war die Replikation mit einem normalen Browser oben („replication up for 220 collections“), das Problem ist also der Pane-Browser, nicht die Instanz. Abschluss erfolgt beim nächsten Laden in Chrome/Safari; dort dann prüfen, dass das Banner „Upgrade abgeschlossen“ zeigt.
- **D5 · welsch-Upgrade geklärt (05.09. 12:50)** — Nicht meine Baustelle, sondern eine andere Sitzung (`ctox-dev/output/welsch-office-upgrade-core-20260905.sh`, systemd-Unit `ctox-office-core-upgrade-20260905`): `ctox upgrade --dev` lief 12:16–12:44 lokal Zeit, Ergebnis `applied release branch-main-20260905T101623Z`, Dienst `active/running`, Shell-Slot 0.1.44 `healthy` (Phase `rollback` von 0.1.45, 12:05). Browser-Prüfung 12:53: Maintenance `completed`, Banner ausgeblendet, `app.js?v=20260903-app-import-fidelity-v337`.
- **D6 · Workjet-Slider/Stundenzettel geprüft** — `claude-workjet` ist auf dem Stand von `origin/main` (0 Commits dahinter, HEAD 8e5b0f4); weder Quelltext noch installierte `Workjet.app` kennen Slider oder Stundenzettel. Beide Konzepte sind im Brief eigenständig definiert (Soul-Achsen 0–100, Stundenzettel = `ctox_runs` je Mitglied + Rückblick). Keine offene Frage mehr.

- **D7 · PR-1 gemergt (05.09. 23:10, Merge-Commit `b4294678f`, PR #58, Head `b2068c0e4`)** — 20/20 Review-Punkte behoben und verifiziert; eigener Testlauf grün; CI am Head identisch mit main-Run `33988826033` (x86-Linux-CLI: Spreadsheets-Oracle-Test + Freeze-Check; drei Desktop-E2E: `npm audit`), alle vier außerhalb der geänderten Dateien. Verträge für PR-3/4/5 stehen in PR #58 §6 und `docs/ctox-rxdb.md#cockpit-projections-pr-1`. Details der Review-Schleife in der W1-Karte unten (Historie).

- **D8 · PR-2 gemergt (06.09. 02:11, Merge `b0e24d470`, PR #59, Head `89ef615c7`, 41 Dateien +3211/−42) — vom Worker selbst gemergt auf Owner-Anweisung.** Neu: `src/core/crew/{mod,runtime,finalization}.rs`, `crew_commands.rs`, Projektionen `ctox_crew_members`/`ctox_crew_learnings`, Scope `CrewManage`, `{{CREW_SOUL_BLOCK}}` in `live_context.rs`, Feld-Policy im nativen RxDB-Plugin (`replication_webrtc/*`), CLI `ctox crew list|show`. Worker-Abnahme laut PR-Text: crew 28/28, command_plane 20/20, channels 131/131, RxDB-Suite grün, JS 116/116; zwei rote Cockpit-Tests per Baseline gemessen (einer auf Basis ebenfalls rot). Mein Review folgt (W3).

- **D9 · PR-2b gemergt (06.09. 06:38, Merge-Commit `ca1c0e362`, PR #61, Head `0aae556a5`, 41 Dateien +1789/−221 gegen main)** — drei Review-Runden (frühes Review 9 Punkte, Stop-Befund Migration, finale 2 Punkte), alle verifiziert; Guard `module-migrations-smoke.mjs` neu; Flaky-Baseline 5× gemessen (Chat-Zwischenstand main 4/5 vs Head 3/5 → main-Befund B7); CI am Head identisch mit main-Rotmenge. Rust-Tests der Fix-Runden: Worker-Abnahme (crew 47/48, cockpit 23/23, command_plane 20/20, channels 131/131, rxdb 420, JS 117/117, Clippy), von mir per Node-Guards und Code-Verifikation gegengeprüft, nicht selbst gefahren (ENOSPC-Vorfall).

### Working

- **W5 · PR-3 „Das Zuhause der Crew“ (Neubau `modules/ctox`)** — Freigabe an den Worker 06:42, Brief `docs/dev/crew-cockpit-brief-pr3.md` (Verträge Stand #58/#59/#61, drei Ansichten + Profil-Drawer, gemeinsame Wesen-Komponente `shared/crew-creature.js` mit neun Zuständen, Gestaltungsregeln, Abnahme mit Browser-Beweis auf thesen). Fertig heißt: Draft-PR mit Screenshots je Scheibe, Abschlussbericht, dann Code-Review + Kimi-UI/UX-Review + eigener Browser-Beweis auf thesen (src/ live), Merge durch mich. **Scheibe 1 (07:14, Draft-PR #62, Commit `414022442`): `shared/crew-creature.js` (74 Zeilen, reine Funktion `creatureState(member, task, events)` → neun Zustände, `crewCreatureHtml` ohne Zufall/Hash), `crew-creature.css` (Animation je Zustand, `prefers-reduced-motion`), Gallery-Fixture `modules/ctox/tests/creature-gallery.html` + Tests + Locale-Labels. Von mir im Browser angesehen (statischer Server `crew-review-static` :8791 auf dem Review-Worktree): vier Formen × neun Zustände klar unterscheidbar — Schlafen (geschlossene Augen, Z), Warteschlange (Liste), Aufwachen, Denken (Gedankenblase), Werkzeug (Hammer), Prüfen (Brille, Dokument), Wartet (Sanduhr), Gescheitert (X-Augen, X), Fertig (lachend, Haken). Entspricht Vision §3.2.** **Scheibe 2–3 (07:32–08:03, Commits `35c4d670b` „replace flow poster with reactive crew workspace“, `852ec7eec`): Neubau `modules/ctox` = index.js 690 + view.js + model.js + paging.js + index.css 546 Zeilen (vorher 4874 + 1814), +3656/−7733 in 23 Dateien; Zuhause-Screenshots (Fixture, hell) zeigen die richtige Hierarchie. Frühes Feedback an den Worker: (a) `creatureState` fällt für aktive Tasks ohne Ereignis in 10 s auf `waking` statt `thinking`; (b) Fixture hell, Shell dunkel → Kit-Tokens (`--line`, `--kit-fill`, …) statt eigenem `--border` mit hellem Fallback (Geometrie-Screenshot zeigt helle Linien in leerem dunklem Fenster); (c) im Geometrie-Labor ohne Datenplane bleibt das Fenster leer → drei Zustände (Zuhause/Sync getrennt/Ladefehler) müssen ohne Daten sichtbar sein; (d) Zustand als Satz statt Einwort. Umgebung: thesen läuft `branch-main-20260905T072559Z` ohne Cockpit-/Crew-Tabellen → Browser-Beweis dort braucht erst ein Daemon-Upgrade (T6-Vorbedingung).** **Mid-Flight-Code-Review 08:35 (Opus, gegengelesen): stark — gebundene Queries mit Guard, Keyset-Paging, keyed Reconciler mit Fokus-/Dirty-Schutz, nichts außerhalb des Hosts, Commands mit korrekten Scopes, drei Zustände vorhanden, alle §1-Entfernungen belegt, 24/24 Tests. **KORREKTUR 08:40: Der Owner hat die neue Gestaltung im Codex-Thread abgelehnt; der Worker bestätigt selbst: „Die konkrete Gestaltung war meine Entscheidung — und sie war schlecht“ (Anforderungen zu wörtlich in Texte/Labels übersetzt; bestehende Wesen-Darstellung mit prozeduraler Bewegung durch einfache SVG-Gesichter ersetzt). Mein eigenes Frühurteil zur Galerie („sieht richtig aus“) war zu milde: lesbar ja, aber Charakter und Bewegungsqualität der alten Wesen fehlen. Merge angehalten. Gestaltungs-Direktive 09:05 (`scratchpad/msg-pr3-design.txt`): Architektur behalten, Präsentation neu — bestehende Crew-Darstellung aus `business-chat.js` (Körperpfade, seeded prozedurale Bewegung, Fortschrittsring, telemetriegetriebene Mikrobewegung, CSS ~5460–5530/6566–6580) nach `shared/crew-creature.*` portieren und um neun Zustände minimal erweitern, keine Comic-Symbole; jeder Fakt einmal, keine Erklärsätze, Szene statt Formular, dunkel primär; Vorher/Nachher-Screenshots.** **OWNER-Anweisung direkt an den Worker (06.09. 08:36): „du musst all deine änderungen zu main mergen“ → Worker mergt PR #62 selbst; Merge-Bedingungen von mir 08:45 (neun Punkte eingearbeitet und belegt, Abnahme §5, Screenshots dunkel/hell je Ansicht, CI-Stand, Merge-Commit, Bericht mit PR-4-Verträgen); mein Review + welsch-Beweis danach.** Neun Änderungswünsche an den Worker 08:40: Kit-Tokens statt erfundener (`--border` etc.), toter Guard `assert-ctox-crew-map.mjs`, Zustands-Mapping (cancelled≠waiting, tooling per offenem call_id, waking begrenzt), Refresh-Fan-out (7 Streams × Voll-Refresh, 10–14 Queries je Tool-Ereignis), rohe Enum-Werte (`waiting_external`, `awaiting_review`, `pause_reason`, `last_error`), harter Cache-Buster + doppelter CSS-Link, globale Sonderregel in `permissions.js`, Kit-Grammatik/module.json, Kleinigkeiten.**
- **W4 (Historie) · PR-2b „Crew-Lifecycle härten“** — Auftrag an den Worker 02:47 (`docs/dev/crew-cockpit-brief-pr2b.md`). Draft-PR #61 (Head `7298b59ca`, +1253/−191, 39 Dateien) seit 03:07; Worker-Abnahme läuft (crew 40/40, Hold/Redirect grün, Shell-V2 37/37; RxDB/Clippy/JS/CI offen, Sync-Kern von main wird integriert). **Frühes Review 03:15 (Opus, Kernpunkte von mir im Code gegengelesen):** alle Brief-Punkte substanziell umgesetzt, keine Assertion geschwächt; **BLOCKER: `ctox_queue_tasks` auf Version 3 ohne Migrationsstrategie 3 (`schema.js:74` vs `:719-722`) → bestehende Browser-DBs scheitern beim Boot**; HOCH: Crew-Fehler löscht manuelle Zuordnung (`runtime.rs:88-92`); HOCH: `retain_attempts(...)?` vor allen Projektionen kann die Gesamtprojektion abbrechen (`projections.rs:380`); dazu sechs mittlere/kleine Punkte. Neun Punkte an den Worker 03:17 — **Zustellung erst 04:34, nach seinem Abschlussbericht** (Codex-Queue liefert erst zwischen Schritten; der Worker hatte währenddessen main bis `f972b3ed6` integriert und PR #61 auf Ready gesetzt, Head `96b98afdd`). Verifikation 04:40 auf `96b98afdd`: nur Punkt 4 behoben, 3 und 5 teilweise, **Blocker 1 (Migration v3) weiterhin offen, obwohl der PR-Text „2→3 mit No-op-Migration“ behauptet** — von mir im Code bestätigt (`schema.js:74` vs `:719-722`, threads ebenso, `assert-declarative-migrations.mjs` prüft das nicht). Stop-Befund an den Worker 04:37 (Strategie 3 + Guard + PR-Text als Korrektur). **Fix-Runde gepusht 05:40 (`f26a015a8`, `6859df005`, `8b92726d5`, +536/−50): unabhängig verifiziert — alle neun Punkte FIXED (Punkt 5 PARTIAL: LCM-Fehler propagiert weiter, Service-Test für den All-archiviert-Fall vorhanden); neuer Guard `module-migrations-smoke.mjs` strikt, fand echten Altfehler (kundenpipeline v0-Strategien); Commit „defer crew event repair“ sicher/idempotent; keine Assertion entfernt, aber E-Mail-Adressen wurden in `safe_prose` erlaubt → **Entscheidung Fable: bleiben verboten** (Nachricht 05:50, letzter Commit angefordert, plus Propagations-Test). Eigene Node-Guards auf `8b92726d5` grün (Migrationen, Schema-Generatoren, Contract, Hashes, Bundle-Reproduzierbarkeit, Data-Plane, Crew-Fixture). Rust-Tests der Fix-Runde nicht selbst gefahren (Target nach ENOSPC gelöscht); Worker-Abnahme am finalen Head: crew 47/48 (Control-Audit-Flake isoliert grün), harness_cockpit 23/23, command_plane 20/20, channels 131/131, rxdb 420, JS 117/117, Clippy grün. CI: nur Windows-CLI offen, rot nur bekannte main-Befunde. Fertig heißt: Fixes drin, Abschlussbericht, finales Review (Diff der Fix-Commits + eigener Testlauf), Merge durch mich als Merge-Commit.
- **W3 · Post-Merge-Review PR-2 (06.09. ab 02:12) — abgeschlossen 02:47, Ergebnis in W4 überführt** — zwei Opus-Reviewer (Rust/Harness/Sync-Plugin; Schema/Contracts/permissions.js), Kimi adversarial (Workjet-Run `local-2026-09-06T001218Z-629b1a36-…`, Brief `briefs/kimi-review-pr2.md`), eigener Testlauf (`baseline/run-pr2-tests.sh`: crew, harness_cockpit, command_plane, channels komplett, rxdb-Crate u. a.). Befunde → Folge-PR-Auftrag an den Worker. Fertig heißt: Befundliste gesendet oder „keine Befunde“, Board aktualisiert, dann Brief PR-3 finalisieren. **Ergebnisse 02:25–02:35:** Rust-Review: Sicherheitsmodell sauber (Feld-Policy auf Master/Live/Query-Fetch + MCP, Query-Orakel geschlossen, Grants immun, kein Turn-Pfad-DB-Zugriff, keine Assertion geschwächt), aber **zwei hohe Lifecycle-Fehler**: Zuordnung vor fünf Zulassungs-Guards (Waisen, falsche Kontinuität, Cockpit-Ereignis ohne Arbeit), manuelle Zuordnung aus derselben Spalte wie die automatische (Retry pinnt Mitglied, 24-h-Malus tot); dazu ungegatete Crew-Projektion, keine Attempt-Retention, Unique-Index im Pump, Rollen-Aliase, masterWrite-Konflikte ungemaskt. Kimi (Run `…001218Z-629b1a36`, integrated): kein kritischer/hoher Vertraulichkeitsbefund; **B1 mittel: Crew-Fehler (alle archiviert / korrupte soul_json) → Lease-Freigabe ohne Failure-Accounting → Endlos-Lease-Schleife, serielle Queue steht**; B2 Cross-Thread-Injection über unbestätigte scope-lose Learnings mit Newlines; B3 Denyliste umgehbar; B4 Orphan-Attempts steuern Kontinuität. Konsolidiert in `docs/dev/crew-cockpit-brief-pr2b.md` (Folge-PR vor PR-3, Auftrag an den Worker 02:47). Schema/Browser-Review: Freigabe, drei kleine Härtungen (in PR-2b §3). **Eigener Testlauf auf `b0e24d470` (02:24–02:29, nach `cargo clean -p wha-proto` wegen durch meinen früheren Abbruch beschädigter Proto-Artefakte): crew 28/28, harness_cockpit 16/16 (beide vom Worker als flaky gemeldeten Tests grün), command_plane 20/20, store_projections 9, store_policy 4, mcp 1, harness_flow 5, capacity 1, worker_attempt 2, prompt_worker_drop 6, worker_activity 2, rxdb-Crate 402/402; `mission::channels::tests` 125/126 — `releasing_failed_queue_task_moves_running_command_to_retry_wait` im Gruppenlauf mit SQLite 517 „database is locked“, isoliert 2× grün (Backlog B7: SQLite-Lock-Flakiness der channels-Tests, nicht PR-spezifisch).** KORREKTUR D8: Worker mergte als Squash und zog Clippy/Shell-V2/CI erst nach dem Merge nach (selbst ausgewiesen).
- **W2 (Historie) · PR-2 „Crew-Identität im Harness“ (Server)** — Worker: Codex-Thread, Freigabe 23:12 (Nachricht `scratchpad/msg-pr2-start.txt`: Basis origin/main mit PR-1, Worktree `pr2-crew-identity`, Vorgaben zu Seeding, reiner Auswahlfunktion, deterministischem Soul-Block nach den Sicherheitsregeln, Learnings-Vertrag, Projektionen/Policy/Commands/CLI). Brief §3 + §2j. Fertig heißt: PR gegen main, Abschlussbericht, dann Review-Schleife wie bei PR-1 (zwei Code-Reviews, Kimi adversarial, Baseline für rote Alttests, eigener Testlauf mit Target `~/.cache/ctox-review-pr1-target`). **06.09. 00:40: Draft-PR #59 offen (Head `da7891a1a`, +626/−2, 15 Dateien: Crew-Schemas, Permission-Verträge, Fixture, Doku); native Integration in Arbeit, Crew-Testlauf 25/27 mit zwei eigenen offenen Befunden; Worker legt selbst einen Baseline-Worktree `pr2-baseline` auf `b4294678f` an.** Grenze dokumentiert: `ctox.coding.turn` läuft ohne Queue-Lease direkt in den Pi-Sidecar und bekommt in PR-2 kein Crew-Mitglied → Backlog B6. **OWNER-Entscheidung 06.09. 00:49 (direkt an den Worker): „merge zu main, sobald du fertig bist.“ Mein Review verschiebt sich auf nach dem Merge; Merge-Bedingungen (Abnahme §3g/§2j, Baseline, CI-Stand, PR-Text = Abschlussbericht, Merge-Commit, Artefakte behalten) an den Worker 00:52.**
- **W1 (Historie) · PR-1 „Harness-Cockpit Foundation“ (Server)** — PR #58 https://github.com/metric-space-ai/ctox/pull/58, Head `523a8d3ba`, Basis `2d88f2267`, 62 Dateien +5107/−343, merged sauber auf `origin/main 0b4b14a25`. Worker: Codex-Thread; Review-Worktree `/Volumes/tmp/worktrees/ctox/review-pr1` (Branch `review/pr1`). **Mein Review 05.09. 17:50–18:03 (zwei Opus-Reviewer, stichprobenartig gegengelesen): mergefähig mit Fixes.** Vertrag 2a–2g erfüllt; Policy dreifach verifiziert (Browser-Read/Write, Grants, MCP); kein Guard geschwächt; Serialität/Gates unverändert; fremde Schema-Diffs (browser, kundenpipeline, reports) sind bewiesene Generator-Regeneration (Basis-Commit war „stale“). 16 Fix-Forderungen an den Worker gesendet (18:03, Datei `scratchpad/review-pr1-fixes.md`): Blocker = blockierende Reads im Turn-Pfad (`service.rs:5712`), Ereignisverlust bei transientem Fehler (`harness_flow.rs:264`), stumme Pause bei Parse-Fehler (`harness_cockpit.rs:37`), `hold_reason`-Überschreiben (`mod.rs:3559`), fehlender `?v=`-Buster auf `rxdb-runtime.js` (Edge-Cache bis 24 h), gecachte Import-Rejection. Kimi-Adversarial-Review (Workjet-Run `local-2026-09-05T160332Z-5e4f884d-…`, Launchpad `~/.local/state/workjet-launchpads/ctox-crew-cockpit`, Brief `briefs/kimi-review-pr1.md`) fertig 18:28: **mergefähig mit Fixes**, 0 kritisch/hoch, 5 niedrig, 2 info; kein bestätigter Weg für Rolle user oder MCP auf Cockpit-Collections oder Control-Commands. Drei Punkte als Nachtrag 17–19 an den Worker: Legacy-Grant-Materialisierung für Cockpit-Collections (`store.rs:15590`), rohes Payload im Audit (`harness_cockpit.rs:63-77`), Terminal-Guard ohne ORDER BY bei Multi-Link-Tasks (`mod.rs:3485`). CI: drei rote Jobs sind Altbefunde von main (Plattform-Freeze-Check auf Basis-Commit reproduziert rot; `npm audit` fast-uri/xmldom, Lockfile nicht im Diff). Worker-Abschlussbericht 18:29 (PR-Text §4): gezielte Rust-Tests grün (8/9/17/130/…), RxDB 399, JS 114/114 mit Wire-Daemon, Spawn-Liveness ok, Geometrie 37/37. **Offen: 6 von 146 Peer-Tests rot** im seriellen Einzellauf (`business_command_idle_wait_rechecks_a_change_seen_before_notifier_arm` Timing 750 ms; `native_peer_consumes_pending_knowledge_command`, `…module_governance_commands`, `…marks_invalid_ticket_commands_failed` = Intake-Verhalten `pending_sync` statt `failed`; `native_peer_sync_config_change_detects_room_rotation`; `…materializes_procedural_knowledge` = `/payload/markdown` vs. `markdown`). Worker behauptet: betroffene Pfade gegenüber Basis unverändert, aber **keine Baseline-Messung**. **Baseline gemessen (19:01–19:24, Worktrees `base-pr1` = `2d88f2267` und `review-pr1` = `fde5d0702`, Target `~/.cache/ctox-review-pr1-target`, `--exact --test-threads=1`, Log `/Volumes/tmp/dev-artifacts/ctox/crew-cockpit/baseline/summary.txt`):** Basis/Head identisch für alle sechs — `idle_wait…notifier_arm` grün/grün (223 s / 181 s; das Rot des Workers war Host-Last), `…room_rotation` grün/grün, `…pending_knowledge_command` rot/rot, `…module_governance_commands` rot/rot, `…invalid_ticket_commands_failed` rot/rot, `…procedural_knowledge` rot/rot. **Keine Regression durch PR-1; vier Altfehler von main** (Backlog B4). **Fix-Runde 1 (Worker-Push 20:06, Commits 3e05b7a66, 4f4443270, edc101047):** unabhängig verifiziert — 14 von 17 Code-Punkten substanziell behoben mit Tests, keine Assertion entfernt; **nicht angefasst: 17 (Legacy-Grants), 18 (Audit-Whitelist), 19 (Terminal-Guard über mehrere Links)**; **neu durch Fix 12: `millis()`→`Option` kann `null` in required/Index-Zeitfelder schreiben (Punkt 20)**. Runde 2 mit 17–20 an den Worker 20:16. Pause-fail-open (Punkt 3) als Entscheidung akzeptiert. **Runde 2 gepusht 22:29 (`b2068c0e4`, +335/−44): unabhängig verifiziert 17–20 FIXED mit echten Tests, keine Assertion entfernt, Doku stimmt; UNIQUE-Constraint `task_id` in `business_command_task_links` bestätigt (`outbound_review.rs:3929`). Eigener Testlauf 22:30–22:33 auf `b2068c0e4`: harness_cockpit 14/14, command_plane 19/19, channels-Guard 1/1, rxdb_peer-Grant-Test 1/1, store_projections 9, store_policy 4, mcp 1, transport 11, harness_flow 5, capacity 1, worker_attempt 2 — alle grün. 20/20 Review-Punkte erledigt.** **Eigener Testlauf auf `edc101047` (20:07–20:14, Target `~/.cache/ctox-review-pr1-target`, TMPDIR Systemplatte): harness_cockpit 13/13, command_plane 18/18, store_projections 9/9, store_policy 4/4, cockpit_mcp_policy 1/1, local_transport 11/11, harness_flow 5/5, queue_capacity 1/1, worker_attempt 2/2 — alle grün** (`baseline/head-tests-summary.txt`). Für den Bau war das Pi-Sidecar-Bundle nötig (`include_bytes!` in `pi_sidecar.rs:42`); selbst gebaut unter `/Volumes/tmp/dev-artifacts/ctox/crew-cockpit/sidecar-build/dist` (npm ci + npm run build, 12,5 MB) und in beide Worktrees gesymlinkt. KORREKTUR: Der Worker hat sein Cargo-Target (31 GiB) trotz meiner Anweisung gelöscht — die Nachricht kam erst nach seinem Turn-Ende an (Codex-Queue liefert zwischen Schritten). Kaltbau nötig. CI zusätzlich rot: aarch64-CLI `E0282` in `office-engine/src/ops.rs:293` (nicht im Diff, main-seitig). Worker-Cargo-Target `~/.cache/ctox-crew-cockpit-target` (31 GiB) bleibt für meinen eigenen Testlauf stehen. Fertig heißt: Fixes im PR, Worker-Abschlussbericht, Kimi-Befunde bewertet, eigener `cargo test`-Lauf der Cockpit-Module grün, dann Draft → Ready und Merge.

### To-Do

- ~~T7 · H2 Projektion + Kommandos~~ erledigt in D12. — Gedächtnis-Dokumente je Wesen nach RxDB (`ctox_crew_members.memory` oder eigene Collection; Schema-Vertrag + Hashes + Fixtures beidseitig), Kommando `ctox.crew.memory.update` → `continuity_apply_diff`, abgeleitetes Fachgebiet je Wesen, Policy-Spiegel in `shared/permissions.js`. Abnahme: `cargo test business_os::crew`, `node rxdb/tests/run-all.mjs`.
- ~~T8 · H3 UI CTOX-App~~ erledigt in `6fe93432b`; offen: Ausdrücke „liest“/„lernt“ (mitgliedsbezogene Ereignisabfrage) und Screenshots (T12). — Profil-Drawer: Persona oben (Regler, Charakter, Stimme), Gedächtnis als zwei Dokumente mit Bearbeiten/Bestätigen, Lebenslauf, Stundenzettel; Learnings-Liste entfällt; Fachgebiet auf der Karte zu Hause; Übernahme sichtbar (Router-Grund an Wesen und Task-Karte); Ausdrücke „liest“ und „lernt“; Pool verwalten (anlegen, archivieren). Screenshots dunkel aus der Shell.
- ~~T9 · H4 Chat-Leiste~~ erledigt in `6fe93432b`. — Wesen = Mitglied des Commands; Übernahmezeile „<Name> übernimmt: <Grund>“; neutrales Wesen bis zur Entscheidung; Stimme aus der Persona; Name wechselt nie im Gespräch.
- ~~T10 · H5 Tickets~~ erledigt in `6fe93432b`. — Ticket zeigt Wesen und Wartegrund als Satz, Zuweisung solange ungeleast, Ticket-Stufen auf Wesen-Ausdrücke abgebildet.
- ~~T13 · beta.38 auf welsch aktivieren~~ → erledigt 12:20: Slot `0.1.46-beta.38` current/healthy, Pane lädt v352; drei Fenster in der Bühne (aktiv 32–492, Nachbarn 396–647 und 515–785 px, Opacity 0,6, 3D-gedreht, per elementFromPoint oben), Chat-Verlauf ohne ID-Fragmente („Der Versuch wird wiederholt · technischer Grund: WORKER runtime API failure · 06:37“, „Milo übernimmt.“). Offen bleibt die Live-Abnahme mit laufendem Task (Wesen im App-Icon) — braucht das Modell-Gateway auf welsch (OWNER-Karte). — stage `0.1.46-beta.35`, activate, `systemctl --user restart ctox.service`; prüfen: drei Chat-Fenster nebeneinander / Karussell, Flow-Karte bei gewähltem Task, Karten mit Wesen, Fehlerkarte, Profil-Drawer (Aussagen-Editor, Stundenzettel, Scroll), Wesen im App-Icon bei laufendem Task (braucht funktionierendes Modell-Gateway auf welsch).
- **T12 · Rest-Beweis Crew zu Hause/Profil/Drag mit echten Daten (Trigger: Admin-Sitzung lokal — OWNER-Entscheidung Rolle User — oder angemeldete Owner-Sitzung auf welsch)** — CTOX-App (Zuhause, Profil mit Gedächtnis, Task mit Übernahmesatz), Chat-Leiste (Übernahmezeile), Tickets (Wesen) dunkel aus der Shell; dann Kimi-Review, dann Owner-Review des Branches.
- **T11 · Abnahme live (startet nach T12; Merge auf main ist Vorbedingung und läuft 07.09.)** — welsch per Control-Plane upgraden, Owner-Auftrag im Chat, Router-Ereignis und Gedächtnis sichtbar, zweiter ähnlicher Auftrag geht an dasselbe Wesen.

- ~~T1 · PR-2~~ → Working W2.
- **T2 · PR-3 „Das Zuhause der Crew“ (Neubau `modules/ctox`)** — Trigger: PR-2 gemerged. Brief-Entwurf steht: `docs/dev/crew-cockpit-brief-pr3.md` (drei Ansichten Zuhause/Arbeitsplatz/Warteschlange + Profil-Drawer, gemeinsame Wesen-Komponente `shared/crew-creature.js` mit neun Zuständen, Gestaltungsregeln, Abnahme mit Browser-Beweis auf thesen und Kimi-UI/UX-Review). Wird nach PR-2 um die endgültigen Crew-Verträge ergänzt.
- **T3 · PR-4 „Crew-Leiste“ (Rework `shared/business-chat.js`)** — Trigger: PR-3 in Review (gemeinsame Wesen-Komponente steht). Brief folgt.
- **T4 · PR-5 „Tickets“** — Trigger: PR-1 gemerged (Routing-/Lease-Felder projiziert). Brief folgt.
- **T5 · Unabhängiges Review (Kimi · Cyber & Review) je PR** — Trigger: PR offen. Fokus: Policy-Gates serverseitig, keine HTTP-Datenbrücke, Retention, unbegrenzte `find()`.
- **T6 · Abnahme live** — Trigger: PR-3/4 auf einer Tenant-Instanz (thesen: `currentSlot: null`, src/ ist live; welsch: Slot 0.1.44 aktiv → Slot-Schnitt aus main nötig). **Vorbedingung neu (06.09.):** thesen-Daemon läuft `branch-main-20260905T072559Z` (vor PR-1) ohne Cockpit-/Crew-Tabellen → vor dem Beweis `ctox upgrade --dev` auf thesen über die Control-Plane (wie welsch am 05.09.), dann Datei-Deploy der Shell (src/ live). Browser-Beweis: eine echte Aufgabe, Fortschritt sichtbar in Text und Wesen, Antwort erscheint, Abbruch funktioniert.

### Backlog + Owner

- ~~OWNER: B7~~ → entschieden und umgesetzt (D20c): Crew-Leiste im Einsatz, Zuhause im Leerlauf.
- ~~OWNER: Sichtbarkeit `ctox_harness_status` für Rolle „User“?~~ → entschieden (D20b): Status ja, Runs/Ereignisse/Learnings nein.
- **OWNER: Modell-Gateway auf welsch antwortet nicht (Responses-Timeouts 45–60 s)** — Anmeldung/Abo des CLIProxy-Zugangs prüfen (Workjet → Anbieter → Neu anmelden, bzw. `ctox`-Provider-Login auf welsch). Bis dahin: Router-Fallback, kein Lernen mit Inhalt, Tasks scheitern.
- **OWNER: Sol (Hard Tasks) startet nicht: `skill_runtime_unavailable`** — Web-Research-Skill-Laufzeit im Workjet prüfen; der Sync-Stall-Brief liegt bereit.
- **OWNER: B7 aus dem Kimi-Review — Crew zu Hause während ein Einsatz läuft?** (erledigt, siehe oben) Heute: läuft ein Task, verschwindet das Zuhause vollständig (nur die Chat-Leiste zeigt die anderen). Vision §2 sagt „die anderen sind zu Hause“. Mein Vorschlag: schmale Crew-Leiste unter der Kennzahlenzeile (Wesen 28 px + Name, Ausdruck live), Zuhause-Karten nur im Leerlauf. Bis zur Entscheidung bleibt es beim Entweder-oder.
- **T11 blockiert durch Anmeldung:** welsch zeigt die Login-Maske; ich darf keine Zugangsdaten eingeben. Für die Live-Abnahme (Auftrag im Chat, Router-Ereignis, Gedächtnis, zweiter ähnlicher Auftrag → dasselbe Wesen) braucht es eine angemeldete Sitzung des Owners im In-App-Browser oder in Chrome.
- **Entscheidung Fable (06.09. 08:20): Live-Abnahme für PR-3/4 auf welsch (Demo), nicht auf thesen (Kundeninstanz).** welsch per Control-Plane auf aktuellen main upgraden (Muster `ctox-dev/output/welsch-office-upgrade-core-20260905.sh`, Owner hat welsch zum Testen freigegeben) und `currentSlot: null` setzen, damit src/ live ist; danach Shell-Datei-Deploy. Zeitpunkt: nach Merge von PR-3. Bis dahin lokaler Beweis im echten Shell-Fenster mit Fixture-Daten. thesen bleibt unangetastet (OWNER, falls dort gewünscht).
- **OWNER: Sichtbarkeit `ctox_harness_status` für Rolle „User“?** Vorschlag: Admin + Founder sehen alles; User sieht nur eigene Tasks und Crew-Namen, keine Kosten. Bis Entscheidung wird Vorschlag umgesetzt.
- ~~OWNER: Lokale Instanz reparieren~~ → erledigt, siehe D4 (Owner hat mir die drei Punkte übertragen, 05.09. 12:46).
- ~~OWNER: welsch-Upgrade~~ → geklärt, siehe D5.
- ~~B3 Workjet-Slider/Stundenzettel~~ → geklärt, siehe D6.
- B1 · Abbruch eines laufenden Turns (Slice-Kill) existiert serverseitig nicht; nur `ctox stop --force`. In PR-1 als bounded Stretch (`ctox.queue.abort_turn`), sonst Folge-PR.
- B2 · Tickets zählen nicht zum Queue-Druck (`pending_queue_task_count_uncached` zählt nur `channel='queue'`). Entscheidung in PR-5: zählen oder explizit „zählen nicht“ dokumentieren.
- B4 · **main ist rot, unabhängig von PR-1:** (a) vier `rxdb_peer`-Tests scheitern auf `2d88f2267` (Baseline oben); (b) CI `assert-app-platform-freeze.mjs` meldet `modules/explorer/index.js` (auf Basis reproduziert); (c) CI Desktop-E2E scheitert an `npm audit` (fast-uri high, xmldom moderate); (d) **alle fünf CLI-Checks** am PR-Head rot wegen `E0282` in `office-engine/src/ops.rs:293` (`text.as_ref().is_empty()`), eingeführt mit main-Commit `0b4b14a25` (fix(office), 05.09. 14:47), auf `origin/main f9024b4b8` (20:54) weiterhin vorhanden; main-CI-Run `33984218175` (c93191408) = failure. Der PR fasst office-engine nicht an; CI läuft auf dem Merge mit main. (e) android-Job: Workflow führt ein Python-Heredoc unter `/usr/bin/sh` aus (`import: not found`), Workflow-Bug auf main. **E0282 lokal reproduziert (21:50, `baseline/main-check.log`, Exit 101) — und zeitgleich von der Office-Sitzung auf main behoben: `ac13948d7` „fix(office): disambiguate metadata text emptiness“ (`text.is_empty()`), identisch mit meinem vorbereiteten Fix. Kein eigener PR nötig; Branch und Worktree `main-fix` entfernt. Der nächste Push des Workers lässt die PR-CI gegen den neuen main laufen.** Freeze-Check und npm audit bleiben Backlog.
- B7 · `mission::channels::tests` und `rxdb_peer::tests` sind im Gruppenlauf SQLite-Lock-flaky (517 „Cannot promote read transaction“, `SQLITE_BUSY`), isoliert grün; auch der Worker musste 146 Peer-Tests einzeln fahren. Testinfrastruktur-Aufgabe: eigene DB je Test oder `busy_timeout` in den Fixtures.
- B6 · Coding-Turns (`ctox.coding.turn`, Pi-Sidecar als Control-Command ohne Queue-Lease) haben nach PR-2 kein Crew-Mitglied. Folge-PR: Coding-Turn als Einsatz eines Mitglieds führen (Zuordnung, Stundenzettel-Zeile, Learnings), ohne die Queue-Semantik zu ändern. Relevant für die Coding-Agents-App und PR-3.
- B5 · `/Volumes/tmp` zu 100 % voll (05.09. 19:16, 1,3 GiB frei): Cargo-Targets fremder Sitzungen `sync-core-offensive` 22 GiB, `thesen-queue-20260905` 16 GiB, dazu Modellverzeichnisse. Nicht meins, nicht gelöscht; Worker auf Systemplatte umgeleitet. OWNER: aufräumen oder freigeben.
- B3 · Workjet-„Slider“ zur Worker-Personalisierung und „Stundenzettel“: im lokalen Checkout `claude-workjet` (WorkerEditorView.swift, Models.swift) gibt es nur Name/Rolle, Modell, Reasoning-Stufe (Choice-Buttons), Aufgabe, Skills-Toggles; weder Slider noch Stundenzettel (`rg -i stundenzettel|timesheet` → 0 Treffer). Beide Konzepte werden hier eigenständig definiert (Soul-Achsen als Slider, Stundenzettel = Run-Einträge + Rückblick je Mitglied).

## Befunde (verifiziert; Zeilenangaben aus den Audits, Stichproben gegengelesen)

### A · Der Harness ist für den Browser fast unsichtbar (Ursache aller drei Oberflächen)

1. `ctox_runs` ist eine leere Hülle: Schema, Registry, MCP-Reader existieren, **kein Writer** (`grep -rl ctox_runs src/core` → nur `mcp_channel.rs`).
2. Per-Tool-/Token-Ereignisse sind durable (`ctox_harness_flow_events`, Writer `service.rs:5292-5305`), erreichen den Browser aber nur als 12-Ereignis-Blob **einer** Kette in `ctox_runtime_settings.harness_flow` (`harness_flow.rs:403-412`), stamp-gated 3 s → 1800 s idle (`rxdb_peer.rs:877-891`), **admin-only** (`policy.rs:317-324`).
3. `ctox_queue_tasks` projiziert weder `lease_expires_at`, `lease_worker_id`, `failure_class`, `failure_attempt_count`, `retry_not_before`, `hold_reason` noch `wait_entity_*` (`QueueTaskView` `channels/mod.rs:281-299`). „Blockiert“ ist deshalb nie „wartet auf X bis T“.
4. Service-Liveness im Browser = PID-Probe (`store_sync_turn_auth.rs:292-317`): `busy`, `worker_active_count`, `worker_phase`, Kapazität, Arbeitszeitfenster, Druckzustand — alle nicht projiziert.
5. Kosten/Modell je Turn nur in `api_model_cost_events` (CLI `ctox cost`), keine Projektion.
6. Steuerung aus dem Browser: create/update/delete/`ctox.command.cancel`. Nicht erreichbar: release, block, capacity, pause, spill/restore, abort (`queue.rs:33-49`, `service_queue_capacity.rs:23-33`).
7. Keine Retention: `ctox_queue_tasks` wächst unbegrenzt (946 Zeilen lokal), repliziert an jeden Browser; nur Tombstones werden nach 7 Tagen gefegt.

### B · CTOX-App (`modules/ctox`, 4874 Zeilen JS)

1. Rendert die **Spezifikation** des Harness (statisches 16-Knoten-Poster mit Enum-Namen `AwaitingReview`, `ReviewUnavailable`, hart kodierte x/y `index.js:411-440`) statt seinen **Zustand**.
2. Nutzt `commandBus.cancel` nie; kein Stop-Knopf. Vier Writes gesamt (`index.js:1155, 2977, 3025, 3068`).
3. `lease_owner`, `status_note`, `error`, `workspace_root`, `ctox_runs` nie gerendert; Fehler zeigen generisches „Aktion fehlgeschlagen“.
4. `find().limit(200)` ohne Selektor/Sort, dann `slice(0,20)`: bei >200 Commands fehlen die neuesten (`index.js:3944-3954`). Live: `business_commands` (190 Docs) wird ~1×/s vollständig geholt (Konsole welsch).
5. `main.innerHTML = …` alle 4 s (`index.js:1888`): Fokus und Drag gehen verloren; alle Element-Refs veralten (live: Klick-Refs nach 2 s ungültig).
6. Redaktion per Regex (`hasSensitiveUiLeak` `index.js:4559-4620`) blendet praktisch jeden Coding-Prompt aus und deaktiviert die Textarea (`:2884`).
7. Fokus-Task aus dem Chat (`sessionStorage['ctox.businessOs.focusTask']`) wird nie gelöscht → Auswahl springt alle 4 s zurück (`index.js:632, 689, 2698-2705`).
8. Alle vier Loads `.catch(() => [])` (`index.js:614-619`): DB kaputt = „Keine Arbeit hier“. Live lokal: „Tasks werden synchronisiert“ ohne Ende.
9. Web-Stack-Panel (Sales-Browser-Secrets) in der Harness-Ansicht (`index.js:3888-3963`).
10. Zähler-Widerspruch live: „Arbeitet (4)“ bei vier Tasks mit Chip „Fehler“; „Zeit 3178 m“ für einen fehlgeschlagenen Task.
11. i18n doppelt (Inline-Tabelle 184 Keys + `locales/*.json`), 50 tote Keys; englische Lane-Labels im deutschen UI, deutsche `aria-label` im englischen.
12. `module.json:34` verspricht „runtime scopes“ links — existiert nicht.
13. Tests (`test.js`, 28): überwiegend Markup-String-Regressionen; kein Write-Pfad getestet.

### C · Crew-Leiste (`shared/business-chat.js`, 8332 Zeilen, davon ~3000 CSS im Template-String)

1. Antwort kommt **einmal, terminal**, über `business_commands.outbound_text` (`store_projections.rs:32-79`); Zwischenstände, `retry_wait`, Review-Urteile erreichen den Chat nie (live bestätigt, F-D2).
2. Fortschritt nur als `title`-Tooltip (`executionProgressTooltip` `:2320`); `executionProgressHeaderHtml` gibt `''` zurück (`:2310`) und wird trotzdem aufgerufen.
3. Composer wird bei `queued|running|blocked` entfernt (`:2515`): kein Nachsteuern, kein Abbruch (`commandBus.cancel` 0×).
4. Wesen-Identität = Hash der Command-ID → Name/Form/Farbe zufällig, wechselt mitten im Gespräch (`:1952-1978`, live Tavi → Milo). Es gibt keine Crew-Mitglieder als Entität.
5. Deep-Link in die CTOX-App per `window.dispatchEvent` auf dem Shell-Window (`:3947`); Modul hört im eigenen iframe (`modules/ctox/index.js:4030`) → wirkt nur bei Remount.
6. Drei konkurrierende Timeouts (30 s `app.js:7514`, 12 s `:3535`, 3,5 s `:3818`), zwei Autoren für `business_chats.messages` (Browser `:4224`, Server `store_projections.rs:268`), Merge per `mergeChatMessages` (`:4480`).
7. `hydrateChatsFromRxDb` = `find().exec()` über alle Chats ohne Selektor/Limit (`:4336`).
8. Keine i18n-Anbindung, 64 nur-deutsche Literale (`:2461-2545`, `:3398-3481`, `:3908-3929`); Server-Literal deutsch (`store_projections.rs:253`).
9. Dialog-Host = globaler `window.__ctoxBusinessDialogHost` des zuletzt gemounteten Moduls (`dialogs.js:8-16`, `app.js:6203-6207`).
10. Zweite parallele Sende-UI in `app.js:14353-14680` mit eigenen Labels und eigenem Dispatch.

### D · Tickets (`modules/tickets`, 1415 Zeilen; Server `src/core/mission/tickets/`)

1. Domäne ist überbaut, aber kohärent: 17 Case-States (`case_state.rs:23-39`) und 21 Work-Item-States (`work_item_status.rs:3-23`) ohne Mapping; Fälle entstehen **nur** über `create_dry_run` (`cases.rs:29`), das kein Business-Command erreicht → Approval/Verification/Writeback-Hälfte ist aus dem Browser unerreichbar (lokal: `ticket_cases` 0, `ticket_approvals` 0, `ticket_self_work_items` 6).
2. Self-Work-Join kaputt: `remote_ticket_id === ticketKey` vergleicht `LT-…` mit `local:LT-…` (`index.js:1252-1254`) → immer „Kein Self-work verknüpft“.
3. Filter auf `remote_status` (Fremdsystem) statt CTOX-Zustand (`index.js:226-234`); Zustände unübersetzt snake_case → Title Case (`:1362`).
4. 12 unbegrenzte `find()` je Refresh (`index.js:573`, alle 80 ms debounced).
5. `module.json:63` „Read-only“ ist falsch: 9 Write-Commands (`index.js:1001-1081`); Permission-Scope `support` statt `tickets` (`command_plane.rs:819-822`).
6. Tickets zählen **nicht** zum Queue-Druck; Ticket-Arbeit ist vom Parallel-Pool ausgeschlossen (`service_queue_capacity.rs:42-55`); Spill-Scorer nur per CLI (`queue.rs:418, 432`).
7. Routing-Felder (`failure_class`, `retry_not_before`, `hold_reason`, `lease_owner`) geladen und verworfen (`index.js:852`).

### E · Gestaltung (Nutzerbefund, live bestätigt)

Große Leerflächen, Text-Labels statt Hierarchie („nicht erfasst“ ×5, „keine Live-Tokenmetriken“ ×16), Zustände nur über Farbe/rote Chips; die Wesen tragen keine Bedeutung (Zustand ≠ Ausdruck; X-Augen existieren im Code `crewEyesMarkupForMode('failed')`, hängen aber am zufälligen Chat-Hash statt am Task-Zustand eines echten Mitglieds).

## Zielbild (Entscheidungen)

1. **Harness bleibt seriell, alle Review-/Validierungs-Gates bleiben.** Neu ist ausschließlich: Sichtbarkeit (Projektionen), Steuerbarkeit (Control-Commands) und Identität (Crew-Mitglieder als durable Entität mit Seele, Lebenslauf, Learnings; Auswahl nach Passung beim Lease).
2. **Server-autoritativ, kein HTTP-Datenpfad.** Alles Neue sind RxDB-Projektionen mit Retention + Indizes und `EXACT_CONTROL_TYPES`-Commands hinter `enforce_command_policy`.
3. **Eine Wesen-Komponente** für Chat, Cockpit und Tickets; Ausdruck = Harness-Zustand (wartet · aufgewacht · denkt · arbeitet mit Werkzeug · prüft · wartet auf X · gescheitert (X-Augen) · fertig), nicht Farbe.
4. **Die CTOX-App wird das Zuhause der Crew:** Die Mitglieder leben dort; wer den aktuellen Task hält, ist „im Einsatz“ (Arbeitsplatz-Ansicht mit Plan-Schritten, Live-Aktivität, Runs: Modell, Tokens, Kosten, Dauer, Urteil, Grund, Steuerung), alle anderen sind zu Hause (ruhen, warten, lesen ihre Learnings). Jedes Mitglied führt einen **Stundenzettel** (je Run: Beginn, Ende, Task, Ergebnis, Aufwand, eigener Rückblick), sichtbar im Profil; Statuskopf = Harness läuft/pausiert, Kapazität, Druck, Warteschlange. Kein Poster, keine Leerflächen.
5. **Chat = Steuerkanal:** Composer bleibt offen, Zwischenstände/Fragen/Urteile erscheinen als Nachrichten des Mitglieds, Abbruch/Retry/Priorität inline, ein Klick öffnet den Task im Cockpit (postMessage).

## Umgebungsfallen

- **Lokales `ctox upgrade --dev` (06.09. 08:50–07.09. 05:11) scheiterte im Rust-Build:** `rxdb_peer.rs` gegen `JsonSchemaType` aus `~/.local/state/workjet-launchpads/ctox-push-wirebudget/src/core/rxdb` (fremdes Launchpad einer anderen Sitzung) aufgelöst → E0599 `as_deref`. Der Installer löschte das Release-Verzeichnis danach; die Ursache des Pfad-Overrides (Patch/Symlink) ließ sich nicht mehr belegen. Konsequenz: Screenshots kommen aus einem selbst gebauten Branch-Binary (`~/.cache/ctox-opt-target/debug/ctox`), nicht aus dem Installer. Zehn Stunden Kompilierzeit unter Last 40–80 auf 10 Kernen; Systemplatte fiel dabei von 89 auf 32 GiB frei (fremde Targets), `~/.cache/ctox-upgrade-target` gelöscht.

- Codex-Worker-cwd ist `~/Documents/ctox` (137 hinter / 233 vor origin/main). PR-Arbeit nur in Worktrees unter `/Volumes/tmp/worktrees/ctox/<branch>` von `origin/main`; nie den Checkout selbst editieren.
- `/Volumes/tmp` ist klein (ENOSPC am 02.09.); Cargo-Target vor Builds mit `df -h` prüfen, sonst `~/.cache/ctox-crew-cockpit-target`. Target nach Gebrauch löschen (≈13 GiB je Testbau).
- `cargo fmt` nur je Datei (`rustfmt <datei>`), nie paketweit.
- RxDB-Wire-Contracts: Fixtures in `src/core/rxdb/tests/fixtures/*.json` ändern, beide Seiten regenerieren, `dist/ctox-rxdb-js.mjs` nie von Hand; `?v=`-Buster in `shared/rxdb-runtime.js` bumpen.
- Shell-Stempel: genau EIN `-shell-v2-`-Token in `index.html`/`app.js` (`grep -o '?v=[^"]*' index.html app.js | grep -- -shell-v2-`), sonst 409 beim Boot.
- welsch: Slot 0.1.44 aktiv → Datei-Deploy nach src/ unsichtbar; thesen: `currentSlot: null` → src/ live.
- In-App-Browser (Claude): Koordinaten-Klicks im 800×500-Frame treffen die Business-OS-Shell nicht zuverlässig; Refs nach Re-Render sofort veraltet → Interaktionen per `find`-Ref direkt danach oder per JS auslösen. Gegen die LOKALE Instanz baut er keinen WebRTC-Datenkanal auf (nur mDNS-Host-Kandidat); gegen welsch funktioniert es über TURN. Lokale Sync-Beweise also nur in Chrome/Safari führen.
- Lokale Instanz (nach D4): `current` → `business-os-shell-v0.1.44`, Wrapper `~/.local/bin/ctox` exportiert `CTOX_STATE_ROOT=releases/…/runtime` (Symlink auf `~/.local/state/ctox`, also derselbe State-Root); das Release-eigene `bin/ctox` verweist auf ein nicht existierendes `bin/ctox-real` und ist damit unbrauchbar, der echte Binary liegt in `~/.local/bin/ctox-real` (04.09. 19:12).
- **Ein-Revisions-Vertrag der Datenebene (main seit 06.09., `rxdb/tests/data-plane-guard-smoke.mjs`):** Shell-Einstieg (`index.html` app.js?v=), `APP_BUILD`, die Loader-Buster in `db.js`/`sync.js`, die Bundle-URL in `rxdb-runtime.js` und `MULTI_TAB_COORDINATOR_EPOCH` müssen EIN Stempel sein. Ein eigener Bundle-Buster (wie mein `20260906-crew-memory-v1`) bricht den Wächter. Beim Stempeln alle Vorkommen des alten Stempels in diesen fünf Dateien ersetzen.
- **`bundle-reproducible-smoke` ist auf main und Branch rot aus demselben Grund:** der Smoke baut das Bundle relativ zu `src/apps/business-os` (Kommentarpfade `rxdb/src/...`), das eingecheckte dist ist aus dem Repo-Wurzelverzeichnis gebaut (`src/apps/business-os/rxdb/src/...`). Kein Inhaltsdrift; Bundle weiter aus der Repo-Wurzel bauen wie in `docs/ctox-rxdb.md`.
- **Lokale Instanz mit eigenem Binary fahren (07.09.):** launchd-Dienst `com.metric-space.ctox.service` mit `launchctl bootout gui/$UID/…` stoppen (KeepAlive!), Wrapper-Env (`~/.config/ctox/business-os.env`, `business-bridge.env`) sourcen. `CTOX_STATE_ROOT` reicht NICHT: Kern-DBs, Secrets, Socket und Peer-Status landen unter `$CTOX_ROOT/runtime` — der Release-Baum löst das mit einem Symlink `runtime -> ~/.local/state/ctox`; im Worktree denselben Symlink anlegen, sonst entsteht eine neue Instanz (eigene Instance-ID, leeres `ctox.sqlite3`) mit dem echten RxDB-Store dahinter. Danach `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.metric-space.ctox.service.plist` zum Wiederherstellen.
- **Shell-Slot schlägt `src/`:** ein aktiver Slot (`ctox business-os shell-update status` → `activeVersion`) liefert seine eigene Shell; ein Daemon-Upgrade ändert daran nichts. Neue Shell = Release-Tag `business-os-shell-v*` auf main pushen (Workflow baut ~2 min), dann `shell-update stage --version X`, `activate`, und `systemctl --user restart ctox.service` (Phase bleibt sonst `restart`). Nach `app.js`-Änderungen immer die Cache-Revision bumpen, sonst hält der Browser die alte Datei bis zu vier Stunden.
- **Generierter Modul-Katalog in `app.js`:** `module.json`-Collections wirken erst nach `node scripts/generate-module-registry.mjs`; die Shell legt den generierten Block über die native Katalogzeile.
- **`runtime`-Symlink und git:** `git checkout -- runtime/<datei>` (die Galerie-HTML ist versehentlich versioniert) ersetzt den Symlink still durch ein echtes Verzeichnis → beim nächsten Start entsteht wieder eine Fremdinstanz (neue Instance-ID, leere DBs, Browser sieht 0 Tasks). Vor jedem Dienststart `[ -L runtime ]` prüfen; die versionierte Datei mit `git update-index --skip-worktree runtime/ctox-creature-animation-gallery.html` stilllegen.
- **Debug-Binary ist für die 2-GB-Instanz zu langsam:** nativer Peer bei 99 % CPU, `desktop_file_index`-Loop 204 s, Browser-`masterChangesSince` läuft in den Timeout („WebRTC replication failed … Timed out“, Watchdog „backlog without progress“). Für Beweise mit echten Daten Release-Build (`cargo build --release -p ctox --bin ctox`, Target `~/.cache/ctox-opt-release-target`).
- **Browser für lokale Beweise:** der In-App-Pane und headless Chromium bekommen gegen die lokale Instanz keinen stabilen Datenkanal; die Claude-in-Chrome-Erweiterung war nicht verbunden. Funktioniert: eigenes Chrome mit CDP (`open -na "Google Chrome" --args --remote-debugging-port=9222 --user-data-dir=~/.cache/ctox-crew-chrome …`) und Playwright `connectOverCDP` (Skripte `src/apps/business-os/output/crew-shots/*.mjs`, aus dem business-os-Verzeichnis starten, damit `playwright` auflöst).
- welsch-Administration nur über `~/Documents/ctox-dev`: `npx tsx output/run-remote-welsch.ts <lokales-skript.sh>` (Skript wird per SFTP hochgeladen und mit bash ausgeführt). Kein `timeout` auf macOS; das Bash-Tool-Timeout nutzen.

- **Systemplatte lief 06.09. 04:55 auf 0** (mein Review-Target ~40 GiB + Worker-Target 63 GiB + Kaltbau für Baseline): Bash-Tool fiel mit ENOSPC aus; Rettung über `Monitor`-Kommando (`rm -rf ~/.cache/ctox-review-pr1-target`), danach 42 GiB frei. Regel: vor jedem Build `df -h /` und Ziel-Target-Größe schätzen; nie zwei Targets (>30 GiB) parallel auf der Systemplatte; Baseline-Messungen, die einen Kaltbau brauchen, an den Worker mit vorhandenem Target delegieren.

## Fehlermuster (eigene)

1. Klick-Beweis ohne DOM-Prüfung: zwei Klicks „wirkten“ nicht, weil der Frame falsch war (2×). Immer DOM-Zustand nach Aktion lesen.
2. Auf `find()`-Refs vertrauen, während das Modul alle 4 s neu rendert (1×).
4. Zweites Cargo-Target auf der Systemplatte neben dem Worker-Target angelegt und zusätzlich einen Baseline-Kaltbau gestartet → Platte voll, Sitzung 5 Minuten handlungsunfähig (1×). Vorher `df` und Worker-Target-Größe prüfen.
3. `mv symlink.new current` auf macOS, wenn `current` ein Symlink auf ein Verzeichnis ist: `mv` legt den neuen Link IN das Zielverzeichnis. Richtig: `ln -sfn <ziel> current` (1×; Streuner in `releases/workjet-sync-efe5ef1a7/current.new` entfernt).

## Evidenzkarte

- Audits (Subagenten-Berichte, im Chat-Transkript dieser Sitzung; Kernaussagen oben übernommen und stichprobenartig geprüft).
- Live-Command welsch: `business_commands/cmd_b964f8bc-2130-42df-80f8-53e7fe9b2961` (RxDB im Browser), Task `queue:system::41c33261fb7b96906277e159`.
- Lokale Maintenance: `http://127.0.0.1:8765/api/business-os/ctox/maintenance`; Peer-Status `~/.local/lib/ctox/releases/business-os-shell-v0.1.44/runtime/business-os-rxdb-peer.status.json`.
- Vision/Onboarding für alle Mitbauenden: `docs/dev/crew-cockpit-vision.md` (Commit e96434448; nach Owner-Hinweis 13:20 ergänzt: der Worker bekam vorher nur den Vertrag, nicht das Wofür).
- Briefs: `docs/dev/crew-cockpit-brief-pr1-pr2.md`, weitere folgen im selben Verzeichnis. Jeder Brief verweist zuerst auf die Vision.
- Board-Artefakt: https://claude.ai/code/artifact/9b10debc-a89e-443d-a93e-8a69d8c86d0b (stabile URL, bei jedem Update dieselbe Datei neu veröffentlichen).
