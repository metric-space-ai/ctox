# Legacy marker file

This file exists to satisfy the CTOX root detection check in `src/core/main.rs`.
Remove it when the runtime no longer uses `contracts/history/creation-ledger.md`
as part of `looks_like_ctox_root`.
