# Accepted Profile Update Proposals

`tools/propose_accepted_profile_update.sh` writes accepted-profile update
proposals here.

These records are intentionally separate from `accepted_profile.env`: a passing
promotion gate can create a proposal, but applying the env change remains an
explicit review step.
