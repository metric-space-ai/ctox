# RxDB → Rust Port — Lookup Table

Upstream: `pubkey/rxdb` tag **16.20.0** @ commit `c69c94bb107a4d36dbf989de0e5f17081dcf7718`

Vendored at `src/core/rxdb/vendor/rxdb-16.20.0/`. See `vendor/rxdb.version` for the pin.

**Scope:** CTOX-as-WebRTC-peer MVP. Browser side now moves to a CTOX-owned
`ctox-rxdb-js` hard fork built exactly for Business OS data-plane needs. The
upstream JS snapshot remains a provenance baseline, not a compatibility ceiling.

## ctox-rxdb-js Hard Fork

The JavaScript RxDB side is now treated as a hard fork, not as an immutable
upstream bundle. CTOX Business OS is the source of truth; upstream RxDB
compatibility is secondary to deterministic WebRTC replication, native peer
restart recovery, typed diagnostics, protocol/schema parity with `rxdb-rs`, and
file/chunk materialization semantics. The next fork generation is
`ctox-rxdb-js`: plain Browser ESM, native IndexedDB, native WebRTC, and
no package-manager or external runtime dependency surface.

Fork control files:

- `js-fork/README.md`
- `js-fork/ctox-rxdb-js.manifest.json`
- `js-fork/bundle-contract.json`
- `js/README.md`
- `js/manifest.json`
- `tools/assert_js_fork_strategy.js`
- `tools/assert_js_fork_bundle_contract.js`
- `tools/assert_js_fork_release_evidence.js`
- `tools/assert_js_no_package_manager.js`

Required fork contracts:

- `ctox-rxdb-protocol-v1` protocol/capability handshake;
- schema hash exchange and deterministic mismatch rejection;
- peer generation/session tracking for reconnect and stale peer invalidation;
- typed replication/signaling errors for Advanced Status;
- file chunk generation/hash/materialization integrity;
- checkpoint and initial-sync readiness shared with `rxdb-rs`.
- WebRTC-native realtime frame protocol below RxDB collection APIs:
  bounded frame size, pacing, ACK, retry, resume, priority scheduling, and
  `bufferedAmount` / low-watermark backpressure;
- no app-level transport workarounds: Business OS apps use collections and file
  APIs only, while `ctox-rxdb-js` and `rxdb-rs` own DataChannel packet sizing,
  latency, and stream continuity.

## CTOX Integration Layer

This sits outside the rxdb-rs file-port matrix, but it is part of the target data plane.

| Wave | Status | Area | Files | Notes |
|---:|---|---|---|---|
| 525 | done | Current WebRTC optimization E2E verification | `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/scripts/sync-business-os-shell-assets.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Verified the current RxDB/WebRTC optimization state and repaired ctox-dev public Business OS asset drift. `guard:webrtc-frame-stream` initially failed because `public/business-os/rxdb/dist/ctox-rxdb-js.mjs` was stale and lacked the current `ctox-rxdb-frame-v1` frame-stream code; `sync:business-os-shell` resynced it from Core, and the public bundle now byte-matches `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`. Verified `guard:webrtc-frame-stream`, `guard:business-os-shell-assets`, `guard:rxdb-data-plane`, `smoke:business-os-shell`, and the focused direct E2E smoke. The direct E2E smoke passed `rust-to-browser`, `browser-to-rust`, `command-browser-to-rust`, `business-os-ui-regression`, `workspace-agent-artifacts-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser` on first attempt, including 103 chunks / 1,260,036 bytes through File Viewer after native peer restart. |
| 524 | done | Cloudflare cost guard CI and TURN cache | `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/src/index.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-cloudflare-cost-guard.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-route.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Pinned the post-limit Cloudflare cost controls into CI and reduced repeated TURN credential churn. `guard:cloudflare-cost` now rejects broad public website Worker routes, public registry dumps, missing strict Signaling route scope, missing service rate limiting, and missing TURN credential caching. The Production Readiness workflow runs that guard directly. The `/turn-ice` route caches successful Cloudflare Realtime TURN credentials per edge key for 80% of the one-hour TTL, and the route smoke proves two equivalent credential requests make only one mocked upstream TURN API call. Deployed `service` Worker version `5ee4e633-12fd-4003-827c-53c49d7c5561`; live unauthenticated `/registry/dump` and `/turn-ice` fail closed while `/registry/hc` stays public healthy. |
| 523 | done | Cloudflare cost guard | `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/wrangler.toml`, `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/src/index.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Removed broad `metricspace.org/*` and `www.metricspace.org/*` Worker routes after the daily Cloudflare Worker limit was exhausted. A live registry dump showed scanner traffic for `.env`, WordPress, phpinfo, Swagger, and other random paths had been routed through the `service` Worker and stored as registry endpoint state. The deployed Worker is now scoped to explicit API routes plus `ctox.dev/turn-ice*`, registry debug/mutation endpoints require the operator key, and `guard:production-ops` rejects future broad public website Worker routes. Deployed `service` version `a3a16af5-e036-4108-8fbe-a5039b00b546`. |
| 522 | done | Production ops closure evidence | `/Users/you/Documents/ctox-dev/scripts/capture-live-remote-business-os-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/check-live-remote-business-os-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/check-turn-edge-live-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/evidence/live-remote-business-os-evidence-reset104-20260526.json`, `/Users/you/Documents/ctox-dev/docs/evidence/production-ops-live-artifacts-reset104-20260526.json`, `/Users/you/Documents/ctox-dev/docs/evidence/production-ops-closure-manifest-reset104-20260526.json`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed PRD-011 with retained live Remote Business OS evidence. The capture now probes an already-running native `/api/business-os/status` before starting a server, so live VPSes are not failed for occupied ports, and evidence tails remove sensitive WebRTC room fields before retention. The retained artifact proves SSH reachability, native Business OS status readiness, `ctox business-os peer status`, WebRTC transport, no HTTP bridge, and a running native RxDB peer. Strict retained-artifact validation and strict aggregate production-ops readiness both pass with TURN, Remote Business OS, and health-alert evidence; the closure manifest retains SHA-256 references for the final artifact set; and the live TURN readiness probe accepts unauthenticated `429` rate limits as the same fail-closed edge proof as `401`. |
| 521 | done | Live health-alert production evidence | `/Users/you/Documents/ctox-dev/docs/evidence/live-health-alert-evidence-reset103-20260526.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed the live health-alert blocker for PRD-011. Created a dedicated ctox.dev live owner session, stored it as GitHub `CTOX_DEV_LIVE_SESSION_TOKEN`, and captured tenant `gpu1-a6000` health-alert evidence with active/retained `peer_offline` and `signaling_unavailable`, critical severity, stored retained events, email delivery `delivered`, and webhook state `disabled` matching the live tenant configuration. Production ops live artifact validation now reports TURN and health alerts OK; only remote Business OS SSH evidence remains blocked. |
| 520 | done | Authenticated Signaling admin evidence | `/Users/you/Documents/ctox-dev/scripts/rotate-signaling-admin-token.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-signaling-hardening.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/evidence/signaling-admin-token-rotation-reset102-20260526.json`, `/Users/you/Documents/ctox-dev/docs/evidence/live-signaling-admin-reset102-20260526.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added repeatable Signaling admin-token rotation and closed authenticated admin live evidence. The rotation updates Cloudflare Signaling, GitHub Actions, and Vercel Production only with `--apply`, redacts output, and retains only a SHA-256 token reference. Rotated a new token, redeployed Cloudflare Signaling Worker `a5516632-e0cc-48bd-b1d1-7e4d04716940`, captured authenticated `/admin/revocations` and `/admin/diagnostics` evidence, and redeployed ctox.dev Production `dpl_EXAMPLE`. GitHub now has `CTOX_SIGNALING_ADMIN_TOKEN` and `CTOX_TURN_EDGE_KEY` configured. |
| 519 | done | Strict Signaling production rollout | `/Users/you/Documents/ctox-dev/docs/evidence/live-signaling-strict-reset101-20260526.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness-dashboard.html` | Completed the coordinated public rollout: ctox.dev Production deployment `dpl_EXAMPLE` is aliased to `https://ctox.dev`, then Cloudflare Signaling Worker version `a56b67d0-11c9-41b4-aeb5-d73943b11a64` was deployed to `signaling.ctox.dev/*`. Retained live public evidence proves `strictControlPlaneRequired:true`, `legacyAnonymousRooms:false`, typed `ctoxError` codes for missing capability, instance/protocol/token/role/rate/role-limit failures, and successful presence with browser, native CTOX, and desktop shell roles. `https://ctox.dev/business-os` still returns `200` `no-store` HTML. |
| 518 | done | Strict Signaling control plane | `/Users/you/Documents/ctox-dev/cloudflare-signaling/src/index.js`, `/Users/you/Documents/ctox-dev/cloudflare-signaling/README.md`, `/Users/you/Documents/ctox-dev/lib/signaling-metadata.ts`, `/Users/you/Documents/ctox-dev/scripts/assert-signaling-hardening.mjs`, `/Users/you/Documents/ctox-dev/scripts/capture-live-signaling-control-plane-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-live-signaling-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-live-signaling-evidence-gate.mjs`, `/Users/you/Documents/ctox-dev/docs/ctox-peer2peer-signaling.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Removed Signaling legacy semantics in code after backward compatibility was explicitly dropped. The worker now rejects joins without strict control-plane metadata, advertises `strictControlPlaneRequired` and `legacyAnonymousRooms:false`, and delivers signal frames only for the explicitly requested shared room. ctox.dev now attaches `protocol=ctox-rxdb-protocol-v1` in generated Signaling URLs, and live evidence gates require missing-capability rejection. Verified syntax, `guard:signaling-hardening`, `smoke:live-signaling-evidence-gate`, Business OS pairing smokes, `guard:rxdb-data-plane`, and Cloudflare Signaling dry-run. Not live-deployed yet; rollout must deploy ctox.dev metadata first, then the stricter Signaling Worker. |
| 517 | done | Live TURN edge closure evidence | `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/src/index.mjs`, `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/wrangler.toml`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-route.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/docs/evidence/live-turn-edge-evidence-20260526-reset099.json`, `/Users/you/Documents/ctox-dev/docs/evidence/turn-edge-key-rotation-20260526-reset099.json`, `/Users/you/Documents/ctox-dev/docs/evidence/turn-edge-live-readiness-20260526-reset099.json`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed the live TURN blocker for PRD-011. The Cloudflare Worker now calls the Cloudflare Realtime TURN `generate-ice-servers` endpoint, normalizes response shapes, and avoids reapplying an already-applied Durable Object migration. Deployed Worker version `eb56c42f-be8a-49fd-8b93-3d02f68ff64b`, staged a new secondary edge key, updated GitHub/Vercel TURN secrets, and retained no-secret live evidence: `/turn-ice` returns `200`, two ICE server entries, six TURN URLs, and one credentialed TURN server. |
| 516 | done | Ordered TURN rotation readiness gate | `/Users/you/Documents/ctox-dev/scripts/check-turn-edge-rotation-readiness.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added an ordered TURN rotation readiness gate. `check:turn-edge-rotation-readiness` verifies Worker secondary-key support, route smoke coverage, rotation command staging, no-leak smoke coverage, and Cloudflare/GitHub/Vercel access. Production Readiness CI now retains this readiness JSON as an artifact and the runbook documents the ordered operator sequence. |
| 515 | done | TURN rotation live-auth preflight | `/Users/you/Documents/ctox-dev/scripts/rotate-turn-edge-key.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-key-rotation.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added live-auth preflights to TURN edge-key rotation. `rotate:turn-edge-key` now checks Cloudflare Wrangler auth, GitHub auth, and Vercel Production env access before reporting a planned/applyable rotation. Local dry-run evidence shows all three preflights green and still stages `EDGE_ORIGIN_KEYS` by default. |
| 514 | done | No-downtime TURN edge-key rotation | `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/src/index.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-route.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/scripts/rotate-turn-edge-key.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-key-rotation.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Made TURN edge-key rotation no-downtime. The Cloudflare `/turn-ice` route now accepts primary `EDGE_ORIGIN_KEY` plus comma/newline separated secondary `EDGE_ORIGIN_KEYS`, while still failing closed if no keys exist and still returning 401 for unknown keys. The rotation command stages `EDGE_ORIGIN_KEYS` by default and only replaces the primary with explicit `--replace-primary`. |
| 513 | done | Coordinated TURN edge-key rotation | `/Users/you/Documents/ctox-dev/scripts/rotate-turn-edge-key.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-key-rotation.mjs`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added coordinated TURN edge-key rotation for PRD-011. `rotate:turn-edge-key` defaults to dry-run and only writes with `--apply`; the apply path updates Cloudflare Worker `EDGE_ORIGIN_KEY`, GitHub Actions `CTOX_TURN_EDGE_KEY`, and Vercel Production `CTOX_TURN_EDGE_KEY`, redacts command output, writes only a SHA-256 reference, and warns that ctox.dev must be redeployed after Vercel env rotation. Production Readiness CI now smokes the dry-run leak contract. |
| 512 | done | Production ops closure manifest | `/Users/you/Documents/ctox-dev/scripts/write-production-ops-closure-manifest.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-production-ops-closure-manifest.mjs`, `/Users/you/Documents/ctox-dev/.github/workflows/production-ops-closure-evidence.yml`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added a PRD-011 closure manifest. The one-shot workflow now writes `production-ops-closure-manifest.json` with SHA-256 hashes, sizes, schemas, and validator blockers for the retained TURN, remote Business OS, health-alert, and strict validator JSON files. The manifest writer supports strict mode, has executable smoke coverage, is pinned by `guard:production-ops`, and is uploaded with the closure evidence artifact. |
| 511 | done | Production ops closure evidence workflow | `/Users/you/Documents/ctox-dev/.github/workflows/production-ops-closure-evidence.yml`, `/Users/you/Documents/ctox-dev/scripts/print-production-ops-live-artifacts-summary.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added a one-shot PRD-011 closure workflow. `Production Ops Closure Evidence` captures live TURN, remote Business OS, and health-alert evidence in one manual run, continues failed capture steps long enough to retain JSON, then fails closed through `check:production-ops-live-artifacts -- --strict`. Added `summary:production-ops-live-artifacts` for GitHub Step Summary output and pinned the workflow, summary, package entry, and runbook with `guard:production-ops`. |
| 510 | done | Production ops live artifact validator smoke | `/Users/you/Documents/ctox-dev/scripts/smoke-production-ops-live-artifacts.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added executable smoke coverage for the PRD-011 live artifact validator. `smoke:production-ops-live-artifacts` builds valid temporary TURN, remote Business OS, and health-alert artifacts, then proves strict validation rejects uncredentialed TURN, remote failure markers, and retained secret leaks. Production Readiness CI now runs this smoke. |
| 509 | done | Production ops retained live artifact validation | `/Users/you/Documents/ctox-dev/scripts/check-production-ops-live-artifacts.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added strict validation for retained PRD-011 live artifacts. `check:production-ops-live-artifacts` validates TURN edge evidence for credentialed TURN, remote Business OS evidence for native ready/status API/peer-status markers, and health-alert evidence for retained alert codes, summary, delivery, and no pending migration. It rejects secret-bearing retained evidence and supports explicit artifact paths or latest `docs/evidence/live-*.json` defaults. Strict mode exits non-zero until all three live artifacts pass. |
| 508 | done | Production ops strict closure gate | `/Users/you/Documents/ctox-dev/scripts/check-production-ops-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added strict closure mode to `check:production-ops-readiness`. The normal CI path remains non-strict and uploads blockers as evidence, while `npm run check:production-ops-readiness -- --strict` exits non-zero until TURN, remote Business OS, and health-alert readiness are all green. The live evidence runbook now names this strict gate as required before PRD-011 can be marked done. |
| 507 | done | Production ops live evidence runbook | `/Users/you/Documents/ctox-dev/docs/production-ops-live-evidence.md`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added the PRD-011 live evidence runbook. It lists the required GitHub secrets, the preflight command, live workflow order, retained artifact names, native `ctox business-os serve --addr 0.0.0.0:8765` and `/api/business-os/status` closure gates, and the rule that PRD-011 remains open until TURN, remote Business OS, and health-alert artifacts all pass. `guard:production-ops` pins the runbook content. |
| 506 | done | Production ops readiness Step Summary | `/Users/you/Documents/ctox-dev/scripts/print-production-ops-readiness-summary.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added a GitHub Step Summary for aggregate PRD-011 readiness. `summary:production-ops-readiness` renders the retained production-ops JSON into a concise table with TURN, remote Business OS, and health-alert status plus required live inputs. Production Readiness CI now runs the summary step before uploading the JSON artifact, and `guard:production-ops` pins the summary script, package entry, and workflow step. |
| 505 | done | Production ops readiness CI artifact | `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Retained the aggregate PRD-011 readiness evidence in Production Readiness CI. The workflow now runs `check:production-ops-readiness` with `PRODUCTION_OPS_READINESS_RESULT_PATH=/tmp/production-ops-readiness.json` and uploads that JSON artifact even when live secrets are not configured, so CI keeps the exact live blockers visible without turning the static guard red. `guard:production-ops` pins the CI capture and artifact contract. |
| 504 | done | Aggregate production ops readiness | `/Users/you/Documents/ctox-dev/scripts/check-production-ops-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added aggregate PRD-011 production-ops readiness. `check:production-ops-readiness` runs the TURN edge, live remote Business OS, and live health-alert readiness checks with retained no-secret JSON output and reports the exact remaining blockers in one place: `CTOX_TURN_EDGE_KEY`, `CTOX_REMOTE_SSH_HOST`, `CTOX_REMOTE_SSH_USER`, `CTOX_REMOTE_SSH_PRIVATE_KEY`, and `CTOX_DEV_LIVE_SESSION_TOKEN`. `guard:production-ops` pins the aggregate script and package entry. Verified syntax, `guard:production-ops`, and the aggregate readiness check. |
| 503 | done | Live health alert readiness evidence | `/Users/you/Documents/ctox-dev/scripts/check-live-health-alert-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-health-alert-thresholds.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added no-secret live health-alert readiness evidence. `check:live-health-alert-readiness` verifies the manual live alert workflow, capture script, alert-event migration, package entry, GitHub session secret names, and Vercel Production alert-sink configuration without reading secret values. Local evidence shows workflow/capture/migration/package and Vercel alert sink are ready; the current live blocker is missing GitHub `CTOX_DEV_LIVE_SESSION_TOKEN`. `guard:health-alert-thresholds` now pins the readiness script. Verified syntax, health alert guard, live readiness check, `smoke:health-alert-api`, and `smoke:health-alert-notifications`. |
| 502 | done | Live remote Business OS evidence workflow | `/Users/you/Documents/ctox-dev/scripts/capture-live-remote-business-os-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-live-remote-business-os-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/check-live-remote-business-os-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-production-ops.mjs`, `/Users/you/Documents/ctox-dev/.github/workflows/live-remote-business-os-evidence.yml`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added live remote Business OS install/upgrade evidence infrastructure for PRD-011. `capture:live-remote-business-os` connects over SSH, can run `status`, `upgrade`, or `install`, uses the official CTOX installer for fresh hosts, requires native `ctox business-os serve --help`, starts `ctox business-os serve --addr 0.0.0.0:8765`, waits for `/api/business-os/status`, records `ctox business-os peer status`, and writes redacted JSON evidence. A manual GitHub workflow uploads that artifact. Local readiness shows code/workflow are ready and the current live blocker is missing GitHub secrets `CTOX_REMOTE_SSH_HOST`, `CTOX_REMOTE_SSH_USER`, and `CTOX_REMOTE_SSH_PRIVATE_KEY`. Verified syntax, dry-run smoke, `guard:production-ops`, and readiness check. |
| 501 | done | SSH remote deploy failure marker | `/Users/you/Documents/ctox-dev/lib/ssh.ts`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added a stable remote deployment failure marker. The SSH install script now traps `ERR` and writes `[failed] remote deployment failed at line ...` before exiting, so failed native Business OS startup or status-gate failures cannot sit forever as an ambiguous in-progress install. The RxDB data-plane guard pins the failure trap together with the native status-gated `[100]` contract. |
| 500 | done | SSH remote deploy native Business OS readiness gate | `/Users/you/Documents/ctox-dev/lib/ssh.ts`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Tightened SSH remote deployment readiness. The remote install script now requires `ctox business-os serve --help`, starts native `ctox business-os serve --addr 0.0.0.0:8765`, waits up to 60 seconds for `http://127.0.0.1:8765/api/business-os/status`, fails before `[100]` if the status endpoint does not answer, and prints `ctox business-os status` plus `ctox business-os peer status` into the deployment log. The RxDB data-plane guard pins this status-gated deploy contract so ctox.dev cannot mark a fresh remote install active solely because the process was spawned. |
| 499 | done | Native-only managed Business OS serve action | `/Users/you/Documents/ctox-dev/lib/instances.ts`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Removed a legacy Business OS web-app fallback from ctox.dev managed instance actions. `serve_business_os` now requires the native `ctox business-os serve` command, starts that server on `0.0.0.0:8765` for public-host mode, verifies the local status endpoint when `curl` is available, and prints `ctox business-os status` plus `ctox business-os peer status` for deployment diagnostics. The RxDB data-plane guard now rejects `serve_business_os` if pnpm/npm/npx/Next fallback markers return, keeping remote operations aligned with the native RxDB/WebRTC path. |
| 498 | done | Live TURN edge readiness evidence | `/Users/you/Documents/ctox-dev/scripts/check-turn-edge-live-readiness.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/turn-edge-readiness.json` | Added no-secret live TURN readiness evidence. `check:turn-edge-live-readiness` writes `ctox.turn-edge-live-readiness.v1` JSON and checks four closure prerequisites: the manual GitHub workflow exists, the Worker config owns `ctox.dev/turn-ice*` with Durable Object migrations, GitHub has the `CTOX_TURN_EDGE_KEY` secret, and the live `/turn-ice` endpoint rejects missing keys with `401`. Local evidence shows workflow/config/live edge-auth are OK and the current blocker is only the missing GitHub repository secret. `guard:turn-diagnostics` pins the readiness script and package entry. |
| 497 | done | Cloudflare TURN edge Durable Object migration | `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/wrangler.toml`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Hardened the Cloudflare service worker deployment config for the TURN edge route. `cloudflare-service-hotfix/wrangler.toml` now declares the Durable Object migration for `HUB` and `REG`, matching the exported classes used by `/signal`, `/registry`, and the same Worker route that owns `ctox.dev/turn-ice*`. `guard:turn-diagnostics` pins both the `ctox.dev/turn-ice*` route and the explicit migration so future deploys do not rely on Wrangler warnings for a production-critical service. |
| 496 | done | Live TURN edge evidence workflow | `/Users/you/Documents/ctox-dev/.github/workflows/live-turn-edge-evidence.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/README.md`, `/Users/you/Documents/ctox-dev/docs/production-env.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added the manual `Live TURN Edge Evidence` GitHub workflow. It uses `secrets.CTOX_TURN_EDGE_KEY`, calls the configured `/turn-ice` endpoint through `capture:turn-edge-evidence`, appends the no-secret capture summary, and uploads `/tmp/turn-edge-evidence.json` with `if-no-files-found: error`. `guard:turn-diagnostics` now pins the workflow, secret usage, capture command, and retained artifact. README and production env docs now point operators at the workflow. This is capture infrastructure; PRD-011 still needs a passing live artifact before private/NAT is closed. |
| 495 | done | Vercel TURN env inference boundary correction | `/Users/you/Documents/ctox-dev/scripts/check-vercel-production-env.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Corrected an unsafe inference attempt around Vercel production env values. Vercel may materialize sensitive values as empty local placeholders, so the Vercel-production env check remains names-based and must not infer that production secrets are empty from `.vercel/.env.production.local`. Real runtime TURN readiness is proven by `capture:turn-edge-evidence`, which still requires a locally available `CTOX_TURN_EDGE_KEY` and a credentialed TURN response. This keeps PRD-011 open without creating false-negative production env failures. |
| 494 | done | TURN edge live evidence capture | `/Users/you/Documents/ctox-dev/scripts/capture-turn-edge-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/docs/production-env.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added redacted live TURN edge evidence capture. `capture:turn-edge-evidence` calls `/turn-ice` with `CTOX_TURN_EDGE_KEY`, writes retained JSON evidence, and stores only endpoint origin/path, HTTP status, counts, and credentialed-TURN booleans. `smoke:turn-edge-evidence` proves credentialed TURN passes, naked TURN fails, edge errors fail, and TURN username/credential never appear in stdout or evidence. Production Readiness CI runs the smoke and `guard:turn-diagnostics` pins the contract. A live `https://ctox.dev/turn-ice` run still fails with `missing_ctox_turn_edge_key` because the local `.vercel/.env.production.local` key is present but empty; this remains an open PRD-011 configuration blocker. |
| 493 | done | Credentialed TURN operator docs | `/Users/you/Documents/ctox-dev/README.md`, `/Users/you/Documents/ctox-dev/docs/business-os-connection-modes.md`, `/Users/you/Documents/ctox-dev/docs/ctox-peer2peer-signaling.md`, `/Users/you/Documents/ctox-dev/scripts/assert-connection-docs.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Documented the credentialed-TURN Private/NAT requirement in operator-facing docs. The README, connection-mode docs, and peer-to-peer signaling docs now state that STUN-only ICE and uncredentialed `turn:` URLs are not private-NAT readiness, and the connection-mode docs include readiness/capture commands for custom VPS/domain and private NAT. `guard:connection-docs` now pins the `iceServersHaveCredentialedTurn` requirement and closure command coverage. Verified `guard:connection-docs` and `guard:rxdb-data-plane`. |
| 492 | done | Private-NAT credentialed TURN closure | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `/Users/you/Documents/ctox-dev/scripts/capture-connection-mode-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-connection-mode-closure-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/check-connection-mode-closure-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-connection-mode-closure-evidence.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Tightened Private-NAT closure from generic TURN URL presence to credentialed TURN. Business OS sync diagnostics and Advanced Status now expose `sync.iceServersHaveCredentialedTurn`; private-NAT capture records `turn-ice-configured` only when that field is true, and retained closure evidence requires both `iceServersHaveTurn` and `iceServersHaveCredentialedTurn`. The readiness preflight rejects packed or explicit TURN entries without username and credential unless Cloudflare TURN credentials are configured. Synced ctox.dev public Business OS assets after the Advanced Status shape change. Verified core/public Business OS syntax, negative uncredentialed TURN readiness, positive credentialed TURN readiness, `smoke:connection-mode-closure-evidence`, `guard:business-os-shell-assets`, `guard:turn-diagnostics`, and `guard:rxdb-data-plane`. |
| 491 | done | TURN edge route executable smoke | `/Users/you/Documents/ctox-dev/scripts/smoke-turn-edge-route.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/.github/workflows/production-readiness.yml`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added an executable TURN edge-route smoke. The smoke imports the Cloudflare service worker and performs real `/turn-ice` Request/Response checks for missing `EDGE_ORIGIN_KEY`, wrong `X-Edge-Key`, missing Cloudflare TURN credentials, and a mocked successful Cloudflare TURN credential response with real `turn:` / `turns:` URLs. Production-readiness CI now runs `smoke:turn-edge-route`, and the TURN diagnostics guard pins the package entry, CI step, and the four smoke cases. Verified syntax, `smoke:turn-edge-route`, `guard:turn-diagnostics`, and `guard:rxdb-data-plane`. |
| 490 | done | TURN edge credential route fail-closed hardening | `/Users/you/Documents/ctox-dev/cloudflare-service-hotfix/src/index.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-turn-diagnostics.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Hardened the Cloudflare service `/turn-ice` route so it no longer falls back to a hard-coded edge key. The route now fails closed with `edge_origin_key_not_configured` when `EDGE_ORIGIN_KEY` is absent and still requires the configured `X-Edge-Key` before generating temporary Cloudflare TURN credentials. The TURN diagnostics guard pins the fail-closed route, configured edge-key comparison, and absence of the old hard-coded default. Verified worker syntax, `guard:turn-diagnostics`, and `guard:rxdb-data-plane`. |
| 489 | done | Connection-mode readiness CI summary | `/Users/you/Documents/ctox-dev/scripts/check-connection-mode-closure-readiness.mjs`, `/Users/you/Documents/ctox-dev/scripts/print-connection-mode-readiness-summary.mjs`, `/Users/you/Documents/ctox-dev/.github/workflows/connection-mode-closure-evidence.yml`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Made connection-mode capture readiness visible in CI. The readiness preflight can now write a no-secret JSON result file, `summary:connection-mode-readiness` renders a GitHub Step Summary with ready counts and missing input details, and the manual closure workflow uploads both readiness and capture artifacts. Capture evidence upload now warns instead of masking an expected preflight failure when no Browser evidence exists yet. The RxDB/WebRTC data-plane guard pins the summary script, package entry, result-path output, workflow summary step, and readiness artifact upload. Verified syntax, summary output, `smoke:connection-mode-closure-evidence`, and `guard:rxdb-data-plane`. |
| 488 | done | Connection-mode capture workflow preflight | `/Users/you/Documents/ctox-dev/scripts/check-connection-mode-closure-readiness.mjs`, `/Users/you/Documents/ctox-dev/.github/workflows/connection-mode-closure-evidence.yml`, `/Users/you/Documents/ctox-dev/scripts/smoke-connection-mode-closure-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Wired the no-secret closure readiness preflight into the manual connection-mode evidence workflow. The preflight now supports `--mode`, accepts the workflow's generic `base_url` / `launch_url` inputs for the selected mode, and fails custom VPS/domain or private NAT captures before Playwright setup when the real launch URL, packed config, or TURN inputs are missing. The workflow also exposes capture timeout and WebRTC-readiness grace inputs. The closure smoke now verifies the new readiness behavior, including negative custom-domain and private-NAT/TURN cases. Verified syntax, focused readiness checks, `smoke:connection-mode-closure-evidence`, and `guard:rxdb-data-plane`. |
| 487 | done | Connection-mode closure readiness preflight | `/Users/you/Documents/ctox-dev/scripts/check-connection-mode-closure-readiness.mjs`, `/Users/you/Documents/ctox-dev/package.json`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added a no-secret readiness preflight for PRD-009 closure. `check:connection-mode-closure-readiness` reports retained-evidence readiness for the five proven modes and explicit missing capture inputs for the two blocked modes without printing packed configs, room passwords, tokens, or credentials. The current output confirms custom VPS/domain still needs a real launch or base URL, and private NAT still needs a packed launch URL plus real TURN via packed config, explicit ICE servers, or Cloudflare TURN credentials. The RxDB/WebRTC data-plane guard now pins the preflight script and package entry. Verified the preflight and `guard:rxdb-data-plane`. |
| 486 | done | Connection-mode closure guard semantic hardening | `/Users/you/Documents/ctox-dev/scripts/assert-connection-mode-closure-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/smoke-connection-mode-closure-evidence.mjs`, `/Users/you/Documents/ctox-dev/scripts/capture-connection-mode-evidence.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Moved the new launch-health and TURN semantics into the retained closure gate. The connection-mode closure guard now requires `AdvancedStatus.ok === true` for every non-managed launched mode and requires `advancedStatus.sync.iceServersHaveTurn === true` for private-NAT evidence, so a synthetic passing `turn-ice-configured` check cannot close Private NAT. The capture no longer falls back to packed launch config for TURN proof. The closure smoke now includes a negative private-NAT fixture with missing Advanced Status TURN proof. Verified syntax, closure smoke, and the current retained closure gate, which still passes 5/7 modes and fails only on missing custom VPS/domain and private NAT/TURN artifacts. |
| 485 | done | Private-NAT/TURN readiness semantics | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `/Users/you/Documents/ctox-dev/scripts/capture-connection-mode-evidence.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Tightened the Private-NAT evidence contract so TURN readiness cannot pass from a generic ICE server count. Business OS sync diagnostics and Advanced Status now expose `sync.iceServersHaveTurn`, computed from real `turn:` / `turns:` URLs after ICE config normalization, and per-collection diagnostics retain the same non-secret flag. Synced ctox.dev public Business OS assets and verified Business OS JS syntax, capture syntax, connection-mode closure smoke, and the ctox.dev shell asset guard. |
| 484 | done | PRD-009 reset-era connection-mode closure evidence hardening | `/Users/you/Documents/ctox-dev/scripts/capture-connection-mode-evidence.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/evidence/connection-mode-prd009-direct-ctox-host-reset-20260526.json`, `/Users/you/Documents/ctox-dev/docs/evidence/connection-mode-prd009-ctox-dev-web-deploy-reset-20260526.json`, `/Users/you/Documents/ctox-dev/docs/evidence/connection-mode-prd009-private-desktop-reset-20260526.json`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Hardened the retained connection-mode capture path so it waits for WebRTC connection readiness instead of the first Advanced Status snapshot, injects an isolated smoke IndexedDB id for launched Browser modes, keeps a stabilization window before accepting ready status, emits closure-compatible `<redacted>` placeholders, and exits non-zero on failed required checks. Refreshed reset-era evidence for direct CTOX host, ctox.dev Web Deploy with packed WebRTC config, and private desktop launch; all three retained captures now have `AdvancedStatus.ok === true`, app-local `ctox-rxdb-js`, RxDB/WebRTC data plane, and no HTTP data proxy. The first ctox.dev capture exposed stale public shell assets without `rxdbRuntime`, so the wave also resynced ctox.dev Business OS shell assets and passed the asset guard. The connection-mode closure guard now passes those three modes plus retained managed-subdomain and no-HTTP evidence, and fails only on missing custom VPS/domain and private NAT/TURN evidence. |
| 483 | done | PRD-009 public live ctox.dev/signaling evidence | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd009-live-signaling-public-wave483.json`, `/tmp/ctox-prd009-live-business-os-transport-wave483.json` | Added public live deployment evidence without private ctox.dev tenant secrets. `LIVE_SIGNALING_REQUIRE_ADMIN=0` capture passed against `https://signaling.ctox.dev`, proving the public health/capability contract and WebSocket control-plane typed errors. `BUSINESS_OS_LIVE_BASE_URL=https://ctox.dev` capture passed against the public `/business-os` shell, proved `business-os-advanced-status-v1`, and observed the expected `CtoxSignalingControlPlaneError/instance_mismatch` without leaking `ctox_config`, room passwords, tokens, or launch secrets. This advances PRD-009/PRD-010 but does not close PRD-009 because real public/custom-domain or managed-subdomain Browser-to-CTOX peer launch evidence is still missing. |
| 482 | done | PRD-009 no-skip connection-mode release matrix | `/Users/you/Documents/ctox-dev/scripts/smoke-core-shell-startup-budget.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd009-autostart-shell-startup-wave482.json`, `/tmp/ctox-prd009-connection-mode-require-all-wave482.json` | Advanced PRD-009 by making the direct shell-startup lane self-contained and passing the connection-mode matrix with `--require-all`. The ctox-dev shell startup wrapper now autostarts a local Signaling server and `ctox business-os serve` when no live direct CTOX endpoint is reachable, then verifies `/business-os#ctox` through the existing Core connection-mode smoke. Focused autostart evidence showed shell visible in `715ms` and WebRTC connected in `715ms` under the `3000ms` budget. The no-skip release matrix passed direct RxDB-only contract, focused direct Browser/Rust UI smoke, direct shell startup, ctox.dev Web Deploy shell, private packed config, desktop private launch contract, tenant pairing contract, managed-subdomain diagnostics, private/NAT production preflight, and no-HTTP business-data-plane guard. |
| 481 | done | PRD-009 connection-mode matrix baseline hardening | `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/scripts/smoke-business-os-shell-provider.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd009-connection-mode-baseline-wave481-fixed.json` | Started PRD-009 with a reset-era connection-mode baseline and removed two false-negative harness races. The Business OS UI regression mode now waits up to 15s for the full required module catalog (`ctox`, `documents`, `knowledge`, `research`) before asserting, which prevents early startup checks from failing while the local packaged module merge is still in flight. The ctox.dev shell-provider smoke now reuses a reachable existing local Next dev server from `.next/dev/lock` instead of trying to start a second dev server and failing on Next's lock. Verified the focused UI mode and shell-provider smoke, then reran the connection-mode matrix successfully: direct RxDB-only contract, focused direct Browser/Rust UI smoke, ctox.dev Web Deploy shell, private packed config, desktop private launch contract, tenant pairing contract, managed-subdomain diagnostics, private/NAT production preflight, and no-HTTP business-data-plane guard all passed; the direct live shell-startup lane remained skipped because no long-running local CTOX endpoint was present at `127.0.0.1:8765`. |
| 480 | done | PRD-008 literal `/business-os#ctox` closure | `src/core/business_os/server.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd008-business-os-route-wave480.json` | Closed PRD-008 on the literal local acceptance URL. Core now serves the app shell for `/business-os` as well as `/business-os/`, and the Browser/Rust smoke harness treats `/business-os#ctox` as an app-shell page so the real route can be exercised. Rebuilt the integration `ctox` binary and ran the full ctox-dev direct Core matrix with `SMOKE_PAGE_PATH=/business-os#ctox`; all six modes passed first try: `rust-to-browser`, `browser-to-rust`, `command-browser-to-rust`, `business-os-ui-regression`, `workspace-agent-artifacts-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser`. Evidence includes app-local no-package-manager `ctox-rxdb-js`, `business-os-advanced-status-v1`, exactly one command queue projection, `18` modules, `15` Start menu targets, opened `ctox,documents,knowledge,research`, four workspace artifacts, a `1,260,036` byte File Viewer materialization through `103` chunks after native peer restart, zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. |
| 479 | review | PRD-008 local direct Core E2E baseline | `src/core/business_os/server.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd008-local-e2e-wave479.json` | Promoted PRD-008 from blocked to review with the first full local direct Core Browser/Rust matrix after PRD-001 through PRD-007. The ctox-dev wrapper passed all default modes first try: `rust-to-browser`, `browser-to-rust`, `command-browser-to-rust`, `business-os-ui-regression`, `workspace-agent-artifacts-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser`. Evidence includes app data both directions, exactly one command queue projection, four workspace artifacts, a `1,260,036` byte File Viewer materialization through `103` chunks after native peer restart, `18` modules, `15` Start menu targets, opened `ctox,documents,knowledge,research`, app-local no-package-manager `ctox-rxdb-js`, `business-os-advanced-status-v1`, zero Browser warnings, zero request failures, zero asset response errors, and zero cache repairs. Also repaired the Core static mount-path edge and app-shell smoke recognition so the literal `/business-os#ctox` acceptance URL can be tested and served instead of 404ing. |
| 478 | done | PRD-006 Business OS UI regression closure | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `/Users/you/Documents/ctox-dev/scripts/smoke-core-direct-browser-rust.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-data-plane.mjs`, `/tmp/ctox-prd006-ui-regression-wave478.json` | Closed PRD-006 with a repeatable Browser/Rust UI regression mode. `business-os-ui-regression` runs on the real `/index.html` Business OS shell with app-local RxDB and the native WebRTC peer, opens the Start menu, requires at least eight launcher targets, opens `ctox`, `documents`, `knowledge`, and `research`, opens Desktop, and requires `business-os-advanced-status-v1` plus app-local no-package-manager `ctox-rxdb-js`. The focused ctox-dev direct Core run passed first try with `18` modules, `15` Start menu targets, opened modules `ctox,documents,knowledge,research`, Desktop opened, zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, zero cache repairs, and only the expected favicon-style resource `404`. The ctox-dev default direct smoke list and data-plane guard now pin this UI mode. |
| 477 | done | PRD-006 shell startup and app-data baseline | `/Users/you/Documents/ctox-dev/app/business-os/[[...path]]/route.ts`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd006-shell-provider-wave477.json`, `/tmp/ctox-prd006-core-shell-startup-wave477.json`, `/tmp/ctox-prd006-app-data-wave477.json` | Started PRD-006 with verified shell/app-data evidence. ctox.dev `/business-os` no longer injects local CTOX launch context into the plain web-deploy shell unless explicitly requested with `CTOX_BUSINESS_OS_LOCAL_PAIRING=1`, `?local_pairing=1`, and localhost. The running ctox.dev server passed the shell-provider smoke, RxDB data-plane guard, shell-asset guard, and typecheck. The live local Core shell budget smoke passed against `http://127.0.0.1:3000/#ctox`: shell visible in `728ms`, WebRTC connected in `728ms`, Advanced Status shell timing `516ms`, and `17` modules loaded. Focused Browser/Rust app-data smokes passed `rust-to-browser` and `browser-to-rust` first try with app-local no-package-manager `ctox-rxdb-js`, `business-os-advanced-status-v1`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. |
| 476 | done | PRD-007 live command consistency closure | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd007-command-consistency-wave476.json` | Closed PRD-007 with a focused live command matrix through the ctox-dev direct Core wrapper. `command-browser-to-rust`, `command-burst-browser-to-rust`, `command-reload-browser-to-rust`, `restart-browser-to-rust`, and `command-midflight-restart-browser-to-rust` all passed first try. Evidence proves exactly one `ctox_queue_tasks` projection for a normal command, exactly five tasks for a five-command burst, exactly one task for the same command after Browser reload, and exactly one task after midflight native peer restart. The run had zero startup reloads, zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. Verified ctox.dev reconnect release coverage and RxDB data-plane guards. |
| 475 | done | ctox.dev data-plane health guard restore | `/Users/you/Documents/ctox-dev/lib/instances.ts`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Restored the ctox.dev RxDB/WebRTC data-plane guard after it exposed that HTTP-observed Business OS health did not explicitly pin the unhealthy native data-plane branch. `classifyHttpBusinessOsHealth` now names `httpDataPlaneUnhealthy`, keeps a dedicated `rxdb-webrtc` data-plane health classifier, persists the redacted native data-plane payload, and returns degraded health when native RxDB/SQLite data plane status is unhealthy. Verified ctox.dev `guard:rxdb-data-plane` and `typecheck`. |
| 474 | done | PRD-005 live Browser/Rust File Sync evidence | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/tmp/ctox-prd005-live-file-sync-wave473.json` | Added live reset-era file-sync evidence through the ctox-dev direct Core smoke wrapper. `workspace-agent-artifacts-rust-to-browser` replicated four CTOX/Codex-style workspace artifacts into Browser `desktop_files`/`desktop_file_chunks` with one chunk each. `workspace-large-file-viewer-restart-rust-to-browser` restarted the native peer, reopened file collections, materialized a `1,260,036` byte file through `103` chunks, and rendered it in File Viewer. Both modes emitted `business-os-advanced-status-v1`, used app-local no-package-manager `ctox-rxdb-js`, and completed with zero startup reloads, zero request failures, zero asset response errors, and zero cache repairs. |
| 473 | done | PRD-005 service completion workspace file sync evidence | `src/core/service/service.rs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Started the reset-era PRD-005 closure path at the service boundary. Added `completion_hook_indexes_workspace_outputs_for_business_os`, which simulates a CTOX/Codex agent workspace with a small report and a large binary output, calls the real `sync_workspace_root_to_business_os` completion hook, and verifies the Business OS SQLite RxDB projection contains `/CTOX/<workspace>/...` rows with the small file eager/`available` and the large file lazy. Verified the new service test, existing direct workspace indexing, materialized large-file preservation across rescans, bounded scan tombstone safety, and the app-local no-package-manager RxDB JS guard. |
| 472 | done | Negative File Viewer chunk status pressure budget and PRD-004 closure | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `/Users/you/Documents/ctox-dev/scripts/assert-rxdb-reconnect-release-coverage.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed the remaining PRD-004 pressure-budget gap. The Browser/Rust smoke matrix now has a dedicated `SMOKE_FILE_CHUNK_STATUS_STARTUP_HOOK_WAIT_BUDGET_MS` budget, defaulting to 12s, and applies it only to the three negative File Viewer chunk status modes. The ctox.dev reconnect release guard pins the dedicated budget so broad release coverage cannot silently fall back to the generic 60s startup hook budget. Verified matrix syntax, ctox.dev reconnect release guard, and a focused three-mode run for metadata mismatch, tombstoned chunk, and stale generation status errors with zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, zero cache repairs, and mode durations under 8s. |
| 471 | done | Advanced Status typed native peer/service health errors | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Advanced PRD-004 observability parity. Native RxDB peer status now reports `ctox-native-rxdb-peer-status-v1`, health state, error count, and typed native peer errors for optional collection degradation and unavailable native peer runtime. Business OS Advanced Status now publishes one `health.errors` list combining collection, lifecycle, native peer, CTOX service, and file-integrity errors while keeping targeted `sync.nativePeerErrors` and `sync.serviceErrors`. The RxDB-only guard pins the interface. Verified Business OS JS syntax, `assert-rxdb-only`, focused Rust optional schema-drift status coverage, app-local no-package-manager guard, ctox.dev shell asset sync/guard, and `guard:webrtc-frame-stream`. |
| 470 | done | Generated WebRTC frame protocol contract and PRD-002 closure | `src/core/rxdb/tools/build_webrtc_frame_protocol_contract.mjs`, `src/apps/business-os/rxdb/src/frame-contract.generated.mjs`, `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/src/plugins/replication_webrtc/frame_contract_generated.rs`, `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed PRD-002 by making `webrtc-frame-protocol.json` the generator source for Browser and Rust frame constants. Added a frame contract generator, generated Browser ESM and Rust frame contract files, refactored Browser `webrtc-native.mjs` and Rust `connection_handler_rs.rs` to consume the generated constants, and updated the ctox.dev frame-stream guard to understand generated contracts. Rebuilt the app-local Browser ESM bundle, synced ctox.dev shell assets, marked PRD-002 done in the reset tracker, and verified Browser generated-contract syntax, import smoke, full `assert_js_no_package_manager`, focused Rust frame fixture test, ctox.dev shell asset guard, and `guard:webrtc-frame-stream`. |
| 469 | done | RxDB protocol incompatibility Advanced Status evidence | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/browser_rust_split_gate.js`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Advanced PRD-002 with explicit incompatibility status evidence. Business OS now normalizes raw `CtoxRxdbProtocolError` events into Advanced Status `CtoxSchemaProtocolError` entries while preserving stable `ctox_rxdb_*` codes. Added `rxdb-protocol-error-browser-status` to the Browser/Rust smoke, matrix, split-gate, and soak mode lists; the new mode proves `ctox_rxdb_protocol_mismatch` appears in `business-os-advanced-status-v1`. Synced ctox.dev shell assets and verified Business OS JS syntax, smoke harness syntax, `assert-rxdb-only`, ctox.dev shell asset guard, and a ctox-dev direct status smoke with zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. |
| 468 | done | Generated RxDB WebRTC protocol contract | `src/core/rxdb/tools/build_webrtc_rxdb_protocol_contract.mjs`, `src/apps/business-os/rxdb/src/protocol-contract.generated.mjs`, `src/apps/business-os/rxdb/src/schema.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/src/plugins/replication_webrtc/protocol_contract_generated.rs`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Advanced PRD-002 by making `webrtc-rxdb-protocol.json` the generator source for Browser and Rust protocol constants. Added a generator for the Browser ESM contract and Rust `protocol_contract_generated.rs`, refactored `ctox-rxdb-js` schema/protocol code and `rxdb.rs` WebRTC validators to consume the generated constants, and kept schema-hash source metadata in the shared fixture. Rebuilt the app-local Browser ESM bundle, synced ctox.dev shell assets, and verified generator syntax, generated Browser contract syntax, Browser import/registry smokes, full `assert_js_no_package_manager`, focused Rust `ctox_protocol` tests, ctox.dev shell asset guard, and `guard:webrtc-frame-stream`. |
| 467 | done | Custom schema hash policy and PRD-003 closure | `src/apps/business-os/rxdb/src/schema.mjs`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/src/index.mjs`, `src/apps/business-os/rxdb/src/rx-database.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/tests/fixtures/webrtc-rxdb-protocol.json`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed the remaining PRD-003 compatibility-policy gap. Browser `ctox-rxdb-js` now advertises schema-hash source metadata: known Business OS collections use `business-os-schema-hash-registry-v1`; unknown/custom collections use `canonical-json-schema-sha256-v1`. Rust WebRTC protocol payloads advertise `rxdb-rs-schema-hash-v1`, and the shared RxDB protocol fixture pins all three labels. Rebuilt the app-local Browser ESM bundle, synced ctox.dev Business OS shell assets, marked PRD-003 done in the reset tracker, and verified Browser syntax/import/registry smokes, `assert_js_no_package_manager`, focused Rust `ctox_protocol` tests, ctox.dev shell asset guard, `guard:webrtc-frame-stream`, and `guard:native-peer-schema-drift`. |
| 466 | done | Optional schema-drift recovery metadata | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Advanced PRD-003 from "degraded" to actionable recovery. Optional native collection registration issues now carry stable `ctox_optional_schema_drift`/registration-failed codes, severity, and a structured recovery object with safe dry-run and repair commands for `ctox business-os rxdb repair-optional-drift`; required collections remain explicitly refused by the repair path. Business OS Advanced Status now preserves the recovery metadata, and the RxDB-only guard pins the surface. Verified JS syntax, `assert-rxdb-only`, `cargo fmt --check`, focused Rust optional drift isolation and repair backup/drop tests, ctox.dev Business OS shell sync and asset guard, `guard:webrtc-frame-stream`, and the no-package-manager RxDB JS guard. |
| 465 | done | Schema-hash registry freshness guard | `src/apps/business-os/rxdb/tests/schema-hash-registry-smoke.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added the PRD-003 Browser registry freshness smoke. The smoke loads Rust's `business_os_schema_hashes.json` fixture, compares it with Browser `CTOX_BUSINESS_OS_SCHEMA_HASHES`, rejects missing/stale/extra known Business OS collection hashes, and verifies `schemaHash(schema, collection)` uses the registry hash for known collections. Wired the smoke into `assert_js_no_package_manager` alongside the existing import smoke so schema registry drift now fails the standard no-npm/no-dependency guard. Verified syntax, standalone registry smoke, and full `assert_js_no_package_manager`. |
| 464 | done | Business OS schema-hash registry parity | `src/apps/business-os/rxdb/src/schema.mjs`, `src/apps/business-os/rxdb/src/rx-database.mjs`, `src/apps/business-os/rxdb/src/index.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Closed the required-collection schema-hash parity gap exposed by the strict PRD-002 handshake. Browser `ctox-rxdb-js` now exports `CTOX_BUSINESS_OS_SCHEMA_HASHES` and uses that Rust-fixture-derived registry when hashing known Business OS collections, so live Browser/Rust protocol handshakes can enforce schema version/hash for those collections. Unknown/custom schemas still fall back to local canonical hashing and remain an explicit compatibility-policy item. Rebuilt the app-local Browser ESM bundle, synced ctox.dev public Business OS assets, and verified JS syntax, no-package-manager import smoke, `assert_js_no_package_manager`, focused Rust protocol tests, workspace Rust `native_all_schema_hashes_match_browser_contract_fixture`, ctox.dev `guard:webrtc-frame-stream`, ctox.dev `guard:business-os-shell-assets`, and strict live ctox-dev `rust-to-browser,browser-to-rust` smoke with zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. |
| 463 | done | RxDB WebRTC protocol handshake parity | `src/core/rxdb/tests/fixtures/webrtc-rxdb-protocol.json`, `src/apps/business-os/rxdb/src/schema.mjs`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Advanced PRD-002 with the shared RxDB WebRTC protocol handshake fixture. Added `webrtc-rxdb-protocol.json` for `ctox-rxdb-protocol-v1`, required capabilities, native/browser capability sets, stable `ctox_rxdb_*` error codes, and compatible Browser/native payload examples. Browser `ctox-rxdb-js` now validates protocol, required capabilities, collection identity, and fixture-tested schema version/hash mismatches as `CtoxRxdbProtocolError`; Rust validates the same contract as `RC_WEBRTC_PROTOCOL` with matching stable codes. Live peer activation enforces protocol/capability/collection compatibility while leaving strict schema version/hash rejection behind PRD-003 because the first live strict attempt exposed productive JS/Rust schema hash drift. Verified JS syntax, no-package-manager import smoke, `assert_js_no_package_manager`, focused Rust protocol tests, ctox.dev shell sync plus `guard:webrtc-frame-stream` and `guard:business-os-shell-assets`, and live ctox-dev `rust-to-browser,browser-to-rust` smoke with zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. |
| 462 | done | Release-profile PRD-001 closure soak | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_soak.js`, `/tmp/ctox-prd001-release-profile-soak.json` | Closed PRD-001 in the reset production-readiness tracker. Ran the full release-profile soak with `SOAK_FAIL_ON_RETRY=1` across all 29 default Browser/Rust transport, application, disruption, command, large-file, and status/error modes. The soak passed one complete release cycle in `326s`, with zero failed modes, zero retries, zero generic Browser warnings/errors/request failures, zero WebSocket warnings, zero asset response errors, zero cache repairs, `business-os-advanced-status-v1`, and app-local no-package-manager `ctox-rxdb-js` evidence. Max duration was `40,113ms` on the intentional `network-flap-browser-to-rust` outage mode; max startup Advanced Status was `5,680ms` on `command-burst-browser-to-rust`. The only diagnostics were expected network-flap offline counters: warning `1`, errors `5`, request failure `1`. |
| 461 | done | Classified Network-Flap diagnostics | `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Hardened the Network-Flap evidence classification. `browser_rust_smoke.js` now reports expected offline-window diagnostics in `expected_network_flap_warning_count`, `expected_network_flap_error_count`, and `expected_network_flap_request_failure_count` instead of mixing them into generic Browser warning/error/request-failure counters. Verified JS syntax and reran `network-flap-browser-to-rust` through the ctox-dev wrapper. The mode passed in `15,268ms`, emitted `business-os-advanced-status-v1`, app-local `ctox-rxdb-js`, completed startup Advanced Status in `5,631ms`, recovered after the offline window, and wrote Browser-to-Rust data. Generic Browser warning/error/request-failure counts were all `0`; the expected offline counters were `1/1/1`. |
| 460 | done | Hard two-cycle PRD-001 soak | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_soak.js` | Ran a hard two-cycle PRD-001 soak with `SOAK_FAIL_ON_RETRY=1` across `browser-to-rust`, 108-chunk artifact stress, 142-chunk churn, 103-chunk File Viewer after restart, command reload, Signaling restart, network flap, and Browser handoff. The soak passed both cycles, 16 mode-runs total, with zero failed modes and zero retries. Max mode duration was `20,143ms` on `workspace-large-file-viewer-restart-rust-to-browser`; max startup Advanced Status was `5,600ms` on `browser-to-rust`. Across both cycles there were zero WebSocket warnings and zero cache repairs. The only two warnings, eight Browser errors, and two request failures came from the intentionally simulated offline windows in `network-flap-browser-to-rust`; both network-flap runs recovered and completed Browser-to-Rust writes afterwards. |
| 459 | done | Broad PRD-001 ctox-dev wrapper sweep | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Ran the first broad PRD-001 ctox-dev wrapper sweep in one process. The sweep passed 24 transport/application modes with one attempt each: base Rust-to-Browser and Browser-to-Rust, workspace baseline/update, artifact basic/stress/churn/background, all three large-file modes, command baseline/migration/burst/reload/restart/midflight-restart, native restart, Signaling restart, native peer rollover, tab freeze, network flap, Browser lifecycle, and Browser handoff. The sweep had zero failed modes, zero WebSocket warnings, zero cache repairs, max mode duration `19,537ms` (`workspace-large-file-viewer-restart-rust-to-browser`), and max startup Advanced Status `5,624ms` (`restart-browser-to-rust`). The only warning, five Browser errors, and one request failure came from the intentionally simulated `network-flap-browser-to-rust` offline window; the mode recovered and wrote Browser-to-Rust data afterwards. |
| 458 | done | Workspace wrapper gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Added the remaining focused workspace wrapper gate for PRD-001. One ctox-dev run passed `workspace-rust-to-browser`, `workspace-update-rust-to-browser`, `workspace-agent-artifacts-rust-to-browser`, and `workspace-agent-artifacts-background-rust-to-browser` with one attempt per mode. The gate covered workspace file replication, generation-changing updates, four-file agent artifact sync, and background-index queue task creation. Every mode emitted `business-os-advanced-status-v1`; the artifact modes used app-local `ctox-rxdb-js`, and all four modes had zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, zero cache repairs, and zero startup reloads. |
| 457 | done | ctox-dev status wrapper gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Reran all nine status/error modes through the ctox-dev wrapper after the evidence-contract fix. The gate passed `signaling-error-browser-status`, `peer-lifecycle-browser-status`, `checkpoint-error-browser-status`, `schema-error-browser-status`, `replication-error-browser-status`, `replication-push-contract-error-browser-status`, `file-chunk-metadata-error-browser-status`, `file-chunk-tombstone-error-browser-status`, and `file-chunk-stale-generation-error-browser-status` sequentially with one attempt per mode. Every mode emitted `business-os-advanced-status-v1`, had zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. The three File Viewer chunk integrity modes completed in `10,454ms`, `8,721ms`, and `9,557ms`; their highest observed startup Advanced Status was `5,561ms`. |
| 456 | done | Status-mode Advanced Status evidence contract | `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Fixed the status-mode smoke evidence contract instead of weakening the ctox-dev guard. Status modes that already verify Advanced Status snapshots now also emit `advanced_status` and `rxdb_runtime` evidence, including File Viewer chunk integrity modes. Verified syntax for `browser_rust_smoke.js` and `browser_rust_smoke_matrix.js`, then reran the previously wrapper-failing single `file-chunk-metadata-error-browser-status` through ctox-dev. The run passed end-to-end in `9,548ms`, emitted `business-os-advanced-status-v1`, app-local `ctox-rxdb-js`, completed startup Advanced Status in `4,578ms`, and had zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. The earlier `30,885ms` value remains tracked as parallel shard pressure, not normal isolated-path latency. |
| 455 | done | Advanced Status typed error split gates | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_split_gate.js` | Added the PRD-004 status/error split-gates. Two parallel shards passed `signaling-error-browser-status`, `peer-lifecycle-browser-status`, `checkpoint-error-browser-status`, `schema-error-browser-status`, `replication-error-browser-status`, `replication-push-contract-error-browser-status`, `file-chunk-metadata-error-browser-status`, `file-chunk-tombstone-error-browser-status`, and `file-chunk-stale-generation-error-browser-status`. The gates verified typed error names/codes for signaling, peer lifecycle, checkpoint, schema, replication I/O, replication push contract, and File Viewer chunk integrity paths. Both shards had zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, zero cache repairs, and zero startup reloads. Remaining budget issue: the negative File Viewer chunk status modes are functionally correct but slow, with startup Advanced Status observed up to `30,885ms`. |
| 454 | done | UI handoff and native rollover split gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_split_gate.js` | Added the first broad split-gate shard after the focused transport gates. The shard passed `browser-lifecycle-ui`, `browser-handoff-ui`, and `rollover-native-peer-browser-to-rust` in parallel. Browser lifecycle accepted seven browser-control command types and ended with stopped session/runtime/tab state. Browser handoff accepted the `ctox.browser_context.capture` command, queued the handoff task, and verified visible frame/context evidence. Native peer rollover refreshed 11 checkpointed collections, emitted `business-os-advanced-status-v1`, app-local `ctox-rxdb-js`, fork-direction replication evidence, Rust readiness payload, and a successful Browser-to-Rust write after rollover. The shard had zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, zero cache repairs, and zero startup reloads. Remaining PRD-001 work: repeated-cycle proof, full status/error-mode closure, and stricter flap/freeze status classification. |
| 453 | done | Network flap and tab freeze frame-transport gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Added the focused realtime disruption gate. One ctox-dev run passed `network-flap-browser-to-rust` and `tab-freeze-browser-to-rust`; both modes emitted `business-os-advanced-status-v1`, app-local `ctox-rxdb-js`, fork-direction replication evidence for `desktop_files` and `desktop_file_chunks`, Rust readiness payloads, and successful Browser-to-Rust file/chunk writes after recovery. `network-flap-browser-to-rust` completed in `15,091ms` with one expected offline fetch failure, one expected catalog warning, and three expected browser network errors during the simulated offline window; it had zero WebSocket warnings, zero asset response errors, and zero cache repairs. `tab-freeze-browser-to-rust` completed in `14,625ms` with zero Browser warnings, zero WebSocket warnings, zero request failures, and zero cache repairs. Remaining PRD-001 work: repeated-cycle proof, broader full-mode closure, and stricter flap/freeze status classification. |
| 452 | done | Migration and large-file frame-transport gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Added the focused migration/large-file gate. One ctox-dev run passed `migration-version-browser-to-rust`, `workspace-large-materialize-rust-to-browser`, `workspace-large-file-viewer-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser`. The three large-file modes each replicated/materialized a 103-chunk, 1,260,036-byte file; File Viewer rendered the payload directly and again after a native peer restart. Startup shell readiness stayed under 200ms in the measured runs, startup Advanced Status stayed between 1,040ms and 4,084ms, and all four modes had zero Browser warnings, zero WebSocket warnings, zero request failures, zero asset response errors, and zero cache repairs. Remaining PRD-001 work: repeated-cycle proof, broader full-mode closure, and network-flap/freeze pressure. |
| 451 | done | Command/reload frame-transport gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `src/core/rxdb/tools/browser_rust_smoke.js` | Added the focused command/reload direct gate under the same frame-transport contract. One ctox-dev run passed `browser-to-rust`, `command-browser-to-rust`, `command-burst-browser-to-rust`, `command-reload-browser-to-rust`, `command-restart-browser-to-rust`, and `command-midflight-restart-browser-to-rust` with one attempt per mode. The gate proved command acceptance, five-command burst delivery, Browser reload recovery, native peer restart recovery, and midflight native restart recovery. All modes emitted `business-os-advanced-status-v1` and app-local `ctox-rxdb-js`; zero request failures and zero cache repairs were observed. `command-restart-browser-to-rust` produced one expected WebSocket close warning while the native peer was intentionally restarted, with no Browser errors. Remaining PRD-001 work: repeated-cycle proof, broader full-mode closure, and network-flap/freeze pressure. |
| 450 | done | Retry-free compact frame-transport gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Reran the compact PRD-001 multi-mode direct gate with a two-attempt retry budget and confirmed every mode still passed on the first attempt, so no retry path was used. The gate covered `rust-to-browser`, 108-chunk stress, 142-chunk churn with four generation changes, native peer restart, and Signaling restart. All modes emitted `business-os-advanced-status-v1`, app-local `ctox-rxdb-js`, zero Browser warnings, zero WebSocket warnings, zero request failures, and zero cache repairs. Remaining PRD-001 work: repeated-cycle proof and broader full-mode closure. |
| 449 | done | Compact multi-mode frame-transport E2E gate | `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Ran the first compact multi-mode direct gate under the Advanced Status frame-transport contract. One sequential ctox-dev run passed `rust-to-browser`, `workspace-agent-artifacts-stress-rust-to-browser`, `workspace-agent-artifacts-churn-rust-to-browser`, `restart-browser-to-rust`, and `restart-signaling-browser-to-rust` with one attempt per mode. Coverage included 108-chunk stress, 142-chunk churn with four generation changes, native peer restart with seven checkpointed collections, and Signaling restart with file collection reconnect. Every mode emitted `business-os-advanced-status-v1`, app-local `ctox-rxdb-js`, zero Browser warnings, zero WebSocket warnings, zero request failures, and zero cache repairs. |
| 448 | done | Signaling restart frame-transport E2E proof | `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added focused Signaling restart evidence under the Advanced Status frame-transport contract. Verified `restart-signaling-browser-to-rust` through ctox-dev with one attempt: Signaling and native peer restarted, `desktop_files` and `desktop_file_chunks` refreshed checkpoint evidence, Browser wrote a file/chunk pair into Rust SQLite after reconnect, total duration `15,748ms`, startup Advanced Status `5,602ms`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and app-local `ctox-rxdb-js` runtime. |
| 447 | done | Native peer restart frame-transport E2E proof | `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added focused restart evidence under the Advanced Status frame-transport contract. Verified `restart-browser-to-rust` through ctox-dev with one attempt: native peer restarted, seven collections refreshed checkpoint evidence, Browser wrote a file/chunk pair into Rust SQLite after restart, total duration `12,663ms`, startup Advanced Status `4,086ms`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and app-local `ctox-rxdb-js` runtime. |
| 446 | done | Artifact churn frame-transport E2E proof | `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Extended the direct frame-transport E2E evidence to the artifact-churn lane. Verified `workspace-agent-artifacts-churn-rust-to-browser` through ctox-dev with one attempt: 20 files / 142 chunks replicated from Rust/CTOX into Business OS, four file generations advanced, four files added, total duration `18,479ms`, startup Advanced Status `5,579ms`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and app-local `ctox-rxdb-js` runtime. |
| 445 | done | Artifact stress frame-transport E2E proof | `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Extended the direct smoke evidence to the focused artifact-stress lane under the same Advanced Status frame-transport contract. The artifact modes now return and print `advanced_status` plus app-local `rxdb_runtime` evidence. Verified `workspace-agent-artifacts-stress-rust-to-browser` through ctox-dev with one attempt: 16 files / 108 chunks replicated from Rust/CTOX into Business OS, total duration `14,141ms`, startup Advanced Status `5,613ms`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and app-local `ctox-rxdb-js` runtime. |
| 444 | done | Live Advanced Status frame transport smoke contract | `src/core/rxdb/tools/browser_rust_smoke.js`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Made `sync.frameTransport` part of the live Browser/Rust health contract. `assertHealthyAdvancedStatusContract()` now rejects green statuses that lack frame transport telemetry, frame limits, `ctox-rxdb-frame-v1` protocol entries, or healthy pending-ACK / queue-depth totals. The `rust-to-browser` smoke now emits `advanced_status` and app-local `rxdb_runtime` evidence so ctox-dev can validate the status proof. Verified `node --check src/core/rxdb/tools/browser_rust_smoke.js` and a focused ctox-dev direct smoke: `rust-to-browser` passed in `10,150ms`, startup Advanced Status in `4,591ms`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and app-local `ctox-rxdb-js` runtime. |
| 443 | done | Advanced Status frame transport health | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/public/business-os`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Wired the Browser frame transport status into Business OS observability. `shared/sync.js` now subscribes to `replicationState.transportStatus$` for each collection and stores sanitized `frameTransport` snapshots. `app.js` now exposes `sync.frameTransport` in Advanced Status with aggregate totals, per-collection entries, queue/ACK/buffer thresholds, and a `frameTransportRealtimeHealthy` check. Hardened the ctox-dev frame-stream guard to require the status bridge, synced public Business OS assets, and verified JS syntax, JS smoke, `npm run guard:webrtc-frame-stream`, and `npm run guard:business-os-shell-assets`. |
| 442 | done | Rust executable WebRTC frame fixture builders | `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added Rust-side executable frame builder parity. Rust now builds `start`, `chunk`, `ack`, and `resume` transport frames through explicit helper functions and the focused `rust_transport_frame_builders_match_shared_fixture` test compares all generated frames against `src/core/rxdb/tests/fixtures/webrtc-frame-protocol.json`. This also fixed a real parity gap: Rust resume frames now include `attempt`, matching the Browser/fixture contract. Hardened the ctox-dev frame-stream guard to require the Rust builders and fixture test. Verified `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml`, focused Rust WebRTC tests (`10 passed`), JS smoke, and `npm run guard:webrtc-frame-stream`. |
| 441 | done | Browser executable WebRTC frame fixture exchange | `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added an executable Browser-side fixture exchange for the real framed sender. The app-local smoke now calls `sendFramed()` against a fake DataChannel, captures emitted `start` and `chunk` frames, returns fixture-compatible window/final ACK frames through `handleTransportFrame`, and asserts the emitted frames respect the shared fixture constraints (`ctoxFrame`, `windowSize`, `attempt`, chunk `seq`, and chunk size). Hardened the ctox-dev frame-stream guard to require this executable sender proof. Verified JS smoke and `npm run guard:webrtc-frame-stream`. |
| 440 | done | Shared WebRTC frame fixture strictness | `src/core/rxdb/tests/fixtures/webrtc-frame-protocol.json`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/public/business-os` | Tightened the shared frame fixture checks on both JS and Rust. The fixture now pins ACK/resume metadata explicitly (`receivedFrames`, `resume`, matching `ackSeq`, and start `windowSize`), the Browser smoke verifies those fields, and the Rust fixture test verifies the same contract. The ctox-dev frame-stream guard caught a stale public Business OS bundle during this wave; ran `npm run sync:business-os-shell`, then verified `npm run guard:webrtc-frame-stream` and `npm run guard:business-os-shell-assets`. Also verified JS smoke and focused Rust WebRTC tests. |
| 439 | done | Rust WebRTC send priority scheduler | `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added Rust-side scheduler parity below RxDB replication. `WebRTCRsConnectionHandler::send` now enqueues outbound frames into per-peer high/normal/low queues, a single drain serializes sends per peer, responses and token/protocol traffic are high priority, oversized `masterWrite` is low priority, and queue depth plus scheduled-frame counters are exposed in the Rust transport status JSON. Added focused tests for scheduler classification and queue-depth status. Hardened the ctox-dev guard to pin Rust scheduler symbols. Verified `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml`, focused Rust WebRTC tests, JS smoke, and `npm run guard:webrtc-frame-stream`. |
| 438 | done | Browser WebRTC send priority scheduler | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/public/business-os` | Added the Browser-side send scheduler below RxDB replication. All peer sends now enter per-peer high/normal/low queues, ACK/resume/protocol/token/response traffic is classified high priority, large `masterWrite` payloads are demoted, and high-priority inline traffic is drained between large frame windows so control traffic is not stuck behind long low-priority transfers. Transport status now exposes queue depth and last-priority counters. The JS smoke verifies high-priority token/control traffic is sent before low-priority writes, and the ctox-dev guard pins scheduler symbols in app-local and public bundles. Rebuilt the app-local bundle, synced public Business OS assets, and verified JS smoke, focused Rust WebRTC tests, `npm run guard:webrtc-frame-stream`, and `npm run guard:business-os-shell-assets`. |
| 437 | done | Replication-state transport status bridge | `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/public/business-os` | Exposed frame transport status through `CtoxWebRtcReplicationState` instead of leaving it only on the peer internals. The state now owns `transportStatus$`, subscribes to peer `transport-status` events, exposes `getTransportStatus()`, and decorates the snapshot with collection, topic, active peer count, and pull/push in-progress flags. Hardened the ctox-dev guard to pin this bridge, rebuilt the app-local bundle, synced ctox-dev public Business OS assets, and verified JS smoke, focused Rust WebRTC tests, `npm run guard:webrtc-frame-stream`, and `npm run guard:business-os-shell-assets`. |
| 436 | done | WebRTC frame guard hardening and public bundle sync | `src/core/rxdb/tests/fixtures/webrtc-frame-protocol.json`, `/Users/you/Documents/ctox-dev/scripts/assert-webrtc-frame-stream.mjs`, `/Users/you/Documents/ctox-dev/public/business-os` | Hardened the ctox-dev WebRTC frame-stream guard so it enforces Browser and Rust transport-status fields in the app-local source, app-local bundle, public ctox-dev bundle, and Rust handler. The stronger guard correctly failed because the public Business OS bundle was stale after the latest app-local fork changes. Ran `npm run sync:business-os-shell`, then verified `npm run guard:webrtc-frame-stream` and `npm run guard:business-os-shell-assets`. Also extended the shared frame fixture ACK with explicit `receivedFrames` and `resume` metadata. |
| 435 | done | Rust WebRTC frame transport status parity | `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added Rust-side status parity for the fork-owned WebRTC frame stream. `WebRTCRsConnectionHandler` now keeps a typed `WebRtcFrameTransportStatus` plus JSON snapshot with protocol limits, active transfers, pending ACKs, incoming transfers, completed ACK cache size, sent/received frames and bytes, retry/resume counts, ACK lag, and placeholder backpressure fields. Pending ACKs now retain sent timestamps so ACK lag can be computed when ACK frames arrive. Added focused Rust coverage for the status counters and fixed the WebRTC protocol fixture include path. Verified `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml` and focused `replication_webrtc::connection_handler_rs::tests`. |
| 434 | done | Browser WebRTC frame transport status | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added the first status surface for the fork-owned WebRTC frame stream. `CtoxWebRtcNativePeer.getTransportStatus()` and `transport-status` events now expose protocol limits, active transfers, pending ACKs, incoming transfers, completed ACK cache size, sent/received frames and bytes, retry/resume counts, last ACK lag, backpressure waits, and current DataChannel buffered amount. Rebuilt the app-local bundle and pinned the counters in the no-package-manager import smoke. Verified `node src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`. |
| 433 | done | WebRTC frame resume and ACK replay | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added `resume` control frames and completed-transfer ACK replay below RxDB replication. On ACK timeout the sender can ask the receiver for the highest contiguous frame already accepted before falling back to retry; if the receiver already completed the transfer, it replays the final ACK from a short-lived completed-transfer cache. The JS smoke now verifies partial resume ACKs and completed-transfer final ACK replay, and Rust implements the same receiver-side resume semantics plus resume probing before window retry. Verified `node src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml`, and focused `replication_webrtc::connection_handler_rs::tests`. |
| 432 | done | WebRTC frame window ACK pacing | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added the first concrete realtime pacing layer under RxDB replication. Browser and Rust now advertise a bounded frame window in `ctox-rxdb-frame-v1` `start` frames, send chunks in small windows, emit intermediate ACKs at contiguous frame boundaries, and block the next send window until the matching ACK arrives. The Browser smoke now verifies a multi-frame payload produces both a window ACK and a final ACK before the original RxDB request is emitted; the Rust handler resolves ACKs by `transferId` plus `ackSeq` and keeps compatibility with legacy ACKs that omit `ackSeq`. Verified `node src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml`, and focused `replication_webrtc::connection_handler_rs::tests`. |
| 431 | done | Browser frame reassembly smoke | `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Added reset-era Browser-side coverage for the existing `ctox-rxdb-frame-v1` reassembly path. The app-local no-package-manager smoke now feeds out-of-order start/chunk frames into `CtoxWebRtcNativePeer`, requires a single ACK for the completed transfer, and verifies the original `masterChangesSince` RxDB request is emitted after reassembly. Verified with `node src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`. This pinned JS reassembly before wave 432 added explicit window ACK pacing. |
| 430 | done | Rust receive path for CTOX WebRTC frame protocol | `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md` | Started the reset-era PRD-001 transport work with the first concrete JS/Rust parity repair. The app-local JS peer already sends oversized payloads as `ctox-rxdb-frame-v1` start/chunk/ack frames; Rust previously treated those JSON frames as normal `WebRTCMessage` payloads and would fail to decode Browser-framed large requests. `WebRTCRsConnectionHandler` now detects transport frames on receive, stores chunks by transfer id, validates transfer id, chunk sequence, frame count, and total byte length, reassembles the original JSON payload, sends the ACK frame, and only then emits the normal RxDB message/response stream. Added UTF-8 chunk-boundary and transport-frame detection regressions. Verified `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml`, focused `replication_webrtc::connection_handler_rs::tests`, and `node src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`. |
| 429 | blocked | WebRTC realtime transport reset | `src/core/rxdb/PORTING.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness-dashboard.html` | Reset the current RxDB/WebRTC closure state after reviewing the actual transport behavior. Previous large-file proofs are not accepted as current production closure because the live local server can fail native peer startup on an `outbound_messages` DB6 schema hash mismatch, and the existing file transfer path is document-level chunk replication with small batches rather than a dedicated realtime WebRTC frame stream. The required next implementation is deep in `ctox-rxdb-js` and `rxdb-rs`: frame envelopes, max frame budgets, pacing, ACK, retry, resume, in-flight byte windows, priority queues, DataChannel `bufferedAmount` backpressure, and JS/Rust protocol parity. PRD-001, PRD-013, PRD-014, PRD-016, PRD-035, and PRD-039 are reopened in the production-readiness tracker; PRD-040 is the new P0 blocker. |
| 428 | done | Post-UI-fix native ICE candidate guard and full 29-mode refresh | `src/core/rxdb/patches/rtc-ice/src/agent/agent_selector.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-428-ice-index-guard-full29-refresh.md` | Revalidated the exact current Business OS/RxDB code after the UI regression fixes. The first post-UI-fix gate exposed a real native WebRTC panic in the patched Rust ICE selector: `index out of bounds` at `agent_selector.rs:443` when a stale candidate-pair ping referenced an emptied local candidate list. Added bounds guards for controlling and controlled candidate pings, rebuilt `ctox` (`runtime/build/core-rxdb-integration-target/debug/ctox`, mtime `2026-05-26 01:46:27`), and reran all 29 strict one-mode groups with no retry path. Retained `/tmp/rxdb-wave493-ice-index-guard-full29-summary.json` plus group artifacts under `/var/folders/hf/x8jpryjx6xs8k0kwmkj5spkw0000gn/T/ctox-rxdb-split-gate/cycle-14-group-00.json` through `cycle-14-group-28.json`: `ok=true`, 29/29 groups, 29/29 modes, failed `[]`, max duration `50,602ms`, max `ctox_sync_config_wait_ms=40`, max Browser warnings/request failures `1/1` only in intentional Network Flap, zero cache repairs, zero startup reloads, and no `panicked`/`index out of bounds` log matches in cycle 14. This historical closure claim is superseded by wave 429 because the current review reopened the WebRTC realtime framing and native peer startup gates. |
| 427 | done | PRD-039 strict closure reaches 12 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-427-prd039-12cycle-closure.md` | Closed the strict exact-current PRD-039 release gate with the final three isolated one-mode full cycles. Retained `/tmp/rxdb-wave489-seq-cycle10-current-final-summary.json`, `/tmp/rxdb-wave490-seq-cycle11-current-final-summary.json`, and `/tmp/rxdb-wave491-seq-cycle12-current-final-summary.json`: all are `ok=true`, 29/29 groups, 29/29 modes, `retryCount=0`, failed `[]`, zero cache repairs, and zero startup reloads. Cycle 10 max duration/sync `26,771ms` / `18ms`; Cycle 11 max duration/sync `28,192ms` / `13ms`; Cycle 12 max duration/sync `54,111ms` / `162ms`. Stress/churn/large chunk coverage stayed at `108/142/103` in all three cycles. Browser warnings/errors/request failures were confined to disruptive restart/offline coverage and stayed inside the explicit strict budgets. PRD-039 now has 12 retry-free full 29-mode cycles on exact current code and is closed for the tracked startup/sync budget acceptance criteria. |
| 426 | done | Exact-code PRD-039 closure reaches 10 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-426-current-code-10cycle.md` | Ran the next exact-current full PRD-039 closure cycle in isolated one-mode groups. Retained `/tmp/rxdb-wave490-cycle10-current-clean/cycle-10-group-00.json` through `cycle-10-group-28.json` plus `/tmp/rxdb-wave490-cycle10-current-clean-summary.json`: `ok=true`, 29/29 groups, 29/29 modes, `retryCount=0`, failed `[]`, max duration `45,925ms`, max `ctox_sync_config_wait_ms=20`, max Browser warnings/errors/request failures `0/1/0`, zero cache repairs, and zero startup reloads. The single Browser error was confined to disruptive `restart-signaling-browser-to-rust` reconnect coverage and stayed inside the explicit budget. Large File Viewer restart reconstructed 103 chunks / 1,260,036 bytes with `waitForFileMs=19`; stress and churn retained 108 and 142 chunks. PRD-039 now has 10 retry-free full cycles on exact current code; 2 cycles remain for strict 12-cycle closure. |
| 425 | done | Exact-code PRD-039 closure reaches 9 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-425-current-code-9cycle.md` | Replaced the rejected Cycle 9 candidate with a fresh isolated one-mode-at-a-time full gate on the exact current tree. Retained `/tmp/rxdb-wave489-cycle09-current-clean/cycle-09-group-00.json` through `cycle-09-group-28.json` plus `/tmp/rxdb-wave489-cycle09-current-clean-summary.json`: `ok=true`, 29/29 groups, 29/29 modes, `retryCount=0`, failed `[]`, max duration `24,559ms`, max `ctox_sync_config_wait_ms=11`, max Browser warnings/errors/request failures `1/1/1`, zero cache repairs, and zero startup reloads. Budget usage was limited to expected disruptive modes: one warning in Command Reload and one warning/error/request failure in intentional Network Flap. Large File Viewer restart reconstructed 103 chunks / 1,260,036 bytes with `waitForFileMs=10`; stress and churn retained 108 and 142 chunks. PRD-039 now has 9 retry-free full cycles on exact current code; 3 cycles remain for strict 12-cycle closure. |
| 424 | done | Cycle 9 candidate rejected, no progress counted | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-424-cycle9-candidate-rejected.md` | Started the next PRD-039 full-cycle candidate after Cycle 8. Groups 0 through 17 passed, including Large Materialize, Large File Viewer, and Large File Viewer restart. Group 18 `network-flap-browser-to-rust` then exited with status `143` / SIGTERM after `10,396ms`; retained partial candidate `/tmp/rxdb-wave484-seq-cycle09-current-after-materialize-fix`, failed group `/tmp/rxdb-wave484-seq-cycle09-current-after-materialize-fix/cycle-09-group-18.json`, and log `/tmp/rxdb-wave484-seq-cycle09-current-after-materialize-fix/09-18-00-network_flap_browser_to_rust.log`. No retry was attempted or counted. PRD-039 remains 8/12 clean full cycles; 4 cycles remain. |
| 423 | done | Exact-code PRD-039 closure reaches 8 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-423-current-code-8cycle.md` | Reran the full 29-mode strict gate after Wave 422's Large File materialize/rescan guard. Retained `/tmp/rxdb-wave483-seq-cycle08-current-after-materialize-fix/cycle-08-group-00.json` through `cycle-08-group-28.json` plus `/tmp/rxdb-wave483-seq-cycle08-current-after-materialize-fix-summary.json`: `ok=true`, 29/29 groups, 29/29 modes, `retryCount=0`, failed `[]`, max duration `23,882ms`, max `ctox_sync_config_wait_ms=34`, max Browser warnings `1`, max Browser errors `5`, and max request failures `1` only in intentional `network-flap-browser-to-rust`, zero cache repairs, and zero startup reloads. Large Materialize, File Viewer, and File Viewer restart all passed; File Viewer restart reconstructed 103 chunks / 1,260,036 bytes with `waitForFileMs=12`. Stress and churn retained 108 and 142 chunk coverage. PRD-039 now has 8 retry-free full cycles on the exact current code; 4 cycles remain for strict 12-cycle closure. |
| 422 | done | Large File materialize rescan guard | `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-422-large-file-materialize-rescan.md` | Fixed the real Large File Viewer restart blocker exposed by the exact-current sequential gate: all 103 chunks and the 1,260,036 byte payload could arrive, but a later workspace scan could downgrade the same materialized file row from `available` back to `lazy`. CTOX now treats unchanged `available` rows as valid for lazy scans, preserves materialized content metadata on lazy rescan, and preserves the existing virtual workspace path when materializing by physical path. Added focused Rust regression `workspace_scan_does_not_downgrade_materialized_large_file`. Proof: focused Rust test passed, and `/tmp/rxdb-wave482-large-file-viewer-restart-materialize-fix.log` passed `workspace-large-file-viewer-restart-rust-to-browser` against a rebuilt CTOX binary with `ctox_sync_config_wait_ms=7`, 103 chunks, payload length `1,260,036`, `renderPayloadMs=503`, `waitForFileMs=83`, zero Browser warnings, zero Browser errors, zero request failures, zero cache repairs, and zero startup reloads. This is not counted as a PRD-039 closure cycle; PRD-039 remains 7/12 clean full cycles. |
| 421 | done | Signaling reconnect repair and tracker correction | `src/apps/business-os/shared/sync.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-421-signaling-reconnect-repair.md` | Corrected the Cycle 8 tracking entry: `/tmp/rxdb-wave470-full-29-current-cycle08-clean.json` and summary are not closure evidence because they show `ok=false`, `retryCount=1`, and a first-attempt `restart-signaling-browser-to-rust` failure after `75,399ms`. Fixed the real reconnect gap by scheduling a bounded restart repair when an already-active WebRTC replication loses its native peer and moves to `reconnecting` through `active$`, not only through the fatal error path. Synced the public Business OS shell into ctox-dev. Focused proof `/tmp/rxdb-wave471-restart-signaling-repair-3x.json` and summary: 3/3 Signaling restart cycles, `retryCount=0`, max duration `13,643ms`, max `ctox_sync_config_wait_ms=6`, zero Browser warnings, zero Browser errors, zero request failures, zero cache repairs, and zero startup reloads. PRD-039 remains at 7 retry-free full cycles; 5 clean full cycles remain for strict 12-cycle closure. |
| 420 | done | Exact-code PRD-039 closure reaches 7 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-420-current-code-7cycle.md` | Retained another full 29-mode gate on the exact current code after the split Cycle 6 proof. `/tmp/rxdb-wave469-full-29-current-cycle07-clean.json` and `/tmp/rxdb-wave469-full-29-current-cycle07-clean-summary.json`: `ok=true`, 29/29 modes, `retryCount=0`, failed `[]`, max duration `16,198ms`, max `ctox_sync_config_wait_ms=23`, Browser warnings `0`, Browser errors `0`, request failures `0`, cache repairs `0`, startup reloads `0`. Stress and churn retained 108 and 142 chunk coverage; Large File Viewer restart reconstructed 103 chunks / 1,260,036 bytes. PRD-039 now has 7 retry-free full cycles on the exact current code under strict production budgets; 5 cycles remain for strict 12-cycle closure. |
| 419 | done | Exact-code PRD-039 closure reaches 6 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-419-current-code-6cycle.md` | Reran Cycle 6 with the bounded split-gate runner after Wave 418's materialize metadata wait and transient peer-retention fixes. Retained `/tmp/rxdb-wave468-split-cycle06-summary.json` plus `/tmp/rxdb-wave468-split-cycle06/cycle-06-group-00.json` through `cycle-06-group-09.json`: `ok=true`, 10/10 groups, 29/29 modes, failed `[]`, max duration `62,625ms`, max `ctox_sync_config_wait_ms=95`, max Browser warnings `0`, request failures `0`, cache repairs `0`, startup reloads `0`. Stress and churn retained 108 and 142 chunk coverage; Large File Viewer restart reconstructed 103 chunks / 1,260,036 bytes. PRD-039 now has 6 retry-free full cycles on the exact current code under strict production budgets; 6 cycles remain for strict 12-cycle closure. |
| 418 | done | Materialize metadata wait and restart transient peer retention | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/revisions/PORTING.wave-418-materialize-restart-transient-fix.md` | Cycle 6 is explicitly not counted for PRD-039 closure. Retained `/tmp/rxdb-wave467-full-29-current-cycle06.json` and summary: `ok=false`, `terminationSignal=SIGTERM`, 19/29 modes reached. It exposed two real issues before termination: `workspace-large-materialize-rust-to-browser` could see correct chunks before the file row changed from `lazy` to `available`, and `restart-browser-to-rust` could drop a still-open `desktop_file_chunks` peer after a transient `masterChangesSince` timeout. Fixed the smoke materialize wait to require available metadata with expected payloads, restored two `masterChangesSince` attempts for all collections, and retained open peers after transient pull timeouts. Synced the public Business OS shell into ctox-dev. Focused proof `/tmp/rxdb-wave468-materialize-restart-fix.json`: Large Materialize and CTOX Restart passed first-attempt, max duration `20,921ms`, max sync wait `28ms`, zero Browser warnings, zero Browser errors, zero request failures, zero cache repairs, and zero startup reloads. PRD-039 remains at 5 retry-free full cycles; 7 cycles remain. |
| 417 | done | Exact-code PRD-039 closure reaches 5 budget-clean cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-417-current-code-5cycle.md` | Reran the full Cycle 5 gate after Wave 416's IndexedDB preflight false-repair fix. Retained `/tmp/rxdb-wave466-full-29-current-cycle05-clean.json` and `/tmp/rxdb-wave466-full-29-current-cycle05-clean-summary.json`: `ok=true`, 29/29 modes, `retryCount=0`, failed `[]`, max duration `17,482ms`, max `ctox_sync_config_wait_ms=18`, max Browser warnings `1` only in the expected Large File Viewer restart WebSocket close window and within budget, max Browser errors `2` only in intentional `network-flap-browser-to-rust`, zero request failures, zero cache repairs, zero startup reloads. Large materialization, File Viewer, and File Viewer restart each reconstructed 103 chunks / 1,260,036 bytes; stress and churn retained 108 and 142 chunk coverage. PRD-039 now has 5 retry-free full cycles on the exact current code under strict production budgets; 7 cycles remain for strict 12-cycle closure. |
| 416 | done | IndexedDB preflight false cache-repair fix | `src/apps/business-os/shared/db.js`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/revisions/PORTING.wave-416-indexeddb-preflight-repair.md` | Cycle 5 is explicitly not counted for PRD-039 closure: retained `/tmp/rxdb-wave465-full-29-current-cycle05.json` and `/tmp/rxdb-wave465-full-29-current-cycle05-summary.json` show `ok=false`, `retryCount=1`, and a first-attempt `migration-version-browser-to-rust` startup outlier with `ctox_sync_config_wait_ms=1,634` above the 1,500ms budget plus an `IndexedDB preflight timed out` Browser warning. Fixed the false repair path by allowing the IndexedDB preflight timeout to continue into the guarded RxDB open instead of throwing into schema-drift recovery, narrowed schema-drift timeout classification to real RxDB open/create timeouts, and made the smoke harness count local RxDB cache repair messages independent of console level. Synced the public Business OS shell into ctox-dev. Focused proof `/tmp/rxdb-wave466-migration-preflight-fix.json`: `migration-version-browser-to-rust` passed in one attempt, duration `7,222ms`, `ctox_sync_config_wait_ms=6`, zero Browser warnings, zero Browser errors, zero request failures, zero cache repairs, and zero startup reloads. PRD-039 remains at 4 clean cycles; 8 cycles remain. |
| 415 | done | Exact-code PRD-039 closure reaches 4 green cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-415-current-code-4cycle.md` | Reran Cycle 4 after Wave 414's public shell sync removed the CTOX module Web Stack Browser error. Retained `/tmp/rxdb-wave464-full-29-current-cycle04-clean.json` and `/tmp/rxdb-wave464-full-29-current-cycle04-clean-summary.json`: `ok=true`, 29/29 modes, `retryCount=0`, failed `[]`, max duration `15,693ms`, max `ctox_sync_config_wait_ms=21`, max Browser warnings `0`, max request failures `4` only in the intentional `network-flap-browser-to-rust` mode and within budget, zero cache repairs, zero startup reloads. Large materialization, File Viewer, and File Viewer restart each reconstructed 103 chunks / 1,260,036 bytes. PRD-039 now has 4 clean full cycles on the exact current code; 8 cycles remain for strict 12-cycle closure. |
| 414 | done | Public Business OS shell asset sync after CTOX module web-stack drift | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-414-public-shell-webstack-sync.md` | The Cycle 4 run surfaced a non-transport Browser error in `command-reload-browser-to-rust`: `loadWebStackOverview is not defined` from the CTOX module. The Core source already contained the Web Stack helpers, but `ctox-dev/public/business-os/modules/ctox/index.js` was stale. Synced Business OS shell assets into ctox-dev with `npm run sync:business-os-shell`, verified `npm run guard:business-os-shell-assets`, `node --check` for the Core and public CTOX module, and retained focused proof `/tmp/rxdb-wave463-command-reload-no-webstack-error.json`: `command-reload-browser-to-rust` passed with `browser_error_count=0`, zero Browser warnings, zero request failures, zero cache repairs, and zero startup reloads. The earlier full Cycle 4 artifact is not counted as clean closure evidence because it captured the now-fixed Browser error. |
| 413 | done | Exact-code PRD-039 closure reaches 3 green cycles | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-413-current-code-3cycle.md` | Extended closure evidence on the exact Wave 412 BatchSize 2 / reconnect-retry / Browser UI harness code. Retained `/tmp/rxdb-wave460-full-29-after-retry-and-harness.json`, `/tmp/rxdb-wave461-full-29-current-cycle02.json`, and `/tmp/rxdb-wave462-full-29-current-cycle03.json`: all three are `ok=true`, 29/29 modes, `retryCount=0`, failed `[]`, zero Browser warnings, zero request failures, zero cache repairs, and zero startup reloads. Worst current-cycle duration is `22,763ms`; worst `ctox_sync_config_wait_ms` is `301ms`. PRD-039 still requires the full 12-cycle closure; 9 cycles remain. |
| 412 | done | BatchSize 2 reconnect handoff and current full gate | `src/apps/business-os/shared/sync-contract.js`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/revisions/PORTING.wave-412-batch2-reconnect-full-gate.md` | The split closure attempt exposed a real `restart-signaling-browser-to-rust` handoff race after the file-chunk BatchSize 2 repair: `channel_pairing_state` could time out on `masterChangesSince` while the native peer was still finishing master-handler subscription after Signaling restart. Kept the smaller `desktop_file_chunks` transport envelope at BatchSize 2 and added a controlled Browser-side retry only for idempotent `masterChangesSince` when the peer remains open; `masterWrite` is not retried. Repaired the Browser UI smoke Matrix page path so `browser-lifecycle-ui` and `browser-handoff-ui` start directly on `/index.html#browser` instead of reloading after startup. Verified JS syntax and diff guards, focused Signaling restart, focused Browser UI Matrix, and retained `/tmp/rxdb-wave460-full-29-after-retry-and-harness.json`: `ok=true`, 29/29 modes, `retryCount=0`, failed `[]`, max duration `18,759ms`, max `ctox_sync_config_wait_ms=19`, zero Browser warnings, zero request failures, zero cache repairs, and zero startup reloads. PRD-039 still needs strict multi-cycle closure with this exact code. |
| 411 | done | Split closure cycle 2 proof | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-411-split-closure-cycle2.md` | Ran the next PRD-039 split closure cycle with the Wave 410 runner. An initial attempt to run two groups in parallel overloaded the local test environment and made simple command modes exceed the 90s budget, so that artifact is explicitly discarded as harness pressure. Reran the full cycle as one group at a time. Retained `/tmp/rxdb-wave411-split-closure-cycle02-summary.json`: `ok=true`, 10/10 groups, 29/29 modes, failed `[]`, max duration `19,069ms`, max `ctox_sync_config_wait_ms=16`, max Browser warnings `0`, max request failures `2` only in Network Flap, zero cache repairs, zero startup reloads. Large materialization, File Viewer, and File Viewer restart each reconstructed 103 chunks / 1,260,036 bytes. This gives the second clean full 29-mode cycle after the current frame-budget fix; PRD-039 still needs the remaining multi-cycle closure passes. |
| 410 | done | Resumable split-gate runner for strict closure shards | `src/core/rxdb/tools/browser_rust_split_gate.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-410-split-gate-runner.md` | Added a bounded split-gate runner for PRD-039 closure work. Long foreground `browser_rust_soak.js` runs continue to receive external `SIGTERM` in the Codex environment, so the new runner executes one shard of isolated strict Matrix modes in parallel, forces `SMOKE_MATRIX_ATTEMPTS=1`, writes a durable group summary after each mode finishes, keeps per-mode logs/results, and prints compact max-duration/sync/warning/request/repair/reload metrics. Verified `node --check` and retained `/tmp/rxdb-wave410-split-runner-check/cycle-01-group-00.json`: group 0 passed 3/3 modes (`browser-to-rust`, `command-browser-to-rust`, `migration-version-browser-to-rust`), max duration `10,760ms`, max `ctox_sync_config_wait_ms=9`, zero Browser warnings, zero request failures, zero cache repairs, zero startup reloads. This does not close PRD-039 by itself; it makes the remaining strict multi-cycle closure practical and auditable. |
| 409 | done | Split strict full 29-mode PRD-039 gate after frame-budget repair | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-409-split-full29-frame-budget.md` | The continuous one-process Soak runner was still externally terminated by `SIGTERM` between smoke modes, so it remains unsuitable as closure evidence in this Codex foreground environment. To avoid fake progress, reran the full PRD-039 release mode set as 29 isolated strict Matrix executions with `SMOKE_MATRIX_ATTEMPTS=1`, the same strict budgets, the active BatchSize 8 + 96 KiB Rust `desktop_file_chunks` response cap, and no retry path. Retained `/tmp/rxdb-wave409-split-full29-summary.json`: `ok=true`, 29/29 modes, 29/29 attempts, failed `[]`, max duration `22,099ms`, max `ctox_sync_config_wait_ms=82`, max Browser warnings `0`, max request failures `2` only in `network-flap-browser-to-rust`, zero cache repairs, zero startup reloads. Large materialization, File Viewer, and File Viewer restart each reconstructed 103 chunks / 1,260,036 bytes; artifact stress and churn passed with the current frame-budget repair. This restores the current full one-cycle gate. PRD-039 still needs the strict multi-cycle closure. |
| 408 | done | File-chunk WebRTC frame budget and current artifact proof | `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/src/replication_protocol/index_mod.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-408-file-chunk-frame-budget.md` | Re-ran the active tree after Wave 407 and reproduced that BatchSize 8 still timed out in `workspace-agent-artifacts-stress-rust-to-browser`: Browser received metadata plus the first 8 chunk rows, then `desktop_file_chunks` timed out on the next `masterChangesSince`. Repaired the real remaining pressure point without returning to a fixed low batch: the Browser initial peer-open replication now goes through the same `pullInProgress` guard as live pulls, and Rust caps `desktop_file_chunks` `masterChangesSince` responses to a safe 96 KiB WebRTC frame budget while checkpointing to the last document actually sent. This keeps the requested BatchSize 8 contract but prevents oversized Base64 chunk response frames from blocking the DataChannel. Verified `cargo fmt --check`, `cargo test --locked --manifest-path src/core/rxdb/Cargo.toml desktop_file_chunk_response_limit`, `cargo build --locked --bin ctox --target-dir runtime/build/core-rxdb-integration-target`, JS syntax checks, and retained `/tmp/rxdb-wave408-artifact-block-frame-budget.json`: normal artifacts `18,796ms`, stress `15,892ms` with 16 files / 108 chunks, Churn `15,855ms` with 20 files / 142 chunks, max `ctox_sync_config_wait_ms=25`, zero Browser warnings, zero request failures, zero cache repairs, zero startup reloads. PRD-039 still needs a fresh full 29-mode gate and strict 12-cycle closure with this exact code. |
| 407 | done | Real file-chunk WebRTC race fix and BatchSize 8 restore | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/src/replication_protocol/index_mod.rs`, `src/apps/business-os/shared/sync-contract.js`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-407-file-chunk-webrtc-race-batch8.md` | Supersedes the BatchSize 3 workaround with the actual race/pressure repair. The Browser WebRTC request path now registers pending responses before sending the DataChannel frame, so fast native `masterChangesSince` replies cannot be dropped. Rust maps `desktop_file_chunks` master change-stream events to lightweight `RESYNC` sentinels, so full chunk payloads no longer compete with pull responses on the same WebRTC channel. With those two fixes in place, restored BatchSize 8 for Browser sync config, ctox-dev public shell assets, Rust SQLite external polling, and direct smoke replication. Retained evidence from the robust path: `/tmp/rxdb-wave446-churn-heartbeat.json` (`ok=true`, Churn 20 files / 142 chunks in `12,999ms`), `/tmp/rxdb-wave447-artifact-block-heartbeat.json` (`ok=true`, artifacts + stress + churn, max duration `16,185ms`), and `/tmp/rxdb-wave448-full-29-heartbeat.json` (`ok=true`, 29/29 modes, 29 attempts, max duration `45,266ms`, max `ctox_sync_config_wait_ms=126`, zero Browser warnings, zero request failures, zero cache repairs, zero startup reloads). PRD-039 still needs the strict 12-cycle closure soak. |
| 406 | done | Re-lock file chunk BatchSize 3 stress path | `src/apps/business-os/shared/sync-contract.js`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-406-file-chunk-batch3-stress.md` | Rechecked the active tree before rerunning the full PRD-039 gate and found the file-chunk transport had drifted back to BatchSize 8 in the Browser contract, ctox-dev public shell asset, Rust SQLite external change polling, and the direct smoke harness. The remaining-modes matrix then reproduced the concrete failure in `workspace-agent-artifacts-stress-rust-to-browser`: `desktop_files` metadata arrived, but `desktop_file_chunks` timed out on `masterChangesSince`, leaving only 10 chunks on attempt 1 and 8 chunks on attempt 2. Restored the proven BatchSize 3 / generic chunk BatchSize 4 contract and rebuilt the real smoke binary. Retained `/tmp/rxdb-wave410-stress-batch3-all-contracts.json`: `ok=true`, 1/1 stress attempt, 16 files, 108 chunks, max chunk count 25, duration `33,269ms`, `ctox_sync_config_wait_ms=17`, zero Browser warnings, zero request failures, zero cache repairs, and zero startup reloads. This restores the stress lane; next gate is the full 29-mode one-cycle run with this exact contract. |
| 405 | done | Restore stable desktop_file_chunks transport envelope | `src/apps/business-os/shared/sync-contract.js`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-405-churn-batch3-restore.md` | Repaired the Churn regression found by the managed full-gate screen run: the active tree had drifted `desktop_file_chunks` back to BatchSize 8, the same response-envelope size previously proven too large in Wave 396. A focused pre-fix Churn run timed out `masterChangesSince` and delivered only the first 8 chunk rows while file metadata was present. Restored the stable BatchSize 3 contract across Browser sync config, the direct smoke harness, and Rust SQLite external change polling, rebuilt the real smoke binary with `cargo build --locked --bin ctox --target-dir runtime/build/core-rxdb-integration-target`, and synced the Business OS public contract into `ctox-dev`. Retained screen evidence `/tmp/rxdb-wave407-churn-batch3-restore-screen-3cycle.json`: `ok=true`, 3 cycles of `workspace-agent-artifacts-churn-rust-to-browser`, `retryCount=0`, durations `23,599ms`, `14,203ms`, `14,084ms`, max `ctox_sync_config_wait_ms=16`, 20 files / 142 chunks each cycle, zero Browser warnings, zero request failures, zero cache repairs, and zero startup reloads. This fixes the Churn regression; PRD-039 closure still requires the full 29-mode one-cycle artifact and strict 12-cycle gate. |
| 404 | blocked | Foreground full gate interrupted by external SIGTERM | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-404-foreground-full-gate-sigterm.md` | Reran the continuous full 29-mode PRD-039 one-cycle strict gate after Wave 403's signal forwarding repair. The data path made strong progress before the foreground runner received an external `SIGTERM` during `network-flap-browser-to-rust`: modes through `tab-freeze-browser-to-rust` printed first-attempt OK evidence, including 103-chunk large materialization, File Viewer, File Viewer restart, restart, Signaling restart, rollover, and tab-freeze with zero cache repairs and zero startup reloads. The Soak cleanup itself worked: retained `/tmp/rxdb-wave404-full-29-after-soak-signal-forwarding-1cycle.json` records `terminationSignal=SIGTERM`, and no Matrix/Smoke/CTOX child remained after cleanup. Because the foreground process was killed before a complete continuous artifact could be retained, this is not closure evidence. Added explicit `matrix_result_path` logging and retained short proof `/tmp/rxdb-wave404-two-mode-soak-result.json`: two-mode Soak writes `modes=2`, `ok=true`. Next run must be launched as a managed background process with PID/log/result polling to avoid Codex foreground-session termination. |
| 403 | done | Soak signal forwarding and orphan cleanup | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-403-soak-signal-forwarding.md` | Hardened the PRD-039 soak wrapper after another interrupted full-gate attempt exposed the remaining parent/child lifecycle gap: the parent Soak process could receive `SIGTERM` while blocked in `spawnSync`, leaving Matrix/Smoke/CTOX children running and writing an unusable summary with `modes=0`. The wrapper now runs the Matrix as a detached child process group with async `spawn`, forwards `SIGINT`/`SIGTERM`/`SIGHUP`, force-kills after a grace window, and records `terminationSignal` in the retained summary. Verified syntax and retained controlled-abort proof `/tmp/rxdb-wave403-soak-signal-forwarding.json`: `ok=false`, `terminationSignal=SIGTERM`, child process `signal=SIGTERM`, `modes=0` because the abort happened before the first Matrix result, and no Smoke/Matrix/CTOX child remained on the test port. This fixes the runner cleanup problem; it is not PRD-039 closure. Next gate is rerunning the continuous full 29-mode one-cycle strict run. |
| 402 | done | Matrix child lifecycle repair for command reload | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-402-matrix-child-lifecycle-command-reload.md` | Repaired the Wave 401 full-gate harness blocker by removing the `/bin/zsh` heartbeat wrapper around each smoke child and returning to direct `spawnSync(process.execPath, [browser_rust_smoke.js])` with stdout/stderr log files and the existing mode timeout. This avoids orphaned shell wrappers and `SIGTERM` results that prevented `SMOKE_MATRIX_RESULT_PATH` from being written even after child evidence existed. Verified syntax and retained focused proof `/tmp/rxdb-wave402-command-reload-direct-spawnsync.json`: `command-reload-browser-to-rust`, `ok=true`, 1/1 attempt, duration `12,755ms`, `ctox_sync_config_wait_ms=25`, `reload_verified=1`, exactly one queue task, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and zero startup reloads. Next gate is rerunning the full 29-mode one-cycle strict run. |
| 401 | blocked | Full 29-mode gate blocked by matrix termination | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-401-full-29-shell-sequential-termination.md` | Started the full 29-mode PRD-039 one-cycle strict gate after Wave 400's shell/harness repair. The run is not closure-capable because the matrix process was terminated by `SIGTERM` during `command-reload-browser-to-rust` before writing `SMOKE_MATRIX_RESULT_PATH`; retained soak summary `/tmp/rxdb-wave401-full-29-after-shell-sequential-1cycle.json` records `ok=false`, `error="matrix result was not written"`, `signal="SIGTERM"`. Console/log evidence before termination is useful but not sufficient for closure: `browser-to-rust`, `command-browser-to-rust`, `migration-version-browser-to-rust`, and `command-burst-browser-to-rust` passed first attempt with zero startup reloads; `command-burst-browser-to-rust` now runs in `15,077ms` with `shellReadyWaitMs=1,581`, zero Browser warnings, zero WebSocket warnings, and zero request failures. The interrupted `command-reload-browser-to-rust` child log still showed accepted command projection and `reload_verified=1`, but also an IndexedDB closing replication error during page reload; next work is to harden matrix child lifecycle/result writing and the command-reload shutdown window before rerunning the full gate. |
| 400 | done | Shell startup and smoke harness budget repair | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-400-shell-startup-harness-budget.md` | Repaired two PRD-039 timing regressions without adding data fallbacks. The smoke root now copies only the protected Business OS entry files (`app.js`, `index.html`) and symlinks the rest of the app tree, reducing `smoke_root_prepare_ms` to `4-5ms` while still protecting the real source from stale external writers. The smoke CLI now exits explicitly after successful cleanup so the matrix runner no longer waits on stale internal handles. Business OS keeps the Shell UI modules dynamic but loads the small critical shell imports sequentially and warms them after session validation; this removes the parallel module-graph stalls that previously caused 8s shell waits or 60s smoke-hook reloads. Retained strict focused proof `/tmp/rxdb-wave402-command-burst-shell-sequential-3cycle.json`: `ok=true`, 3 cycles of `command-burst-browser-to-rust`, `retryCount=0`, durations `15,054ms`, `15,055ms`, `15,115ms`, `shellReadyWaitMs=144/1,642/1,115`, `smoke_root_prepare_ms=4/4/5`, zero Browser warnings, zero WebSocket warnings, zero request failures, zero cache repairs, and zero startup reloads under a 30s duration budget. The full 29-mode one-cycle gate and strict 12-cycle closure remain open. |
| 399 | done | Large File Viewer restart budget repair | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-399-large-file-viewer-restart-lazy-sync.md` | Repaired the Wave 398 large File Viewer restart blocker without loosening budgets. The smoke harness now emits outer startup/evaluate timings and inner setup timings, and skips the redundant startup Advanced Status wait for file/command collections in large materialization modes because those collections are explicitly started, peer-open checked, and validated inside the mode. The controlled native-peer restart path now suspends all currently active collections before the native peer restart and resumes only the File Viewer critical set, preventing inactive module reconnect work from starving the command/file reconnect. Business OS now keeps inactive module collection sync lazy by default; `eagerModuleSync=1` or `ctox.businessOs.eagerModuleSync=1` remains the explicit opt-in. Retained focused proof `/tmp/rxdb-wave399-large-file-viewer-restart-lazy-module-sync-3cycle.json`: `ok=true`, 3 cycles of `workspace-large-file-viewer-restart-rust-to-browser`, `retryCount=0`, durations `69,844ms`, `77,646ms`, `80,493ms`, max `ctox_sync_config_wait_ms=65`, 103 chunks / 1,260,036 bytes in every cycle, zero Browser cache repairs, zero startup reloads, zero WebSocket warnings, and max request failures `2` inside the existing budget. Next gate is the full retained 29-mode one-cycle strict run, then the 12-cycle closure. |
| 398 | blocked | 12-cycle closure retry finds large File Viewer restart budget blocker | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-398-large-file-viewer-restart-budget.md` | Started the strict 12-cycle PRD-039 closure soak after Wave 397 restored the full one-cycle baseline. The run became non-closure-capable in cycle 1, so it was stopped instead of spending hours on an invalid run. Retained partial matrix evidence `/tmp/rxdb-wave398-full-29-after-chunk-batch-cycle1-blocker.json`: the first 13 modes passed first try, including Browser/Rust file, command, migration, burst, reload, workspace, 4-file artifacts, 16-file / 108-chunk stress, 20-file / 142-chunk churn, background indexing, workspace update, large materialization, and non-restart large File Viewer. The blocker is `workspace-large-file-viewer-restart-rust-to-browser`: attempt 1 correctly reconstructed 103 chunks / 1,260,036 bytes with zero cache repair and zero startup reloads, but exceeded the 90s budget at `104,279ms`; attempt 2 passed at `78,001ms`. Phase timings show the payload path itself is fast (`restartMs=17,167`, `mountViewerMs=7,757`, `renderPayloadMs=503`, `waitForFileMs=12`), so the unexplained duration overhead is around the restart-mode harness/window before or around measured phases. Next work is to instrument and remove that unmeasured restart-mode latency before rerunning the 12-cycle closure. |
| 397 | done | Full 29-mode strict gate after chunk batch repair | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-397-full-29-after-chunk-batch.md` | Reran the retained full 29-mode one-cycle strict gate after Wave 396's chunk batch repair. Retained `/tmp/rxdb-wave397-full-29-after-chunk-batch-1cycle.json`: `ok=true`, 29/29 modes, `retryCount=0`, elapsed `1859s`, zero Browser cache repairs, zero startup reloads, max `ctox_sync_config_wait_ms=91`, max duration `86,939ms`, max Browser warnings `3`, max request failures `2`. The formerly blocked `workspace-large-materialize-rust-to-browser` passed first try in `60,588ms` with 103 chunks / 1,260,036 bytes; large File Viewer restart passed in `81,700ms`; midflight command restart passed in `86,939ms` with exactly one queue task. This restores the full one-cycle baseline. PRD-039 still requires the same strict profile over the 12-cycle closure soak before completion. |
| 396 | done | Large-file chunk transport batch repair | `src/apps/business-os/shared/sync-contract.js`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-396-large-file-chunk-batch.md` | Repaired the Wave 395 large-file chunk starvation path without loosening budgets. The root cause was the 103-chunk large-file path being constrained to one `desktop_file_chunks` document per Browser pull, native WebRTC batch, and SQLite external change-stream poll. A first BatchSize 8 experiment proved too large for the WebRTC response envelope and timed out; the retained fix uses BatchSize 3, keeping each chunk response below the data-channel message-size risk while avoiding one-chunk-per-second progress. Rebuilt the real smoke binary with `cargo build --locked --bin ctox --target-dir runtime/build/core-rxdb-integration-target`. Retained `/tmp/rxdb-wave396-large-materialize-batch3-3cycle.json`: `ok=true`, 3 cycles of `workspace-large-materialize-rust-to-browser`, `retryCount=0`, durations `63,758ms`, `60,706ms`, `78,250ms`, 103 chunks and 1,260,036 bytes materialized each cycle, max `ctox_sync_config_wait_ms=43`, zero request failures, zero Browser cache repairs, and zero startup reloads. Next gate is the retained full 29-mode one-cycle strict run. |
| 395 | blocked | Full 29-mode gate finds large-file chunk starvation | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-395-large-file-chunk-starvation.md` | Reran the retained full 29-mode one-cycle strict gate after the two focused Wave 392 blockers were green. Retained `/tmp/rxdb-wave395-full-29-after-focused-blockers-1cycle.json`: the run is not closure-capable. The first 11 modes passed first try, covering Browser→Rust file/chunk, command, migration-version, burst, reload idempotency, simple workspace, 4-file agent artifacts, 16-file / 108-chunk stress, 20-file / 142-chunk churn, background indexing, and workspace generation update. The next mode, `workspace-large-materialize-rust-to-browser`, failed twice: Browser saw the `desktop_files` row and folder rows but `chunkCount=0` / `totalChunkCount=0` for the 1,260,036 byte file, then exceeded the 90s duration budget (`108,040ms` and `109,366ms`). Sync-config stayed fast (`14/15ms`) and there were no cache repairs or startup reloads. This is now the current PRD-039 blocker: under full sequence pressure the large-file metadata can arrive before `desktop_file_chunks` make progress. |
| 394 | done | Stale-generation sync-config spike isolation | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-394-stale-generation-sync-config-proof.md` | Reran the second Wave 392 blocker as a focused strict gate after the smoke binary rebuild and midflight command repair. Retained `/tmp/rxdb-wave394-stale-generation-sync-config-3cycle.json`: `ok=true`, 3 cycles of `file-chunk-stale-generation-error-browser-status`, `retryCount=0`, durations `60,665ms`, `60,518ms`, `60,743ms`, max `ctox_sync_config_wait_ms=27`, expected `CtoxFileChunkIntegrityError` / `ctox_file_chunk_missing`, requested stale generation had `0` chunks, live generation had one chunk, zero request failures, zero Browser cache repairs, and zero startup reloads. One cycle observed two shell import-stall warnings inside the current budget. The two Wave 392 focused blockers are now green in isolation; next gate is a full retained 29-mode one-cycle strict run before the 12-cycle closure attempt. |
| 393 | done | Midflight command restart SQLite-lock repair | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-393-midflight-command-sqlite-lock.md` | Rebuilt the real smoke binary with `cargo build --locked --bin ctox --target-dir runtime/build/core-rxdb-integration-target` after verifying `cargo check`. The Rust native command consumer now serializes command acceptance with the native RxDB write lock, retries transient SQLite busy/locked acceptance failures instead of projecting the command as failed, keeps SQLite connections on the shared busy-timeout/WAL settings, and reuses an existing queue task for a command id if a retry observes partial progress. Retained focused proof `/tmp/rxdb-wave393-command-midflight-sqlite-lock-3cycle.json`: `ok=true`, 3 cycles of `command-midflight-restart-browser-to-rust`, `retryCount=0`, durations `60,585ms`, `60,402ms`, `60,419ms`, max `ctox_sync_config_wait_ms=26`, every command ended `status=accepted`, every task ended `task_status=queued`, and every command produced exactly one queue task. Zero Browser cache repairs and zero startup reloads; one intentional midflight restart cycle observed three shell-asset request failures inside the existing budget. The remaining Wave 392 blocker is the isolated stale-generation sync-config spike before rerunning the full 29-mode strict gate. |
| 392 | blocked | Full 29-mode lazy-sync regression finds command restart SQLite lock | `src/apps/business-os/app.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-392-full-29-lazy-sync-retry-blockers.md` | Reran the full 29-mode PRD-039 one-cycle regression after Wave 391's lazy inactive-module sync repair. Retained `/tmp/rxdb-wave392-full-29-budgeted-lazy-module-sync-1cycle.json`: all 29 modes eventually passed, but the run is invalid under `SOAK_FAIL_ON_RETRY=1` because two first attempts retried. The first real blocker is `command-midflight-restart-browser-to-rust`: attempt 1 had stable startup (`ctox_sync_config_wait_ms=263`) but Rust failed the native business command store with SQLite `database is locked`, projecting the command as failed with zero queue rows; attempt 2 passed. The second blocker is `file-chunk-stale-generation-error-browser-status`: attempt 1 produced the expected file-integrity error and had no Browser/WebSocket/request/cache/reload issue, but `ctox_sync_config_wait_ms=4,052` exceeded the 1,500ms budget; attempt 2 passed at `276ms`. Positive regression signal: max mode duration across attempts was `65,487ms`, zero Browser cache repairs, zero startup reloads, and the lazy-sync Churn path stayed fast in the full matrix (`24,990ms`, 20 files / 142 chunks, zero warnings/request failures). Next repair targets the Rust command-store SQLite lock under midflight restart, then the isolated late sync-config spike. |
| 391 | done | Lazy inactive-module sync startup budget repair | `src/apps/business-os/app.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-391-lazy-module-sync-startup-budget.md` | Repaired the Wave 390 startup-pressure blocker without loosening budgets or adding fallbacks. Business OS now starts critical collections and the active module over RxDB/WebRTC, but no longer eagerly starts every inactive module collection during shell boot; inactive module collections start when their module is opened, with an opt-in `eagerModuleSync=1`/localStorage switch for diagnostics. Advanced Status now exposes `sync.moduleSyncPolicy` with `lazy-active-module`, started-module count, and deferred modules so regressions remain observable. Verified syntax with `node --check src/apps/business-os/app.js`. Retained focused 3-cycle proof `/tmp/rxdb-wave391-lazy-module-sync-churn-background-3cycle.json`: `ok=true`, 3 cycles across Churn and Background, `retryCount=0`, max duration `62,299ms`, max `ctox_sync_config_wait_ms=553`, max warning count `2`, `browser_websocket_warning_count=0`, zero request failures, zero Browser cache repairs, zero startup reloads. Churn still replicated 20 files / 142 chunks with four generation replacements and four added files; Background replicated 4 files / 4 chunks with queue-task workspace-root indexing. Full 29-mode / 12-cycle closure still remains open. |
| 390 | blocked | Churn + Background sequence exposes optional collection startup pressure | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-390-optional-collection-startup-pressure.md` | Retained `/tmp/rxdb-wave390-churn-background-3cycle.json` from the combined Churn+Background focused gate after Wave 389. The first `workspace-agent-artifacts-churn-rust-to-browser` attempt timed out at `185,403ms` despite `ctox_sync_config_wait_ms=443`; console evidence showed many optional non-file collection WebRTC warnings (`WebSocket is closed before connection is established`, `masterChangesSince` timeouts) before the page closed. The retry then failed during server startup, making the focused sequence non-closure-capable. This isolated a production-relevant startup-pressure issue: data replication itself was not corrupted, but eager inactive-module collection startup could saturate the Browser/Signaling/native peer under agent-artifact churn. Next repair is to keep shell startup on required/active collections and defer inactive module collections until opened, while exposing the policy in Advanced Status. |
| 389 | done | Background-indexer PRD-039 budget repair | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-389-background-index-budget-repair.md` | Reduced `workspace-agent-artifacts-background-rust-to-browser` to its actual acceptance purpose: proving queue-task workspace-root background indexing over RxDB/WebRTC. The 16-file / 108-chunk load remains covered by the explicit stress mode; the background mode now uses the four-file agent artifact seed and emits `phase_timings` for background artifact wait and Advanced Status. Updated the matrix expectation from 16 files to 4 files. Verified syntax and retained focused proof `/tmp/rxdb-wave389-background-index-focused.json`: `ok=true`, duration `69,663ms`, `ctox_sync_config_wait_ms=923`, 4 files / 4 chunks, `phase_timings={"waitBackgroundArtifactsMs":11,"advancedStatusMs":16}`, zero request failures, zero cache repairs, zero startup reloads. Retained 3-cycle soak `/tmp/rxdb-wave389-background-index-3cycle.json`: `ok=true`, 3/3 cycles, `retryCount=0`, max duration `57,880ms`, max `ctox_sync_config_wait_ms=1,034`, zero request failures, zero cache repairs, zero startup reloads. Full 29-mode / 12-cycle closure still remains open. |
| 388 | blocked | Full 12-cycle retry after Agent Churn batch repair finds background-index pressure | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-388-background-index-duration-blocker.md` | Restarted the strict 12-cycle / 29-mode PRD-039 closure candidate after Wave 387's Churn batch verification repair. The run was manually stopped after it became non-closure-capable, so no retained final JSON exists. Console evidence still identified two performance blockers before abort: `workspace-agent-artifacts-churn-rust-to-browser` first attempt now produced correct data but remained narrowly over budget at `94,799ms` (`phase_timings={"mutateAndSyncMs":27331,"waitChangedArtifactsMs":4894}`, 20 files / 142 chunks, zero cache repairs, zero startup reloads), then retry passed at `86,721ms`. The next mode, `workspace-agent-artifacts-background-rust-to-browser`, failed attempt 1 with `ctox_sync_config_wait_ms=1,634ms` and `durationMs=126,237ms` while still replicating 16 files / 108 chunks with zero cache repairs and zero startup reloads. PRD-039 remains open; next work is to reduce the background-indexer smoke path and the remaining Churn overhead under full sequence pressure, without loosening the budgets. |
| 387 | done | Agent Churn batch artifact verification under PRD-039 budget | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-387-agent-churn-batch-verification.md` | Repaired the Wave 386 duration blocker by replacing sequential per-file workspace artifact polling with batch polling over `desktop_files` and `desktop_file_chunks`. This avoids rescanning all chunks once per expected file and verifies all expected files/generations in one polling pass. Added Churn phase timings (`mutateAndSyncMs`, `waitChangedArtifactsMs`). Retained focused proof `/tmp/rxdb-wave387-agent-churn-batch-focused.json`: `workspace-agent-artifacts-churn-rust-to-browser`, `ok=true`, 20 files / 142 chunks, 4 changed generations, 4 added files, duration `67,118ms`, `ctox_sync_config_wait_ms=324`, `phase_timings={"mutateAndSyncMs":16948,"waitChangedArtifactsMs":793}`, zero cache repairs, zero startup reloads. Retained focused 3-cycle soak `/tmp/rxdb-wave387-agent-churn-batch-3cycle.json`: `ok=true`, 3/3 cycles, `retryCount=0`, max duration `83,276ms`, max `ctox_sync_config_wait_ms=368`, zero request failures, zero cache repairs, zero startup reloads. The Agent Churn blocker is fixed in isolation; PRD-039 still needs the full 29-mode / 12-cycle closure soak. |
| 386 | blocked | Full 12-cycle PRD-039 retry finds Agent Churn duration blocker | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-386-agent-churn-duration-blocker.md` | Restarted the strict 12-cycle / 29-mode PRD-039 closure run after Wave 385's Large File phase diagnostics. Retained `/tmp/rxdb-wave386-full-29-budgeted-safe-12cycle.json`: cycle 1 completed all 29 modes with no retries; cycle 2 then failed `workspace-agent-artifacts-churn-rust-to-browser` on duration budget. Attempt 1 produced correct sync evidence but took `115,510ms`; attempt 2 also produced correct sync evidence but took `92,209ms`. Both attempts replicated all 20 expected workspace files and 142 chunks, including 4 changed generations and 4 added files, with `ctox_sync_config_wait_ms=412/327`, zero Browser cache repairs, zero startup reloads, and zero request failures. This is a product-performance blocker, not a data-integrity blocker: the Agent Churn path is correct but too slow under long-run sequence pressure. PRD-039 remains open; next work is to add phase timings to Agent Churn and reduce its sync/materialization latency before rerunning the full 12-cycle gate. |
| 385 | done | Large File Viewer phase timings and focused PRD-039 isolation | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-385-large-file-viewer-phase-timings.md` | Added explicit phase timings to the large File Viewer smoke paths so the 90s duration-budget blocker from Wave 384 can be separated into native restart, collection wait, viewer import, viewer mount/materialization, payload render, file wait, and Advanced Status phases. Retained focused one-mode proof `/tmp/rxdb-wave385-large-file-viewer-restart-phase-timings.json`: `workspace-large-file-viewer-restart-rust-to-browser`, `ok=true`, duration `51,601ms`, `ctox_sync_config_wait_ms=291`, 103 chunks / 1,260,036 bytes, zero cache repairs, zero startup reloads, phase timings `restartMs=12,130`, `mountViewerMs=12,103`, `renderPayloadMs=502`, and `waitForFileMs=21`. Retained focused 3-cycle proof `/tmp/rxdb-wave385-large-file-viewer-restart-phase-3cycle.json`: 3 cycles across `workspace-large-materialize-rust-to-browser`, `workspace-large-file-viewer-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser`, 9/9 attempts, `ok=true`, `retryCount=0`, elapsed `470s`, max duration `73,708ms`, max `ctox_sync_config_wait_ms=363`, zero cache repairs, zero startup reloads. The isolated Large File path is below the 90s gate; PRD-039 remains open until the full 29-mode / 12-cycle release soak passes with the same budgets. |
| 384 | blocked | 12-cycle PRD-039 closure attempt after Network-Flap budget isolation | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-384-12cycle-large-file-restart-budget-blocker.md` | Restarted the strict 12-cycle / 29-mode PRD-039 closure run with normal-mode budgets (`SMOKE_BROWSER_WARNING_BUDGET=8`, `SMOKE_BROWSER_REQUEST_FAILURE_BUDGET=4`, `SMOKE_SYNC_CONFIG_WAIT_BUDGET_MS=1500`, `SMOKE_MODE_DURATION_BUDGET_MS=90000`) and Network-Flap-only budgets (`SMOKE_NETWORK_FLAP_BROWSER_WARNING_BUDGET=80`, `SMOKE_NETWORK_FLAP_REQUEST_FAILURE_BUDGET=80`). Cycle 1 completed all 29 modes with no retries. Cycle 2 then failed `workspace-large-file-viewer-restart-rust-to-browser` on the first attempt because the mode took `91,303ms`, exceeding the 90s duration budget. Evidence still showed correct data-path behavior for the failed attempt: `ctox_sync_config_wait_ms=300`, 103 chunks / 1,260,036 bytes, Advanced Status, zero cache repairs, zero startup reloads, `browser_warning_count=2`, `browser_request_failure_count=2`, and `browser_error_count=2` from the forced restart window. The run was aborted because `SOAK_FAIL_ON_RETRY=1` makes any retry invalid for closure. PRD-039 remains open; next work is to reduce or isolate the Large File Viewer restart-mode duration before rerunning 12 cycles. |
| 383 | done | Network-flap budget isolation for PRD-039 soak | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-383-network-flap-budget-isolation.md` | The first strict 12-cycle PRD-039 candidate was aborted in cycle 2 after `network-flap-browser-to-rust` correctly recovered and replicated but exceeded the global Browser warning/request-failure budgets during the intentional offline window (`16` warnings, `18` request failures on the first attempt). This made the gate semantically wrong: intentional offline diagnostics were treated like normal startup regressions. The matrix now supports explicit Network-Flap-only budgets via `SMOKE_NETWORK_FLAP_BROWSER_WARNING_BUDGET` and `SMOKE_NETWORK_FLAP_REQUEST_FAILURE_BUDGET` while keeping the stricter global `SMOKE_BROWSER_WARNING_BUDGET` and `SMOKE_BROWSER_REQUEST_FAILURE_BUDGET` for all normal modes. Verified `node --check src/core/rxdb/tools/browser_rust_smoke_matrix.js` and retained `/tmp/rxdb-wave383-network-flap-mode-budget.json`: `network-flap-browser-to-rust`, `ok=true`, 1/1 attempt, `ctox_sync_config_wait_ms=318`, `durationMs=65,981`, `browser_warning_count=2`, `browser_request_failure_count=0`, `browser_error_count=20`, zero cache repairs, zero startup reloads, app-local no-package-manager `ctox-rxdb-js`. The 12-cycle closure run must be restarted with the mode-specific budgets. |
| 382 | done | Full 29-mode budgeted 3-cycle soak | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-382-full-29-budgeted-3cycle.md` | Extended the PRD-039 budget gate from one cycle to three complete 29-mode Browser/Rust RxDB/WebRTC cycles with strict warning, request-failure, sync-config, duration, retry, cache-repair, and startup-reload budgets enabled. Retained `/tmp/rxdb-wave382-full-29-budgeted-safe-3cycle.json`: `ok=true`, 3 cycles, 87 mode executions, `retryCount=0`, elapsed 2078s, max mode duration `49,143ms`, no mode above 60s, `ctox_sync_config_wait_ms` min `267ms` / max `282ms`, `startup_smoke_hook_wait_ms` max `64ms`, zero failed attempts, zero Browser cache repairs, zero startup reloads, zero asset response errors, and app-local no-package-manager `ctox-rxdb-js`. Browser warnings totalled `18` and request failures `7`, bounded to shell import retry / intentional restart windows; `65` Browser errors are confined to intentional network-flap/offline diagnostics. This raises PRD-039 confidence but does not close it; the final gate remains the same strict budget profile over the 12-cycle release soak. |
| 381 | done | Full 29-mode budgeted startup matrix | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-381-full-29-budgeted-startup.md` | Ran the complete 29-mode Browser/Rust RxDB/WebRTC matrix with strict PRD-039 budgets enabled after the safe shell-import classification repair. Retained `/tmp/rxdb-wave381-full-29-budgeted-safe-1cycle.json`: `ok=true`, 1 cycle, 29 modes, `retryCount=0`, elapsed 672s, max mode duration `48,928ms`, `ctox_sync_config_wait_ms` min `269ms` / max `279ms`, zero Browser request failures, zero Browser cache repairs, zero startup reloads, zero asset response errors, and app-local no-package-manager `ctox-rxdb-js`. Browser warnings totalled `7` and were bounded to shell import retries in four fresh-start modes; network-flap produced the expected 20 Browser errors but still reconnected and replicated. This clears the full one-cycle PRD-039 budget gate; remaining closure work is the same budget profile over the 12-cycle release soak. |
| 380 | done | Safe shell import and cache-repair classification gate | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-380-safe-shell-import-classification.md` | Replaced the risky Wave 379 preload/short-timeout experiment with the safe production fix. Kept the conservative `8000ms` shell import timeout and single retry, moved deferred custom icon registration out of the critical shell import race, and narrowed `isRxDbSchemaDriftError()` so generic shell import `timed out` errors no longer trigger local RxDB cache repair. The smoke matrix keeps the new optional hard budgets for Browser warnings, request failures, per-mode duration, and `ctox_sync_config_wait_ms`. Verified `/tmp/rxdb-wave380-browser-start-cache-classification.json`: `browser-to-rust`, `ok=true`, `retryCount=0`, `durationMs=19,422`, `ctox_sync_config_wait_ms=272`, zero Browser warnings, zero request failures, zero cache repairs. Verified `/tmp/rxdb-wave380-restart-cache-classification.json`: `workspace-large-file-viewer-restart-rust-to-browser`, `restart-signaling-browser-to-rust`, and `command-midflight-restart-browser-to-rust`, `ok=true`, `retryCount=0`, all first attempts, max duration `42,919ms`, `ctox_sync_config_wait_ms` `275/273/273ms`, zero Browser warnings, zero request failures, zero cache repairs, zero startup reloads, and app-local no-package-manager `ctox-rxdb-js`. |
| 379 | superseded | Shell startup import budget experiment | `src/apps/business-os/index.html`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-379-shell-import-budget.md` | Tried to reduce restart-mode latency with Shell `modulepreload` hints and a `2500ms` dynamic import timeout. The focused restart run improved the worst path, but follow-up normal-start budget checks proved the approach was unsafe: critical shell imports could time out on fresh profiles and the generic `timed out` classifier could misroute a shell import failure into RxDB cache repair. The preload/short-timeout/retry-loop changes were reverted. The useful part of this wave, the optional smoke-matrix warning/request-failure/duration/sync-config budgets, remains active and was validated safely in Wave 380. |
| 378 | done | External Signaling 12-cycle release soak and startup-budget findings | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-378-external-signaling-29mode-12cycle.md` | Reran the full 29-mode Browser/Rust RxDB/WebRTC release soak for 12 cycles with external Signaling and no smoke rebuild. Retained `/tmp/rxdb-wave378-external-signaling-29mode-12cycle.json`: `ok=true`, 12 cycles, 29 modes, 348 mode executions, `retryCount=0`, elapsed 9,243,949ms, zero startup reloads, zero Browser cache repairs, zero asset response errors, zero post-run smoke/profile temp artifacts, app-local no-package-manager `ctox-rxdb-js`, and repeated command, workspace, agent-artifact, large-file, restart, network-flap, and typed negative-status coverage. The startup-config fetch profile stayed product-fast compared with the old harness-skewed run (`ctox_sync_config_wait_ms` min `266ms`, max `712ms`; smoke-hook max `621ms`). This is strong data-path evidence but not PRD-039 closure: the long soak exposed remaining startup/import hardening work, with 13 attempts above 60s, max mode duration `80,148ms`, 130 Browser warnings, 33 request failures, and repeated shell/module dynamic-import retry warnings under restart and large-file stress. Next hardening target is to isolate shell/module imports from data-plane restart churn and make the warning/request-failure budget strict enough for production closure. |
| 377 | done | External Signaling 29-mode startup-budget baseline | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-377-external-signaling-29mode-baseline.md` | Reran the full 29-mode Browser/Rust RxDB/WebRTC matrix for one cycle after moving the smoke Signaling server into an external child process. Retained `/tmp/rxdb-wave377-external-signaling-29mode-1cycle.json`: `ok=true`, 1 cycle, 29 modes, `retryCount=0`, elapsed 678,110ms, zero startup reloads, zero Browser cache repairs, zero request failures, zero asset response errors, zero WebSocket warnings, zero Browser warnings, max smoke-hook wait 44ms, max mode duration 47,938ms, and post-run smoke/profile temp artifact count `0`. The critical startup metric is now stable across the whole matrix: `ctox_sync_config_wait_ms` min `267ms`, max `271ms`. The only Browser errors are the 20 expected offline errors in `network-flap-browser-to-rust`, and the 29 resource 404s are the known favicon/probe requests. This proves the previous 13-14s sync-config numbers were harness-induced, but the release gate still needs the same external-Signaling timing profile over the 12-cycle soak before PRD-039 can close. |
| 376 | done | Startup timing harness isolation and sync identity cache | `src/core/business_os/store.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-376-external-signaling-startup.md` | Investigated the slow `ctox_sync_config_wait_ms` values from Wave 375 instead of treating them as product truth. A direct CTOX server probe with the same binary and a separate Signaling process reached `/api/business-os/sync/config` in about 300ms, while the Browser smoke still measured about 13.9s. Root cause was the smoke harness running the Signaling server in the same Node event loop as the startup measurement; native peer startup opens many WebRTC collection rooms and starved the host-side fetch loop. The smoke harness now uses `src/core/rxdb/tools/local_signaling_server.js` as an external child process by default, keeping inline Signaling only for `signaling-error-browser-status` where controlled error injection is required. Added a small Rust sync-identity cache so repeated sync-config calls do not reread stable instance/password identity from the secret store while the native peer is starting. Verified `cargo build --locked --bin ctox --target-dir runtime/build/core-rxdb-integration-target`, JS syntax, and focused smokes: pre-fix `browser-to-rust` was `ctox_sync_config_wait_ms=13903`; post-fix `browser-to-rust` was `269ms`; `restart-signaling-browser-to-rust` was `270ms`; inline `signaling-error-browser-status` was `271ms`. All retained zero startup reloads, zero cache repairs, zero request failures, and app-local no-package-manager `ctox-rxdb-js`. Full 12-cycle / 29-mode soak should be rerun with external Signaling to replace the old harness-skewed timing aggregate. |
| 375 | done | Full expanded release soak after temp cleanup | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-375-full-29-mode-12cycle-clean.md` | Completed the full direct Browser/Rust RxDB/WebRTC release-length soak after the disk-log, startup-wait, max-buffer, tombstone, and temp-root cleanup repairs. Retained `/tmp/rxdb-wave376-full-29-mode-12cycle-after-temp-cleanup.json`: `ok=true`, 12 cycles, 29 modes, 348 smoke executions, `retryCount=0`, elapsed 12,985,053ms, zero startup hook reloads, zero Browser cache repairs, zero asset response errors, max smoke-hook wait 49ms, app-local no-package-manager `ctox-rxdb-js`, Browser/Rust command and reload idempotency proofs, workspace file/update proofs, CTOX/Codex agent artifact sync, stress, churn, and background-indexer proofs, 1,260,036 byte / 103 chunk large-file materialization and File Viewer restart evidence, CTOX restart, Signaling restart, native peer rollover, tab freeze, network flap, midflight command restart, typed Signaling/peer/checkpoint/schema/replication/file-integrity negative-status coverage, and post-run smoke/profile temp artifact count `0`. This closes the previous long-run harness artifact blocker: the old cycle-7 `SIGKILL` point was passed without retry. Open hardening is now performance, not correctness: `ctox_sync_config_wait_ms` peaked at 14,651ms, 13 attempts exceeded 60s by design or restart cost, max mode duration was 151,046ms in `workspace-large-file-viewer-restart-rust-to-browser`, and one restart window produced three expected WebSocket close warnings while still replicating the file. |
| 374 | done | Long-soak temp artifact cleanup and command reload stability | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-374-smoke-temp-cleanup.md` | Reran the full 12-cycle / 29-mode closure after Wave 373. Cycles 1-6 completed all 29 modes. Cycle 7 then killed `command-reload-browser-to-rust` twice with `SIGKILL`; the first attempt had already reached the local sync-config endpoint, the retry died after 95ms before evidence. Focused `command-reload-browser-to-rust` immediately passed 3 cycles with `retryCount=0`, proving the Browser reload command idempotency path itself stayed green. The real long-run issue was temp artifact pressure: more than 600 stale `ctox-rxdb-smoke-*` / `ctox-rxdb-browser-profile-*` directories remained from prior smoke executions. The smoke harness now puts Browser profiles inside the per-attempt smoke root, standalone smokes clean their generated roots by default, and the matrix gives every attempt an explicit `CTOX_SMOKE_ROOT` that it removes even after child `SIGKILL`; `CTOX_SMOKE_KEEP_ARTIFACTS=1` remains the debug opt-out. Verified JS syntax, cleared stale temp artifacts, retained `/tmp/rxdb-wave375-command-reload-cleanup-3cycle.json` with 3 clean command reload cycles, zero retries, zero startup reloads, zero cache repairs, zero request failures, and confirmed post-run temp artifact count stayed `0`. The full 12-cycle / 29-mode closure must be restarted with temp cleanup enabled. |
| 373 | done | Disk-backed soak/matrix logs for large File Viewer stability | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-373-disk-backed-soak-logs.md` | Restarted the full 12-cycle / 29-mode closure run after Wave 372. Cycles 1-6 completed all 29 modes; cycle 7 progressed through large materialization (`1,260,036` bytes / `103` chunks) and then killed `workspace-large-file-viewer-rust-to-browser` twice with `SIGKILL` before the child smoke could emit startup evidence. An isolated direct `workspace-large-file-viewer-rust-to-browser` run passed immediately, proving the data path and File Viewer materialization were green. The failure was isolated to long-run harness resource pressure from buffered child output, not to RxDB/WebRTC data sync. The matrix now writes each smoke attempt to temporary disk logs for evidence parsing and streams them back, removing pipe-buffer kills; the soak runner now streams matrix stdout/stderr instead of retaining each full cycle in memory. Verified syntax and retained `/tmp/rxdb-wave373-large-file-focused-3cycle-after-disk-logs.json`: 3 cycles across `workspace-large-materialize-rust-to-browser`, `workspace-large-file-viewer-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser`, `retryCount=0`, `1,260,036` byte payloads, `103` chunks, Advanced Status on restart, zero startup reloads, zero cache repairs, and zero request failures. The full 12-cycle / 29-mode closure run must be restarted with the disk-backed logging repair. |
| 372 | done | Network-flap long-run matrix buffer hardening | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-372-network-flap-max-buffer.md` | Restarted the 12-cycle / 29-mode closure run after Wave 371. Cycle 1 completed all 29 modes, and cycle 2 progressed through restart, Signaling restart, rollover, and tab freeze before `network-flap-browser-to-rust` was killed with `SIGKILL` after producing the expected high-volume offline WebSocket/schema-import diagnostics. Focused `network-flap-browser-to-rust` reproduced that the data path itself is green over 3 cycles, so the failure was isolated to the matrix harness buffering child output from the intentionally noisy offline window. `browser_rust_smoke_matrix.js` now raises the `spawnSync` `maxBuffer` to 32 MiB by default (`SMOKE_MATRIX_MAX_BUFFER_BYTES` override) and records `result.error` code/message in the retained attempt JSON. Verified matrix syntax and retained `/tmp/rxdb-wave372-network-flap-focused-2cycle-after-maxbuffer.json`: 2 cycles, `retryCount=0`, Browser recovers from offline, app-local no-package-manager `ctox-rxdb-js`, Browser file/chunk payload reaches Rust SQLite, zero startup reloads, zero cache repairs, and zero request failures. The full 12-cycle / 29-mode closure run must be restarted with this matrix-buffer repair. |
| 371 | done | Long-soak startup wait diagnostics for midflight restart | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-371-midflight-startup-wait.md` | Restarted the full 12-cycle / 29-mode closure run after Wave 370. Cycle 1 completed all 29 modes without retry, including the tombstone fixture. Cycle 2 progressed through the stress, churn, restart, Signaling restart, rollover, tab-freeze, and network-flap paths, then failed before the Browser-side command assertion in `command-midflight-restart-browser-to-rust`: both attempts timed out waiting for the local smoke CTOX server's `/api/business-os/sync/config` endpoint after 20s, with no sync evidence emitted. This isolated the issue to harness startup churn after many sequential CTOX/Browser process restarts, not to command replication payloads. The smoke harness now gives the CTOX sync-config endpoint an explicit `SMOKE_SYNC_CONFIG_WAIT_MS` budget (default 60s), emits `ctox_sync_config_wait_ms`, and detects both exit codes and signal exits while waiting. Verified JS syntax and retained focused `/tmp/rxdb-wave371-midflight-focused-3cycle-after-startup-wait.json`: 3 cycles, `retryCount=0`, stable command projection with exactly one `ctox_queue_tasks` row per command, zero startup reloads, zero cache repairs, zero request failures, and measured sync-config waits around 6.5s. The full 12-cycle / 29-mode closure run must be restarted with this harness startup-diagnostics repair. |
| 370 | done | File chunk tombstone negative-fixture retry repair | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-370-file-chunk-tombstone-retry-repair.md` | The first expanded 12-cycle / 29-mode closure attempt was stopped after cycle 2 because `file-chunk-tombstone-error-browser-status` retried once with `sqlite failed: database is locked (5)`. That made the run invalid under `SOAK_FAIL_ON_RETRY=1` even though the retry passed. Hardened the SQLite test helper with a bounded `.timeout 10000` / retry loop, deferred file-collection startup for the tombstone fixture, and made the fixture clear local live chunks while preserving file metadata so File Viewer consistently exercises the missing-content integrity path instead of racing a Browser push of stale live chunks back into SQLite. Verified JS syntax, focused tombstone smoke, and retained `/tmp/rxdb-wave370-tombstone-3cycle.json`: 3 cycles, `retryCount=0`, stable `CtoxFileChunkIntegrityError` / `ctox_file_chunk_missing`, `live_chunk_count=0`, zero startup reloads, zero cache repairs, zero request failures, and no WebSocket warnings. The full 12-cycle / 29-mode closure run must be restarted with this harness repair. |
| 369 | done | Full expanded 29-mode one-cycle release baseline | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-369-full-29-mode-1cycle.md` | Reran the complete direct Browser/Rust release baseline after adding background-indexed agent artifacts to the required mode set. Retained `/tmp/rxdb-wave369-full-29-mode-1cycle.json` passed with `ok=true`, 1 cycle, 29 modes, `retryCount=0`, elapsed 982s, zero startup hook reloads, zero Browser cache repairs, max smoke-hook wait 91ms, app-local no-package-manager `ctox-rxdb-js`, Browser/Rust command and file proofs, explicit workspace artifact stress and churn, background-indexed 16-file / 108-chunk agent workspace proof without explicit sync invocation, large-file materialization and File Viewer restart evidence, CTOX restart, Signaling restart, native peer rollover, tab freeze, network flap, midflight command restart, and typed Signaling/peer/checkpoint/schema/replication/file-integrity negative-status coverage. The 22 Browser errors, 7 Browser warnings, 2 request failures, and 1 WebSocket warning are confined to intentional disturbance paths such as network flap and Signaling restart or bounded import retry probes; no mode failed, no retry was used, and no startup/cache repair occurred. |
| 368 | done | Agent workspace background indexer coverage | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-368-agent-background-index.md` | Added `SMOKE_MODE=workspace-agent-artifacts-background-rust-to-browser` to verify the intended CTOX agent path without an explicit `ctox business-os files sync-workspace` call. The mode creates a real queue task with `--workspace-root`, starts the native CTOX Business OS peer with the background file indexer enabled, lets the Rust background scan write workspace artifacts into SQLite, and then requires the Browser to receive the files and chunks over RxDB/WebRTC. The startup harness now allows this mode to activate file collections before the desktop shell finishes loading while preserving the stricter shell-ready gate for all other modes. Verified JS syntax, direct smoke, retained matrix `/tmp/rxdb-wave368-agent-artifact-background-index-matrix.json`: 16 files, 108 chunks, max 25 chunks per file, app-local no-package-manager `ctox-rxdb-js`, `background_indexer=1`, `background_queue_task_created=1`, zero startup reloads, zero cache repairs, zero request failures, and no WebSocket warnings. Workflow guard now passes with `modes=29`, ctox-dev reconnect release coverage remains green, and the RxDB-only data-plane guard remains green. |
| 367 | done | Agent workspace churn and generation replacement | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-367-agent-artifact-churn.md` | Added `SMOKE_MODE=workspace-agent-artifacts-churn-rust-to-browser` to verify that CTOX/Codex workspace files do not only appear once, but continue to sync correctly after follow-up writes. The mode first syncs the 16-file stress workspace, then updates four existing files including two multi-chunk files, adds four new files including a multi-chunk delta, reruns `ctox business-os files sync-workspace`, and requires Browser-side payload equality plus changed generation ids for every updated file. Promoted the mode into the smoke matrix, soak runner, and manual release-soak required mode list. Verified direct smoke and retained matrix `/tmp/rxdb-wave367-agent-artifact-churn-matrix.json`: 20 files, 142 chunks, max 32 chunks per file, `updated_generation_changes=4`, `added_count=4`, zero startup reloads, zero cache repairs, zero request failures, and no WebSocket warnings. Workflow guard now passes with `modes=28`, ctox-dev reconnect release coverage remains green, and the RxDB-only data-plane guard remains green. |
| 366 | done | Agent workspace artifact chunk stress | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-366-agent-artifact-stress.md` | Added `SMOKE_MODE=workspace-agent-artifacts-stress-rust-to-browser` to cover higher-volume CTOX/Codex workspace output over the strict RxDB/WebRTC file path. The mode seeds 16 nested agent artifacts including Markdown, JSON, JSONL, TOML, CSV, and four multi-chunk text artifacts, runs `ctox business-os files sync-workspace`, and verifies Browser-side virtual paths, payload equality, generation ids, total chunks, and max per-file chunk count. Promoted the mode into the default smoke matrix, soak runner, and manual release-soak workflow required mode list so artifact stress cannot silently drop out of release evidence. Verified JS syntax, direct smoke, retained matrix `/tmp/rxdb-wave366-agent-artifact-stress-matrix.json`, `node src/core/rxdb/tools/assert_rxdb_soak_workflow.js` with `modes=27`, and `npm run guard:rxdb-reconnect-release-coverage` in ctox-dev. The green evidence replicated 16 files, 108 chunks, max 25 chunks per file, zero startup reloads, zero cache repairs, zero request failures, and no WebSocket warnings. |
| 365 | done | Release soak workflow drift repair for command reload coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-365-command-reload-release-coverage.md` | Promoted the new `command-reload-browser-to-rust` idempotency mode into the manual GitHub Actions RxDB/WebRTC soak workflow default and release-required mode lists. This keeps local `browser_rust_soak.js`, smoke-matrix support, and CI release coverage aligned so Browser reload command idempotency cannot silently drop out of release evidence. Verified `node src/core/rxdb/tools/assert_rxdb_soak_workflow.js` with `modes=26`, `npm run guard:rxdb-reconnect-release-coverage` in ctox-dev, and the RxDB-only data-plane guard. |
| 364 | done | Command idempotency after Browser reload | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-364-command-reload-idempotency.md` | Added `SMOKE_MODE=command-reload-browser-to-rust` to close the remaining Browser reload command-idempotency gap. The smoke dispatches through the real Business OS CommandBus, immediately reloads `/index.html`, waits for the app-local RxDB/WebRTC data plane to become healthy again, and requires exactly one `ctox_queue_tasks` projection for the same command id. Added the mode to the evidence-required matrix and default soak mode list. Verified JS syntax, direct smoke, retained matrix `/tmp/rxdb-wave364-command-reload-matrix.json`, and retained focused command soak `/tmp/rxdb-wave364-command-reload-3cycle.json`: `ok=true`, 3 cycles, 9 mode executions across normal command, Browser reload command, and midflight CTOX restart command paths, `retryCount=0`, every command produced exactly one queue task, `reload_verified=1` in all reload attempts, zero startup reloads, zero cache repairs, zero request failures, max smoke-hook wait 42ms. The single Browser warning/error in the aggregate came from the intentional reload/import retry boundary; the command path stayed green and idempotent. |
| 363 | done | Full 12-cycle reconnect closure soak | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-363-full-12cycle-reconnect-closure.md` | Closed the multi-hour direct Browser/Rust reconnect gate after the Wave 362 peer shutdown and scoped restart cleanup. Retained `/tmp/rxdb-wave362-reconnect-cleanup-full-25-12cycle.json` passed with `ok=true`, 12 cycles, 25 modes, 300 mode executions, `retryCount=0`, elapsed 7401s, zero startup hook reloads, zero Browser cache repairs, zero request failures, zero asset response errors, max smoke-hook wait 74ms, app-local no-package-manager `ctox-rxdb-js`, 103-chunk / 1,260,036 byte large-file materialization and File Viewer restart evidence, CTOX restart, Signaling restart, native peer rollover, tab freeze, network flap, midflight command restart, command burst, migration-version command acceptance, Rust-origin workspace file/update/agent-artifact sync, and typed Signaling/peer/checkpoint/schema/replication/file-integrity negative-status coverage. The 458 Browser errors and 6 Browser warnings in the aggregate summary are expected from deliberately forced offline Network Flap and import-timeout retry probes; no mode failed and no retry was used. |
| 362 | done | WebRTC peer shutdown and scoped collection restart cleanup | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-362-reconnect-cleanup.md` | The full 12-cycle closure run after Wave 361 reached cycle 12 and failed at `restart-browser-to-rust` because the Browser-side `CtoxWebRtcNativePeer.close()` called a non-existent `connection.close()` on internal connection records. That could abort bridge shutdown and leave stale WebRTC/Signaling peers in collection rooms while IndexedDB was closing. The CTOX-owned `ctox-rxdb-js` fork now closes DataChannels/PeerConnections through `removeConnection()`, rejects all pending request and request-observer waiters on peer shutdown, and serializes incoming request-handler errors back over the data channel instead of surfacing unhandled Browser exceptions. A second failure in `workspace-large-file-viewer-restart-rust-to-browser` showed `restartCollections(['desktop_files', ...])` was stopping every active Business OS bridge but only restarting the requested file collections; scoped restarts now stop only the requested collections so partial File Viewer repair cannot stall `ctox_runtime_settings` or other shell-critical bridges. Verified JS syntax, RxDB-only guard, no-package-manager import smoke, isolated `restart-browser-to-rust`, isolated `workspace-large-file-viewer-restart-rust-to-browser`, and retained full 25-mode one-cycle soak `/tmp/rxdb-wave362-reconnect-cleanup-full-25-1cycle.json` with `ok=true`, 25 modes, `retries=0`, elapsed 555s, max smoke-hook wait 42ms, zero startup reloads, zero cache repairs, app-local no-package-manager `ctox-rxdb-js`, 103-chunk large-file restart proof, CTOX restart, Signaling restart, native peer rollover, tab freeze, network flap, midflight command restart, and typed negative-status coverage. The 12-cycle release-soak closure must be rerun with this reconnect cleanup. |
| 361 | done | Full 25-mode regression pass after dynamic import repair | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-361-full-25-after-import-timeout.md` | Reran the complete direct Browser/Rust 25-mode matrix after Wave 360. Retained `/tmp/rxdb-wave360-full-25-mode-1cycle-after-import-timeout.json` passed with `ok=true`, `retryCount=0`, 25 modes, elapsed 569s, max smoke-hook wait 37ms, zero startup hook reloads, zero cache repairs, app-local no-package-manager `ctox-rxdb-js`, Browser/Rust file and command proofs, workspace update generation proof, multi-file agent artifacts, 103-chunk large-file materialization, File Viewer restart, CTOX restart, Signaling restart, native peer rollover, tab freeze, network flap, midflight command restart, and typed Signaling/checkpoint/schema/replication/file-integrity negative status evidence. The 12-cycle closure soak is now the next gate. |
| 360 | done | Shell dynamic import timeout/retry and deferred icon registration | `src/apps/business-os/app.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-360-shell-import-timeout-retry.md` | The first full 12-cycle / 25-mode run after Wave 359 passed cycles 1 and 2, then failed in cycle 3 at `workspace-update-rust-to-browser`: Browser authentication and `business_module_catalog` were present, but `activeModule` stayed unset and required data bridges had not started. This was another shell-startup blocker, not a WebRTC/RxDB payload failure. Business OS now wraps boot-critical dynamic imports with an 8s timeout and one cache-busted retry, applies the same protection to module and schema imports, and defers custom module icon registration so icon work cannot block the first visible workspace. Verified JS syntax, RxDB-only guard, focused `workspace-update-rust-to-browser`, and retained 3-cycle sequence `/tmp/rxdb-wave360-import-timeout-3cycle.json` covering workspace update, Browser command burst, and native peer rollover with `retries=0`, zero startup hook reloads, and no cache repairs. Full 25-mode rerun and 12-cycle closure remain open. |
| 359 | done | Boot-critical preload removal and full 25-mode regression pass | `src/apps/business-os/index.html`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-359-boot-preload-removal-full-25.md` | The first full 25-mode rerun after Wave 358 correctly failed at `rollover-native-peer-browser-to-rust`: the data path completed after a harness reload, but startup evidence showed `startup_smoke_hook_reload_count=1` and `startup_smoke_hook_wait_ms=120112`. The root cause was the HTML boot path starting `modulepreload` fetches for `shared/db.js`, `shared/sync.js`, and the large local RxDB bundle before the small shell bootloader module, allowing rare app-script starvation in fresh Browser profiles. Business OS now removes those preloads from the critical path, bumps the shell build key to `20260523-bootgraph3`, and the smoke harness waits for the real app-installed status surface instead of treating an inline smoke placeholder as readiness. Verified JS syntax, RxDB-only guard, focused `rollover-native-peer-browser-to-rust`, then retained full 25-mode one-cycle soak `/tmp/rxdb-wave358-full-25-mode-1cycle-after-preload-removal.json`: all 25 modes passed with `retries=0`, elapsed 581s, zero startup hook reloads, zero cache repairs, app-local no-package-manager `ctox-rxdb-js`, Browser/Rust command/file proofs, 103-chunk large-file materialization, restart/rollover/tab-freeze/network-flap coverage, and typed Signaling/checkpoint/schema/replication/file-integrity status evidence. Full 12-cycle release-soak closure remains open. |
| 358 | done | Initial shell catalog refresh gate and stricter startup evidence | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-358-shell-catalog-refresh-gate.md` | The next full 12-cycle attempt exposed a real shell-ready starvation path, not lost RxDB payload: `command-burst-browser-to-rust` had modules loaded and required collections healthy, but `activeModule` stayed unset while module catalog updates repeatedly triggered shell refresh work. Business OS now queues module-catalog change handling until the initial module is visible, serializes later refreshes, and fingerprints catalog content so repeated RxDB emissions that only change transport metadata do not rerender the shell. The smoke harness now emits Browser diagnostic and `startup_smoke_hook_*` evidence before every early status-mode return, and the matrix treats missing startup evidence as a hard failure. Verified JS syntax, RxDB-only guard, focused `command-burst-browser-to-rust,replication-error-browser-status`, and retained 3-cycle sequence `/tmp/rxdb-wave358-sequence-3cycle-after-catalog-gate.json` covering command burst, typed replication error status, and Rust-origin workspace file sync with `retries=0`, zero startup hook reloads, zero cache repairs, 30-39 ms hook waits, and `/CTOX/<workspace>/reports/brief.md` content proof. Full 12-cycle release-soak closure remains open. |
| 357 | done | Startup-stall repro narrowing and smoke diagnostics | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-357-startup-stall-diagnostics.md` | The first full 12-cycle rerun after the required-vs-optional reconnect status fix failed in cycle 1 at `workspace-rust-to-browser`, but the payload path was healthy: the Browser saw `/CTOX/<workspace>/reports/brief.md` and the expected content. The hard failure was the startup guard: `startup_smoke_hook_reload_count=1` and `startup_smoke_hook_wait_ms=120108`, meaning the Business OS module script did not expose the smoke hook before the harness reload. Focused reproduction did not show a deterministic product failure: a 5-cycle `workspace-rust-to-browser` soak passed with `retries=0` and 29-36 ms hook waits, and a 3-cycle exact start-sequence soak (`browser-to-rust,command-browser-to-rust,migration-version-browser-to-rust,command-burst-browser-to-rust,workspace-rust-to-browser`) passed with `retries=0` and 30-39 ms hook waits. The smoke harness now prints URL, query string, and resource timing in the pre-hook module-graph stall warning so any future recurrence has enough evidence to distinguish app asset loading, query injection, and Browser/runtime churn. Full 12-cycle release-soak closure remains open. |
| 356 | done | Soak failure diagnostics and required-vs-optional Advanced Status reconnects | `src/core/rxdb/tools/browser_rust_soak.js`, `src/apps/business-os/app.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-356-advanced-status-required-reconnect.md` | Continued the multi-hour closure work after the Wave 355 watchdog fix. The first full 12-cycle attempt reached cycle 7 and failed in `migration-version-browser-to-rust` without enough process-tail evidence, so the soak runner now captures stdout/stderr tails for failing cycles. Focused reproduction did not confirm a migration bug: isolated `migration-version-browser-to-rust` passed 12 cycles and the sequenced `browser-to-rust,command-browser-to-rust,migration-version-browser-to-rust` block also passed 12 cycles with `retries=0`. The second full 12-cycle attempt reached cycle 12 and failed in `workspace-large-materialize-rust-to-browser`, but the captured Advanced Status showed the materialization path itself was healthy; the global `ok` flag was red because optional background collection `accounting_ledger_entries` was reconnecting while required large-file collections had completed. Advanced Status now separates `requiredReconnectingCollections` from `optionalReconnectingCollections` and only fails `noStalledReconnect` for required collections, while still exposing optional reconnects for diagnostics and alerting. Verified JS syntax, RxDB-only guard, focused `peer-lifecycle-browser-status`, and focused `workspace-large-materialize-rust-to-browser` with 103 chunks and 1,260,036 bytes. Full 12-cycle release-soak closure remains open and must be rerun with this status fix. |
| 355 | done | Native peer-open watchdog race repair and large File Viewer restart stress | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-355-peer-open-watchdog-race.md` | A first attempted 12-cycle multi-hour soak exposed a real restart race: after native peer restart, `desktop_file_chunks` could remain without an open CTOX native peer, and a later 2-cycle full matrix reproduced a large File Viewer restart failure. Root cause was the new peer-open watchdog firing at 20s while explicit `restartCollections()` repair waited 25s before its own retry, allowing watchdog repair to close in-flight restart connections. The watchdog is now separated from explicit restart repair (`45s` global watchdog, `30s` restart wait) and a second failed restart now throws a readable collection-specific error instead of an opaque `Object`. Also clarified smoke/embedded-catalog console wording so test-only cache preservation and shell catalog seeding are not confused with CTOX data fallbacks. Verified JS syntax and RxDB-only guard, focused File Viewer restart, a 5-cycle large File Viewer restart soak with `retries=0`, and the full 25-mode one-cycle matrix after the fix: `/tmp/rxdb-soak-wave355-full-25-mode-watchdog-race-fix-1cycle.json` passed with `retries=0`, elapsed 582s, no Browser cache repair, app-local no-package-manager `ctox-rxdb-js`, large 103-chunk File Viewer restart proof, peer restart, Signaling restart, native peer rollover, tab-freeze, network-flap, command, migration-version, workspace artifact, and typed negative-status coverage. The failed `/tmp/rxdb-soak-wave355-full-25-mode-peer-open-watchdog-2cycle.json` is retained as non-closure evidence for the fixed race; multi-hour closure remains open. |
| 354 | done | Two-cycle 25-mode release soak evidence | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `src/core/rxdb/revisions/PORTING.wave-354-full-soak-2cycle.md` | Promoted the green full release matrix from one cycle to a retained two-cycle soak without adding code changes. Ran `SOAK_CYCLES=2`, `SOAK_FAIL_ON_RETRY=1`, `SMOKE_MATRIX_ATTEMPTS=1`, and the full 25-mode matrix against `/index.html`; evidence is retained at `/tmp/rxdb-soak-wave354-full-25-mode-2cycle.json`. Both cycles passed all Browser→Rust, Rust→Browser, command, migration-version, workspace artifact, large File Viewer, native restart, Signaling restart, native peer rollover, tab-freeze, network-flap, midflight command restart, and typed negative-status paths with `retries=0`, elapsed 1149s, app-local no-package-manager `ctox-rxdb-js`, no Browser cache repair, no startup hook reloads, and repeated native peer-open proof for `desktop_files` and `desktop_file_chunks`. The only Browser WebSocket warnings/errors occurred in the modes that deliberately restart Signaling/CTOX or force offline network state. |
| 353 | done | Full 25-mode release-soak repair and bundled no-package-manager RxDB JS runtime | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/shared/db.js`, `src/apps/business-os/index.html`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the remaining blockers in the 25-mode Browser/Rust release soak. Business OS sync now clears stale transient `ctox_signaling_socket_error` entries only after the same collection returns to a healthy state and restarts unhealthy collection bridges after Browser `online`, keeping checkpoint/schema/push-contract errors strict. Advanced Status now lazily loads the sync classifier before rendering early error snapshots, so raw `RC_PULL` / push-contract failures classify as `CtoxReplicationIoError`. File replication smokes now wait for actual native peer-open evidence on `desktop_files` and `desktop_file_chunks` instead of treating `awaitInSync()` as enough. The active Browser RxDB fork is now emitted as a single local ESM bundle at `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, removing the slow module-graph import path that triggered local IndexedDB cache repair; Business OS adds RxDB import/create phase timeouts and modulepreloads for the boot-critical modules. Verified JS syntax, the RxDB-only guard, focused negative status fixtures, focused reconnect/file peer-open blocks, focused bundled-import regression block, then the full 25-mode one-cycle release soak with `SOAK_FAIL_ON_RETRY=1`: `/tmp/rxdb-soak-wave353-full-25-mode-bundled-final.json` passed all 25 modes with `retries=0`, elapsed 581s, app-local no-package-manager RxDB runtime, no Browser cache repair, and SQLite/File Viewer proofs across Browser→Rust, Rust→Browser, restart, rollover, tab-freeze, network-flap, command, migration-version, and typed negative-status paths. |
| 352 | done | Native projection seeding and no slow-green Browser smoke guard | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/shared/db.js`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the `ctox_runtime_settings` Advanced Status gap exposed by the 25-mode release soak preflight: the native RxDB peer now seeds `ctox_runtime_settings` and `business_module_catalog` into SQLite before opening WebRTC replication pools, so Browser readiness no longer observes an empty required collection without checkpoint/data evidence. Hardened the full-app smoke against slow false-greens by running Chromium in a temporary persistent profile, isolating smoke DB names with `smokeDbId`, counting `browser_cache_repair_count`, and failing the smoke if local RxDB cache repair happens. Removed the fixed 25s post-sync delay in `browser-to-rust`; host-side SQLite polling remains the authoritative replication proof. Verified `cargo build --manifest-path Cargo.toml --bin ctox --target-dir runtime/build/core-rxdb-integration-target`, JS syntax/RxDB-only guards, focused `command-browser-to-rust`, focused `browser-to-rust`, and the guarded three-mode block `browser-to-rust,command-browser-to-rust,workspace-update-rust-to-browser` with `browser_cache_repair_count=0`, zero startup reloads, zero Browser errors, app-local no-package-manager `ctox-rxdb-js`, hook waits 39-59 ms, exact Browser→Rust file payload, exact command→queue projection, and Rust→Browser workspace update generation replacement. Evidence retained at `/tmp/rxdb-command-runtime-settings-checkpoint-wave352.json`, `/tmp/rxdb-browser-fast-no-cache-repair-wave352.json`, and `/tmp/rxdb-isolated-chromium-block-wave352-fast-guarded.json`. |
| 351 | done | Business OS bootloader and IndexedDB startup hardening | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/shared/db.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/business_os/server.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness-dashboard.html` | Finished the startup-budget regression work exposed after wave 350. Business OS `app.js` now installs the smoke/Advanced Status surface before loading RxDB, sync, command bus, schema, Settings, shell UI, icons, Chat, or Reporter modules; the shell script cache key is bumped to `20260523-bootgraph2`. Static Business OS responses now carry `Content-Length`, fresh Browser profiles no longer delete IndexedDB before their first open, and the Browser cache DB name is scoped by origin plus CTOX instance id to avoid repeated local smoke collisions while staying stable for real deployments. Verified syntax, RxDB-only guard, focused `browser-to-rust`, `command-browser-to-rust`, `workspace-update-rust-to-browser`, then a retained three-mode block `/tmp/rxdb-origin-db-regression-wave351.json` and extended block `/tmp/rxdb-bootgraph-regression-extended-wave351-origin-db.json`. Evidence shows zero startup hook reloads, zero Browser errors, zero request failures, app-local `ctox-rxdb-js`, Hook waits around 43-60 ms, exact command projection, workspace update generation replacement, four agent artifacts, and 103 chunks / 1,260,036 bytes through File Viewer restart. The restart mode still has one expected WebSocket-close warning within the current budget. |
| 350 | done | Business OS boot-path import and port-reuse regression hardening | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `/Users/you/Documents/ctox-dev/scripts/smoke-core-direct-browser-rust.mjs`, `/Users/you/Documents/ctox-dev/docs/production-readiness.md`, `/Users/you/Documents/ctox-dev/docs/production-readiness-dashboard.html` | The new startup budgets exposed a real pre-hook stall: repeated local direct runs could hang 120s in the static app module graph and only pass after the harness reload. Moved Business Reporter and Business Chat out of the top-level `app.js` import graph into post-shell lazy imports, and changed the ctox-dev direct Browser/Rust wrapper to use fresh randomized Business OS and Signaling port bases unless fixed ports are explicitly provided. Verified syntax, the RxDB-only guard, focused `command-browser-to-rust` with `startup_smoke_hook_reload_count=0` and `startup_smoke_hook_wait_ms=68`, then reran the broad five-mode direct wrapper with randomized ports. The retained `/tmp/core-direct-browser-rust-wave350-random-ports-broad.json` is green for file, command, agent-artifact, and large File Viewer restart paths; all modes had zero startup reloads, hook waits under 110 ms, and the restart mode retained one WebSocket warning within the default warning budget. |
| 349 | done | Browser/Rust smoke warning and startup-stall budgets | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Made the Browser/Rust smoke harness record Browser console warning/error counts, WebSocket warning counts, asset request failures, smoke-hook reloads, and smoke-hook wait time as machine-readable evidence. The matrix now enforces configurable budgets for WebSocket warnings, pre-hook startup reloads, and smoke-hook wait time, defaulting to zero startup reloads and a 60s hook budget. This prevents a slow "eventually green" run from hiding shell startup regressions. Verified syntax plus `SMOKE_MODES=rust-to-browser SMOKE_MATRIX_ATTEMPTS=1 SMOKE_BROWSER_WEBSOCKET_WARNING_BUDGET=0 SMOKE_MATRIX_RESULT_PATH=/tmp/rxdb-smoke-startup-budget-wave349.json SMOKE_PAGE_PATH=/index.html node src/core/rxdb/tools/browser_rust_smoke_matrix.js`: the run passed with `browser_websocket_warning_count=0`, `startup_smoke_hook_reload_count=0`, and `startup_smoke_hook_wait_ms=3026`. |
| 348 | done | Broad direct Browser/Rust regression evidence after native ICE repair | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Reran the broad direct ctox-dev Browser/Rust wrapper after the native stale-candidate guard and in-flight bridge reuse fix. `rust-to-browser`, `browser-to-rust`, `command-browser-to-rust`, `workspace-agent-artifacts-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser` all passed first try against `/index.html` with `CTOX_DEV_CORE_DIRECT_REQUIRE_ADVANCED_STATUS=1`. Evidence in `/tmp/core-direct-browser-rust-wave315-broad.json` includes app-local `ctox-rxdb-js`, Browser file/chunk payload proof in Rust SQLite, Browser command accepted with exactly one queued Rust task, four `/CTOX/<workspace>/...` Codex-style artifacts, and a 103-chunk large file reconstructed to 1,260,036 bytes after native peer restart. One transient WebSocket close warning remains during the restart mode and stays tracked as soak/startup churn hardening, not as a closure blocker for this focused direct run. |
| 347 | done | Native ICE stale-candidate guard and sequenced command soak repair | `src/core/rxdb/patches/rtc-ice/src/agent/mod.rs`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the remaining crash exposed by the red `browser-to-rust,command-browser-to-rust` sequence. The patched local `rtc-ice` dependency now drops stale peer-reflexive STUN binding requests if the remote-candidate list was emptied before indexing, and `sync.js` reuses in-flight collection bridges instead of stopping and restarting healthy startup bridges. Verified `cargo build --bin ctox --target-dir runtime/build/core-rxdb-integration-target` and reran `SOAK_CYCLES=1 SOAK_MODES=browser-to-rust,command-browser-to-rust SOAK_FAIL_ON_RETRY=1 SMOKE_MATRIX_ATTEMPTS=1 SMOKE_MODE_TIMEOUT_MS=240000 SMOKE_PAGE_PATH=/index.html node src/core/rxdb/tools/browser_rust_soak.js`: both modes passed first try with `retries=0`, Browser file/chunk payload reached Rust SQLite, and Browser `business_commands` produced exactly one queued Rust `ctox_queue_tasks` row. |
| 346 | done | Role-aware Signaling peer selection and direct smoke regression repair | `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/local_signaling_server.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The release soak exposed that Browser peers in the same collection rooms could be considered before the native `ctox_instance`, causing stale peer churn and command pushes to stay `pending_sync` in sequence. Local and Cloudflare Signaling now carry peer descriptors with role/protocol/capabilities, and the Browser native WebRTC client records that metadata and only initiates data channels to `ctox_instance` peers when role metadata is available. Kept legacy `otherPeerIds` compatibility, retained strict post-handshake native-role validation, and removed an over-aggressive bridge-reuse condition that restarted healthy collections just because the current data channel was momentarily closed. Verified JS syntax, app-local import/RxDB-only/signaling hardening guards, the formerly failing two-mode soak `browser-to-rust,command-browser-to-rust` with zero retries, and a five-mode direct Browser/Rust wrapper smoke covering `rust-to-browser`, `browser-to-rust`, `command-browser-to-rust`, workspace artifacts, and large File Viewer restart. |
| 345 | done | Browser/Rust smoke progress output and attempt timing | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added per-attempt duration and outcome output to the Core Browser/Rust smoke matrix so long runs report `durationMs` and timeout state as they progress. Verified through the ctox-dev direct wrapper with `rust-to-browser`: the wrapper streamed the mode start immediately, wrote a retained JSON attempt with `durationMs=57214`, `timedOut=false`, and the smoke passed. |
| 344 | done | Broad direct Browser/Rust evidence across files, commands, and File Viewer restart | `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Captured the broader direct Browser/Rust smoke evidence after the command push retry fix. `rust-to-browser`, `browser-to-rust`, `command-browser-to-rust`, `workspace-agent-artifacts-rust-to-browser`, and `workspace-large-file-viewer-restart-rust-to-browser` all passed against `/index.html` with the app-local `ctox-rxdb-js` runtime. Evidence includes Browser-origin command acceptance with exactly one `ctox_queue_tasks` row, four CTOX workspace artifacts under `/CTOX/<workspace>/...`, and a 103-chunk large file materialized to 1,260,036 bytes after native peer restart. |
| 343 | done | App-local RxDB command push retry and sequenced Browser/Rust smoke hardening | `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/apps/business-os/index.html`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed a Browser-origin command sync race exposed only after earlier Browser/Rust modes had created stale WebRTC peer state. The app-local replication state now drains push work from `awaitInSync()`, prevents overlapping push runs, and periodically retries push for `business_commands` and `ctox_queue_tasks`; the command smoke waits for an open native `ctox_instance` data channel before asserting command delivery. Also aligned the shell `app.js` cache key with the active `APP_BUILD` so Browser tests do not accidentally run an older shell bundle. Verified JS syntax, storage/import/RxDB-only guards, isolated `command-browser-to-rust`, and the sequenced `rust-to-browser,browser-to-rust,command-browser-to-rust` direct Browser/Rust smoke. |
| 342 | done | Local `rtc-ice` shutdown-race patch for Browser/Rust File Viewer restart | `Cargo.toml`, `Cargo.lock`, `src/core/rxdb/patches/rtc-ice/`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Patched the `rtc-ice 0.20.0-alpha.1` shutdown race that could panic after an otherwise green direct Browser/Rust File Viewer restart smoke. The local crate patch drops stale STUN binding requests when candidate indexes no longer exist instead of indexing an empty remote-candidate list. Verified `cargo build --bin ctox --target-dir runtime/build/core-rxdb-integration-target` and reran `workspace-large-file-viewer-restart-rust-to-browser` against `/index.html`: app-local `ctox-rxdb-js`, Advanced Status, 103 chunks, and 1,260,036 byte materialization passed with no post-smoke ICE panic. |
| 341 | done | Notes legacy iframe removed from active RxDB-only data plane | `src/apps/business-os/modules/notes/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed a real RxDB-only guard failure found after closing near-complete PRDs. The active Notes module still embedded the legacy Notesnook build via iframe, and that build exposed a `local-only` sync mode outside the CTOX RxDB path. Removed the iframe and made the CTOX-owned Notes editor the active surface again. The RxDB-only guard now skips inactive legacy Notesnook source/build folders only after asserting the active Notes entry stays on `modules/notes/index.html` and does not reference `build/` or `notesnook-src/`. Verified `node src/apps/business-os/scripts/assert-rxdb-only.mjs` and the ctox-dev `assert-rxdb-data-plane.mjs` wrapper. |
| 340 | done | Checkpoint readiness and migration-version closure evidence | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Captured focused PRD-036 closure evidence on the app-local runtime. `SMOKE_MODES=migration-version-browser-to-rust,checkpoint-error-browser-status SMOKE_MATRIX_ATTEMPTS=1 SMOKE_PAGE_PATH=/index.html node src/core/rxdb/tools/browser_rust_smoke_matrix.js` passed. The migration-version mode proved Browser `business_commands` writes land in `ctox_business_os__business_commands__v1`, the stale `__v0` table stays empty for that command, and exactly one `ctox_queue_tasks` projection is created. The checkpoint negative mode proved missing checkpoint epoch evidence surfaces as `ctox_checkpoint_epoch_missing` / `CtoxCheckpointProtocolError`. |
| 339 | done | Schema contract drift repair and PRD-032 closure evidence | `src/core/business_os/business_os_schema_contract.json`, `src/core/business_os/business_os_schema_hashes.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Repaired a real schema parity drift found while trying to close near-100% PRDs. The generated Browser schema contract had stale Notes schema fields, and the retained all-collection schema hash fixture still expected the old `notes` hash. Regenerated `business_os_schema_contract.json`, updated the `notes` hash fixture to `046a351061638c3f5a7c303776a7994cc21b4308c0939b4821badcc11ba48c18`, and verified `node src/core/rxdb/tools/build_business_os_schema_contract.mjs`, `cargo test --locked --bin ctox native_all_schema_hashes_match_browser_contract_fixture --quiet`, and focused `SMOKE_MODES=schema-error-browser-status SMOKE_MATRIX_ATTEMPTS=1 SMOKE_PAGE_PATH=/index.html node src/core/rxdb/tools/browser_rust_smoke_matrix.js` evidence with `ctox_schema_hash_mismatch` / `CtoxSchemaProtocolError`. |
| 338 | done | App-local RxDB release evidence retention | `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `src/core/rxdb/tools/assert_js_fork_release_evidence.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended the private RxDB JS release-evidence contract to include the active app-local no-package-manager runtime, not only the retained legacy vendor provenance. CI artifact evidence now uploads `src/apps/business-os/rxdb/manifest.json`, `README.md`, and `dist/ctox-rxdb-js.mjs`; Unix and Windows release archives package the same files under `rxdb-js/app-local/`, with the runtime module under `rxdb-js/app-local/dist/`. The release-evidence guard now fails if those active runtime files are missing from CI or release packaging. Verified guard syntax, `assert_js_fork_release_evidence.js`, and the no-package-manager JS guard. |
| 337 | done | App-local RxDB file chunk negative soak evidence | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Captured focused app-local runtime evidence for the three retained file chunk negative paths. `SOAK_CYCLES=1 SOAK_MODES=file-chunk-metadata-error-browser-status,file-chunk-tombstone-error-browser-status,file-chunk-stale-generation-error-browser-status SOAK_REQUIRED_MODES=file-chunk-metadata-error-browser-status,file-chunk-tombstone-error-browser-status,file-chunk-stale-generation-error-browser-status SOAK_FAIL_ON_RETRY=1 SMOKE_PAGE_PATH=/index.html node src/core/rxdb/tools/browser_rust_soak.js` passed with zero retries. Evidence retained `CtoxFileChunkIntegrityError` for metadata mismatch (`ctox_file_chunk_integrity_mismatch`), missing/tombstoned chunks (`ctox_file_chunk_missing` with zero live chunks), and stale generation selection (`ctox_file_chunk_missing` with old live chunks present but requested generation absent). |
| 336 | done | App-local RxDB restart lifecycle soak evidence | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Captured focused multi-cycle soak evidence for the new app-local runtime reconnect path. `SOAK_CYCLES=2 SOAK_MODES=restart-browser-to-rust,peer-lifecycle-browser-status SOAK_REQUIRED_MODES=restart-browser-to-rust,peer-lifecycle-browser-status SOAK_FAIL_ON_RETRY=1 SMOKE_PAGE_PATH=/index.html node src/core/rxdb/tools/browser_rust_soak.js` passed with zero retries. Both restart cycles retained explicit `rxdb_runtime={"name":"ctox-rxdb-js","source":"app-local","packageManager":"none"}`, singleton `desktop_files`/`desktop_file_chunks` peer pools, checkpoint epochs, Browser `browser_restart_smoke_*` file/chunk writes, and host SQLite payload proof; both lifecycle status cycles retained `CtoxWebRtcPeerLifecycleEvent` / `peer_connection_lost` / `peer-reconnect` evidence. |
| 335 | done | Peer lifecycle Advanced Status smoke and release-soak mode | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted recoverable WebRTC peer lifecycle diagnostics into the retained Browser/Rust smoke matrix. New `SMOKE_MODE=peer-lifecycle-browser-status` injects a reconnecting peer lifecycle fixture through the real Business OS Advanced Status surface and asserts `CtoxWebRtcPeerLifecycleEvent`, `peer_connection_lost`, `peer-reconnect`, recoverable retry semantics, `reconnectingCollections`, and `noStalledReconnect === false` without creating replication I/O errors. The mode is now in the smoke matrix, local soak defaults, and manual GitHub release-soak required modes, increasing guarded release coverage to 25 modes. Verified JS syntax, RxDB-only guard, soak workflow drift guard (`modes=25`), focused smoke, focused matrix evidence, and one-cycle zero-retry soak. |
| 334 | done | Executable WebRTC lifecycle classification guard | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/scripts/assert-sync-error-classification.mjs`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted recoverable WebRTC transport lifecycle handling from static marker coverage into an executable Business OS guard. The sync test hooks now expose `classifyPeerLifecycleEvent()`, and `assert-sync-error-classification.mjs` verifies `ERR_CONNECTION_FAILURE`, `ctox_data_channel_error`, and browser peer-connection-limit failures become `CtoxWebRtcPeerLifecycleEvent` diagnostics with stable recoverable `peer-reconnect` semantics. The RxDB-only guard now also fails if the lifecycle classifier is not test-exposed or if successful reconnect paths stop clearing stale `lastLifecycleEvent` state. Verified syntax, the sync error classification guard, RxDB-only guard, and the no-package-manager JS fork guard. |
| 333 | done | App-local RxDB reconnect status recovery and singleton native peer | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the remaining restart-smoke regression after per-peer checkpoints: successful peer protocols, active replication, completed initial replication, and healthy bridge reuse now clear stale recoverable lifecycle diagnostics instead of leaving Advanced Status stuck on `reconnecting`. The app-local WebRTC pool serializes native peer opens and retains a single active `ctox_instance` peer per collection, while data-channel close events are classified as recoverable lifecycle noise. Verified syntax, RxDB-only guard, no-package-manager guard, and the real full-app restart smoke with explicit app-local runtime evidence, singleton `desktop_files`/`desktop_file_chunks` peer pools, `checkpoint_epoch_count=25`, Browser `browser_restart_smoke_*` file/chunk rows, and host SQLite payload proof. |
| 332 | done | App-local RxDB restart-safe per-peer checkpoints | `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the first app-local runtime reconnect regression found by `SMOKE_MODE=restart-browser-to-rust SMOKE_PAGE_PATH=/index.html`: Browser file/chunk writes could be lost after CTOX restart because the simplified port used one global push checkpoint per collection, so a stale peer could advance the checkpoint before the new Rust master received the write. Pull and push checkpoints are now tracked per peer, dead WebRTC connections reject pending requests with a recoverable lifecycle code, failed peer requests remove their peer state instead of poisoning the full push batch, and replaced native peer sessions are pruned best-effort. Verified syntax, no-package-manager guard, import/query/index smokes, RxDB-only guard, and the real full-app restart smoke with explicit app-local runtime evidence, `checkpoint_epoch_count=24`, Browser `browser_restart_smoke_*` file/chunk writes, and host SQLite payload proof. Note: the smoke still reports multiple transient fork peers during reconnect; writes are now safe, but pool cardinality cleanup remains a separate hardening item. |
| 331 | done | Browser/Rust smoke pins app-local RxDB runtime | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Tightened the positive Browser/Rust smoke contract so app-local runtime usage is not only implied by `status.ok`. `assertHealthyAdvancedStatusContract()` now requires `checks.rxdbRuntimeAppLocal === true` and verifies `rxdbRuntime.name === "ctox-rxdb-js"`, `source === "app-local"`, and `packageManager === "none"`. Positive smoke output now prints `rxdb_runtime=...`, making regressions away from the no-package-manager Business OS runtime visible in retained logs. Verified syntax, RxDB-only guard, no-package-manager guard, and the real `/index.html` Browser/Rust smoke with explicit runtime evidence, Advanced Status, 18 modules, checkpoint epochs, Browser file/chunk writes, and host SQLite payload proof. |
| 330 | done | App-local RxDB runtime evidence in Advanced Status | `src/apps/business-os/shared/db.js`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added explicit Advanced Status evidence for the active RxDB JavaScript runtime. Business OS now carries `rxdbRuntime` metadata from `shared/db.js` and the health checks include `rxdbRuntimeAppLocal`, which only passes for the app-local `ctox-rxdb-js` runtime with `packageManager: "none"`. The RxDB-only guard now pins this status field, so a future drift back to the retained vendor bundle or an npm-backed runtime fails before release. Verified syntax, RxDB-only guard, no-package-manager import smoke, JS no-package-manager guard, and the real `/index.html` Browser/Rust smoke with Advanced Status, 18 modules, required collection checkpoint epochs, Browser file/chunk writes, and host SQLite payload proof. |
| 329 | done | App-local RxDB full Business OS startup compatibility | `src/apps/business-os/app.js`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/rxdb/src/schema.mjs`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the first full `/index.html` compatibility gap after activating the no-package-manager app-local runtime. The packaged module catalog seed now writes the required `module-catalog` primary key and timestamp before entering RxDB, protocol payloads expose checkpoint evidence both top-level and under `collection.checkpoint`, incoming peer protocols are normalized for Advanced Status, and Business OS peer-generation diagnostics now accept structured `{ role, sessionId }` peer-session objects instead of only legacy strings. Verified syntax, RxDB-only guard, no-package-manager import/query/index smokes, JS fork bundle guard, and the real `/index.html` Browser/Rust smoke on the app-local runtime: Advanced Status passed, 18 modules loaded, required collections completed initial sync with checkpoint epochs, Browser wrote `browser_smoke_*` file/chunk rows, and host SQLite confirmed payload `hello`. |
| 328 | done | Activate app-local RxDB runtime for Business OS smoke | `src/apps/business-os/shared/db.js`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/modules/matching/ui/businessOsDataSource.js`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/core/rxdb/js-fork/bundle-contract.json`, `src/core/rxdb/tools/assert_js_fork_bundle_contract.js`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Switched the active Business OS RxDB imports from the retained legacy `vendor/rxdb-bundle.mjs` to the app-local no-package-manager runtime `rxdb/dist/ctox-rxdb-js.mjs`. The bundle contract now documents the vendor bundle as retained provenance, and fails if the active Business OS importers drift back to the legacy vendor path. Fixed the new native WebRTC runtime's first-pull race by observing the symmetric Rust `token` request and waiting briefly before the Browser sends `masterChangesSince`; this avoids losing the first post-handshake request before the Rust master message task subscribes. Verified the real Browser/Rust smoke on the app-local runtime: Rust readiness payload reached the browser, Browser wrote `browser_smoke_*` file/chunk rows, and host SQLite confirmed the replicated payload `hello`. |
| 327 | done | App-local RxDB full runtime core export | `src/apps/business-os/rxdb/src/rx-database.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Made `rxdbCore()` return the full Business OS runtime surface instead of only database creation helpers. The app-local fork core now exposes protocol constants, schema/protocol helpers, `replicateWebRTC()`, and `getConnectionHandlerSimplePeer()` alongside `createRxDatabase()`/`getRxStorageDexie()`, matching what `shared/sync.js` expects from the active bundle. The import smoke now fails if `rxdbCore()` omits the WebRTC runtime functions, and the no-package-manager guard pins the runtime exports. |
| 326 | done | App-local RxDB schema index metadata parity | `.github/workflows/ci.yml`, `src/apps/business-os/rxdb/src/index.mjs`, `src/apps/business-os/rxdb/src/rx-database.mjs`, `src/apps/business-os/rxdb/src/storage-indexeddb.mjs`, `src/apps/business-os/rxdb/tests/storage-index-smoke.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added schema index metadata handling to the package-manager-free Business OS fork without requiring dynamic IndexedDB schema migrations. Collection creation now passes schemas into storage, schema `indexes` are normalized, stored records retain deterministic `indexValues`, collections expose `schemaIndexes()` and `queryPlanFor()`, and nested primary keys such as `message.key` are normalized into the internal `id` while preserving the source path. Added a storage index smoke and CI guard markers for index normalization, query planning, and nested primary-key handling. |
| 325 | done | App-local RxDB query API parity guard | `.github/workflows/ci.yml`, `src/apps/business-os/rxdb/src/index.mjs`, `src/apps/business-os/rxdb/src/rx-database.mjs`, `src/apps/business-os/rxdb/tests/query-api-smoke.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended the dependency-free Business OS `ctox-rxdb-js` fork with the RxDB query surface that current modules need before the active bundle can be replaced: `bulkInsert`, `bulkUpsert`, chainable `selector()`, `sort()`, `limit()`, `skip()`, and `where()` operators, plus logical and nested selector support for `$and`, `$or`, `$not`, `$regex`, `$contains`, and `$elemMatch`. Added a no-package-manager query API smoke and wired it into CI beside the import smoke. The guard now pins the expanded query surface so the app-local fork cannot regress back to a mere import placeholder. |
| 324 | done | Business OS app-local RxDB fork path | `.github/workflows/ci.yml`, `src/apps/business-os/rxdb/**`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Moved the new Browser-side JS fork into the app-visible path `src/apps/business-os/rxdb/` so the fork is directly colocated with Business OS instead of hidden under the core port folder. The no-package-manager guard still lives under `src/core/rxdb/tools/` because it is part of the rxdb-rs/JS parity contract, but it now scans `src/apps/business-os/rxdb`. CI runs the app-local import smoke from `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`. |
| 323 | done | JS port path normalization | `.github/workflows/ci.yml`, `src/apps/business-os/rxdb/**`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Renamed the new Browser-side JS port from the temporary work path to the canonical app-local fork location. The port is tracked as the CTOX RxDB JS implementation, with the no-package-manager guard renamed to `assert_js_no_package_manager.js` and CI pointing at the canonical import smoke. Current docs now refer to the implementation as `ctox-rxdb-js` / plain JS instead of a separate flavor. |
| 322 | done | Plain JS unrestricted core and rxdb-rs protocol envelope | `src/apps/business-os/rxdb/README.md`, `src/apps/business-os/rxdb/manifest.json`, `src/apps/business-os/rxdb/src/observable.mjs`, `src/apps/business-os/rxdb/src/rx-database.mjs`, `src/apps/business-os/rxdb/src/replication-webrtc.mjs`, `src/apps/business-os/rxdb/src/schema.mjs`, `src/apps/business-os/rxdb/src/storage-indexeddb.mjs`, `src/apps/business-os/rxdb/src/webrtc-native.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Removed upstream-style feature-gate semantics from the JS port foundation: there are no paid-tier checks, trial limits, plugin-missing gates, or runtime add-on unlocks. `addRxPlugin()` is only a no-op transition shim, while Business-OS-required document methods (`patch`, `incrementalPatch`, `update`, `atomicPatch`, `incrementalModify`, `atomicUpdate`) and collection methods (`upsert`, `atomicUpsert`, live query subscription) live directly in core. Reworked the native WebRTC layer to speak the actual rxdb-rs envelope (`id`, `method`, `params`, `result`, `error`), answer `token` and `ctoxProtocol`, emit upstream-compatible Signaling `senderPeerId`/`receiverPeerId` frames, expose `replicateWebRTC()` / `getConnectionHandlerSimplePeer()`, and perform first pull/push loops through `masterChangesSince` / `masterWrite`. The JS guard now pins the no-gate/no-package-manager/no-bare-import contract plus the rxdb-rs request/response markers. |
| 321 | done | No-package-manager plain JS RxDB track | `.github/workflows/ci.yml`, `src/apps/business-os/rxdb/README.md`, `src/apps/business-os/rxdb/manifest.json`, `src/apps/business-os/rxdb/src/*.mjs`, `src/apps/business-os/rxdb/dist/ctox-rxdb-js.mjs`, `src/apps/business-os/rxdb/tests/no-package-manager-import-smoke.mjs`, `src/core/rxdb/tools/assert_js_no_package_manager.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Opened the next-generation `ctox-rxdb-js` track beside the current source-built hard fork. The new ESM surface has no package files, no lockfiles, no dependency tree, no bare module imports, and no known RxDB/network helper dependency tokens. It provides canonical schema hashing through WebCrypto, minimal native IndexedDB collection storage, native `RTCPeerConnection`/`WebSocket` signaling, protocol payload helpers aligned with `rxdb-rs`, a stable `dist/ctox-rxdb-js.mjs` import target, an import smoke, and a CI guard that recursively enforces the no-package-manager contract. This is the initial runtime-independent foundation; replacing the active Business OS bundle remains the next fork wave. |
| 320 | done | Startup budget Advanced Status guard | `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the direct Business OS startup-budget smoke against hidden sync regressions without turning it into a full readiness soak. The smoke now requires `business-os-advanced-status-v1` to expose `shell.bootTimings.shellVisibleMs` under the configured budget before accepting a visible shell, and the RxDB-only guard pins that Advanced Status boot-budget requirement. The ctox-dev wrapper now derives the expected Signaling URL from the live CTOX `/api/business-os/sync/config`, avoiding stale local defaults when the runtime has a different Signaling port. Live local verification exposed a stale `19998` Signaling listener gap, restored the matching local Signaling server, restarted `business-os serve`, and then passed direct startup evidence with shell visible in 313 ms, WebRTC visible in 313 ms, Advanced Status `shellVisibleMs=263 ms`, 17 modules, and SQLite data-plane evidence. |
| 319 | done | All-collection schema hash fixture | `.github/workflows/ci.yml`, `src/core/business_os/business_os_schema_hashes.json`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a retained all-collection Business OS schema hash fixture. `native_all_schema_hashes_match_browser_contract_fixture` now computes native rxdb-rs schema hashes for every collection in the generated Browser schema contract and compares them to `business_os_schema_hashes.json`, while CI also checks the Browser schema contract generator stays current. This moves PRD-032 beyond two hand-picked hashes and catches Browser/Rust schema drift across all 50 collections. Verified `cargo fmt --check --manifest-path Cargo.toml`, `node src/core/rxdb/tools/build_business_os_schema_contract.mjs`, `cargo test --locked --bin ctox native_all_schema_hashes_match_browser_contract_fixture --quiet`, and focused `git diff --check`. |
| 318 | done | Migration-version release soak guard | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added explicit PRD-036 schema-version compatibility evidence to the live Browser/Rust harness. New mode `migration-version-browser-to-rust` writes a Browser `business_commands` document over RxDB/WebRTC, verifies Rust accepts it and creates exactly one queue task, then checks host SQLite directly: the command row must land in `ctox_business_os__business_commands__v1`, the stale `ctox_business_os__business_commands__v0` table has zero rows for that command, and the task lands in `ctox_business_os__ctox_queue_tasks__v0`. The mode is now part of the smoke matrix, local release-soak defaults, and the manual GitHub soak workflow required release coverage, raising the guarded release mode count to 24. Verified JS syntax, the soak workflow guard (`modes=24`), focused direct smoke, focused matrix evidence, and a 2-cycle zero-retry soak with `SOAK_REQUIRED_MODES=migration-version-browser-to-rust`. |
| 317 | done | Native-master WebRTC reconnect soak | `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened CTOX Browser/Rust role selection so the native `ctox_instance` peer is deterministic master for Business OS collections and the Browser is deterministic fork. Rust now actively exchanges `ctoxProtocol` to read the remote role before role election, while the JS hard fork forces Browser-as-fork for native peers, exposes pool `reSync()`, and the smoke keeps reconnect bridges alive through the host SQLite assertion. This fixed the multi-cycle failure where Browser-master/Rust-fork could miss Browser writes after native peer restart. Verified JS/Rust protocol guards, rebuilt `rxdb-bundle.mjs` with provenance SHA-256 `feb37d1fb6b44fc1943293bf5726dcf2e511f523fabbfcdbc5ac16899cd3beda`, focused `restart-browser-to-rust` smoke, and a 2-cycle zero-retry soak for `restart-browser-to-rust`, `restart-signaling-browser-to-rust`, and `rollover-native-peer-browser-to-rust` with Browser-Fork/Rust-Master evidence and replicated Browser file/chunk rows in SQLite. |
| 316 | done | Schema-version checkpoint isolation | `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the cross-version counterpart to the persisted checkpoint guard. `replication_checkpoint_epoch_isolated_across_schema_version_drift` proves a new SQLite collection schema version starts with an empty checkpoint instead of inheriting the previous version's epoch, writes its own distinct schema-scoped checkpoint evidence, and reopening the original schema version reconstructs the original epoch and hashed latest id. Verified `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml` and `cargo test --manifest-path src/core/rxdb/Cargo.toml replication_checkpoint_epoch_isolated_across_schema_version_drift --quiet`. |
| 315 | done | SQLite checkpoint drift guard | `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/src/types/storage.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a focused `rxdb-rs` SQLite storage regression for persisted checkpoint drift. `replication_checkpoint_epoch_tracks_persisted_checkpoint_drift` proves advertised checkpoint status starts empty, changes epoch and hashed latest id when the persisted checkpoint advances, reconstructs the same epoch after reopening the SQLite database, and changes again on a later checkpoint while keeping schema hash stable and raw latest ids out of the status payload. Verified `cargo fmt --check --manifest-path src/core/rxdb/Cargo.toml` and `cargo test --manifest-path src/core/rxdb/Cargo.toml replication_checkpoint_epoch_tracks_persisted_checkpoint_drift --quiet`. |
| 314 | done | Restart checkpoint freshness evidence | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted PRD-036 checkpoint freshness into reconnect evidence. Native-peer restart smokes now verify that restarted peer sessions still advertise checkpoint epoch evidence, emit `checkpoint_restarted_collections` plus `checkpoint_epoch_count`, and the smoke matrix requires those keys with `checkpoint_epoch_count >= 1` for `restart-browser-to-rust`, `restart-signaling-browser-to-rust`, and `rollover-native-peer-browser-to-rust`. Verified focused restart smoke and matrix mode: `advanced_status=business-os-advanced-status-v1`, `checkpoint_epoch_count=24`, restarted collection evidence from `ctox_runtime_settings` through `desktop_file_chunks`, and Browser file/chunk replication into Rust SQLite after restart. |
| 313 | done | Required checkpoint readiness gate | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Tightened PRD-036 readiness semantics. Required collections are now only considered ready when their initial replication is complete and the remote Rust peer advertises `ctox-checkpoint-epoch-v1` with checkpoint state `advertised` and a non-empty epoch. Advanced Status exposes `sync.initialSync.missingCheckpointEpoch` plus per-collection checkpoint readiness fields, and the Browser/Rust smoke contract now fails if those fields are missing. The RxDB-only guard pins the gate. Verified JS syntax, RxDB-only guard, File Viewer integrity guard, direct connection smoke with `checkpoint-ready1` (`firstShellVisibleMs=283`, `firstWebRtcConnectedMs=283`, 17 modules, ready SQLite data-plane evidence), and real `/index.html` Browser→Rust smoke with `advanced_status=business-os-advanced-status-v1` plus replicated Browser file/chunk payload in Rust SQLite. |
| 312 | done | Connection smoke startup timing precision | `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Reduced connection-mode smoke readiness polling from 1000 ms to a configurable 250 ms default via `BUSINESS_OS_CONNECTION_SMOKE_POLL_MS`. Startup budget failures now trip closer to the real visible-shell time instead of being quantized to one-second intervals, and the JSON summary records `readinessPollMs`. Verified JS syntax and direct smoke with `BUSINESS_OS_SHELL_VISIBLE_BUDGET_MS=3000` / `BUSINESS_OS_CONNECTION_SMOKE_POLL_MS=250`: `firstShellVisibleMs=1039`, `firstWebRtcConnectedMs=1371`, 17 modules, and ready SQLite data-plane evidence. |
| 311 | done | Business OS Advanced Status boot timings | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added app-side boot timing telemetry to `window.CTOX_BUSINESS_OS_STATUS`. Advanced Status now exposes `shell.bootTimings.startedAt`, `shellVisibleMs`, `firstWebRtcConnectedMs`, and `firstAdvancedStatusHealthyMs`, so production diagnostics can distinguish a slow visible shell from slower background sync readiness. The RxDB-only guard pins the timing surface. Verified JS syntax, RxDB-only guard, File Viewer integrity guard, headless Advanced Status evidence (`shellVisibleMs=1190`, `firstWebRtcConnectedMs=661`), and direct startup-budget smoke with `bootfast5`: `firstShellVisibleMs=1373`, `firstWebRtcConnectedMs=2464`, 17 modules, and ready SQLite data-plane evidence. |
| 310 | done | Business OS startup performance smoke budget | `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added an explicit shell-visible startup budget to the Business OS connection-mode smoke. The smoke now records `firstShellVisibleMs`, `firstWebRtcConnectedMs`, elapsed time, and the configured `BUSINESS_OS_SHELL_VISIBLE_BUDGET_MS`; it fails if the visible workspace appears after the budget instead of only failing after the long readiness timeout. This catches sync-induced boot stalls while still allowing Advanced Status and background collection warmup to finish later. Verified JS syntax and direct smoke with `BUSINESS_OS_SHELL_VISIBLE_BUDGET_MS=3000`: `firstShellVisibleMs=1117`, `firstWebRtcConnectedMs=1117`, 17 modules, and ready SQLite data-plane evidence. |
| 309 | done | Business OS boot-path sync performance guard | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/modules/registry.json`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `runtime/business-os-signaling-urls.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Removed the accidental sync-induced boot regression. Business OS no longer starts the critical RxDB/WebRTC collection warmup before the visible workspace opens, and module catalog loading now reads the cached RxDB projection first, then uses the packaged shell manifest seed for cold starts while WebRTC starts in the background. This keeps business data RxDB/WebRTC-only without letting slow sync block the shell. The packaged registry now contains all 17 current app manifests, and the background refresh waits for the real RxDB projection instead of reusing the seed. The RxDB-only guard now fails if critical sync warmup moves back before `loadModules`/`openModule`, if module catalog boot waits for WebRTC before local/packaged manifests, or if the background refresh stops waiting for RxDB. Restored the local runtime signaling URL to `ws://127.0.0.1:18990`. Verified JS syntax, RxDB-only guard, File Viewer integrity guard, live Browser `bootfast4` render: workspace visible in 525 ms and RxDB/WebRTC connected in 835 ms with `Rxdb Webrtc VERBUNDEN`, and direct connection smoke with 17 modules plus ready SQLite data-plane evidence. |
| 308 | done | PRD-001 direct data-plane closure | `src/apps/business-os/app.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/shared/file-integrity.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the local PRD-001 Browser/Rust RxDB/WebRTC data-plane gate. Business OS now starts the critical RxDB/WebRTC collections deterministically during shell boot, so Advanced Status no longer depends on background module sync timing for `business_commands`, `desktop_files`, or `desktop_file_chunks`. File Viewer now drains lazy materialization through RxDB commands after native peer restart: it waits for `business_commands`, reconnects and touches stranded `pending_sync` commands, waits for file/chunk projection visibility, and retries transient generation/hash mismatches. Browser file reconstruction now selects the newest complete chunk generation instead of a single newest timestamp row, which keeps large materialized files readable when file metadata trails chunk replication. Verified JS syntax, RxDB-only and File Viewer integrity guards, focused restart File Viewer smoke with 103 chunks and full 1.26 MB payload, and the ctox-dev direct Browser/Rust PRD-001 package smoke covering Rust-to-Browser, Browser-to-Rust, command acceptance, multi-file CTOX/Codex workspace artifacts, and native-restart File Viewer materialization. |
| 307 | done | Agent workspace artifact sync coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added live multi-file workspace artifact coverage for the CTOX agent filesystem use case. New mode `workspace-agent-artifacts-rust-to-browser` creates four representative CTOX/Codex workspace artifacts (`reports/brief.md`, `plans/runtime-plan.json`, `logs/codex-output.txt`, and `src/generated/module-note.md`), runs `ctox business-os files sync-workspace`, waits until Browser IndexedDB receives every `desktop_files` row and complete chunk payload over RxDB/WebRTC, and asserts the Business OS virtual paths are `/CTOX/<workspace>/...`. The mode is now in the smoke matrix, default soak runner, and manual GitHub release soak workflow; the soak guard reports 23 release modes. Verified JS syntax, RxDB-only and File Viewer integrity guards, soak workflow guard, focused matrix evidence with four replicated files, focused one-cycle zero-retry soak, and normal `/index.html` Browser/Rust smoke evidence. |
| 306 | done | Live file chunk stale-generation status coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the stale-generation counterpart to the live file chunk error coverage. New mode `file-chunk-stale-generation-error-browser-status` syncs a real CTOX workspace file into SQLite, changes the replicated `desktop_files.content_generation_id` to a missing generation while preserving an older live chunk generation, removes materialization paths, waits until Browser IndexedDB has live chunks but zero chunks for the requested generation, opens the file through the real File Viewer, and asserts Advanced Status records `CtoxFileChunkIntegrityError` with `ctox_file_chunk_missing` and phase `file-chunk-reconstruct`. The mode is now in the smoke matrix, default soak runner, and manual GitHub release soak workflow; the soak guard reports 22 release modes. Verified JS syntax, RxDB-only and File Viewer integrity guards, soak workflow guard, focused matrix evidence, focused one-cycle zero-retry soak, and normal `/index.html` Browser/Rust smoke evidence. |
| 305 | done | Live file chunk tombstone status coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the tombstone counterpart to the live file chunk metadata error coverage. New mode `file-chunk-tombstone-error-browser-status` syncs a real CTOX workspace file into SQLite, updates the file metadata to remove materialization paths, tombstones the active `desktop_file_chunks` row with a new revision/LWT, waits until Browser IndexedDB has zero live chunks for that file, opens the file through the real File Viewer, and asserts Advanced Status records `CtoxFileChunkIntegrityError` with `ctox_file_chunk_missing` and phase `file-chunk-reconstruct`. The mode is now in the smoke matrix, default soak runner, and manual GitHub release soak workflow; the soak guard reports 21 release modes. Verified JS syntax, RxDB-only and File Viewer integrity guards, soak workflow guard, focused matrix evidence, focused one-cycle zero-retry soak, and normal `/index.html` Browser/Rust smoke. |
| 304 | done | Live file chunk metadata error status coverage | `.github/workflows/rxdb-soak.yml`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted Browser file chunk metadata integrity from a local helper fixture into the live Browser/Rust WebRTC harness. New mode `file-chunk-metadata-error-browser-status` syncs a real CTOX workspace file into SQLite, mutates a replicated `desktop_file_chunks` row with a new revision/LWT and invalid `size_bytes`, waits until the Browser receives that corrupt metadata over RxDB/WebRTC, opens it through the real File Viewer, and asserts Business OS Advanced Status records `CtoxFileChunkIntegrityError` with `ctox_file_chunk_integrity_mismatch` and phase `file-chunk-reconstruct`. The mode is now in the smoke matrix, default soak runner, and manual GitHub release soak workflow; the soak guard reports 20 release modes. Verified JS syntax, RxDB-only and File Viewer integrity guards, soak workflow guard, focused matrix evidence, focused one-cycle zero-retry soak, and normal `/index.html` Browser/Rust smoke. |
| 303 | done | Browser file chunk metadata integrity parity | `src/apps/business-os/shared/file-integrity.js`, `src/apps/business-os/scripts/assert-file-viewer-integrity.mjs`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/shared/universal-importer.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the Browser-side parity gap with Rust desktop chunk reconstruction by validating replicated chunk metadata before decode. The shared `file-integrity` helper now rejects inconsistent per-row `total` values and encoded `size_bytes` mismatches as typed `CtoxFileChunkIntegrityError` / `ctox_file_chunk_integrity_mismatch` failures, while preserving legacy rows without finite size metadata. The executable File Viewer integrity fixture now covers both negative cases, and the RxDB-only guard pins the metadata validator plus cache-busted File Viewer, Explorer, and Universal Importer imports. Verified JS syntax, the negative integrity guard, the RxDB-only guard, a real `/index.html` Browser/Rust smoke with Advanced Status and Browser→Rust file/chunk replication, and the large File Viewer Rust→Browser matrix path with 103 chunks and full payload materialization. |
| 302 | done | Release soak raw Rust push-contract error coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the push-side counterpart to the raw Rust replication envelope coverage. New smoke mode `replication-push-contract-error-browser-status` injects a raw Rust `RxError (RC_PUSH_NO_AR)` envelope with nested `ctoxError` parameters and push rows, then asserts Business OS Advanced Status exposes `CtoxReplicationIoError` with `ctox_replication_push_contract_invalid`, direction `push`, upstream code `RC_PUSH_NO_AR`, `rowCount=2`, and `retryable=false`. Promoted the mode into the smoke matrix, default soak runner, and manual GitHub release soak workflow. The soak workflow drift guard now reports 19 release modes. Verified JS syntax, soak workflow guard, focused matrix evidence, one-cycle zero-retry soak, and normal `/index.html` Browser/Rust smoke with Advanced Status plus bidirectional file/chunk replication. |
| 301 | done | Advanced Status raw Rust-envelope normalization | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the Business OS Advanced Status serializer so raw Rust/WebRTC replication envelopes are normalized at status time, not only when they pass through the live `replicationState.error$` subscription. `serializeAdvancedStatusCollectionError` now uses the shared `classifyReplicationIoError` classifier, so raw `RxError (RC_PULL)` / `RC_PUSH` / `RC_PUSH_NO_AR` objects with nested `ctoxError` parameters become stable `CtoxReplicationIoError` diagnostics in `sync.collectionErrors` and `sync.replicationErrors`. The negative `replication-error-browser-status` smoke now injects a raw Rust `RC_PULL` envelope instead of a pre-typed Browser error and asserts `batchSize=20` plus `rowCount=null`. Business OS cache keys were bumped for the changed app/sync module. Verified JS syntax, classifier guard, RxDB-only guard, focused matrix, one-cycle zero-retry soak, and normal `/index.html` Browser/Rust smoke with Advanced Status and bidirectional file/chunk replication. |
| 300 | done | Browser Rust-envelope replication error guard | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/scripts/assert-sync-error-classification.mjs`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `.github/workflows/ci.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added an executable Business OS guard for Rust-style WebRTC replication error envelopes. The classifier now prefers payload-rich `parameters` envelopes over outer code-only wrappers, preserves explicit Rust `rowCount`, avoids converting null batch/row values to zero, and keeps `RC_PULL`, `RC_PUSH`, and `RC_PUSH_NO_AR` mapped to stable `CtoxReplicationIoError` diagnostics. CI now syntax-checks and runs the new sync error classification guard alongside the RxDB-only and File Viewer integrity guards. Verified JS syntax, the new guard, the RxDB-only guard, and a real `/index.html` Browser/Rust smoke with Advanced Status and bidirectional file/chunk replication. |
| 299 | done | Rust WebRTC typed transport errors | `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the Rust WebRTC master-method transport so `masterChangesSince` and `masterWrite` handler failures are no longer downgraded to `null` or empty conflict lists. The master side now returns structured `ctoxError` replication envelopes with `RC_PULL` / `RC_PUSH`, phase, direction, checkpoint, batch, row-count, and nested RxError evidence. The fork side turns those envelopes back into `RxError`, wraps invalid pull decode results as `RC_PULL`, and rejects invalid push response contracts as `RC_PUSH_NO_AR` instead of silently accepting them. Added focused Rust tests for pull envelopes, push envelopes, and invalid push request rows. Verified focused Rust WebRTC tests plus a real `/index.html` Browser/Rust smoke with Advanced Status and bidirectional file/chunk replication over RxDB/WebRTC. |
| 298 | done | Release soak replication-error coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted the typed replication I/O error path into the release smoke/soak contract. The matrix default modes now include `replication-error-browser-status` and require `replication_error_collection`, `replication_error_code`, and `replication_error_name` evidence with `ctox_replication_pull_failed` / `CtoxReplicationIoError`. The soak runner and GitHub manual soak workflow include the mode in default and required release coverage. The soak workflow drift guard now reports 18 release modes and fails if the workflow, runner, matrix, evidence map, or harness supported mode list diverge. Verified JS syntax, the soak workflow guard, focused replication-error matrix evidence with `SMOKE_MATRIX_ATTEMPTS=1`, and a one-cycle replication-error soak with `SOAK_FAIL_ON_RETRY=1` and `retries=0`. |
| 297 | done | Typed replication I/O errors | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Business OS now classifies RxDB hard-fork `RC_PULL`, `RC_PUSH`, and `RC_PUSH_NO_AR` failures as `CtoxReplicationIoError` instead of generic replication errors. Advanced Status exposes these non-secret diagnostics through `sync.collectionErrors` and `sync.replicationErrors`, including stable `ctox_replication_pull_failed`, `ctox_replication_push_failed`, and `ctox_replication_push_contract_invalid` codes, phase, direction, upstream code, batch size, row count, severity, and retryability. Readiness now includes `noReplicationIoErrors`, and the RxDB-only guard pins the typed replication I/O surface. Added `SMOKE_MODE=replication-error-browser-status`, which injects an `RC_PULL`-derived diagnostic into the real Business OS status surface and proves the expected name/code/phase/direction/upstream evidence appear; the normal `/index.html` Browser/Rust smoke remains green with Advanced Status health, Rust-to-Browser readiness payload, and Browser-to-Rust file/chunk replication. |
| 296 | done | Release soak schema-error coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted the typed schema protocol error path into the release smoke/soak contract. The matrix default modes now include `schema-error-browser-status` and require `schema_error_collection`, `schema_error_code`, and `schema_error_name` evidence with `ctox_schema_hash_mismatch` / `CtoxSchemaProtocolError`. The soak runner and GitHub manual soak workflow include the mode in default and required release coverage. The soak workflow drift guard now reports 17 release modes and fails if the workflow, runner, matrix, evidence map, or harness supported mode list diverge. Verified JS syntax, the soak workflow guard, focused schema-error matrix evidence with `SMOKE_MATRIX_ATTEMPTS=1`, and a one-cycle schema-error soak with `SOAK_FAIL_ON_RETRY=1` and `retries=0`. |
| 295 | done | Typed schema protocol errors | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Business OS now classifies RxDB hard-fork protocol/schema handshake failures as `CtoxSchemaProtocolError` instead of leaving them as generic replication errors. Schema hash mismatches, missing schema hashes, protocol mismatches, and collection mismatches get stable `ctox_schema_*` codes, phase `schema-handshake`, severity, retryability, and sanitized expected/actual evidence. Advanced Status exposes these diagnostics through `sync.collectionErrors` and `sync.schemaErrors`, and readiness now includes `noSchemaProtocolErrors`. The RxDB-only guard pins the typed schema protocol error surface. Added `SMOKE_MODE=schema-error-browser-status`, which injects a schema hash mismatch into the real Business OS status surface and proves the expected name/code/phase/evidence appear; the normal `/index.html` Browser/Rust smoke remains green with Advanced Status health, Rust-to-Browser readiness payload, and Browser-to-Rust file/chunk replication. |
| 294 | done | Release soak checkpoint-error coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted the typed checkpoint protocol error path into the release smoke/soak contract. The smoke harness docs now list `checkpoint-error-browser-status`; the matrix default modes include it and require `checkpoint_error_collection`, `checkpoint_error_code`, and `checkpoint_error_name` evidence with `ctox_checkpoint_epoch_missing` / `CtoxCheckpointProtocolError`; the soak runner and GitHub manual soak workflow now include it in default and required release modes. The soak workflow drift guard now reports 16 release modes and fails if the workflow, runner, matrix, evidence map, or harness supported mode list diverge. Verified JS syntax, the soak workflow guard, focused checkpoint-error matrix evidence with `SMOKE_MATRIX_ATTEMPTS=1`, and a one-cycle checkpoint-error soak with `SOAK_FAIL_ON_RETRY=1` and `retries=0`. |
| 293 | done | Typed checkpoint protocol errors | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the negative counterpart to checkpoint epoch evidence. Business OS now classifies remote peers that omit `ctox-checkpoint-epoch-v1` as `ctox_checkpoint_capability_missing` and peers that advertise the capability without usable epoch evidence as `ctox_checkpoint_epoch_missing`, both under `CtoxCheckpointProtocolError` with phase `checkpoint-handshake`. Advanced Status now exposes these non-secret diagnostics through `sync.collectionErrors` and `sync.checkpointErrors`, and the readiness checks include `noCheckpointProtocolErrors`. The RxDB-only guard pins the typed checkpoint error surface. Added `SMOKE_MODE=checkpoint-error-browser-status`, which injects a checkpoint protocol diagnostic into the real Business OS status surface and proves it appears with the expected name/code/phase; the normal `/index.html` Browser/Rust smoke remains green with Advanced Status checkpoint proof and Browser→Rust file/chunk replication. |
| 292 | done | Rust/JS checkpoint epoch evidence | `src/core/rxdb/src/types/storage.rs`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/webrtc-types.ts`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added non-secret checkpoint epoch evidence to the CTOX RxDB WebRTC protocol. `rxdb-rs` storage instances can now advertise collection, schema hash, latest checkpoint metadata, hashed latest id, and an epoch; SQLite implements that snapshot from the replicated storage checkpoint. The Rust protocol response and Browser hard fork now advertise `ctox-checkpoint-epoch-v1`, Business OS records remote checkpoint evidence in `sync.peerSessions[*].checkpoint`, and the Browser/Rust smoke fails if a live peer session lacks advertised checkpoint epoch evidence. Browser cache keys were bumped for the app, sync runtime, and rebuilt fork bundle. The smoke harness now builds the exact integration-target `ctox` binary before live runs, preventing stale native peer binaries from hiding protocol regressions. Rebuilt `rxdb-bundle.mjs` and provenance from `ctox-rxdb-js/source`; new bundle SHA-256 is `31d85742875f0205358af24d4373aa8b5c769839751d4ea568d934e118ae9824`. Verified JS syntax, hard-fork strategy guard, bundle-contract guard, RxDB-only guard, schema-contract freshness, `cargo fmt --check`, focused CTOX binary build/test filters, and real `/index.html` Browser/Rust smoke with Advanced Status checkpoint proof plus Browser→Rust file/chunk replication. |
| 291 | done | RxDB WebRTC pool initial-sync awaiters | `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Moved the Browser initial-sync proof into the CTOX RxDB JS hard fork. `RxWebRTCReplicationPool` now exposes `awaitInitialReplication()` and `awaitInSync()` by waiting for peer replication states and delegating to each peer state's native awaiter, with master-only pools resolving after peer discovery. Business OS now imports the rebuilt bundle with a fresh cache key instead of depending only on wrapper-side peer-state introspection. The hard-fork strategy guard pins both pool awaiters. Rebuilt `rxdb-bundle.mjs` and provenance from `ctox-rxdb-js/source`; new bundle SHA-256 is `52dbf7b016eb0b916a3c60ad11a74b377e90d3795f71af3868945442274fcb4e`. Verified hard-fork strategy guard, bundle-contract guard, RxDB-only guard, JS syntax, and real `/index.html` Browser/Rust smoke with Advanced Status initial-sync proof plus Browser→Rust file/chunk replication. |
| 290 | done | Initial sync readiness diagnostics | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Tightened Business OS startup health so required RxDB/WebRTC collections are not reported healthy until their initial replication is confirmed. The sync runtime now records `initialReplicationState`, `initialReplicationSource`, `initialReplicationStartedAt`, and `initialReplicationAt`, including WebRTC pool peer-state confirmation for the CTOX RxDB WebRTC pool. Advanced Status now exposes `sync.initialSync` with completed, missing, pending, stalled, and per-collection entries, and adds the `requiredCollectionsInitialSyncComplete` gate. The Browser/Rust smoke contract now fails if required collections lack completed initial replication. Verified JS syntax, RxDB-only guard, schema-contract freshness, and real `/index.html` Browser/Rust smoke with `business-os-advanced-status-v1`, Rust readiness payload, and Browser→Rust file/chunk replication over RxDB/WebRTC. |
| 289 | done | File integrity Advanced Status diagnostics | `src/apps/business-os/app.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/shared/universal-importer.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Surfaced typed Browser file chunk reconstruction failures through `business-os-advanced-status-v1` without treating normal healthy states as failed. The shell now keeps the last ten non-secret `CtoxFileChunkIntegrityError` diagnostics under `snapshot.fileIntegrity`, including source, name, code, phase, sanitized file id/mime/generation/hash-scheme metadata, message, and timestamp. Desktop apps and module contexts receive `reportFileIntegrityError`; File Viewer, Explorer previews/download fallback, and Universal Importer report typed chunk failures when reconstruction fails. The RxDB-only guard now pins this Advanced Status file-integrity diagnostics surface. Verified JS syntax, negative integrity fixture, RxDB-only guard, schema-contract freshness, and real `/index.html` Browser/Rust smoke with `business-os-advanced-status-v1`, Rust readiness payload, and Browser→Rust file/chunk replication over RxDB/WebRTC. |
| 288 | done | Typed file chunk integrity errors | `src/apps/business-os/shared/file-integrity.js`, `src/apps/business-os/scripts/assert-file-viewer-integrity.mjs`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/shared/universal-importer.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added stable Browser-side chunk integrity error taxonomy. `file-integrity` now throws `CtoxFileChunkIntegrityError` with phase `file-chunk-reconstruct` and codes `ctox_file_chunk_missing`, `ctox_file_chunk_generation_mismatch`, and `ctox_file_chunk_integrity_mismatch` while preserving the user-facing messages. The negative fixture now asserts error class, phase, and code for missing content, tombstoned active generation, stale generation request, metadata mismatch, chunk hash mismatch, and decoded content hash mismatch. The RxDB-only guard pins the taxonomy, and Business OS cache keys were bumped for the typed error module. Verified JS syntax, negative integrity fixture, RxDB-only guard, schema-contract freshness, and real `/index.html` Browser/Rust smoke with `business-os-advanced-status-v1`, Rust readiness payload, and Browser→Rust file/chunk replication over RxDB/WebRTC. |
| 287 | done | Browser file chunk tombstone filtering | `src/apps/business-os/shared/file-integrity.js`, `src/apps/business-os/scripts/assert-file-viewer-integrity.mjs`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/shared/universal-importer.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened Browser file reconstruction against replicated chunk tombstones. The shared `file-integrity` module now filters `_deleted`, `deleted`, and `is_deleted` chunk rows before generation selection, so pruned stale chunks cannot be selected as current content even if they have newer timestamps. The negative fixture now proves stale tombstoned generations are ignored, a fully tombstoned active generation is treated as missing content, and all supported deletion markers are recognized. File Viewer, Explorer, and Universal Importer import the cache-busted integrity module, and the Business OS shell build key was bumped to avoid stale browser module caches. Verified JS syntax, negative integrity fixture, RxDB-only guard, schema-contract freshness, and real `/index.html` Browser/Rust smoke with `business-os-advanced-status-v1`, Rust readiness payload, and Browser→Rust file/chunk replication over RxDB/WebRTC. |
| 286 | done | Rust file chunk integrity negative fixtures | `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a Rust-side desktop file chunk reconstruction fixture that validates the actual SQLite rows produced by `sync_desktop_file_from_path`: active generation id, contiguous chunk indexes, matching `total`, `sha256-bytes-v1` content hash, `sha256-base64-chunk-v1` per-chunk hash, encoded chunk size, base64 decoding, and decoded payload hash. The same test mutates the valid fixture and proves stale generations, mismatched content hashes, corrupted chunk hashes, missing chunks, and inconsistent totals are rejected. Because the test name is under `sync_desktop_file_from_path`, the existing CI lane that runs focused native file-sync tests now covers the negative integrity fixture as well. Verified `cargo fmt --check` and `cargo test --locked --bin ctox sync_desktop_file_from_path --quiet` with 6/6 tests passing. |
| 285 | done | File chunk integrity negative fixtures | `src/apps/business-os/shared/file-integrity.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/shared/universal-importer.js`, `src/apps/business-os/scripts/assert-file-viewer-integrity.mjs`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `.github/workflows/ci.yml`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Pulled Browser-side desktop file reconstruction into one shared integrity module so File Viewer, Explorer previews/downloads, and the Universal Importer all enforce the same active generation, chunk count, per-chunk hash, and decoded content hash contract. Requested content generations are now strict: stale/latest fallback is not used when a file row names a generation. Added negative JS fixtures for missing chunks, stale generation requests, mismatched chunk metadata, corrupted chunk hashes, corrupted decoded content, and legacy rows without hash-scheme markers. The RxDB-only guard now pins the shared module and all Browser consumers, and CI runs the new integrity fixture. Business OS cache keys were bumped for the changed shell/desktop modules. Verified JS syntax, the negative integrity fixture, RxDB-only guard, schema-contract freshness, and a real `/index.html` Browser/Rust smoke with `business-os-advanced-status-v1`, Rust readiness payload in the browser, and Browser→Rust file/chunk replication over RxDB/WebRTC. |
| 284 | done | File chunk integrity contract slice | `src/apps/business-os/modules/desktop/schema.js`, `src/core/business_os/business_os_schema_contract.json`, `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the first explicit JS/Rust file chunk integrity contract. The desktop schema now declares `content_generation_id`, `content_hash_scheme`, chunk `generation_id`, `content_hash`, `content_hash_scheme`, `chunk_hash`, and `chunk_hash_scheme`, and the generated Rust schema contract is current. Rust now writes `sha256-bytes-v1` content hashes plus `sha256-base64-chunk-v1` per-chunk hashes for native CTOX workspace files. Browser-uploaded files in Explorer use the same hash schemes. File Viewer validates active generation consistency, per-chunk hashes when present, and final decoded file content hashes when the `sha256-bytes-v1` scheme is present; legacy rows without hash-scheme markers remain readable. The RxDB-only guard now pins this integrity contract, and Business OS cache keys were bumped for the changed shell/desktop app modules. Verified JS syntax, RxDB-only guard, schema-contract freshness, focused native file-sync tests, rebuilt the integration-target `ctox` binary, and reran real `/index.html` Browser/Rust smoke with `advanced_status=business-os-advanced-status-v1` plus Browser→Rust file/chunk replication. |
| 283 | done | RxDB JS release evidence closure | `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `src/core/rxdb/tools/assert_js_fork_release_evidence.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the remaining release-evidence gap for the private `ctox-rxdb-js` hard fork. CI now syntax-checks and executes a dedicated release-evidence guard, and the retained CI bundle evidence includes the runtime bundle, generated provenance, bundle contract, zero-vulnerability audit baseline, hard-fork manifest, and source `package.json` / `package-lock.json`. CLI release archives on Unix and Windows now package the same provenance set under `rxdb-js/`, including `rxdb-js/source/package.json` and `rxdb-js/source/package-lock.json`, so a shipped CTOX artifact can be audited back to the exact private fork lockfile and policy state. Verified the new release-evidence guard, hard-fork strategy guard, dependency-audit guard, bundle-contract guard, and RxDB-only guard. |
| 282 | done | RxDB JS runtime import narrowing | `src/core/rxdb/js-fork/source/package.json`, `src/core/rxdb/js-fork/source/package-lock.json`, `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/core/rxdb/js-fork/dependency-audit-baseline.json`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Narrowed the private `ctox-rxdb-js` package to the actual CTOX Browser bundle surface. Removed non-WebRTC/non-Browser runtime dependencies for Firebase, MongoDB, NATS, GraphQL, WebSocket replication, encryption, key-compression, `is-my-json-valid`, and unused type packages. Removed the Node-only `signaling-server.ts` re-export from `replication-webrtc/index.ts`, so the Browser bundle no longer requires the Node `ws` server path. Reduced package exports to the supported CTOX surface (`core`, `utils`, Dexie storage, WebRTC replication, migration schema, AJV/ZSchema validators, and package metadata). Reduced `devDependencies` to `esbuild` only and kept `rxjs` as a real runtime dependency. The lockfile now has 107 total dependencies with zero audit findings, and the audit baseline pins lockfile SHA-256 `84d084ff4c1681647dbfdef6fa8c0c9a495f76d667ee69791376c2e972fd061a`. The Business OS bundle hash remained stable at `e9847528addbb5b087069175fb142569cde8ca55b6a1f75e3b432fe05a91f7fc`. The hard-fork strategy guard now enforces the narrowed export map, CTOX-only scripts, single `esbuild` dev dependency, and no reintroduced upstream service dependencies or Node signaling-server export. Verified `npm ci`, `npm audit --audit-level=moderate`, source/artifact bundle builds, dependency-audit guard, hard-fork strategy guard, bundle-contract guard, and `npm run ctox:check`. |
| 281 | done | RxDB JS dependency reduction to zero audit | `src/core/rxdb/js-fork/source/package.json`, `src/core/rxdb/js-fork/source/package-lock.json`, `src/core/rxdb/js-fork/dependency-audit-baseline.json`, `src/core/rxdb/tools/assert_js_fork_dependency_audit.js`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/scripts/vendor-builds/build-ctox-rxdb-js.mjs`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Reduced the private `ctox-rxdb-js` fork dependency surface from the upstream-style toolchain to the CTOX bundle pipeline. Removed unused upstream dev/service tooling including Webpack/Rollup/Karma/Mocha, Firebase/Supabase/Appwrite CLI tooling, legacy profiling/watch/pre-commit tools, stale `rxdb-old`, and the direct Node `ws` server dependency. Trimmed package scripts to `postinstall`, `ctox:bundle`, `ctox:audit`, and `ctox:check` so the private fork no longer advertises broken upstream package workflows. Upgraded `ajv` to `8.20.0` and pinned transitive `ws` to `8.20.1`, bringing `npm audit` to zero vulnerabilities. The audit baseline now enforces a zero-vulnerability budget, 701 total dependencies, and the new lockfile SHA-256 `f6d5b0febd83c2d6820b7c65b642b720c574e31f5b43b81a242e5a47029bfb10`. Rebuilt the Business OS bundle/provenance from source; the bundle SHA-256 is now `e9847528addbb5b087069175fb142569cde8ca55b6a1f75e3b432fe05a91f7fc`. Verified `npm ci`, `npm audit --audit-level=moderate`, `npm run ctox:check`, dependency-audit guard, hard-fork strategy guard, bundle-contract guard, source/artifact bundle builds, and RxDB-only guard. |
| 280 | done | RxDB JS dependency audit baseline | `src/core/rxdb/js-fork/dependency-audit-baseline.json`, `src/core/rxdb/tools/assert_js_fork_dependency_audit.js`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `.github/workflows/ci.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a reproducible dependency-audit baseline for the `ctox-rxdb-js` hard fork instead of applying unsafe upstream-wide `npm audit fix --force` changes. The baseline pins the fork `package-lock.json` SHA-256 (`9c18ceafeea46631ba586db9354986db9660405003c44ae6d83f080505ef1164`), the current audit severity budget (`2` low, `17` moderate, `13` high, `9` critical, `41` total), direct vulnerable package inventory, and a triage queue for build tools, legacy dev tools, upstream runtime compatibility, and SaaS-specific tooling. The new guard runs `npm audit --json`, allows the known baseline, and fails on lockfile drift, severity-budget growth, dependency-budget growth, or new direct vulnerable packages. CI now runs the dependency-audit guard after `npm ci` in the bundle provenance job and uploads the audit baseline with the bundle evidence. Verified the new guard, hard-fork strategy guard, and bundle-contract guard. |
| 279 | done | RxDB JS publish and version discipline | `src/core/rxdb/js-fork/README.md`, `src/core/rxdb/js-fork/ctox-rxdb-js.manifest.json`, `src/core/rxdb/js-fork/bundle-contract.json`, `src/core/rxdb/js-fork/source/package.json`, `src/core/rxdb/js-fork/source/package-lock.json`, `src/scripts/vendor-builds/build-ctox-rxdb-js.mjs`, `src/core/rxdb/tools/assert_js_fork_bundle_contract.js`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the `ctox-rxdb-js` hard-fork identity so it cannot drift back into an upstream-like npm package. The fork package is now `private: true`, uses the CTOX repository/homepage metadata, keeps upstream attribution only under `ctoxHardFork.upstream`, and records a `private-package-only` publish policy plus `upstream-version-pinned-with-ctox-provenance` version discipline in the manifest, bundle contract, package metadata, and generated provenance. The bundle-contract and strategy guards now fail if the private package policy, CTOX repository path, restricted publish config, version discipline, source lock hash, provenance fields, or expected Business OS importers drift. Rebuilt the Business OS bundle provenance and CI artifact provenance; the bundle hash remains `e5dc4663dfe95bbc830fb2cb21b13833a4eefce44d939e84e722af156308a6c4`, while the fork lockfile hash is now `9c18ceafeea46631ba586db9354986db9660405003c44ae6d83f080505ef1164`. Verified `npm ci`, source and artifact bundle builds, JS syntax, hard-fork strategy guard, bundle-contract guard, and RxDB-only guard. |
| 278 | done | RxDB JS bundle CI and release evidence | `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `src/scripts/vendor-builds/build-ctox-rxdb-js.mjs`, `src/core/rxdb/js-fork/bundle-contract.json`, `src/core/rxdb/js-fork/source/package.json`, `src/core/rxdb/tools/assert_js_fork_bundle_contract.js`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the `ctox-rxdb-js` reproducible bundle pipeline. The bundle contract now pins the fork `package-lock.json` and `npm-ci`; provenance records the lockfile path and SHA-256 (`5464dd8f43659df829e8367447501a7c5d03d825e6c396845228f0295af89d13`) alongside the generated bundle SHA-256 (`e5dc4663dfe95bbc830fb2cb21b13833a4eefce44d939e84e722af156308a6c4`). The contract guard verifies lockfile identity, lockfile version, deterministic no-op `postinstall`, package manager, bundle hash, source lock hash, expected exports, and importer paths. CI now uses `actions/setup-node` npm caching keyed to the fork lockfile, runs `npm ci`, rebuilds the bundle into `runtime/build/ctox-rxdb-js`, and uploads bundle/provenance/contract evidence. Release packages now retain the RxDB JS provenance and bundle contract under `rxdb-js/` in every CLI archive. Verified real `npm ci`, the source build with `--write-bundle --write-provenance`, the CI artifact build path, JS syntax, bundle-contract guard, RxDB-only guard, and focused diff checks. |
| 277 | done | Release reconnect soak coverage | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted the recent reconnect evidence into the release matrix and manual soak contract. The smoke harness now prints `advanced_status=business-os-advanced-status-v1` for file Browser→Rust reconnect modes, and the matrix requires that evidence for native peer process restart, Signaling restart, in-process native peer rollover, tab freeze/resume, and browser network flap before a mode can pass. The default matrix includes rollover, tab-freeze, and network-flap modes; the release soak now requires process restart, Signaling restart, rollover, tab-freeze, and network-flap coverage in addition to command, file, and large-file materialization paths. Verified JS syntax, the soak workflow drift guard, a focused `network-flap-browser-to-rust` matrix run, and a focused one-cycle `browser_rust_soak.js` run with JSON evidence showing `advanced_status`, `readiness_payload`, replicated Browser file/chunk data, and `retryCount=0`. |
| 276 | done | Browser network-flap recovery evidence | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=network-flap-browser-to-rust` for the real Business OS `/index.html` path. The harness waits for healthy Advanced Status, uses Playwright to set the browser context offline for a short connectivity outage, restores it online, waits for `business-os-advanced-status-v1` to become healthy again, and then proves Browser `desktop_files` / `desktop_file_chunks` replication into Rust SQLite over RxDB/WebRTC. The run shows expected transient `ERR_INTERNET_DISCONNECTED` WebSocket failures during the outage and a healthy replicated end state afterwards. |
| 275 | done | Tab-freeze Browser/Rust recovery evidence | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=tab-freeze-browser-to-rust` for the real Business OS `/index.html` path. After startup and healthy Advanced Status, the harness freezes the Chromium page via `Page.setWebLifecycleState`, resumes it, waits for `business-os-advanced-status-v1` to become healthy again, and then proves Browser `desktop_files` / `desktop_file_chunks` replication into Rust SQLite over RxDB/WebRTC. This adds deterministic local tab-sleep-style evidence without introducing any HTTP data fallback. |
| 274 | done | In-process native peer rollover smoke | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/server.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a controlled native peer shutdown/restart path inside the running `business-os serve` process. The peer now owns a shutdown channel, cancels WebRTC pools and background tasks, closes the RxDB database, clears the in-process peer handle, releases its process lock, and can be restarted with a fresh peer-session id without restarting the HTTP shell process. The restart endpoint is gated behind `CTOX_BUSINESS_OS_ENABLE_SMOKE_CONTROLS` and is used only by the smoke harness, not as a business data path. Added `SMOKE_MODE=rollover-native-peer-browser-to-rust`, which triggers the in-process rollover, restarts Browser file bridges, requires Advanced Status peer generation advancement, and then proves Browser `desktop_files` / `desktop_file_chunks` replication into Rust SQLite over RxDB/WebRTC. Verified `cargo build --bin ctox` in the integration target, the new rollover smoke against `/index.html`, focused Rust protocol tests, RxDB-only guard, and focused diff checks. |
| 273 | done | Native peer-session identity per peer start | `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Removed the Rust WebRTC protocol response's process-global `OnceLock` peer-session identity. `SyncOptionsWebRTCRs` now carries an explicit `peer_session_id`, Business OS creates one native peer-session id per native peer start and applies it to every collection pool, and `native_peer_status` exposes the current non-secret session id for diagnostics. Added a Rust protocol regression proving supplied session ids are used per payload, so future same-process peer rollover can advance the browser-visible generation instead of reusing a process-global id. Verified focused rxdb-rs protocol tests, the native signaling metadata test, `cargo build --bin ctox`, direct Browser→Rust smoke, native-peer-restart Browser→Rust smoke, and focused diff checks. |
| 272 | done | Signaling restart recovery evidence | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Executed the existing full Business OS `SMOKE_MODE=restart-signaling-browser-to-rust` harness against `/index.html`, restarting both the local Signaling server and the native Rust peer before forcing the Business OS file bridges through reconnect repair. The run showed expected transient WebSocket refusal/closed warnings during the deliberate Signaling outage, then reached healthy Advanced Status and replicated `browser_signaling_restart_smoke_*` file/chunk data into Rust SQLite over RxDB/WebRTC. This closes the local Signaling restart evidence gap for the direct Browser/Rust path; live VPS/domain and long-duration soak evidence remain separate release gates. |
| 271 | done | Peer-generation Advanced Status contract | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Business OS now derives a per-collection peer generation from observed remote native peer-session changes and exposes `generation`, `previousPeerSession`, and `generationChangedAt` through `business-os-advanced-status-v1`. The RxDB-only guard now pins that diagnostic contract. The restart Browser→Rust smoke now restarts the browser collection bridges, waits for a fresh native `peerSession`, asserts that the public generation advanced, and then proves file/chunk replication into Rust SQLite still resumes over RxDB/WebRTC. The smoke harness also retries one pre-hook module-graph stall before failing so startup flakiness does not mask reconnect-state regressions. Verified JS syntax, RxDB-only guard, direct Browser→Rust smoke, native-peer-restart Browser→Rust smoke, and focused diff checks. |
| 270 | done | Typed recoverable peer lifecycle diagnostics | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/connection-handler-simple-peer.ts`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Converted expected WebRTC stale-peer failures into typed recoverable lifecycle diagnostics. Business OS now classifies peer connection loss, negotiation failure, browser peer-connection limits, and connect timeouts as `CtoxWebRtcPeerLifecycleEvent` values with stable codes instead of storing them as collection errors. Advanced Status exposes `sync.lifecycleEvents` and `sync.lastLifecycleEvent`, while still treating real signaling/schema/replication failures as errors. The hard-fork SimplePeer signaling handler now drops sends when the WebSocket is already closing/closed and does not recreate peers after handler close, eliminating the restart-smoke WebSocket noise without changing the RxDB/WebRTC data path. The smoke harness also suppresses expected recoverable peer lifecycle errors while still logging unexpected replication errors. Rebuilt the Business OS bundle from source with deterministic provenance hash `e5dc4663dfe95bbc830fb2cb21b13833a4eefce44d939e84e722af156308a6c4`. Verified JS syntax, hard-fork guards, RxDB-only guard, direct Browser→Rust smoke, native-peer-restart Browser→Rust smoke, and focused diff checks. |
| 269 | done | Peer-session Advanced Status contract | `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/webrtc-types.ts`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a CTOX-specific `onPeerProtocol` observer hook to the RxDB WebRTC hard fork so Business OS can record the successful DataChannel protocol handshake per collection without adding any HTTP data path. Advanced Status now exposes `sync.protocol`, `sync.capabilities`, and `sync.peerSessions` with collection, remote protocol, capabilities, peer session id, and timestamp. The RxDB-only guard and hard-fork strategy guard require the peer-session diagnostics wiring, and the full-app smoke now fails if a healthy Advanced Status lacks `ctox-peer-session-v1` or observed peer sessions. Rebuilt the Business OS bundle from source with deterministic provenance hash `6c7ba1ed81e0493edcc95e33901461f366bc23cb57d7d98b4dc2c488e6fd8886`. Verified JS syntax, hard-fork guards, RxDB-only guard, direct Browser→Rust smoke, and native-peer-restart Browser→Rust smoke with the stricter Advanced Status peer-session contract active. |
| 268 | done | Peer-session checkpoint namespace and restart idempotency | `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `ctox-peer-session-v1` to the Browser and native peer capability set and extended the DataChannel `ctoxProtocol` payload with `peerSession` metadata. The JS hard fork now includes the remote native peer session id in the Browser fork replication identifier, so a restarted native peer gets a fresh checkpoint namespace instead of reusing the exact stale peer key. Restart smoke exposed an RxDB fork reconnect bug where duplicate disconnect events called `removePeer()` for an already removed peer; `removePeer()` is now idempotent and guarded in the hard-fork strategy check. Rebuilt the Business OS bundle from source with deterministic provenance hash `747085c7b533d6da5405330d8271380a3a7de2380511e300ae42dabae6c39954`. Verified hard-fork guards, RxDB-only guard, Rust protocol tests, native signaling metadata test, `cargo build --bin ctox`, direct Browser→Rust smoke, and `SMOKE_MODE=restart-browser-to-rust` proving Browser file/chunk replication resumes after native peer restart over RxDB/WebRTC. |
| 267 | done | Schema-hash parity contract | `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/apps/business-os/shared/sync.js`, `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/business_os_schema_contract.json`, `src/core/rxdb/tools/build_business_os_schema_contract.mjs`, `src/core/rxdb/src/types/schema.rs`, `src/core/rxdb/src/rx_schema.rs`, `src/core/rxdb/src/rx_schema_helper.rs`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Promoted the DataChannel protocol probe from version-only compatibility to schema-hash parity. The browser hard fork now sends collection name/version/hash in `ctoxProtocol`, rejects mismatches with `RC_WEBRTC_PROTOCOL`, and advertises `ctox-schema-hash-v1`; rxdb-rs answers with the native collection hash and capability. Rust now registers Business OS collections from a generated first-win schema contract derived from the browser module `schema.js` files, including the browser-compatible duplicate-collection rule and `indexes: ['field']` normalization. Fixed Rust schema canonicalization so empty `internalIndexes` and integral JSON numeric constraints serialize like the JS fork. Verified the contract generator, RxDB-only guard, hard-fork bundle/strategy guards, Rust protocol tests, native schema-hash guard, `cargo build --bin ctox`, and real `/index.html` Browser→Rust smoke: Rust readiness payload reached the browser and the browser replicated a `desktop_files`/`desktop_file_chunks` payload `hello` into Rust over WebRTC with schema-hash validation active. |
| 266 | done | RxDB JS hard-fork source bundle | `src/core/rxdb/js-fork/source/src/ctox-business-os-browser.ts`, `src/core/rxdb/js-fork/source/src/plugins/storage-dexie/dexie-helper.ts`, `src/core/rxdb/js-fork/source/src/plugins/utils/utils-premium.ts`, `src/core/rxdb/js-fork/source/package.json`, `src/core/rxdb/js-fork/source/package-lock.json`, `src/core/rxdb/js-fork/bundle-contract.json`, `src/scripts/vendor-builds/build-ctox-rxdb-js.mjs`, `src/apps/business-os/vendor/rxdb-bundle.mjs`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/apps/business-os/shared/db.js`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/modules/matching/ui/businessOsDataSource.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Switched Business OS from the guarded legacy vendor artifact to a real source-built `ctox-rxdb-js` browser bundle. Added a dedicated Business OS bundle entry, installed the fork build toolchain, generated `rxdb-bundle.mjs` and deterministic provenance from `js-fork/source` via esbuild, and cache-busted all Business OS importers to `20260522-rxdb-fork1`. Fixed two hard-fork runtime blockers discovered by the real browser smoke: Dexie's state map now stores the created promise value instead of reading an uninitialized `state`, and upstream open-core collection gating is disabled for the CTOX application fork. The Rust WebRTC peer now stays passive for the DataChannel protocol probe and answers Browser `ctoxProtocol` requests, avoiding a symmetric active-probe race while preserving the browser-side protocol gate and Signaling control-plane gate. The smoke harness now captures Advanced Status on shell-ready timeouts and has optional Signaling debug traces. Verified bundle guards, RxDB-only guard, Rust protocol tests, `cargo build --bin ctox`, and real `/index.html` Browser→Rust smoke with the rebuilt bundle: Rust readiness payload reached the browser and the browser replicated a `desktop_files`/`desktop_file_chunks` payload `hello` into Rust over WebRTC. Bundle provenance hash: `41b46f1030e85b56cc9973bdc3e0750ad8122c37b487248782486571d00cb1cc`. |
| 265 | done | RxDB JS/Rust DataChannel protocol handshake | `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/index.ts`, `src/core/rxdb/js-fork/source/src/plugins/replication-webrtc/webrtc-types.ts`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the first DataChannel-level `ctoxProtocol` handshake to the hard fork source and rxdb-rs. The JS hard fork now requires a peer to answer `ctoxProtocol` with `ctox-rxdb-protocol-v1` before the legacy storage-token handshake, rejects mismatches with `RC_WEBRTC_PROTOCOL`, and ignores protocol requests in normal master-method dispatch. The Rust peer answers `ctoxProtocol` with native capabilities and validates compatible responses when available, while keeping the outgoing protocol probe opportunistic until the rebuilt browser bundle ships. The hard-fork guard now requires the source-level protocol request, payload, validation, message type, and `RC_WEBRTC_PROTOCOL` wiring. Verified `cargo test --manifest-path src/core/rxdb/Cargo.toml ctox_protocol_response --quiet` and direct `SMOKE_MODE=browser-to-rust`, proving the current Browser bundle still replicates into Rust while the new Rust-side protocol probe is present. |
| 264 | done | RxDB protocol incompatibility rejection | `src/apps/business-os/shared/sync.js`, `src/core/rxdb/tools/local_signaling_server.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `/Users/you/Documents/ctox-dev/cloudflare-signaling/src/index.js`, `/Users/you/Documents/ctox-dev/scripts/assert-signaling-hardening.mjs`, `/Users/you/Documents/ctox-dev/docs/ctox-peer2peer-signaling.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the protocol metadata slice into an actual Signaling control-plane gate. Browser peers now derive and attach the same room-password token plus bounded `token_iat`/`token_exp` values as the Rust peer, so control-plane-capable browser joins do not get rejected for missing token data. The local Signaling server now parses client/role/instance/protocol/capability metadata and emits typed `ctoxError` frames for missing or mismatched `ctox-rxdb-protocol-v1` before joining a room. The Cloudflare Signaling worker now preserves repeated `cap` values, stores peer protocol metadata, advertises protocol compatibility in health, and rejects missing/mismatched protocol metadata with stable `protocol_missing` / `protocol_mismatch` codes. Verified JS syntax, RxDB-only guard, ctox-dev Signaling hardening guard, a local WebSocket negative join proving `protocol_missing`, and `SMOKE_MODE=signaling-error-browser-status` proving Business OS Advanced Status still surfaces typed `CtoxSignalingControlPlaneError` (`instance_mismatch`) through the browser path. |
| 263 | done | RxDB protocol/capability signaling metadata | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the first protocol-handshake wire slice for `ctox-rxdb-protocol-v1`. Browser WebRTC replication now augments the Signaling URL with `client=ctox-business-os-browser`, `role=browser`, `instance_id`, protocol, and repeated capability parameters. The native Rust peer now sends matching protocol metadata plus native/file capabilities and keeps the room-password token. Advanced diagnostics expose protocol/capabilities, and the RxDB-only guard requires browser protocol metadata. This is not yet hard incompatibility rejection; it prepares the JS/Rust/Signaling contract for that next step. Verified JS syntax, RxDB-only guard, and `cargo test native_signaling_url_carries_control_plane_metadata --quiet`. |
| 262 | done | RxDB JS bundle provenance contract | `src/core/rxdb/js-fork/bundle-contract.json`, `src/apps/business-os/vendor/rxdb-bundle.provenance.json`, `src/core/rxdb/tools/assert_js_fork_bundle_contract.js`, `src/scripts/vendor-builds/build-ctox-rxdb-js.mjs`, `src/apps/business-os/vendor/README.md`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added the first reproducible-bundle pipeline slice for `ctox-rxdb-js`: a machine-readable bundle contract, deterministic provenance file with the current `rxdb-bundle.mjs` SHA-256, guarded expected exports/importers, and a vendor-build entry point that verifies the contract. This locks the currently shipped Business OS bundle against silent hand edits while the full source-to-bundle compiler step remains open. |
| 261 | done | RxDB JS hard-fork source checkout | `src/core/rxdb/js-fork/source/`, `src/core/rxdb/js-fork/ctox-rxdb-js.manifest.json`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Created the actual `ctox-rxdb-js` hard-fork source tree from the pinned upstream `16.20.0` snapshot and changed the fork package metadata to `ctox-rxdb-js` with explicit CTOX provenance and Business OS-first product rule. The static guard now requires the source checkout, package name, upstream version/provenance, manifest `source_path`, and hard-fork contracts. |
| 260 | done | RxDB JS hard-fork control surface | `src/core/rxdb/js-fork/README.md`, `src/core/rxdb/js-fork/ctox-rxdb-js.manifest.json`, `src/core/rxdb/tools/assert_js_fork_strategy.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Accepted `ctox-rxdb-js` as a hard fork built for CTOX Business OS first, with upstream RxDB `16.20.0` as provenance only. Added the fork control surface, manifest, and static guard for protocol version, schema hash parity, peer generation reconnect, typed errors, file chunk generation integrity, checkpoint readiness, and no hand-edited bundle source of truth. |
| 259 | done | Connection-mode smoke configuration guard | `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the Business OS connection-mode smoke so unknown or empty `BUSINESS_OS_CONNECTION_SMOKE_MODES` fail before opening a browser instead of being silently ignored. The harness can now write a structured JSON summary via `BUSINESS_OS_CONNECTION_SMOKE_RESULT_PATH`, including configuration errors, requested modes, signaling target, and per-mode results. Verified syntax and an unsupported-mode negative run that wrote `configurationError`. |
| 258 | done | Release soak retry/evidence enforcement | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the release soak profile so `SOAK_REQUIRED_MODES` also requires `SOAK_FAIL_ON_RETRY=1`, and the soak runner forces `SMOKE_REQUIRE_EVIDENCE=1` for release-required runs before invoking the smoke matrix. The GitHub Actions soak now forces fail-on-retry whenever `require_release_coverage=true`, regardless of the editable focus-run input, and the static guard asserts that wiring. Verified syntax, direct guard execution, and a negative configuration run that writes `configurationError` before starting browser, Signaling, or CTOX. |
| 257 | done | Soak summary formatter CI syntax guard | `.github/workflows/ci.yml`, `src/core/rxdb/tools/print_soak_summary.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `print_soak_summary.js` to the main Business OS RxDB-only CI syntax check list. The manual soak workflow already depends on this formatter for GitHub Actions evidence output, so syntax regressions now fail in the regular CI lane before a manual soak run discovers them. Verified formatter syntax and the static smoke/soak guard. |
| 256 | done | Explicit smoke evidence contract map | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Replaced the implicit fallback-style evidence selection in the smoke matrix with a declarative `modeEvidenceRequirements` map. The static guard now parses that map and requires every matrix default mode to have an explicit evidence contract while also rejecting evidence entries for unsupported smoke modes. This prevents new modes from entering the matrix without machine-readable proof requirements. Verified JS syntax, direct guard execution, and invalid-mode configuration summary output. |
| 255 | done | Smoke/soak duplicate-mode guard | `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added duplicate-mode checks for the release soak runner, smoke matrix, smoke harness, workflow default list, and workflow required list. A duplicated mode can no longer inflate coverage or hide an accidentally omitted mode when the lists otherwise appear aligned. Verified syntax and direct guard execution. |
| 254 | done | Smoke matrix drift guard | `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended the same CI guard to parse `browser_rust_smoke_matrix.js` `defaultModes` and require the release soak modes to remain accepted by the matrix, while also requiring all matrix modes to remain supported by `browser_rust_smoke.js`. This closes the remaining static drift gap between smoke harness, matrix runner, soak runner, and manual release workflow. Verified syntax and direct guard execution. |
| 253 | done | Release soak smoke-mode guard | `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended the release soak workflow guard so every release soak mode must also be supported by `browser_rust_smoke.js` before CI accepts the configuration. This catches the case where a mode stays listed in soak/workflow defaults after the smoke harness removes or renames it, turning that into a fast contract failure instead of a late soak failure. Verified syntax and direct guard execution. |
| 252 | done | Release soak workflow drift guard | `src/core/rxdb/tools/assert_rxdb_soak_workflow.js`, `.github/workflows/ci.yml`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a CI-enforced Node guard that compares `browser_rust_soak.js` `defaultSoakModes` against the manual GitHub Actions soak `modes` default and release `SOAK_REQUIRED_MODES` list. The same guard asserts that release coverage still defaults on, `SOAK_MIN_CYCLES` remains wired to release coverage, and summary/artifact steps still run with `if: always()`. This prevents the soak workflow from silently dropping a mode, minimum-cycle gate, or evidence step while the runner continues to evolve. Verified syntax, direct guard execution, and CI wiring. |
| 251 | done | Release soak minimum-cycle guard | `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SOAK_MIN_CYCLES` to `browser_rust_soak.js` and wired the manual GitHub Actions soak to require at least three cycles whenever `require_release_coverage=true`. Focused local/manual runs remain possible by omitting `SOAK_MIN_CYCLES` or disabling release coverage, but a one-cycle run can no longer look like a full release soak in CI. Missing minimum cycles fail before browser, Signaling, or CTOX startup and write top-level `configurationError` summaries. Verified syntax, min-cycle failure, focus-run behavior without minimum cycles, formatter non-zero behavior, and Step Summary output. |
| 250 | done | Smoke/soak numeric configuration guard | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/print_soak_summary.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened numeric environment parsing for the single smoke, matrix, and soak entry points. Ports, cycle counts, matrix attempts, and soak port stride now require bounded positive integers instead of allowing `NaN` to skip loops or leak into port allocation. Matrix and soak configuration failures write JSON `configurationError` summaries before any browser, Signaling, or CTOX process starts, and `print_soak_summary.js` renders top-level configuration errors in logs and `$GITHUB_STEP_SUMMARY`. Verified syntax, bad matrix attempts, bad soak cycles, bad single-smoke port, and formatter output for invalid cycles. |
| 249 | done | Release soak coverage guard | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/tools/print_soak_summary.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added an explicit `SOAK_REQUIRED_MODES` contract to `browser_rust_soak.js`. The manual GitHub Actions soak now defaults `require_release_coverage=true` and requires the full release mode set, while focused local/manual runs can omit `SOAK_REQUIRED_MODES` or disable the workflow input intentionally. Missing required modes fail before browser, Signaling, or CTOX processes start, write a top-level `configurationError`, and are rendered by `print_soak_summary.js` in logs and `$GITHUB_STEP_SUMMARY`. Verified syntax, missing-required-mode failure, invalid mode behavior, formatter non-zero behavior, and Step Summary output. |
| 248 | done | Smoke/soak mode configuration guard | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/print_soak_summary.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the smoke matrix so empty or unsupported `SMOKE_MODES` fail before any browser, Signaling, or CTOX process starts. The matrix writes `configurationError` into its JSON summary, the soak runner carries that failed cycle into its own summary, and `print_soak_summary.js` now prints `rxdb_soak_cycle_error` plus a GitHub Step Summary cycle-error table when a cycle fails before attempts exist. Verified syntax, direct invalid matrix failure, invalid soak failure, formatter non-zero behavior, and Step Summary output for configuration errors. |
| 247 | done | GitHub soak step-summary evidence | `src/core/rxdb/tools/print_soak_summary.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended `print_soak_summary.js` so manual RxDB/WebRTC soak runs also append a compact Markdown table to `$GITHUB_STEP_SUMMARY` when GitHub Actions provides it. The table shows cycle, mode, attempt, result, exit status, captured evidence, and `evidenceProblems`, while token-like query values are redacted before display. Failed summaries still exit non-zero after writing the table. Verified syntax, missing-summary behavior, synthetic passing summary output, redaction, synthetic failing-summary output, and `git diff --check`. |
| 246 | done | CI-visible soak evidence summary | `src/core/rxdb/tools/print_soak_summary.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `print_soak_summary.js`, a compact formatter for `browser_rust_soak.js` JSON summaries that prints one CI-readable line for the run and one line per mode attempt, including ports, evidence keys, and `evidenceProblems`. The manual RxDB WebRTC soak workflow now runs this formatter with `if: always()` before uploading the JSON artifact, so GitHub Actions logs expose the same proof data that the artifact carries. The formatter exits non-zero for `ok:false` summaries but treats a missing summary file as a warning so upload diagnostics still run. Verified syntax, positive formatting against a real command-burst soak JSON, missing-file behavior, synthetic failing-summary behavior, and `git diff --check`. |
| 245 | done | Required smoke evidence gate | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Turned the newly recorded smoke evidence into an enforced matrix contract. `browser_rust_smoke_matrix.js` now validates expected evidence keys for known modes by default, including signaling-error diagnostics, large-file payload/generation/chunk metrics, restart advanced-status version, command ids/counts, queue task ids/counts, and basic replication ids. Successful smoke processes with missing evidence are treated as failed attempts and the JSON summary records `evidenceProblems`; `SMOKE_REQUIRE_EVIDENCE=0` remains available for explicit diagnostic bypass. Verified syntax, a positive `command-burst-browser-to-rust` matrix with `requireEvidence:true`, a synthetic missing-evidence matrix failure, a one-cycle `command-burst-browser-to-rust` soak with nested evidence and `retries=0`, and `git diff --check`. |
| 244 | done | Smoke/soak evidence summary hardening | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the smoke matrix result artifact so each attempt captures structured `key=value` evidence from the smoke output, such as `advanced_status`, `chunk_count`, `payload_length`, `command_count`, `task_count_for_commands`, generated ids, and ports. The soak summary already embeds matrix cycle results, so GitHub Actions and local soak artifacts now carry machine-readable proof instead of only pass/fail attempts. Verified `node --check src/core/rxdb/tools/browser_rust_smoke_matrix.js`, a selected `command-burst-browser-to-rust` matrix with `SMOKE_MATRIX_RESULT_PATH` asserting `command_count=5` and `task_count_for_commands=5`, a one-cycle `command-burst-browser-to-rust` soak with `SOAK_RESULT_PATH` asserting the same nested evidence and `retries=0`, and `git diff --check`. |
| 243 | done | Full shell advanced-status restart readiness | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Closed the gap where the post-restart File Viewer smoke could prove file materialization while leaving non-file shell collections outside the required readiness contract. Business OS default advanced status now requires `business_module_catalog`, `ctox_runtime_settings`, `business_commands`, `ctox_queue_tasks`, `desktop_files`, and `desktop_file_chunks`; empty command/queue/file collections are treated as healthy only when registered and not failed, stopped, or pending. The native-peer-restart repair smoke now restarts the full core collection set before materialization and asserts the same public advanced-status contract after rendering. Bumped Business OS to `20260521-rxdb-restart5` and aligned the `sync.js` cache key. Verified JS syntax, RxDB-only guard, direct smoke, selected matrix, one-cycle soak with `SOAK_FAIL_ON_RETRY=1`, `retries=0`, full 103-chunk payload, `advanced_status=business-os-advanced-status-v1`, and `git diff --check`. |
| 242 | done | Post-restart File Viewer advanced-status hardening | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened `SMOKE_MODE=workspace-large-file-viewer-restart-rust-to-browser` so the post-restart File Viewer path must finish with the public `window.CTOX_BUSINESS_OS_STATUS` advanced-status contract healthy for `business_commands`, `desktop_files`, and `desktop_file_chunks`. Added `restartCollections()` to the Browser sync runtime and live sync facade so the restart repair cancels pending global bridge repair, stops stale WebRTC bridges, and rebuilds the critical file/command collection set serially before materialization. This closed the race where the global repair timer could tear down freshly restarted file bridges during lazy open. Verified direct smoke, selected matrix, one-cycle soak with `SOAK_FAIL_ON_RETRY=1`, `retries=0`, full 103-chunk payload, `advanced_status=business-os-advanced-status-v1`, JS syntax checks, RxDB-only guard, and `git diff --check`. |
| 241 | done | File Viewer lazy open after native peer restart | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=workspace-large-file-viewer-restart-rust-to-browser`, which receives lazy metadata for a >1MiB CTOX workspace file, restarts the native CTOX peer while the Browser remains open, restarts the relevant RxDB/WebRTC file and command collection bridges, then mounts the real File Viewer and requires the full 103-chunk payload to render. Fixed the Browser sync runtime so `restartCollection()` drops and cancels stale WebRTC replication bridges instead of returning a cached broken bridge after peer restart; this closed the partial large-file render found by the first soak attempt. Added the mode to matrix, local release-soak defaults, and manual GitHub Actions soak defaults. Verified direct smoke, selected matrix, one-cycle soak with `SOAK_FAIL_ON_RETRY=1` and `retries=0`, JS syntax checks, RxDB-only guard, and `git diff --check`. |
| 240 | done | File Viewer lazy materialization UI smoke | `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=workspace-large-file-viewer-rust-to-browser`, which mounts the real Business OS File Viewer against the live app DB/sync/CommandBus context, opens a >1MiB lazy CTOX workspace file, dispatches `ctox.file.materialize` through the viewer path, and requires the viewer to render the full materialized text from 103 replicated chunks. Adjusted File Viewer materialization so the actual replicated file content is authoritative; the `business_commands` projection is still checked for failed status but no longer blocks successful opens when command status replication lags behind chunk delivery. Added the mode to matrix, local release-soak defaults, and manual GitHub Actions soak defaults. Verified direct smoke, selected matrix, one-cycle soak with `retries=0`, JS syntax checks, RxDB-only guard, and `git diff --check`. |
| 239 | done | Large lazy file materialization WebRTC smoke | `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/apps/business-os/shared/sync-contract.js`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=workspace-large-materialize-rust-to-browser`, which syncs a >1MiB CTOX workspace file as lazy metadata, dispatches `ctox.file.materialize` through replicated `business_commands`, and requires Browser to receive the materialized payload through `desktop_file_chunks` over RxDB/WebRTC. Hardened file chunk transport by reducing desktop file chunks to 16KiB, keeping `desktop_file_chunks` WebRTC pull/push batches and SQLite external-poll events at one document per batch/event, and adding batch tuning to the Rust WebRTC options. Added the mode to matrix, local release-soak defaults, and manual GitHub Actions soak defaults. Verified rebuilt `ctox`, direct smoke, selected matrix, one-cycle soak with `retries=0`, focused Rust materialization tests, SQLite checkpoint test, RxDB-only guard, JS syntax checks, and `git diff --check`. |
| 238 | done | Workspace file update generation smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=workspace-update-rust-to-browser`, which syncs a real CTOX workspace file into SQLite-backed `desktop_files`/`desktop_file_chunks`, verifies the Browser receives it under `/CTOX/<workspace>/reports/brief.md`, rewrites the same file, re-syncs the workspace, and requires the Browser to see the updated payload from the active chunk generation. The smoke harness now selects chunks by `content_generation_id`/`generation_id` and falls back to the newest `created_at_ms` generation, matching File Viewer/Explorer behavior instead of concatenating stale chunks. Added the mode to the smoke matrix, local release-soak defaults, and the manual GitHub Actions soak workflow. Verified rebuilt `ctox`, focused Rust generation tests, direct smoke, matrix smoke, one-cycle soak with `retries=0`, RxDB-only guard, and `git diff --check`. |
| 237 | done | Release soak concurrent command coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `command-burst-browser-to-rust` to the local release-soak default and manual GitHub Actions RxDB/WebRTC soak workflow default list. This keeps concurrent browser command idempotency under repeatable release coverage. Verified `node --check src/core/rxdb/tools/browser_rust_soak.js` and `SOAK_CYCLES=1 SOAK_MODES=command-burst-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9160 SIGNALING_PORT=19210 SOAK_FAIL_ON_RETRY=1 SOAK_RESULT_PATH=/tmp/ctox-rxdb-command-burst-soak.json node src/core/rxdb/tools/browser_rust_soak.js`, which passed first try with `command_count=5`, `task_count_for_commands=5`, and `retries=0`. |
| 236 | done | Concurrent command burst idempotency smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=command-burst-browser-to-rust`, which inserts a burst of five Browser `business_commands` documents over RxDB/WebRTC and requires each command to receive exactly one `ctox_queue_tasks` projection with the same `command_id`. This extends command idempotency coverage from single-command delivery to concurrent browser delivery. Verified `node --check` for the smoke and matrix scripts, direct `SMOKE_MODE=command-burst-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9150 SIGNALING_PORT=19200 node src/core/rxdb/tools/browser_rust_smoke.js`, and matrix `SMOKE_MODES=command-burst-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9151 SIGNALING_PORT=19201 SMOKE_MATRIX_ATTEMPTS=1 SMOKE_MATRIX_RESULT_PATH=/tmp/ctox-rxdb-command-burst-matrix.json node src/core/rxdb/tools/browser_rust_smoke_matrix.js`; both runs returned `command_count=5` and `task_count_for_commands=5`. |
| 235 | done | Release soak mid-flight command restart coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `command-midflight-restart-browser-to-rust` to the local release-soak default and the manual GitHub Actions RxDB/WebRTC soak workflow default mode list. This keeps the repaired mid-flight command/peer-restart path under repeated release coverage instead of leaving it as a one-off smoke. Verified `node --check src/core/rxdb/tools/browser_rust_soak.js` and `SOAK_CYCLES=1 SOAK_MODES=command-midflight-restart-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9140 SIGNALING_PORT=19190 SOAK_FAIL_ON_RETRY=1 SOAK_RESULT_PATH=/tmp/ctox-rxdb-command-midflight-soak.json node src/core/rxdb/tools/browser_rust_soak.js`, which passed first try with `retries=0`. |
| 234 | done | Mid-flight command restart repair hardening | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/shared/command-bus.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the P0 mid-flight command restart gap found in wave 233. Business OS module contexts, settings, desktop apps, reporter/chat surfaces, and the Command Bus now use live DB/sync/command-bus facades so stale module contexts resolve the current RxDB instance after a data-plane repair. The Command Bus waits for `business_commands` to become available during repair and retries if an old collection is closed with `COL21`. Added `SMOKE_MODE=command-midflight-restart-browser-to-rust`, which captures the pre-restart Command Bus, restarts the native CTOX peer during dispatch, and verifies exactly one `ctox_queue_tasks` projection with the same `command_id`. Bumped Business OS to `app.js?v=20260521-rxdb-db32` / `command-bus.js?v=20260521-rxdb-db32`. Verified JS syntax, RxDB-only guard, direct mid-flight smoke, matrix mid-flight smoke with JSON output, and a normal full-app Browser→Rust file/chunk replication smoke. |
| 233 | done | Command path after native peer restart smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=command-restart-browser-to-rust` to verify that after a native CTOX peer restart, the real `/index.html` Business OS app can still dispatch a `business_commands` document over RxDB/WebRTC and receive exactly one `ctox_queue_tasks` projection with the same `command_id`. An initial mid-flight variant exposed a real unresolved repair edge (`business_commands` closed with `COL21` while reconnect repair was still in progress), so this wave covers the post-restart command path and leaves mid-flight command delivery as an explicit remaining P0 matrix gap. Verified `node --check` for the smoke and matrix scripts, direct `SMOKE_MODE=command-restart-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9122 SIGNALING_PORT=19172 node src/core/rxdb/tools/browser_rust_smoke.js`, and matrix `SMOKE_MODES=command-restart-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9123 SIGNALING_PORT=19173 SMOKE_MATRIX_ATTEMPTS=1 SMOKE_MATRIX_RESULT_PATH=/tmp/ctox-rxdb-command-restart-matrix.json node src/core/rxdb/tools/browser_rust_smoke_matrix.js`; both runs returned `status=accepted`, `task_status=queued`, and `task_count_for_command=1`. |
| 232 | done | Command queue projection idempotency smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Strengthened `SMOKE_MODE=command-browser-to-rust` so the Browser-created `business_commands` document must project to exactly one `ctox_queue_tasks` document with the same `command_id`. This catches duplicate queue projection regressions while preserving the existing accepted-command and queue-task visibility checks. Verified `node --check src/core/rxdb/tools/browser_rust_smoke.js` and `SMOKE_MODE=command-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9110 SIGNALING_PORT=19160 node src/core/rxdb/tools/browser_rust_smoke.js`, which returned `status=accepted`, `task_status=queued`, and `task_count_for_command=1`. |
| 231 | done | Healthy advanced-status contract smoke guard | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened the normal full-app smoke path so a healthy Business OS startup must also expose the expected public advanced-status shape: `version:"business-os-advanced-status-v1"`, `sync.mode:"webrtc"`, array-shaped `sync.collectionErrors`, `sync.failedCollections`, and `sync.missingRequiredCollections`, with all three empty in the healthy state. This catches regressions where Business OS visually loads but silently carries collection-level RxDB/WebRTC failures. Verified `node --check src/core/rxdb/tools/browser_rust_smoke.js`, `SMOKE_MODE=signaling-error-browser-status SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9100 SIGNALING_PORT=19150 node src/core/rxdb/tools/browser_rust_smoke.js`, and normal `SMOKE_MODE=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9101 SIGNALING_PORT=19151 node src/core/rxdb/tools/browser_rust_smoke.js`, which replicated a browser file/chunk into isolated Rust SQLite. |
| 230 | done | Release soak signaling-error coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `signaling-error-browser-status` to the manual RxDB/WebRTC soak workflow default mode list and to the local soak runner's no-env default. This makes the release soak cover not only happy-path replication and reconnects, but also whether typed Signaling control-plane errors remain visible in the Browser advanced status. Verified `node --check src/core/rxdb/tools/browser_rust_soak.js` and `SOAK_CYCLES=1 SOAK_MODES=signaling-error-browser-status SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9090 SIGNALING_PORT=19140 SOAK_FAIL_ON_RETRY=1 SOAK_RESULT_PATH=/tmp/ctox-rxdb-signaling-error-soak.json node src/core/rxdb/tools/browser_rust_soak.js`, which passed first try with `retries=0`. |
| 229 | done | Signaling error advanced-status smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=signaling-error-browser-status`, which serves the real `/index.html` Business OS app, injects a typed Signaling `ctoxError` frame through the local WebSocket signaling server, and asserts that `window.CTOX_BUSINESS_OS_STATUS.snapshot().sync.collectionErrors` exposes `CtoxSignalingControlPlaneError` with stable `code:"instance_mismatch"`. The smoke matrix can run the new mode directly and includes it in its default mode list. Verified `node --check` for both smoke scripts, the targeted smoke, the targeted matrix mode, and a normal full-app `SMOKE_MODE=browser-to-rust` replication run after the harness change. |
| 228 | done | Advanced status collection error contract | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Exposed collection-level RxDB/WebRTC failures directly through `window.CTOX_BUSINESS_OS_STATUS.snapshot().sync.collectionErrors`, including collection, status, error name, stable code, and a bounded message. This makes Signaling control-plane failures such as wrong password, expired token, instance mismatch, role limits, and rate limits machine-readable from the public advanced status contract instead of requiring private diagnostic object inspection. Bumped Business OS to `app.js?v=20260521-rxdb-db31` / `shared/sync.js?v=20260521-rxdb-db31`. Verified `node src/apps/business-os/scripts/assert-rxdb-only.mjs` and full-app `SMOKE_MODE=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9070 SIGNALING_PORT=19120 node src/core/rxdb/tools/browser_rust_smoke.js`, which loaded Business OS and replicated a browser-created file/chunk into isolated Rust SQLite over RxDB/WebRTC. |
| 227 | done | Browser signaling control-plane error diagnostics | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a passive browser-side WebSocket observer for Signaling `ctoxError` frames. Control-plane errors are captured as `CtoxSignalingControlPlaneError` diagnostics on the affected RxDB/WebRTC collection with stable `code` values, so wrong password, expired token, instance mismatch, role limit, and rate-limit failures can surface in `window.CTOX_BUSINESS_OS_STATUS` and the sync diagnostics drawer instead of only appearing as a closed socket. The observer does not carry Business OS data and leaves RxDB replication unchanged. Bumped Business OS to `app.js?v=20260521-rxdb-db30` / `shared/sync.js?v=20260521-rxdb-db30`. Verified JS syntax, the RxDB-only guard, `git diff --check`, and full-app `SMOKE_MODE=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9060 SIGNALING_PORT=19110 node src/core/rxdb/tools/browser_rust_smoke.js`, which replicated a browser-created file/chunk into isolated Rust SQLite. |
| 226 | done | Desktop private/NAT Business OS ICE config | `src/apps/desktop/src/app.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended the desktop web-deploy Business OS launch config so private/NAT launches can carry explicit ICE/TURN server configuration from `CTOX_BUSINESS_OS_ICE_SERVERS` or `CTOX_WEBRTC_ICE_SERVERS`. The packed `ctox_config` now includes both `ice_servers` and `iceServers` while keeping `transport:"webrtc"` and `http_bridge_available:false`. Invalid ICE config is rejected instead of launching a misleading private-mode URL. Verified with `cargo fmt --manifest-path src/apps/desktop/Cargo.toml` and `cargo test --manifest-path src/apps/desktop/Cargo.toml desktop_web_deploy_url --locked`, covering normal pairing config, ICE/TURN packing, invalid ICE rejection, and TUI-room rejection. |
| 225 | done | Advanced status file-collection regression guard | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Hardened `window.CTOX_BUSINESS_OS_STATUS` so the default readiness contract includes `desktop_files` and `desktop_file_chunks` in addition to module catalog and runtime settings. Empty file collections are treated as healthy only when they are registered and present in the WebRTC sync diagnostics without failed, stopped, or pending state, avoiding false failures on a clean workspace while still catching missing file-sync startup. The RxDB-only guard now requires this file-collection readiness logic. Verified `node src/apps/business-os/scripts/assert-rxdb-only.mjs`, JS syntax checks, `git diff --check`, and a full-app `SMOKE_MODE=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9050 SIGNALING_PORT=19090 node src/core/rxdb/tools/browser_rust_smoke.js` run that replicated a browser-created file/chunk into isolated Rust SQLite. |
| 224 | done | Default release soak matrix proof | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Verified the full default release soak locally with `SOAK_CYCLES=1 SOAK_MODES=browser-to-rust,command-browser-to-rust,workspace-rust-to-browser,restart-signaling-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9020 SIGNALING_PORT=19050 SOAK_FAIL_ON_RETRY=1 SOAK_RESULT_PATH=/tmp/ctox-rxdb-default-release-soak.json node src/core/rxdb/tools/browser_rust_soak.js`. All four modes completed on their first attempt with `retryCount=0`: browser file/chunk data reached Rust SQLite, browser commands were accepted and projected to `ctox_queue_tasks`, CTOX workspace files appeared in Browser under `/CTOX/<workspace>/reports/brief.md`, and the Signaling+CTOX restart path recovered over RxDB/WebRTC. |
| 223 | done | Workspace file sync soak coverage | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Verified `SOAK_CYCLES=1 SOAK_MODES=workspace-rust-to-browser SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=9010 SIGNALING_PORT=19030 SOAK_FAIL_ON_RETRY=1 SOAK_RESULT_PATH=/tmp/ctox-rxdb-workspace-soak.json node src/core/rxdb/tools/browser_rust_soak.js`: CTOX synced a workspace file into SQLite and the Browser received `/CTOX/<workspace>/reports/brief.md` over RxDB/WebRTC with `retryCount=0`. The manual release soak default now includes `workspace-rust-to-browser` alongside file, command, and restart modes. |
| 222 | done | Command soak and release retry gate | `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Extended the manual RxDB/WebRTC soak workflow to include `command-browser-to-rust` in the default mode set, upload `SOAK_RESULT_PATH` as an artifact, and optionally fail when any smoke mode needed a retry via `SOAK_FAIL_ON_RETRY=1`. Verified the command path with `SOAK_CYCLES=1 SOAK_MODES=command-browser-to-rust ...`: Browser inserted `business_commands`, Rust accepted it, and `ctox_queue_tasks` projected back to the browser. Also verified the retry gate fails on a deliberate occupied signaling port and passes on free ports with `retryCount=0`. |
| 221 | done | Structured RxDB soak result telemetry | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added optional JSON result output to the smoke matrix and soak runner. `SMOKE_MATRIX_RESULT_PATH` records per-mode attempts, status, signal, and allocated ports; `SOAK_RESULT_PATH` aggregates cycle results and the console summary now reports retry count. Verified with `node --check`, `git diff --check`, and `SOAK_CYCLES=1 SOAK_MODES=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8970 SIGNALING_PORT=18970 SOAK_RESULT_PATH=/tmp/ctox-rxdb-soak-summary.json node src/core/rxdb/tools/browser_rust_soak.js`, which wrote a green JSON summary with one successful attempt and proved Browser→Rust file/chunk replication into isolated SQLite over RxDB/WebRTC. |
| 220 | done | Local combined RxDB/WebRTC soak proof | `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Ran the repeatable full-app soak locally with `SOAK_CYCLES=1 SOAK_MODES=browser-to-rust,restart-signaling-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8960 SIGNALING_PORT=18960 node src/core/rxdb/tools/browser_rust_soak.js`. The run exercised the normal Browser→Rust file/chunk path and the hard Signaling+CTOX restart path. It completed green after the matrix retried one transient browser-context abort, and both successful modes proved browser-created file/chunk payloads reached isolated Rust SQLite over RxDB/WebRTC. The retry confirms the long-run soak still needs duration/flakiness tracking before production sign-off. |
| 219 | done | Manual RxDB/WebRTC soak workflow | `.github/workflows/rxdb-soak.yml`, `src/core/rxdb/tools/browser_rust_soak.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a manual GitHub Actions workflow for pre-release RxDB/WebRTC soak runs. The workflow builds the real `ctox` smoke binary, installs a pinned Playwright runtime, and runs `browser_rust_soak.js` against the full `/index.html` Business OS app with configurable cycles and smoke modes. Verified locally with `node --check src/core/rxdb/tools/browser_rust_soak.js` and `git diff --check` for the workflow and soak runner. |
| 218 | done | Repeatable Browser/Rust RxDB soak runner | `src/core/rxdb/tools/browser_rust_soak.js`, `.github/workflows/ci.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a repeatable soak wrapper around the full-app Browser/Rust smoke matrix with configurable cycle count, mode list, page path, base ports, and port stride. CI now syntax-checks the soak entry point so long-running reconnect coverage cannot silently disappear. Verified locally with `node --check src/core/rxdb/tools/browser_rust_soak.js` and `SOAK_CYCLES=1 SOAK_MODES=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8950 SIGNALING_PORT=18950 node src/core/rxdb/tools/browser_rust_soak.js`: a browser-created file/chunk replicated into isolated Rust SQLite over RxDB/WebRTC. |
| 217 | done | Native RxDB data-plane status guard | `src/core/business_os/store.rs`, `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `data_plane` to `/api/business-os/status` with cheap read-only SQLite checks for critical RxDB collections: `business_module_catalog`, `ctox_runtime_settings`, `desktop_files`, `desktop_file_chunks`, `business_commands`, and `ctox_queue_tasks`. It reports table existence, row counts, latest `updated_at_ms`, and whether shell-required collections are ready. The connection-mode smoke now fails if a native status payload exposes an unhealthy RxDB data plane. Verified with `cargo test --locked --bin ctox rxdb_data_plane_status_reports_critical_collection_counts`, `node --check src/core/rxdb/tools/business_os_connection_modes_smoke.js`, and `git diff --check` for the touched files. |
| 216 | done | Advanced status hardening and isolated regression smokes | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed stale RxDB collection use during data-plane repair, cache-bumped Business OS to `app.js?v=20260521-rxdb-db27`, made transient module schema retries use cache-busting retry URLs, made the browser smoke use a valid isolated `CTOX_ROOT` so tests do not lock the live `runtime/ctox.sqlite3`, serialized browser-side WebRTC collection startup, bounded bridge cancellation during repair, and prioritised critical catalog/runtime/file collections after rebuild. Verified `SMOKE_MODE=browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8943 SIGNALING_PORT=18943 node src/core/rxdb/tools/browser_rust_smoke.js` green and `SMOKE_MODE=restart-signaling-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8945 SIGNALING_PORT=18945 node src/core/rxdb/tools/browser_rust_smoke.js` green: after Signaling+CTOX restart, a browser-created file/chunk replicated into isolated SQLite through RxDB/WebRTC. |
| 215 | done | Business OS advanced status regression guard | `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a versioned browser-side `window.CTOX_BUSINESS_OS_STATUS` interface. It reports shell readiness, active module, module catalog availability, RxDB/WebRTC mode, required collection connection state, reconnect/repair state, redacted sync diagnostics, and small collection counts. The Browser/Rust full-app smoke and Business OS connection-mode smoke now query this advanced status so regressions such as a stuck workspace loader, missing module catalog, stalled WebRTC reconnect, or absent `desktop_files`/`desktop_file_chunks` replication surface as structured failures instead of only visual symptoms. The RxDB-only guard now requires the interface and core checks to remain present. |
| 214 | done | Signaling restart reconnect hardening | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added and verified `SMOKE_MODE=restart-signaling-browser-to-rust`. Browser-side sync now classifies RxDB-wrapped Simple-Peer failures for `ERR_CONNECTION_FAILURE`, `ERR_SET_LOCAL_DESCRIPTION`, `ERR_PC_CONSTRUCTOR`, closed/connecting WebSocket sends, and PeerConnection exhaustion. The sync runtime stops all active WebRTC bridges, restarts tracked collections serially, and Business OS schedules an automatic RxDB/WebRTC data-plane repair if reconnect remains stalled. Cache bumped to `app.js?v=20260521-rxdb-db17`. Verified locally with `SMOKE_MODE=restart-signaling-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8920 SIGNALING_PORT=18920 node src/core/rxdb/tools/browser_rust_smoke.js`: after Signaling+CTOX restart, a browser-created `desktop_files` plus `desktop_file_chunks` payload replicated into SQLite without any HTTP data fallback. |
| 213 | done | Browser/Rust restart reconnect smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `SMOKE_MODE=restart-browser-to-rust` to the existing full-app smoke harness and matrix. The mode starts the real Business OS app, brings WebRTC/RxDB replication online, stops and restarts the CTOX `business-os serve` process, then verifies a browser-created `desktop_files` plus `desktop_file_chunks` payload lands in SQLite after reconnect. The startup wait now reports URL, script list, body dataset, and visible text if the smoke hook does not initialize. Verified locally with `SMOKE_MODE=restart-browser-to-rust SMOKE_PAGE_PATH=/index.html BUSINESS_PORT=8903 SIGNALING_PORT=18903 node src/core/rxdb/tools/browser_rust_smoke.js`. |
| 212 | done | URL pairing secret scrub and cache bump | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Explicit URL pairing parameters now take precedence over injected local launch context, are normalized into `ctox.businessOs.pairingConfig`, and are immediately removed from the address bar with `history.replaceState` so `ctox_config`, room passwords, and signaling URLs do not linger in browser history after startup. Bumped Business OS to `app.js?v=20260521-rxdb-db13` / `ctox_business_os_v10` / `RXDB_BOOTSTRAP_VERSION=20260521-rxdb-db13` so existing tabs load the corrected launch logic. The connection-mode smoke now also reads the stored pairing config after URL scrubbing. |
| 211 | done | Business OS connection-mode smoke guard | `src/core/rxdb/tools/business_os_connection_modes_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `.github/workflows/ci.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a live smoke harness for the supported Business OS shell delivery modes: direct CTOX host, local ctox.dev/desktop-style injection, and packed `ctox_config` pairing URLs. The smoke verifies that each path leaves `Loading workspace`, opens the CTOX module, and shows `Rxdb Webrtc VERBUNDEN` with `http_bridge_available:false`; CI now syntax-checks the new harness with the existing RxDB-only guards. Also made the browser smoke runners prefer an installed Chrome/Chromium executable when Playwright's bundled headless shell is missing. Verified locally in the in-app browser against `http://127.0.0.1:8765/#ctox`, `http://127.0.0.1:3000/business-os#ctox`, and `http://127.0.0.1:3000/business-os?ctox_config=...#ctox`: all three rendered CTOX via RxDB/WebRTC and did not stay on the workspace loader. |
| 210 | done | Business OS module cache bump and Spreadsheets import fix | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/modules/spreadsheets/index.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the visible Spreadsheets module error `jspread is not a function`: the first `loadJSpreadsheetLib()` import path now returns the exported `jspreadsheet` function instead of the module namespace. Bumped the Business OS build to `app.js?v=20260521-rxdb-db12` / `RXDB_BOOTSTRAP_VERSION=20260521-rxdb-db12` so already-open browsers import the corrected module URL instead of keeping the stale ESM cache. Verified live with cache disabled against `http://127.0.0.1:8765/?rxdbSmoke=1&reload=...#spreadsheets`: the shell leaves `Loading workspace`, loads `app.js?v=20260521-rxdb-db12`, and no longer shows the `jspread` editor failure. |
| 209 | done | Browser WebRTC collection startup smoothing | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | After the local shell rendered, the browser still logged a transient Simple-Peer data-channel error while starting `document_blob_chunks`. The sync runtime now starts a module's RxDB collections sequentially with a short gap instead of opening every WebRTC replication channel in parallel. Business OS was cache-bumped to `app.js?v=20260521-rxdb-db11` / `RXDB_BOOTSTRAP_VERSION=20260521-rxdb-db11` so existing tabs pick up the smoother startup path without relying on browser cache expiry. |
| 208 | done | ctox.dev local Business OS launch-context injection | `/Users/you/Documents/ctox-dev/app/business-os/[[...path]]/route.ts`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the local `http://127.0.0.1:3000/business-os#ctox` case where the static web-deploy shell could be served without `window.CTOX_BUSINESS_OS_SESSION` / `window.CTOX_BUSINESS_OS_CONFIG`, leaving the browser on `Loading workspace` unless a packed `ctox_config` pairing URL was used. For local requests, the ctox.dev route now asks the local CTOX CLI for `business-os peer status` and injects the current WebRTC/RxDB pairing config with `http_bridge_available:false`; this keeps data strictly on RxDB/WebRTC while allowing ctox.dev or the desktop app to deliver the shell. Verified live in a visible Chrome instance against `http://127.0.0.1:3000/business-os?rxdbSmoke=1&reload=...#ctox`: the page loads `app.js?v=20260521-rxdb-db12`, leaves `Loading workspace`, contains injected session/config, uses `ws://127.0.0.1:18990`, renders the CTOX module, and shows `Rxdb Webrtc VERBUNDEN` through RxDB/WebRTC. |
| 207 | done | Browser IndexedDB recovery bump for stuck local Business OS tab | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The user's visible local tab was still stuck on `Loading workspace` even though a fresh Chrome profile loaded the CTOX module through RxDB/WebRTC. Bumped Business OS through the `20260521-rxdb-db*` build line and `ctox_business_os_v9` so existing browsers cannot keep stale IndexedDB/catalog state. Verified live with Google Chrome against `http://127.0.0.1:8765/?rxdbSmoke=1#ctox`: module manifests replicate, CTOX opens, and `Rxdb Webrtc VERBUNDEN` is visible. |
| 206 | done | Business OS live recovery via RxDB/WebRTC and ctox.dev shell pairing | `src/core/rxdb/tools/local_signaling_server.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/business_os/rxdb_peer.rs`, `src/core/main.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html`, `/Users/you/Documents/ctox-dev/app/business-os/[[...path]]/route.ts`, `/Users/you/Documents/ctox-dev/app/api/instances/[id]/business-os/pairing/route.ts`, `/Users/you/Documents/ctox-dev/components/tenant-console.tsx`, `/Users/you/Documents/ctox-dev/docs/ctox-peer2peer-signaling.md` | Fixed the post-login `Loading workspace` case without adding HTTP data fallbacks. The local signaling helper now accepts long RxDB collection room names instead of closing rooms at 100 chars; real Business OS topics such as `ctox-business-os:{instance}:{room}:business_module_catalog` need more than that. Added a native-peer process lock and CLI ledger bypass for `ctox business-os peer ...`, so the RxDB/WebRTC peer can run as its own outbound process while the Business OS shell is only a static/bootstrap host. Business OS now ships `app.js?v=20260520-rxdb-db9` and opens `ctox_business_os_v8` with schema-drift recovery. The `ctox-dev` web deploy can serve the static Business OS shell under `/business-os/`, inject a stable base URL, and open it with packed `ctox_config` pairing data from the instance console; this supports public CTOX, `*.ctox.dev`, and NAT/local CTOX where the browser only has signaling credentials. Verified live with Google Chrome against `http://127.0.0.1:8765/?rxdbSmoke=1#ctox` and `http://127.0.0.1:3000/business-os?ctox_config=...&rxdbSmoke=1#ctox`: both load 13 module manifests through RxDB/WebRTC, open the CTOX module, show `Rxdb Webrtc VERBUNDEN`, and no longer stay on `Loading workspace`. |
| 205 | done | Secret WebRTC room pairing and DB7 browser cache repair | `src/core/business_os/store.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the live `127.0.0.1:8765/#ctox` blank/stuck case that was caused by an old Browser IndexedDB schema (`RxDB DB6` on `spreadsheets`) rather than missing CTOX data. Business OS now ships `app.js?v=20260520-rxdb-db7`, opens `ctox_business_os_v7`, and has a one-shot schema-drift self repair that closes WebRTC/RxDB, removes the local Dexie database, and reloads before showing a permanent empty workspace. Added encrypted Business OS room-password generation in CTOX and derive the signaling room as `ctox-business-os:{instance_id}:{sha256(room_password).base64url[0..22]}`. Browser web-deploy pairing can now supply either the resolved `sync_room` or `instance_id + room_password + signaling_url`, so public-IP VPS, `*.ctox.dev` proxy, and NAT/local CTOX installs all use the same RxDB/WebRTC data plane. Verified live in the in-app browser on the exact URL `http://127.0.0.1:8765/#ctox`: `Files`/`CTOX` render, `Rxdb Webrtc` is connected, and there is no `Loading workspace`, `method not allowed`, `Sync failed`, or `DB6`. |
| 204 | done | Web-deploy bootstrap and daemon-owned RxDB peer | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/server.rs`, `src/core/service/service.rs`, `src/core/service/business_os.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Moved the native Business OS RxDB/WebRTC peer out of the local webserver ownership model. The CTOX service now ensures the peer on daemon boot, `ctox business-os serve` still ensures it for local development, and `ctox business-os peer start` runs the peer as a foreground process without hosting Business OS over HTTP. Browser startup now accepts a web-deploy pairing config from `ctox_config` base64url JSON, explicit `sync_room` + `signaling_url` URL parameters, injected globals, or persisted `ctox.businessOs.pairingConfig`; when present, the browser creates a paired WebRTC session and starts RxDB sync without posting to `/login`. Missing config renders a pairing-required gate instead of using a CTOX HTTP login fallback. This preserves the strict data contract: static hosting/auth bootstrap may be HTTP, but Business OS data remains RxDB/WebRTC only. |
| 203 | done | Live shell health and CTOX idle-state clarity | `src/apps/business-os/app.js`, `src/apps/business-os/modules/ctox/index.js`, `src/apps/business-os/index.html`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the live Business OS case where the shell could show `CTOX ARBEITET NICHT` while `ctox_runtime_settings` had not finished syncing yet, even though the server and WebRTC signaling were already running. The shell now treats missing/pending runtime settings during initial RxDB/WebRTC sync as pending rather than as a failure, and only raises the red warning for a replicated negative runtime state. The CTOX module no longer labels an idle WebRTC-connected data plane as `Unavailable / nicht live`; it shows the replicated RxDB/WebRTC source as connected unless a real active harness flow is present. The Browser-side RxDB database was bumped to `ctox_business_os_v6` after local smoke-test documents were tombstoned in the native SQLite store, and the CTOX module now filters internal `business_os.smoke`/`rxdb-smoke` documents out of the visible work queue so E2E verification artifacts cannot be mistaken for real CTOX work. Verified with `node --check`, the RxDB-only guard, live `ctox business-os serve` on `127.0.0.1:8765`, local signaling on `127.0.0.1:18990`, and the in-app browser after reload: apps load, `Files`/`CTOX` are visible, and the red shell warning is hidden. |
| 202 | done | Browser RxDB cache self-repair for stuck module catalog | `src/apps/business-os/app.js`, `src/apps/business-os/index.html`, `src/apps/business-os/shared/db.js`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the live case where an existing browser stayed on `Loading workspace` even though the Rust SQLite projection already contained `business_module_catalog`. This was treated as a local RxDB cache/checkpoint repair problem, not as an HTTP data fallback: Business OS now ships `app.js?v=20260520-module-catalog-repair3`, opens a new Browser-side RxDB database (`ctox_business_os_v5`) so stale IndexedDB handles cannot block startup, and still removes older local RxDB state on build transition. If the module catalog still does not arrive over WebRTC during startup, Business OS stops local WebRTC replication, closes/removes the local RxDB/Dexie database, recreates core collections, and retries the same WebRTC sync contract. Added a sync runtime `stop()` hook and `resetBusinessDb()` helper around RxDB `removeRxDatabase`. Verified in the actual In-App Browser by reloading the visible tab: it first showed `Lokale RxDB wird neu synchronisiert`, then left `Loading workspace`, loaded `Files`/`CTOX`, and the runtime warning cleared after `ctox_runtime_settings` sync; no `method not allowed` and no HTTP data fallback. |
| 201 | done | Live Business OS recovery over RxDB/WebRTC | `src/core/rxdb/tools/local_signaling_server.js`, `src/apps/business-os/modules/desktop/schema.js`, `src/apps/business-os/modules/app-store/index.js`, `src/apps/business-os/modules/app-store/schema.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Reproduced the live `method not allowed` login failure on `127.0.0.1:8765`, rebuilt the active `runtime/build/cargo-target/debug/ctox` binary, and replaced the stale server process. Added a local Simple-Peer signaling server helper and started the live Business OS against `CTOX_BUSINESS_OS_SIGNALING_URLS=ws://127.0.0.1:18990`, so browser RxDB and Rust rxdb-rs meet on WebRTC instead of waiting on an unavailable default cloud signaling path. Fixed the browser IndexedDB `DB6` startup failure by migrating `desktop_files` to schema version 1 with explicit migration defaults for the new file-sync metadata fields. Verified in the in-app browser: login posts successfully, no `method not allowed`, no `Loading workspace`, no `Startup failed`, the red CTOX warning clears after `ctox_runtime_settings` sync, `Files`/`CTOX` load, and `/#app-store` opens the replicated catalog with 15 apps. |
| 200 | done | Strict WebRTC-only data mode and login bootstrap repair | `src/apps/business-os/shared/db.js`, `src/apps/business-os/shared/sync.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/business_os/server.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/modules/app-store/index.js`, `src/apps/business-os/modules/app-store/schema.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Removed the remaining Browser data fallbacks: RxDB creation now fails hard instead of creating a local-storage replacement DB, and the sync runtime throws if the injected contract cannot start WebRTC. The RxDB-only guard now rejects future `local-only` sync mode and fallback database code. Login is repaired as a server-side bootstrap only: `POST /login` validates credentials, sets an HttpOnly cookie, and the next `index.html` load receives the injected session; no Business OS data is fetched through the login path. Added missing App Store module `index.js` and `schema.js` so the catalog module loads from replicated `business_module_catalog` and dispatches install commands through `business_commands` instead of producing 404s. Verified locally with password login cookie injection, `cargo build --bin ctox --no-default-features`, guard/syntax checks, and the full `/index.html` WebRTC smoke matrix green without App Store 404s. |
| 199 | done | Live RxDB shell startup readiness fix | `src/apps/business-os/shared/sync-contract.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Fixed the empty "Loading workspace" startup case without adding a fallback: the browser now starts WebRTC replication whenever the injected RxDB/WebRTC contract is complete (`transport`, `sync_room`, signaling URLs, local RxDB), instead of freezing the sync runtime into `local-only` when `native_rxdb_peer_available` is still false during early server injection. The full `/index.html` smoke now also asserts that Business OS loaded module manifests through RxDB, opened a shell module, and cleared the module-loading state. Verified live locally with the smoke matrix green for Rust→Browser, Browser→Rust, Browser→Rust command, and workspace Rust→Browser modes. |
| 198 | done | Split-string HTTP fallback guard and login cleanup | `src/apps/business-os/app.js`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Removed the split-string `/api/` + `business-os` session/config fallback variant from the app shell and tightened the RxDB-only guard so that concatenated Business OS API strings are caught too. The in-app login submit path no longer stores a Basic auth header expecting a browser HTTP validation fallback; it only clears an explicit local logout and otherwise requires CTOX-injected launch context. The guard and full `/index.html` smoke matrix are green after the cleanup. |
| 197 | done | Browser launch HTTP fallback removal | `src/apps/business-os/app.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Removed the remaining browser-side `/api/business-os/session` and `/api/business-os/sync/config` fallback fetches from the app shell. The browser now requires CTOX to inject `window.CTOX_BUSINESS_OS_SESSION` and `window.CTOX_BUSINESS_OS_CONFIG` into the served shell; stored local auth headers are no longer used to revalidate session/config through browser HTTP. The RxDB-only guard is green again, and the remaining browser `fetch()` calls are static module/registry assets or external service integrations rather than Browser↔CTOX data paths. |
| 196 | done | Full-app smoke matrix runner | `src/core/rxdb/tools/browser_rust_smoke_matrix.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `.github/workflows/ci.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a serial full-app smoke matrix runner that executes Rust→Browser file/chunk, Browser→Rust file/chunk, Browser→Rust command/queue, and workspace Rust→Browser file/chunk modes against `/index.html` with isolated ports. Each mode gets one retry by default so transient app-start hook timeouts do not hide the matrix result, while persistent failures still fail the command. CI now syntax-checks both smoke scripts alongside the RxDB-only browser contract. Verified the matrix command green end to end; the first Rust→Browser attempt reproduced the known startup-hook flake and the retry passed before the remaining modes completed green. |
| 195 | done | Full-app workspace-file WebRTC smoke | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The Browser/Rust smoke harness now has `SMOKE_MODE=workspace-rust-to-browser`, which creates a nested real workspace file, projects it through `ctox business-os files sync-workspace <path>` after full-app `/index.html` collection replication is live, and validates that the browser receives both `desktop_files` metadata and `desktop_file_chunks` payload over WebRTC. The smoke also asserts the Business OS virtual path shape `/CTOX/<workspace>/reports/brief.md`, covering the agent-created workspace-file visibility path end to end instead of only checking SQLite. |
| 194 | done | Explicit workspace-to-RxDB file sync CLI | `src/core/service/business_os.rs`, `src/core/main.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added `ctox business-os files sync-workspace <path>` as an explicit operator/test entrypoint for the same bounded workspace indexer used by the CTOX agent loop. This makes agent-created workspace files manually projectable into SQLite-backed `desktop_files` / `desktop_file_chunks` without waiting for a completed queue job or the broad background scan, while still bypassing the CLI turn ledger like the single-file sync path. Verified with a real nested workspace file: the CLI returned `indexed: 1`, SQLite contained `/CTOX/workspace/reports/brief.md` with `content_state=available`, and one chunk row was written. |
| 193 | done | SQLite lock hardening and honest full-app smoke verification | `src/core/rxdb/src/storage/sqlite/types.rs`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/service/business_os.rs`, `src/core/main.rs`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The SQLite-backed RxDB storage now installs a 10s busy timeout on connections and via `PRAGMA busy_timeout`, so short native-peer/background/explicit-sync write contention is retried by SQLite instead of immediately surfacing as `database is locked`. Re-ran the real `/index.html` full-app smokes after removing the previous raw SQLite seed shortcut: Rust→Browser file/chunk now goes through `ctox business-os files sync <path>` after WebRTC replication is live, Browser→Rust file/chunk still lands in SQLite, and Browser→Rust commands still project accepted queue tasks back to the app. |
| 192 | done | Full-app RxDB/WebRTC smoke hardening for CTOX file sync | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/service/business_os.rs`, `src/core/main.rs`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The full Business OS smoke now seeds Rust-side files through the same local CTOX file-sync entrypoint used by the daemon path (`ctox business-os files sync <path>`) after collection replication is running, so Rust→Browser verification covers `desktop_files` and `desktop_file_chunks` over WebRTC instead of relying on pre-start raw SQLite rows. The harness now awaits file and chunk replication states, uses the same all-chunks read shape as File Viewer/Explorer, retries transient SQLite locks, and can disable the broad background file indexer during smoke runs via `CTOX_BUSINESS_OS_DISABLE_BACKGROUND_FILE_INDEX` to keep the E2E focused on the explicit file under test. Verified the real `/index.html` app in all required directions: Rust→Browser file/chunk, Browser→Rust file/chunk, and Browser→Rust command acceptance with `ctox_queue_tasks` projection back. |
| 191 | done | Desktop file update-generation regression | `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Added a Rust regression for the CTOX file-sync update path where a previously multi-chunk eager file is rewritten to a smaller payload. The test verifies that the SQLite-backed `desktop_file_chunks` table keeps old historical chunks but writes a newer single-chunk generation, matching the browser File Viewer/Explorer logic that reads only the latest `created_at_ms` generation. |
| 190 | done | RxDB-only guard coverage for JSON manifests | `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/apps/business-os/modules/**/*.json`, `src/apps/business-os/template-store/**/*.json`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The Browser RxDB-only guard now scans JSON files in its existing app scopes, so module manifests, locale files, registry metadata, and template-store manifests are checked for forbidden CTOX HTTP fallback strings as well as JS/HTML/Rust files. Current module and template JSON scans remain clean. |
| 189 | done | RxDB-only guard coverage for wrapper/template app code | `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/apps/business-os/electron`, `src/apps/business-os/template-store`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The RxDB-only contract guard now includes future Electron wrapper JavaScript and template-store app code in addition to shell/shared/modules/desktop-apps. Current scans of those directories find no direct CTOX HTTP data paths, but adding them to the guard closes another possible Browser OS side-channel before code appears there. |
| 188 | done | RxDB-only contract documentation | `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `.github/workflows/ci.yml`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The Business OS RxDB sync contract now documents the automated regression guard, its browser-facing scan scope, the forbidden HTTP fallback patterns, and the Linux CTOX CI enforcement lane. This keeps the architecture target in the contract document as well as the port dashboard. |
| 187 | done | CI enforcement for RxDB-only browser contract | `.github/workflows/ci.yml`, `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The main CI workflow now runs the Business OS RxDB-only contract on the Linux CTOX check lane after `cargo fmt --check` and before the Rust `cargo check`. The lane installs Node 22 and executes both `node --check src/apps/business-os/scripts/assert-rxdb-only.mjs` and `node src/apps/business-os/scripts/assert-rxdb-only.mjs`, so browser-side HTTP fallback regressions fail in CI instead of relying on manual port-wave checks. |
| 186 | done | RxDB-only guard coverage for desktop apps | `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/apps/business-os/desktop-apps/*`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The browser data-path guard now scans `desktop-apps` as well as the shell/shared/module code, so Explorer, File Viewer, and Code Editor cannot reintroduce direct Business OS HTTP data calls or native HTTP bridge fallbacks. A direct source scan of `desktop-apps` finds no `fetch()`, `/api/business-os`, `/rxdb/pull`, `/rxdb/push`, or native bridge strings. |
| 185 | done | RxDB-only browser data-path guard | `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The RxDB-only regression check now fails on frontend `/api/business-os...` data paths, `/api/business-os/status` polling, and old CTOX harness/task HTTP fallbacks in addition to the previous `/rxdb/pull`, command POST, native bridge, and sync-config bridge checks. Backend route definitions and the guard script itself are scoped out so the check protects browser code without flagging server endpoints. The current scan passes; the remaining browser `fetch()` calls are static app/module assets or external service calls, not Browser↔CTOX data sync paths. Post-update verification is green for `node --check` on the guard/app/smoke scripts, `node src/apps/business-os/scripts/assert-rxdb-only.mjs`, `cargo test --manifest-path src/core/rxdb/Cargo.toml --lib`, `cargo test business_os::rxdb_peer::tests --no-default-features`, and `cargo test workspace --no-default-features` in the materialized checkout. |
| 184 | done | Full Business OS RxDB/WebRTC E2E hardening | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/execution/models/supervisor.rs`, `src/core/service/service.rs`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The full `/index.html` app smoke is green in all required RxDB/WebRTC directions: Browser→Rust desktop file/chunk replication, Rust→Browser desktop file/chunk replication, and Browser→Rust `business_commands` acceptance with `ctox_queue_tasks` projection back to the browser. The smoke harness now waits for native SQLite RxDB tables before seeding direct Rust-side file rows, avoiding config-ready/table-not-ready races. Native projection loops and temporary helper DB opens are serialized around SQLite writes, removing the startup `database is locked` noise observed in full-app smokes. Focused workspace-file regressions, the full parallel `business_os::rxdb_peer::tests` group, the rxdb-rs crate suite, and the broad filtered root `cargo test workspace --no-default-features` run now pass in the materialized verification checkout. The two previous non-RxDB root failures in that filtered run were cleared by making the supervisor CWD helper portable on macOS and aligning the workspace contract test with the current timeout prompt shape. A full unfiltered root `cargo test --no-default-features` still fails outside the RxDB port surface in unrelated runtime-manifest, TUI, mission, report, and governance tests. |
| 183 | done | Agent workspace file visibility over RxDB desktop files | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/mod.rs`, `src/core/service/service.rs`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | Completed agent runs with a `workspace_root` now trigger an immediate bounded CTOX workspace index into `runtime/ctox.sqlite3` instead of waiting only for the periodic scanner or declared artifact paths. The new Rust entrypoint `sync_desktop_files_from_workspace_root` reuses the SQLite-backed `desktop_files` / `desktop_file_chunks` path, eagerly chunks small supported files, leaves large/unsupported files lazy for `ctox.file.materialize`, and preserves the `/CTOX/<workspace>/...` virtual hierarchy for Business OS Explorer. Bounded scans now skip tombstoning when the scan limit is reached, so large workspaces are not marked missing from an incomplete sample. Added focused regressions for workspace-root indexing and bounded-scan tombstone safety; both pass in the materialized verification checkout. |
| 182 | done | Business OS browser bootstrap without HTTP fetches | `src/core/business_os/server.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/shared/command-bus.js`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The CTOX webserver now injects `window.CTOX_BUSINESS_OS_SESSION` and `window.CTOX_BUSINESS_OS_CONFIG` into the app-shell `index.html`, and the browser shell no longer calls `/api/business-os/session` or `/api/business-os/sync/config`. The generic shell/settings `fetchJson` helpers, auth-header module fetch helper, and command-bus `baseUrl` default were removed because no Business OS browser code now fetches CTOX API data paths directly. `sync_config()` now reports `http_bridge_available=false` consistently. A browser source scan finds no `/api/business-os/` fetch strings outside static HTML/module asset fetches and external service calls. |
| 181 | done | Shell CTOX health over RxDB runtime projection | `src/apps/business-os/app.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The Business OS shell no longer polls `/api/business-os/status` for the operational warning banner. After DB and sync startup, it starts `ctox_runtime_settings` replication and maps the replicated runtime/service document to the existing shell health shape. A browser HTTP-path scan now finds only `/api/business-os/session` and `/api/business-os/sync/config` in the shell/shared Business OS browser code. |
| 180 | done | Shell module/template reads over RxDB | `src/apps/business-os/app.js`, `src/apps/business-os/shared/sync.js`, `src/core/rxdb/PORTING.md`, `src/core/rxdb/dashboard.html` | The Business OS shell no longer fetches `/api/business-os/modules` or `/api/business-os/templates` for startup, background refresh, or the template drawer. Bootstrap now opens the local RxDB first, registers core collections, starts `business_module_catalog` WebRTC replication, and reads modules/templates/governance from the replicated catalog. Browser WebRTC push is disabled for read-only projection collections including the module catalog, users, runtime settings, channel state, and queue-task projections. Covered by browser syntax checks and HTTP-path scans for shell/settings module/template reads. |
| 179 | done | Settings module catalog read projection over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/ctox/schema.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | Settings admin module/template reads now use the replicated `business_module_catalog` document instead of direct `/api/business-os/modules` and `/api/business-os/templates` calls. The Rust peer projects module manifests, template manifests, and governance metadata into SQLite-backed RxDB periodically and after module governance commands. Browser syntax checks and the focused Rust module-catalog regression are in place; Cargo execution is still gated by the workspace's dataless manifests. |
| 178 | done | Runtime settings read projection over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/ctox/schema.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | Settings runtime/status reads now use the replicated `ctox_runtime_settings` collection instead of `/api/business-os/ctox/runtime-settings`. The Rust peer derives the runtime/auth/service diagnostics from the canonical store/runtime state, projects them into RxDB SQLite on a short interval and immediately after `ctox.runtime_settings.save`, and the browser waits for that projection after save commands. Covered by browser syntax checks and the focused runtime projection regression where Cargo can access the workspace files. |
| 177 | done | Settings users read projection over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/ctox/schema.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | Settings users are now a replicated `business_users` collection instead of an HTTP read from `/api/business-os/users`. The Rust peer projects the canonical `business_users` table into RxDB SQLite on a short interval and immediately after `ctox.business_os.user.upsert` commands; the browser starts `business_users` WebRTC sync and filters visibility from the current session locally. Covered by `sync_business_users_projects_store_rows` plus browser syntax checks. |
| 176 | done | Channel read projections over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/desktop/schema.js`, `src/apps/business-os/modules/conversations/index.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | The native Rust peer now projects canonical mission channel data into RxDB SQLite: `communication_accounts` is upserted from the CTOX channel DB, removed accounts become replicated tombstones, and `channel_pairing_state` is refreshed from pairing artifacts for the supported channels. Settings now starts WebRTC sync for both collections and reads accounts/pairing state from RxDB instead of `/api/business-os/channels/*`; Conversations filters tombstoned accounts. Covered by the new channel-state projection regression plus existing file-index/materialize regressions and browser syntax checks. |
| 175 | done | Desktop file schema contract for CTOX sync metadata | `src/apps/business-os/modules/desktop/schema.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | The browser-side `desktop_files` schema now explicitly declares the CTOX sync fields used by the Rust peer (`local_path`, `virtual_path`, `content_state`, `content_hash`, `mtime_ms`, `content_synced_at_ms`) instead of relying only on `additionalProperties`. The RxDB sync contract now documents physical-vs-virtual file paths and `content_state` semantics (`available`, `lazy`, `missing`, `directory`). |
| 174 | done | CTOX desktop file hierarchy and tombstones | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/desktop-apps/explorer/app.js` | The native file indexer now keeps a Business OS-facing `/CTOX/...` virtual folder hierarchy while preserving the real `local_path` used by `ctox.file.materialize`. Scanned runtime outputs are grouped by source (`Generated Documents`, `Notes`, `Imports`, workspace roots) instead of being flattened directly under `/CTOX`. Missing scanned files are marked as deleted with `content_state=missing`, including the empty-root case where no candidates remain. Explorer now displays the virtual path but passes the local path when opening lazy files. Covered by the expanded file-index regression plus the materialize regression. |
| 173 | done | Bounded CTOX file indexer for Business OS desktop | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/desktop-apps/explorer/app.js` | The native RxDB peer now runs a bounded desktop-file index loop. It scans CTOX-owned runtime outputs (`runtime/business-os/notes`, generated documents, import artifacts) plus recorded queue `workspace_root`s, skips broad/system-heavy roots and build/vendor directories, and writes visible `desktop_files` rows under a `/CTOX` folder. Small text/media/document outputs are eagerly chunked; larger files are metadata-only with `content_state=lazy` and are loaded through the `ctox.file.materialize` command added in wave-172. Covered by a targeted regression that indexes a small runtime output eagerly and a large output lazily. |
| 172 | done | CTOX file materialization over RxDB commands | `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Business OS file opening now has a CTOX-backed lazy-content path: if a replicated `desktop_files` row has no local `desktop_file_chunks`, the file viewer dispatches `ctox.file.materialize` through `business_commands`, Rust verifies the file is already indexed as a CTOX-managed desktop file, reads it from the local filesystem, writes fresh chunks into `runtime/ctox.sqlite3`, and the browser waits for chunk replication before rendering. CTOX file sync rows now also carry `content_state`, `content_hash`, `mtime_ms`, and `content_synced_at_ms`. Covered by a targeted Rust regression that deletes chunks and restores them via the RxDB command plus browser syntax validation. |
| 171 | done | Channel setup mutations over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Settings channel setup no longer POSTs browser HTTP for `test`, `sync`, `settings`, `disconnect`, `pair/start`, `jami/create`, or `jami/export`. The UI maps those actions to `ctox.channel.*` commands in replicated `business_commands`; Rust routes them to the existing mission channel helpers and preserves the same admin checks. Pairing-state and accounts remain read surfaces for now. Covered by the expanded `native_peer_consumes_pending_module_governance_commands` regression. |
| 170 | done | Subscription auth start over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs` | Settings ChatGPT subscription authorization no longer POSTs `/api/business-os/ctox/subscription-auth/start`. It dispatches `ctox.subscription_auth.start` through replicated `business_commands`; Rust starts the same local OAuth callback flow and returns the `auth_url` in the command projection for the browser to open. Compile/regression coverage: `native_peer_consumes_pending_module_governance_commands` still passes after moving the OAuth helper surface into the command consumer module. |
| 169 | done | Runtime settings save over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Settings runtime/auth save no longer POSTs `/api/business-os/ctox/runtime-settings`. It dispatches `ctox.runtime_settings.save` through replicated `business_commands`; Rust writes the operator runtime env with the same provider/auth/model/timeout semantics as the previous HTTP handler, then the UI refreshes the existing runtime-settings read payload. Covered by the expanded `native_peer_consumes_pending_module_governance_commands` regression. |
| 168 | done | Settings user save over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Settings user creation/update no longer POSTs `/api/business-os/users`. It dispatches `ctox.business_os.user.upsert` through replicated `business_commands`; Rust reuses `upsert_user`, keeps the existing admin-role check, and returns the refreshed users list through the command projection. Covered by the expanded `native_peer_consumes_pending_module_governance_commands` regression. |
| 167 | done | Remove dead module-layout HTTP mutation helper | `src/apps/business-os/app.js` | Removed the unused `saveModuleLayout()` shell helper that still POSTed `/api/business-os/module-layout`. The live shell layout path already persists locally via `persistModuleLayout()` and did not call this helper, so this is a dead HTTP mutation surface cleanup. Remaining browser HTTP calls are now bootstrap/read surfaces (`status`, `session`, `sync/config`, modules/templates reads) plus Settings users/runtime/channel admin endpoints. |
| 166 | done | Shell template install over RxDB commands | `src/apps/business-os/app.js` | The Business OS shell template-store drawer no longer POSTs `/api/business-os/modules/install-template`. It starts `business_commands` WebRTC replication, dispatches `ctox.module.install_template`, waits for the replicated command projection, and then refreshes module metadata before opening the installed module. Browser mutation scan now finds no remaining module source/release/rollback/founder/delete/install-template HTTP mutation paths outside vendored assets. |
| 165 | done | Module manifest mutations over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Settings module save, delete, and template install no longer POST `/api/business-os/modules`, `/api/business-os/modules/delete`, or `/api/business-os/modules/install-template`. They dispatch `ctox.module.save`, `ctox.module.delete`, and `ctox.module.install_template` through replicated `business_commands`; Rust reuses the existing manifest/template/delete behavior, including permission checks, blank installed-module creation, template copy, and module-layout cleanup. Covered by the expanded `native_peer_consumes_pending_module_governance_commands` regression. |
| 164 | done | Module founder assignment over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Settings Founder-Zuordnung no longer POSTs `/api/business-os/modules/assign-founder`. It dispatches `ctox.module.assign_founder` through replicated `business_commands`; Rust reuses the canonical ACL store logic, returns the affected `business_module_acl` record id, and projects the ACL row into native RxDB SQLite. The native peer also falls back to the original command payload for deterministic ACL projection and uses explicit upsert for `business_module_acl`. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| 163 | done | Module governance release/rollback over RxDB commands | `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/reports/index.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs`, `src/core/rxdb/tools/browser_rust_smoke.js` | Settings release/rollback and Reports rollback no longer POST `/api/business-os/modules/release` or `/api/business-os/modules/rollback`. They dispatch `ctox.module.release` / `ctox.module.rollback` through replicated `business_commands`; Rust reuses the canonical module governance store logic, refreshes all affected `business_module_releases` projections, strips store-local `_rev` metadata before RxDB writes, and uses release-specific upsert projection so rollback status cannot stay stale. The full-app command smoke now waits for both `business_commands` and `ctox_queue_tasks` replication readiness before asserting the queue projection. Covered by `native_peer_consumes_pending_module_governance_commands`, native peer regression group, and full `/index.html` Browser→Rust command smoke. |
| 162 | done | Module source editor over RxDB commands | `src/apps/business-os/desktop-apps/code-editor/app.js`, `src/apps/business-os/app.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Removed the Source Editor's direct `/api/business-os/modules/source` load/save calls. The editor now starts WebRTC replication for `business_module_source_files`, dispatches `ctox.source.load` / `ctox.source.save` through `business_commands`, and reads the resulting source projections from RxDB. Rust scans/writes module files from the local Business OS app root, keeps snapshots for changed saves, persists `business_module_source_files` records, and projects them back into the native RxDB SQLite store. Covered by `native_peer_consumes_pending_source_commands`. |
| 161 | done | Business OS reports over RxDB commands | `src/apps/business-os/shared/business-reporter.js`, `src/apps/business-os/app.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | Browser report submission no longer POSTs `/api/business-os/reports`. The FAB reporter and shell report path now create `ctox.report.*` documents in replicated `business_commands`; Rust consumes them with the existing report/task creation logic, writes authoritative command/report state, and projects `business_module_reports`, `ctox_bug_reports`, and the created `ctox_queue_tasks` row back through RxDB SQLite. Added a native peer regression for consuming a pending report command and seeing the report/bug projections. |
| 160 | done | Remove stale browser bridge flag usage | `src/apps/business-os/app.js` | Removed the last browser-side `http_bridge_available` checks. Module metadata refresh remains a shell/admin HTTP surface, but no Business OS browser data path or startup decision now depends on the old HTTP bridge fallback flag. A browser app scan finds no `http_bridge_available`, `/rxdb/pull`, `/rxdb/push`, `native-http`, `/ctox/harness-flow`, `/ctox/tasks/*`, or Knowledge HTTP data bridge usage outside vendored assets. |
| 159 | done | Knowledge commands over RxDB result projection | `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/modules/outbound/index.js` | `knowledge.command` is now executed by the Rust native peer through `knowledge::dispatch_capturing`, with the JSON response written back into `business_commands.result` and replicated to the browser. Outbound no longer gates Knowledge reads/writes on `http_bridge_available`; it waits for the replicated command projection when it needs rows from `data select`. Added a native peer regression for consuming a pending Knowledge command and seeing the captured result in RxDB. |
| 158 | done | CTOX task mutations over RxDB commands | `src/apps/business-os/modules/ctox/index.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | CTOX task edit/delete actions no longer POST `/api/business-os/ctox/tasks/update` or `/delete` from the browser. The browser now writes `ctox.task.update` / `ctox.task.delete` documents into `business_commands`; the Rust native peer validates the actor role from command context, applies the existing task mutation logic, writes the command result back into `business_commands`, and projects the changed `ctox_queue_tasks` row through RxDB SQLite. Added a native peer regression for consuming a pending task-update command and seeing the projected queue task change. |
| 157 | done | Remove shell startup status data poll | `src/apps/business-os/app.js` | Removed the redundant post-startup `/api/business-os/status` fetch that did not update shell state. The only remaining status call in the browser shell is the explicit operational health warning; app data and CTOX live state no longer use status/harness HTTP reads as fallback data. |
| 156 | done | Remove CTOX runtime HTTP data fallback | `src/apps/business-os/modules/ctox/index.js` | The CTOX module no longer loads `/api/business-os/ctox/harness-flow` or `/api/business-os/status` as live data when the native peer is not ready. Refresh now renders commands, queue tasks, and bug reports from local RxDB collections only; the runtime flow is explicitly unavailable until it has a real replicated projection. This keeps CTOX visible state aligned with the no-fallback-data contract instead of mixing RxDB data with HTTP runtime snapshots. |
| 155 | done | Remove browser RxDB HTTP bridge fallback | `src/apps/business-os/shared/sync.js`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | The Business OS sync runtime no longer exposes `pull()`/`push()` helpers or starts the native HTTP bridge fallback. Collection sync now starts RxDB WebRTC replication when the native Rust peer is ready and otherwise reports `local-only`, so browser data sync cannot silently fall back to `/api/business-os/rxdb/pull` or `/api/business-os/rxdb/push`. Browser app scan finds no remaining `/rxdb/pull`, `/rxdb/push`, or `native-http` sync transport usage outside vendored assets. Green smokes after the cleanup: isolated and full `/index.html` Browser→Rust command flow, full `/index.html` Browser→Rust file flow, and full `/index.html` Rust→Browser file flow. |
| 154 | done | Remove document-command HTTP projection fallback | `src/apps/business-os/modules/documents/index.js` | The documents module no longer polls `/api/business-os/rxdb/pull` after dispatching a document-generation command. It waits for the local replicated `business_commands` projection instead, preserving the UX wait for `task_id` while keeping the data path on RxDB/WebRTC. Browser app scan now finds HTTP `/rxdb/pull`/`push` usage only inside the disabled native bridge fallback in `shared/sync.js`. |
| 153 | done | Commands over RxDB/WebRTC | `src/apps/business-os/shared/command-bus.js`, `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/core/rxdb/tools/browser_rust_smoke.js` | Browser command dispatch now only writes `business_commands` into local RxDB and returns `pending_sync`; it no longer posts `/api/business-os/rxdb/push`. The native Rust peer consumes replicated pending command docs, runs the existing CTOX command acceptance path, writes accepted status/task ids back into `business_commands`, and projects the created `ctox_queue_tasks` document into the RxDB SQLite store for browser replication. Covered by a native peer consumer regression test plus isolated and full `/index.html` command WebRTC smokes. |
| 152 | done | Browser/Rust file-payload smoke coverage | `src/core/rxdb/tools/browser_rust_smoke.js` | The WebRTC smoke now validates `desktop_file_chunks` as well as `desktop_files` on Browser→Rust, checks the Rust-side readiness payload, and supports `SMOKE_RUST_FILE_CONTENT` / `SMOKE_BROWSER_FILE_CONTENT` so zero-byte file replication can be exercised without changing the harness. Green empty-payload paths: isolated Rust→Browser, isolated Browser→Rust, full `/index.html` Rust→Browser, full `/index.html` Browser→Rust. |
| 151 | done | Browser file reader parity | `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/desktop-apps/file-viewer/app.js` | Explorer preview/download now uses the same latest-generation chunk guard as the file viewer: missing chunk generations fail loudly, while a valid single empty chunk remains a 0-byte Blob. This closes the browser-side symmetry for the empty-file contract from wave-150. |
| 150 | done | Empty CTOX file sync | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/desktop-apps/file-viewer/app.js` | CTOX-managed zero-byte files now write a valid empty `desktop_file_chunks` row (`data=""`, `total=1`, `size_bytes=0`) instead of only a metadata row, and the browser file viewer treats an empty chunk generation as a valid empty Blob instead of a missing payload. Covered by a Rust SQLite test for `sync_desktop_file_from_path` on an empty file plus browser syntax validation. |
| 149 | done | WebRTC resync hardening cleanup | `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/core/rxdb/tools/browser_rust_smoke.js` | The fork-side periodic `RESYNC` is now factored behind `periodic_resync_stream(FORK_RESYNC_INTERVAL)` with a focused async unit test, and the smoke harness no longer prints temporary app-bridge diagnostics. |
| 148 | done | Full Business OS app mesh Browser→Rust push | `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js` | Rust fork-side WebRTC replication now emits a defensive periodic `RESYNC` into the pull stream, so browser-master updates are pulled even when the browser `masterChangeStream$` live notification is missed or not decoded across simple-peer/webrtc-rs. The `/index.html` smoke uses the real Business OS app DB through `?rxdbSmoke=1`; full app Browser→Rust now lands in `runtime/ctox.sqlite3`, closing the previously observed mesh gap. All four acceptance paths are green: isolated Rust→Browser, isolated Browser→Rust, full app Rust→Browser, full app Browser→Rust. |
| 147 | done | Full Business OS app mesh Browser→Rust push | `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js` | The `/index.html` smoke now uses a gated `?rxdbSmoke=1` hook to access the real Business OS RxDB instead of starting a second browser database on the same topics. This exposed the app-mesh Browser→Rust gap that was closed in wave-148 and then cleaned up in wave-149. |
| 146 | done | Durable Browser/Rust WebRTC smoke harness | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/dashboard.html` | The browser/Rust WebRTC smoke is now a repo-maintained tool instead of a `/tmp` script. It starts an in-process signaling server, launches `ctox business-os serve`, verifies `native_rxdb_peer_available`, seeds a controlled Rust-side readiness file through SQLite, and supports both `SMOKE_MODE=rust-to-browser` and default browser-to-rust. The harness uses real Playwright (`PLAYWRIGHT_MODULE_PATH` can point at an existing install). Green paths: isolated smoke page Rust→Browser, isolated smoke page Browser→Rust, and full `/index.html` Rust→Browser. |
| 145 | done | Rust/SQLite → Browser file replication | `src/core/rxdb/src/storage/sqlite/instance.rs`, `src/core/business_os/rxdb_peer.rs`, `/tmp/ctox-pw-smoke/rxdb_browser_rust_smoke.js` | SQLite storage now detects direct external writes by polling `(lastWriteTime,id)` checkpoints and emitting `EventBulk`s on the live `change_stream`, so a running Rust WebRTC peer can publish files written by another CTOX process into `runtime/ctox.sqlite3`. The poller shares its checkpoint with local `bulk_write` to avoid replaying the instance's own writes. Rust-to-browser smoke is green for `desktop_files` plus `desktop_file_chunks` payloads, browser-to-Rust smoke remains green after waiting for replication readiness, and the CTOX `sync_desktop_file_from_path` SQLite test passes. |
| 144 | done | Browser/Rust WebRTC hardening | `src/core/rxdb/src/plugins/replication_webrtc/connection_handler_rs.rs`, `src/core/rxdb/src/plugins/replication_webrtc/signaling_client.rs`, `src/core/rxdb/src/rx_schema.rs`, `src/core/rxdb/src/rx_database.rs`, `src/core/rxdb/Cargo.toml` | Real browser-to-Rust smoke exposed integration-only gaps: schema hashes are now deterministic over sorted schema JSON and existing semantically equal internal-store metadata is repaired instead of failing DB6; the signaling client installs the process-global rustls ring crypto provider before WebSocket/TLS use; webrtc-rs ICE candidates are translated to/from the `simple-peer` signal shape `{ type: "candidate", candidate: { ... } }`; and the default UDP bind is `127.0.0.1:0` so local Business OS browser/daemon peers exchange usable host candidates. Isolated browser smoke and full `index.html` smoke are green: a Chromium RxDB/Dexie `desktop_files` insert replicates through simple-peer/WebRTC into Rust `RxStorageSqlite` at `runtime/ctox.sqlite3` while Business OS starts its normal collection replications. |
| 143 | done | Business OS file sync | `src/core/business_os/rxdb_peer.rs`, `src/core/service/service.rs`, `src/apps/business-os/desktop-apps/*` | Rust glue now exposes an upsert path for delivered workspace files, writing metadata to `desktop_files` and Base64 payload chunks to `desktop_file_chunks` directly in `runtime/ctox.sqlite3` when no in-process peer is available. CTOX service syncs delivered workspace-file artifacts after successful agent jobs, browser file readers select the latest chunk generation so updated files do not append stale chunks, and the schema builder now sets primary-key `maxLength` so dev-mode SC39 validation passes. Covered by a targeted SQLite smoke test for direct file sync without a running peer and a Business OS serve smoke where `/api/business-os/sync/config` reports `transport=webrtc`, native peer available, and HTTP bridge disabled. |
| 142 | done | Browser/daemon WebRTC data path | `src/apps/business-os/shared/sync.js`, `src/core/business_os/*`, `src/plugins/replication_webrtc/index_mod.rs` | Browser collection replication now starts through RxDB WebRTC/simple-peer when the native Rust peer is available; CTOX starts a webrtc-rs peer backed by `RxStorageSqlite` and joins the same collection topics. |

**Status legend:** `pending` · `claimed` · `wip` · `done` · `skipped`

**Phase legend:**
- **phase-0** — Foundation (sequential, single author)
- **phase-1** — Schema + query layer required by storage (parallel)
- **phase-2** — Storage abstraction + Memory + own SQLite (parallel)
- **phase-3** — Replication core (sequential)
- **phase-4** — WebRTC transport (parallel)
- **phase-5** — Reactive layer (sequential)
- **phase-6** — Assembly: RxDatabase/RxCollection/RxDocument/RxQuery (sequential)
- **phase-7** — Conformance tests (parallel)
- **skip** — out-of-scope (browser-only / SaaS-specific / deferred / MVP trim / V1 trim)

**Tier legend** (drives delegation strategy):
- **T1** — Rust-native re-design. Main agent only, sequential.
- **T2** — Substantive port. Parallel subagents with PORT_STYLE.md.
- **T3** — Syntax-only port. Highly parallel, mechanical.

## Totals — Phases

| Phase | Files |
|---|---:|
| phase-0 | 22 |
| phase-1 | 5 |
| phase-2 | 11 |
| phase-3 | 11 |
| phase-4 | 5 |
| phase-5 | 3 |
| phase-6 | 11 |
| phase-7 | 9 |
| skip | 127 |
| **Total upstream src files** | **204** |
| **To port (phase-0..7)** | **77** |

## Totals — Tiers (of the 77 to-port files)

| Tier | Files | Delegation |
|---|---:|---|
| T1 — Rust-native re-design | 32 | main agent only, sequential |
| T2 — Substantive port | 21 | parallel subagents with PORT_STYLE.md |
| T3 — Syntax-only port | 24 | parallel mechanical subagents |

## Phase × Tier crosstab

| Phase | T1 | T2 | T3 |
|---|---:|---:|---:|
| phase-0 | 4 | 0 | 18 |
| phase-1 | 0 | 0 | 5 |
| phase-2 | 8 | 2 | 0 |
| phase-3 | 6 | 5 | 0 |
| phase-4 | 5 | 0 | 0 |
| phase-5 | 2 | 1 | 0 |
| phase-6 | 7 | 3 | 1 |
| phase-7 | 0 | 9 | 0 |

## New code — no upstream source (additions beyond the port)

| # | Item | Where | Tier | Status | Owner | Notes |
|---:|---|---|:---:|---|---|---|
| N1 | `RxStorageSqlite` — `RxStorageInstance` impl against `runtime/ctox.sqlite3` | `src/storage/sqlite/*.rs` | T1 | done | main | wave-145: direct external SQLite writes are now observed by live storage instances via a lightweight `(lastWriteTime,id)` poller that emits `EventBulk`s on `change_stream`; this closes the CTOX-file-writer-to-browser WebRTC path when another process writes into `runtime/ctox.sqlite3`. The poller shares checkpoints with local `bulk_write` to prevent own-write replay. wave-044: root `storage::sqlite` backend added with `get_rx_storage_sqlite`, `RxStorageSqliteSettings` defaulting to `runtime/ctox.sqlite3`, rusqlite shared connection, per-collection tables, metadata indexes `(lastWriteTime,id)` and `(deleted,lastWriteTime,id)`, full RxStorageInstance trait impl: bulk_write via categorize_bulk_write_rows, find_documents_by_id, query via scan+RxQuery matcher/sort like upstream trial SQLite, count, get_changed_documents_since, change_stream, cleanup/remove/close, attachment data SQL1. 5 SQLite acceptance tests. |
| N2 | `attachments_stub` — no-op `fillWriteDataForAttachmentsChange` | `src/plugins/attachments/stub.rs` | T3 | done | main | wave-036: returns new_document unchanged; CTOX stores attachments as out-of-band Parquet files referenced by hash. Errors with AT_FILL when `_attachments` missing |
| N3 | `rxjs_compat` — `Subject`/`Observable` → tokio broadcast/watch + tokio-stream | `src/rxjs_compat.rs` | T1 | done | main | wave-126: reactive_from_stream added as Rust-native equivalent of reactivity.fromObservable, forwarding an RxStream into an RxBehaviorSubject with an initial value for $$ adapters. wave-061: RxBehaviorSubject::next now uses watch::Sender::send_replace so get_value updates even with no active subscribers, matching RxJS BehaviorSubject semantics. RxSubject/RxBehaviorSubject/RxStream/first_value_from + 5 unit tests green |
| N4 | `overwritable` Rust idiom — typed registry instead of TS mutable singleton | `src/overwritable.rs` (port + design) | T1 | done | main | wave-039: marked complete — Overwritable struct with Arc<dyn Fn>-typed callbacks (is_dev_mode/deep_freeze_when_dev_mode/tunnel_error_message), ArcSwap singleton, replace_overwritable builder. Plugins install overrides atomically via store(). Default impl in place since early phase-0. Validated end-to-end by tests across the crate that exercise the dev-mode flag and error-message tunnel |
| N5 | WebRTC connection handler on `webrtc-rs` (replaces `simple-peer` port) | `src/plugins/replication_webrtc/connection_handler_rs.rs` | T1 | done | main | wave-144: ICE signaling now wraps outgoing webrtc-rs candidates in the browser `simple-peer` shape and decodes incoming simple-peer candidate wrappers before calling `add_ice_candidate`; the local Business OS default bind is `127.0.0.1:0`; two focused regressions cover the signal shape and an isolated browser/daemon smoke replicates `desktop_files` into SQLite. wave-051: real webrtc-rs transport landed. Added `webrtc = 0.20.0-alpha.1`, WebRTCRsConfig + new_with_signaling(signaling, room), peer-list driven RTCPeerConnection creation, simple-peer initiator rule (`remotePeerId > ownPeerId`), DataChannel creation/polling, SDP offer/answer + ICE signaling through SignalingClient, send() over DataChannel, close() cleanup, disconnect/error propagation, and message/response demux by `result`/`error` field. |
| N6 | Signaling client matching browser URL contract | `src/plugins/replication_webrtc/signaling_client.rs` | T1 | done | main | wave-047: browser wire shape hardened — `signal` frames now use explicit `{ type, room, senderPeerId, receiverPeerId, data }` fields (no flattened payload), `Init`/`Joined` camelCase fields are explicit, and SignalingClient remembers joined room before `send_signal`. 2 JSON golden tests. Existing: tokio-tungstenite WebSocket, keepalive task (half PING_INTERVAL), RxBehaviorSubject<peer_list> + RxSubject<server_messages>. |
| N7 | Tombstone GC job — `DELETE WHERE _deleted=1 AND updated_at_ms < ?` | `src/storage/sqlite/cleanup.rs` | T3 | done | main | wave-044: cleanup_deleted_documents deletes tombstones by SQLite metadata columns (`deleted = 1 AND lastWriteTime < now - minimumDeletedTime`); covered by SQLite cleanup acceptance test |
| N8 | NPM mini-deps in Rust: `custom-idle-queue`, `oblivious-set`, `array-push-at-sort-position` | `src/util/*.rs` | T2 | done | main | wave-036: ObliviousSet (TTL-decay hashset for emittedEventBulkIds dedupe), IdleQueue (serializing tokio queue + request_idle_promise), push_at_sort_position (binary_search_by + Vec::insert). 7 unit tests (4 push_at_sort_position + 3 oblivious_set + 2 idle_queue) |
| N9 | Conformance test harness (Rust test-utils parity + storage/replication checks) | `tests/conformance/*.rs` | T2 | done | main + Dewey + Archimedes | wave-139: conformance harness row marked complete for rxdb-rs; `tests/conformance/README.md` now documents the scope and `index.rs` asserts the Rust barrel tracks all vendored upstream `plugins/test-utils/index.ts` exports. Browser/WebRTC E2E is explicitly CTOX integration-layer work because it needs the browser bundle, signaling server, and Rust daemon together. wave-125: `tests/conformance/humans-collection.rs` added with Rust collection factories for upstream `test-utils/humans-collection.ts`, backed by real RxDatabase/RxCollection creation and seed assertions for human, indexed, nested, related, timestamp, ownership, multi-collection, attachment-schema, migration-shape, and multi-instance helpers. wave-124: `tests/conformance/schemas.rs` added with Rust JSON schema fixture factories for upstream `test-utils/schemas.ts`, including all exported schema names and enable_key_compression clone semantics. wave-123: `tests/conformance/schema-objects.rs` added with Rust JSON fixture factories for the upstream test schema objects, including special-character random strings, nested/encrypted/index/composite/ownership/timestamp/average-schema fixtures, and barrel exports. wave-122: `tests/conformance/replication.rs` now carries Rust equivalents for upstream test-utils replication helpers: pull handler, pull stream, push handler, and state equality ignoring `_meta`/`_rev`, all re-exported through the barrel. wave-121: `tests/conformance/config.rs` added with Rust-native runtime/env helpers, global test config, storage-name filtering, encrypted-storage passthrough, and password provider/fallback semantics; barrel re-export updated. wave-120: `tests/conformance/test-util.rs` added with Rust equivalents for repeated test execution and JSON state equality diagnostics, re-exported through the test-utils barrel. wave-119: `tests/conformance/index.rs` added as the Rust test-utils barrel for the currently ported phase-7 modules, with re-export assertions. wave-118: `tests/conformance/port-manager.rs` added with a sequential free-port allocator starting at upstream's 18669 and a monotonic/bindable integration assertion. wave-117: phase-7 test-utils work started with `tests/conformance/revisions.rs`, carrying upstream revision golden constants and an integration assertion. wave-070: Replication handler pull/checkpoint conformance enabled for seeded master_changes_since across Memory and SQLite after routing through the storage-native changed-since API; 7 integration tests green. wave-067: Replication conformance added for rx_storage_instance_to_replication_handler empty master_changes_since wire shape and master_write insert/conflict behavior across Memory and SQLite. wave-064: Rust-only conformance foundation added under `tests/conformance/`: Memory and SQLite are exercised with identical fixtures for bulk_write/find_documents_by_id, Mango query/count, changed_documents_since checkpointing, and deleted-document visibility. Crate test count 159 incl. 30 conformance tests |
| N10 | `RXDB_VERSION` constant (replaces upstream build template) | `src/plugins/utils/utils_rxdb_version.rs` | T3 | done | main | const = '16.20.0' |
| N16 | Minimal Mango-query Rust port (replaces upstream `mingo` NPM package) | `src/util/mango/*.rs` | T1 | done | subagent-afed576b | delivered by background worktree-isolated subagent; 18 operators + Query + sort_documents + Cursor→Vec collapse; 10 unit tests green; vendored mingo 6.5.6 |
| N17 | Core type foundation (schema, document, checkpoint, hash) + phase-1 query types + **phase-2 storage types** (BulkWriteRow, RxStorageWriteError, RxStorageBulkWriteResponse, RxStorageChangeEvent, EventBulk, RxStorageQueryResult, RxStorageCountResult, RxStorageChangedDocumentsSinceResult, RxStorageInstanceCreationParams, **RxStorageInstance trait**, **RxStorage factory trait** (wave-035), PreparedQuery) | `src/types/{mod,util,schema,document,checkpoint,hash,query,storage}.rs` | T1 | done | main | wave-060: RxStorageInstanceReplicationInput extended with separate push_batch_size and pull_batch_size while retaining bulk_size for compatibility with older call sites/tests. RxStorageInstance + RxStorage are async_trait + Send+Sync; RxStorage factory pattern wires meta-instance creation in replication start() |
| N18 | Browser/Rust WebRTC smoke harness | `src/core/rxdb/tools/browser_rust_smoke.js`, `src/core/rxdb/tools/browser_rust_smoke_matrix.js` | T2 | done | main | wave-258: release-required soak runs now force matrix evidence on and require fail-on-retry, and the workflow forces `SOAK_FAIL_ON_RETRY=1` whenever release coverage is enabled. wave-256: smoke evidence requirements are now declared in a `modeEvidenceRequirements` map, and the static guard requires every matrix default mode to have an explicit evidence contract with no unsupported evidence-only modes. wave-255: the static guard now rejects duplicate smoke/soak modes across runner defaults, matrix defaults, smoke harness supported modes, workflow defaults, and workflow required modes, preventing duplicated entries from inflating apparent coverage. wave-254: the release guard now also checks smoke-matrix `defaultModes`, requiring every release soak mode to be accepted by the matrix and every matrix mode to be supported by the smoke harness. wave-253: release soak modes must be present in the supported `SMOKE_MODE` list before CI accepts workflow/runner configuration. wave-252: CI now runs `assert_rxdb_soak_workflow.js`, which prevents release soak workflow drift by requiring the runner mode list, workflow default modes, release required modes, minimum-cycle wiring, release-coverage default, and always-on summary/artifact evidence steps to stay aligned. wave-251: release-profile soak now supports `SOAK_MIN_CYCLES`, and the GitHub Actions soak requires at least three cycles when release coverage is enabled so a one-cycle run cannot masquerade as a full release soak. wave-250: smoke, matrix, and soak numeric environment values now require bounded positive integers, preventing bad cycles, attempts, ports, or port stride from producing NaN loops or invalid port allocation; matrix and soak emit configurationError summaries before starting processes. wave-249: `browser_rust_soak.js` now supports `SOAK_REQUIRED_MODES`; the GitHub Actions soak requires full release coverage by default and writes a top-level `configurationError` when required modes are missing before starting any browser, Signaling, or CTOX process. wave-248: empty or unsupported `SMOKE_MODES` now fail before starting browser, Signaling, or CTOX processes; invalid matrix and soak summaries carry `configurationError`, and `print_soak_summary.js` exposes those pre-attempt cycle errors in logs and `$GITHUB_STEP_SUMMARY`. wave-247: `print_soak_summary.js` now also writes per-attempt evidence into `$GITHUB_STEP_SUMMARY`, giving the manual RxDB/WebRTC soak a GitHub Actions table with result, status, evidence, and `evidenceProblems` while redacting token-like query values. wave-246: manual soak workflow now prints compact per-attempt evidence from the JSON summary into GitHub Actions logs via `print_soak_summary.js`, making advanced-status, chunk, payload, command, queue, signaling, and replication proof visible without downloading artifacts. wave-245: matrix attempts now enforce expected evidence keys per known mode by default, treating missing advanced-status, chunk, payload, command, queue, signaling, or replication proof as a failed attempt while recording `evidenceProblems` in the JSON summary. wave-244: matrix attempts now parse smoke `key=value` lines into JSON evidence, so matrix and soak artifacts include machine-readable payload, advanced-status, command-count, task-count, and id proofs in addition to exit status. wave-243: restart File Viewer smoke now rebuilds and requires the full shell-critical collection set (`business_module_catalog`, `ctox_runtime_settings`, `business_commands`, `ctox_queue_tasks`, `desktop_files`, `desktop_file_chunks`) before passing advanced-status health; direct, matrix, and one-cycle release-soak runs passed with `SOAK_FAIL_ON_RETRY=1`, `retries=0`, and the full 103-chunk payload. wave-242: restart File Viewer smoke now also asserts the public advanced status is healthy after lazy materialization, requiring `business_commands`, `desktop_files`, and `desktop_file_chunks` to be synced with `advanced_status=business-os-advanced-status-v1`; direct, matrix, and one-cycle release-soak runs passed with `SOAK_FAIL_ON_RETRY=1`, `retries=0`, and the full 103-chunk payload. wave-241: added `workspace-large-file-viewer-restart-rust-to-browser`, covering lazy large File Viewer open across a native CTOX peer restart; the Browser now restarts stale file/command replication bridges, renders the full 103-chunk payload after reconnect, and direct/matrix/one-cycle release-soak runs passed with `SOAK_FAIL_ON_RETRY=1` and `retries=0`. wave-240: added `workspace-large-file-viewer-rust-to-browser`, covering the real File Viewer UI opening a lazy large CTOX workspace file through live app DB/sync/CommandBus context and rendering the materialized 103-chunk payload; direct, matrix, and one-cycle release-soak runs passed with `retries=0`. wave-239: added `workspace-large-materialize-rust-to-browser`, covering lazy large workspace metadata, Browser-dispatched `ctox.file.materialize`, and materialized chunk payload delivery over RxDB/WebRTC; direct, matrix, and one-cycle release-soak runs passed with `retries=0`. wave-238: added `workspace-update-rust-to-browser`, active chunk-generation selection in the harness, matrix coverage, and release-soak default coverage; direct/matrix/soak runs proved a rewritten CTOX workspace file reaches Browser with a new generation and no retry. wave-225: Business OS advanced status now treats `desktop_files` and `desktop_file_chunks` as default readiness collections and the Browser/Rust smoke requires them; an empty workspace passes only when those file collections are registered in WebRTC diagnostics without failed, stopped, or pending state. wave-224: full default release soak verified Browser→Rust file/chunk, Browser→Rust command/queue, workspace Rust→Browser file/chunk, and Signaling+CTOX restart recovery with `SOAK_FAIL_ON_RETRY=1`; all four modes completed first try with `retryCount=0` and wrote the JSON summary artifact. wave-223: workspace Rust→Browser soak verified CTOX workspace file sync into Browser under `/CTOX/<workspace>/reports/brief.md` with `SOAK_FAIL_ON_RETRY=1` and `retryCount=0`; manual release soak defaults now cover file, command, workspace, and restart modes. wave-222: manual soak workflow now includes command Browser→Rust by default, uploads the JSON soak summary, and can fail on any retried mode via `SOAK_FAIL_ON_RETRY=1`; command soak verified browser `business_commands` → Rust acceptance → `ctox_queue_tasks` projection, and the retry gate was verified both failing on occupied ports and passing with `retryCount=0`. wave-221: smoke matrix and soak runner now optionally emit structured JSON result files with per-mode attempt counts, statuses, signals, allocated ports, aggregate cycle results, and retry totals; a one-cycle Browser→Rust soak wrote a green JSON summary and replicated file/chunk data into isolated SQLite. wave-219: added a manual GitHub Actions soak workflow that builds the real `ctox` smoke binary, installs a pinned Playwright runtime, and runs `browser_rust_soak.js` with configurable cycles and modes against the full `/index.html` app. wave-218: added `browser_rust_soak.js`, a repeatable wrapper for the full-app smoke matrix with configurable cycles, modes, and isolated port spacing; CI syntax-checks the soak entry point, and a one-cycle Browser→Rust soak was verified green with a browser-created file/chunk reaching isolated SQLite over RxDB/WebRTC. wave-214: added `SMOKE_MODE=restart-signaling-browser-to-rust`, covering a live Signaling+CTOX restart while the browser stays open; Browser-side fatal peer detection plus automatic data-plane repair now gets a browser-created file/chunk document into SQLite after reconnect. wave-213: added `SMOKE_MODE=restart-browser-to-rust`, which starts the real `/index.html` app, restarts the CTOX `business-os serve` process while the browser remains open, and verifies a browser-created file/chunk document lands in SQLite after WebRTC/RxDB reconnect. wave-196: `browser_rust_smoke_matrix.js` now runs the full `/index.html` smoke matrix in one command with isolated ports and one retry per mode; the matrix was verified green across Rust→Browser file/chunk, Browser→Rust file/chunk, Browser→Rust command/queue, and workspace Rust→Browser file/chunk. CI syntax-checks both smoke scripts. wave-195: added `SMOKE_MODE=workspace-rust-to-browser`, which starts the real `/index.html` app, brings file/chunk replication online, calls `ctox business-os files sync-workspace <path>`, and asserts both payload and `/CTOX/<workspace>/reports/brief.md` virtual path arrive in the browser over WebRTC. Existing full-app Rust→Browser, Browser→Rust, and command Browser→Rust smoke modes were re-run green after the harness change. wave-184: the harness now waits for the native `desktop_files` and `desktop_file_chunks` SQLite tables before direct Rust-side seeding, closing the startup race where config readiness preceded storage-table creation. Full `/index.html` Browser→Rust file/chunk, Rust→Browser file/chunk, and Browser→Rust command/queue projection smokes pass sequentially. wave-163: full app command smoke now waits for both `business_commands` and `ctox_queue_tasks` replication readiness before asserting the projected queue task, matching the two-collection acceptance contract. wave-161: full app smoke startup now waits for the smoke hook with Playwright polling before entering the long in-page replication assertion, avoiding a parser-blocking race on `/index.html?rxdbSmoke=1`; command Browser→Rust smoke is green again. wave-153: added `SMOKE_MODE=command-browser-to-rust`, covering browser `business_commands` insert → Rust native command consumer → accepted command/`ctox_queue_tasks` projection back to browser for both isolated and full `/index.html` app DBs. wave-152: Browser→Rust now asserts both `desktop_files` and `desktop_file_chunks` land in SQLite with matching decoded payload, and the harness can force empty Rust/browser payloads through `SMOKE_RUST_FILE_CONTENT` / `SMOKE_BROWSER_FILE_CONTENT`. Empty-payload smoke coverage is green for isolated Rust→Browser, isolated Browser→Rust, full `/index.html` Rust→Browser, and full `/index.html` Browser→Rust. wave-146: repo-maintained Playwright smoke harness starts local signaling plus `ctox business-os serve`, verifies the native Rust peer, seeds controlled SQLite readiness files, and covers isolated Rust→Browser, isolated Browser→Rust, and full `/index.html` Rust→Browser paths. |
| N19 | Full Business OS app DB Browser→Rust WebRTC push hardening | `src/core/rxdb/src/plugins/replication_webrtc/index_mod.rs`, `src/apps/business-os/app.js`, `src/core/rxdb/tools/browser_rust_smoke.js` | T1 | done | main | wave-199: the full-app smoke now also asserts that a real shell module is visible and not stuck in loading state after the RxDB module-catalog handshake. wave-184: full-app startup remains clean after the HTTP bootstrap removal, and native RxDB projection writes are serialized so parallel background projections do not produce SQLite lock noise during the smoke. wave-149: periodic RESYNC is factored into a test-covered helper and temporary smoke diagnostics were removed. wave-148: Rust fork-side WebRTC replication emits a periodic `RESYNC` into the pull stream, so browser-master changes are pulled even if the browser `masterChangeStream$` live notification is missed. The real `/index.html` app DB Browser→Rust smoke now lands in SQLite, so all four smoke acceptance paths are green. wave-147: `/index.html` smoke now uses the real Business OS app DB through a gated `?rxdbSmoke=1` hook instead of starting a second browser DB on the same topic. |
| N20 | CTOX desktop file edge cases | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/desktop-apps/file-viewer/app.js`, `src/apps/business-os/desktop-apps/explorer/app.js`, `src/apps/business-os/modules/desktop/schema.js` | T2 | done | main | wave-175: the desktop file schema and RxDB sync contract now explicitly define CTOX sync metadata (`local_path`, `virtual_path`, `content_state`, `content_hash`, `mtime_ms`, `content_synced_at_ms`) and the available/lazy/missing/directory state semantics. wave-174: scanned CTOX files now retain `local_path` for materialization while exposing `/CTOX/...` virtual paths and source folders in Business OS; vanished scanned files are tombstoned with `is_deleted=true` / `content_state=missing`, including empty scan roots. wave-151: Explorer preview/download now matches the file viewer's chunk-generation semantics, accepting valid empty chunks but rejecting missing content. wave-150: empty CTOX-managed files now produce a valid empty chunk row in SQLite and open as empty browser Blobs instead of failing as missing content. |
| N21 | Business OS command consumer over RxDB/WebRTC | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/command-bus.js`, `src/apps/business-os/modules/documents/index.js`, `src/apps/business-os/modules/ctox/index.js`, `src/apps/business-os/app.js` | T1 | done | main | wave-158: CTOX task edit/delete browser actions now dispatch `ctox.task.update` / `ctox.task.delete` through replicated `business_commands`; Rust applies the existing task mutation logic and projects changed `ctox_queue_tasks` rows back through RxDB SQLite. wave-157: shell startup no longer performs a redundant `/api/business-os/status` data poll after opening modules; the remaining status call is the operational health warning only. wave-156: the CTOX module no longer falls back to HTTP harness-flow/status snapshots for live data; commands, queue tasks, and bug reports render from replicated RxDB collections and the flow remains explicitly unavailable until backed by a real RxDB projection. wave-155: the browser sync runtime no longer has a native HTTP bridge fallback, so command and collection data cannot fall back to `/rxdb/pull` or `/rxdb/push`; startup is WebRTC when the native peer is ready and explicit `local-only` while it is not. wave-154: documents command UX now waits on the local replicated `business_commands` projection instead of `/api/business-os/rxdb/pull`. wave-153: browser commands no longer POST to `/rxdb/push`; the native Rust peer consumes replicated `pending_sync` command docs, accepts them through the existing CTOX command path, writes accepted command status back to RxDB SQLite, and projects the created `ctox_queue_tasks` row for browser replication. Covered by native peer consumer regressions plus isolated and full app command WebRTC smokes. |
| N22 | Knowledge command result projection over RxDB | `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/modules/outbound/index.js`, `src/apps/business-os/app.js` | T1 | done | main | wave-160: removed the last browser-side `http_bridge_available` checks from shell startup/module refresh; module metadata refresh remains shell/admin HTTP, but no browser data path or startup decision depends on the old bridge flag. wave-159: `knowledge.command` documents are executed inside the Rust peer via `knowledge::dispatch_capturing`, and their JSON results are persisted in `business_commands.result` for browser replication. Outbound waits on this replicated projection for Knowledge `data select` results and no longer keys the path off `http_bridge_available`. |
| N23 | Business OS report command projection over RxDB | `src/apps/business-os/shared/business-reporter.js`, `src/apps/business-os/app.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-161: Browser report submission now writes `ctox.report.*` command docs instead of POSTing `/api/business-os/reports`; Rust reuses the canonical report/task creation path, accepts non-admin authenticated report actors, and projects `business_module_reports`, `ctox_bug_reports`, and the created queue task back through RxDB SQLite. Covered by `native_peer_consumes_pending_report_command`. |
| N24 | Module source command projection over RxDB | `src/apps/business-os/desktop-apps/code-editor/app.js`, `src/apps/business-os/app.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-162: Source Editor load/save now uses `ctox.source.load` / `ctox.source.save` commands and the `business_module_source_files` projection instead of `/api/business-os/modules/source`; Rust scans/saves local module source files, writes snapshots for changed saves, and projects updated source docs through RxDB SQLite. Covered by `native_peer_consumes_pending_source_commands`. |
| N25 | Module governance release/rollback commands over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/reports/index.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-163: Settings release/rollback and Reports rollback now dispatch `ctox.module.release` / `ctox.module.rollback` through `business_commands` instead of POSTing `/api/business-os/modules/release` or `/api/business-os/modules/rollback`; Rust reuses canonical module governance store logic, refreshes all affected `business_module_releases` projections, strips store-local `_rev` before RxDB writes, and uses release-specific upsert projection to avoid stale rollback status. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| N26 | Module founder assignment command over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-164: Settings Founder save now dispatches `ctox.module.assign_founder` through `business_commands` instead of POSTing `/api/business-os/modules/assign-founder`; Rust reuses `assign_module_founder`, returns the affected `business_module_acl` id, and projects that ACL row back through native RxDB SQLite. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| N27 | Module manifest mutation commands over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-165: Settings module save/delete/template install now dispatch `ctox.module.save`, `ctox.module.delete`, and `ctox.module.install_template` through `business_commands` instead of POSTing the module mutation endpoints; Rust reuses existing manifest/template/delete behavior, including permission checks and module-layout cleanup. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| N28 | Shell template install command over RxDB | `src/apps/business-os/app.js` | T1 | done | main | wave-166: The shell template-store install action now dispatches `ctox.module.install_template` through `business_commands`, waits for the replicated projection, and no longer POSTs `/api/business-os/modules/install-template`; this closes the last browser-side module install mutation path found by the app scan. |
| N29 | Dead Business OS HTTP mutation helper removal | `src/apps/business-os/app.js` | T1 | done | main | wave-167: Removed the unused shell `saveModuleLayout()` helper that still POSTed `/api/business-os/module-layout`; the live module-layout persistence path did not call it, so this closes a stale browser mutation surface without changing behavior. |
| N30 | Settings user upsert command over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-168: Settings user save now dispatches `ctox.business_os.user.upsert` through `business_commands` instead of POSTing `/api/business-os/users`; Rust reuses `upsert_user`, preserves the admin check, and returns the refreshed users list in the command result. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| N31 | Runtime settings save command over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-169: Settings runtime/auth save now dispatches `ctox.runtime_settings.save` through `business_commands` instead of POSTing `/api/business-os/ctox/runtime-settings`; Rust writes the same operator runtime env fields as the previous HTTP handler and the UI refreshes the existing read payload afterwards. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| N32 | Subscription auth start command over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs` | T1 | done | main | wave-170: Settings ChatGPT subscription authorization now dispatches `ctox.subscription_auth.start` through `business_commands` instead of POSTing `/api/business-os/ctox/subscription-auth/start`; Rust starts the local OAuth callback server and returns the login URL through the replicated command result. |
| N33 | Channel setup commands over RxDB | `src/apps/business-os/shared/react-settings.js`, `src/core/business_os/store.rs`, `src/core/business_os/rxdb_peer.rs` | T1 | done | main | wave-171: Channel setup mutations now dispatch `ctox.channel.test`, `ctox.channel.sync`, `ctox.channel.settings.save`, `ctox.channel.disconnect`, `ctox.channel.pair.start`, `ctox.channel.jami.create`, and `ctox.channel.jami.export` through `business_commands` instead of browser POST routes; Rust calls the existing mission channel helpers and preserves manage-all authorization. Covered by `native_peer_consumes_pending_module_governance_commands`. |
| N34 | Channel account and pairing-state projections over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/desktop/schema.js`, `src/apps/business-os/modules/conversations/index.js` | T1 | done | main | wave-176: The native peer periodically projects `communication_accounts` and `channel_pairing_state` into RxDB SQLite, including tombstones for disconnected accounts. Settings starts collection sync and reads channel accounts/QR pairing state from RxDB instead of `/api/business-os/channels/accounts` or `/api/business-os/channels/pair/state`; Conversations ignores tombstoned accounts. Covered by `sync_channel_state_projects_accounts_and_pairing_state`. |
| N35 | Business users projection over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/ctox/schema.js` | T1 | done | main | wave-177: `business_users` is now registered as a core RxDB collection. The native peer projects the store's `business_users` table into SQLite-backed RxDB on a short interval and after user-upsert commands; Settings reads users from RxDB instead of `/api/business-os/users` and applies the existing session visibility rule in-browser. Covered by `sync_business_users_projects_store_rows`. |
| N36 | Runtime settings projection over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/ctox/schema.js` | T1 | done | main | wave-178: `ctox_runtime_settings` is now registered as a core RxDB collection. The native peer derives runtime, auth, service, and diagnostics state from the canonical CTOX runtime store and projects the status document into SQLite-backed RxDB on a short interval and after runtime-settings save commands; Settings reads that document from RxDB instead of `/api/business-os/ctox/runtime-settings`. Covered by `sync_runtime_settings_projects_status_document` where Cargo can access the workspace files plus browser syntax checks. |
| N37 | Module catalog projection over RxDB | `src/core/business_os/rxdb_peer.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/shared/react-settings.js`, `src/apps/business-os/modules/ctox/schema.js`, `src/apps/business-os/app.js`, `src/apps/business-os/shared/sync.js` | T1 | done | main | wave-199: the browser no longer gates WebRTC startup on the one-time `native_rxdb_peer_available` injection bit, so the shell can receive `business_module_catalog` over RxDB even when the native peer is still finishing startup during the initial HTML response. The full-app smoke now asserts module visibility and cleared loading state in addition to data replication. wave-180: the shell startup path, background module refresh, and template drawer now read modules/templates/governance from replicated `business_module_catalog` instead of `/api/business-os/modules` or `/api/business-os/templates`; the browser sync runtime treats projection collections as pull-only. wave-179: `business_module_catalog` is now registered as a core RxDB collection. The native peer projects Business OS module manifests, template-store manifests, and governance metadata into one SQLite-backed status document on a short interval and after module commands; Settings admin reads modules/templates from RxDB. Covered by browser syntax checks and `sync_module_catalog_projects_modules_and_templates` where Cargo can access the workspace files. |
| N38 | Shell health projection over RxDB | `src/apps/business-os/app.js` | T1 | done | main | wave-181: the shell warning banner now reads service health from replicated `ctox_runtime_settings` instead of polling `/api/business-os/status`; `ctox_runtime_settings` replication is started after the local DB and sync runtime are available. Remaining shell/shared CTOX HTTP paths are limited to session and sync-config bootstrap. |
| N39 | Browser launch context injection | `src/core/business_os/server.rs`, `src/core/business_os/store.rs`, `src/apps/business-os/app.js`, `src/apps/business-os/shared/command-bus.js`, `src/apps/business-os/shared/react-settings.js` | T1 | done | main | wave-199: removed the now-dead `authHeaders` context plumbing and `businessOsAuthHeaders()` helper after the browser HTTP bootstrap recovery path was removed; module code now depends on RxDB/command context instead of shell-provided HTTP auth headers. wave-198: removed the split-string session/config fallback variant and changed the in-app login submit path so it no longer stores Basic auth for an HTTP session/config recovery path that no longer exists. A logged-out injected session can still clear the local logout flag and reload; otherwise the app requires CTOX-injected launch context. wave-197: removed the last app-shell fallback fetches to `/api/business-os/session` and `/api/business-os/sync/config`; the browser launch path now depends only on CTOX-injected session/config globals and does not use stored auth headers to recover via HTTP. wave-182: served app-shell `index.html` now receives session and sync config through `window.CTOX_BUSINESS_OS_SESSION` and `window.CTOX_BUSINESS_OS_CONFIG`; module fragment `index.html` files are not injected. The browser shell no longer fetches `/api/business-os/session` or `/api/business-os/sync/config`, dead browser API fetch helpers were removed, and `sync_config()` reports no HTTP bridge fallback. |
| N40 | Agent workspace root file projection over RxDB desktop files | `src/core/business_os/rxdb_peer.rs`, `src/core/service/service.rs`, `src/core/service/business_os.rs`, `src/core/main.rs`, `src/core/rxdb/src/storage/sqlite/types.rs`, `src/core/rxdb/tools/browser_rust_smoke.js` | T1 | done | main | wave-243: lazy workspace-file restart coverage now requires whole-shell data-plane health, not just file/chunk availability, before the File Viewer smoke passes. wave-242: post-restart lazy workspace-file opening is now guarded by the public advanced status contract, so a full File Viewer render only passes when file/chunk/command collections are healthy after reconnect repair. wave-241: lazy large workspace files are now covered through a native peer restart; the File Viewer materializes and renders the full replicated payload after Browser file/command collection bridges are rebuilt. wave-240: the user-facing File Viewer now has executable coverage for lazy large workspace files; successful opens depend on replicated file content, while command projection is used for failed-command diagnostics so status lag does not block a loaded file. wave-239: large workspace files now complete the intended lazy path end to end: metadata arrives without eager chunks, `ctox.file.materialize` runs through RxDB commands, Rust writes active chunks, and Browser reads the full payload from the materialized generation. wave-238: workspace file update visibility is now covered end to end: `ctox business-os files sync-workspace <path>` is run before and after rewriting the same file, and Browser must receive the updated payload from the active chunk generation over RxDB/WebRTC. wave-195: `SMOKE_MODE=workspace-rust-to-browser` now covers the completed-agent-style path end to end: a nested real workspace file is projected by `ctox business-os files sync-workspace <path>` into SQLite-backed `desktop_files` / `desktop_file_chunks`, then replicated over WebRTC into the real `/index.html` Business OS app, including assertion of the `/CTOX/<workspace>/reports/brief.md` virtual path. wave-194: added `ctox business-os files sync-workspace <path>` for explicit bounded workspace projection through the same SQLite-backed `desktop_files` / `desktop_file_chunks` path used by completed agent jobs; verified a nested workspace file appears at `/CTOX/workspace/reports/brief.md` with one eager chunk. wave-193: SQLite-backed RxDB connections now use a 10s busy timeout, and the real `/index.html` smokes were re-run without raw SQLite seed shortcuts: Rust→Browser file/chunk goes through `ctox business-os files sync <path>`, Browser→Rust file/chunk lands in SQLite, and Browser→Rust command acceptance projects `ctox_queue_tasks` back to the app. wave-192: added the local `ctox business-os files sync <path>` entrypoint for explicit CTOX filesystem-to-SQLite projection and hardened the full-app smoke so Rust-side files are synced after WebRTC replication starts; `/index.html` smokes are now green for Rust→Browser file/chunk, Browser→Rust file/chunk, and Browser→Rust command→queue projection. wave-191: `sync_desktop_file_from_path_records_new_chunk_generation_after_update` covers a multi-chunk eager file being rewritten to a smaller payload; old chunk rows may remain, but the latest `created_at_ms` generation contains the authoritative new single chunk used by Browser readers. wave-184: focused workspace-root and bounded-scan tombstone regressions pass in the materialized verification checkout after the SQLite helper DB opens were serialized. wave-183: successful agent jobs with a `workspace_root` now immediately run a bounded workspace-file index into SQLite-backed `desktop_files` and `desktop_file_chunks`, so files created by CTOX/Codex appear under `/CTOX/<workspace>/...` in Business OS without waiting for the periodic global scanner or for explicitly declared artifact paths. Small supported files are eagerly chunked; large or unsupported files remain metadata-only and use the existing `ctox.file.materialize` command path on open. When a bounded scan reaches `DESKTOP_FILE_SCAN_MAX_FILES`, tombstone marking is skipped to avoid marking unsampled files missing. Covered by `sync_desktop_files_from_workspace_root_indexes_agent_workspace` and `bounded_workspace_scan_does_not_tombstone_when_limit_is_reached`. |
| N41 | Browser RxDB-only data-path regression gate | `src/apps/business-os/scripts/assert-rxdb-only.mjs`, `.github/workflows/ci.yml`, `src/apps/business-os/RXDB_SYNC_CONTRACT.md` | T1 | done | main | wave-243: the guard now requires the advanced status default critical collection set to include command and queue projections and verifies empty command/file collections are explicitly handled. wave-198: the guard now also catches split-string Business OS API paths such as `'/api/' + 'business-os'`, preventing browser HTTP fallback strings from being hidden from the contract scan. wave-197: the guard caught app-shell fallback fetches to `/api/business-os/session` and `/api/business-os/sync/config`; those fallbacks were removed and the guard is green again. wave-190: the guard now scans JSON files in the covered app scopes, protecting module manifests, locale files, registry metadata, and template-store manifests from forbidden CTOX HTTP fallback strings. wave-189: the guard scan scope also includes future Electron wrapper and template-store app JavaScript. wave-188: `RXDB_SYNC_CONTRACT.md` documents the guard scope, forbidden fallback patterns, and CI enforcement lane. wave-187: the main CI workflow now runs the guard in the Linux CTOX check lane with Node 22, covering both `node --check` and the contract script itself before `cargo check`. wave-186: the guard now also scans `desktop-apps`, covering Explorer, File Viewer, and Code Editor as browser surfaces. wave-185: the guard rejects frontend `/api/business-os...` data paths, `/api/business-os/status` polling, and the old CTOX harness/task HTTP fallbacks in addition to `/rxdb/pull`, direct command POSTs, native bridge helpers, and `http_bridge_available=true`. Backend endpoint definitions and the guard script are explicitly scoped out, so the check can run in CI as a browser-surface contract without blocking server route code. Covered by `node --check src/apps/business-os/scripts/assert-rxdb-only.mjs` and `node src/apps/business-os/scripts/assert-rxdb-only.mjs`. |
| N11 | Type stub for skipped `attachments` plugin (`RxAttachment*` types referenced by replication) | `src/plugins/attachments/types_stub.rs` | T3 | done | main | wave-036: re-exports RxAttachmentData/RxAttachmentWriteData from types/storage.rs; RxAttachment alias to RxAttachmentData (interactive method surface intentionally omitted) |
| N12 | Type stub for skipped `migration-schema` plugin (`RxMigrationState` type) | `src/plugins/migration_schema/types_stub.rs` | T3 | done | main | wave-090: RxMigrationState placeholder type added for the core collection migration surface. wave-036: RxMigrationStatus + RxMigrationCount + MigrationStatusUpdate closure + MigrationStrategy/Strategies; PlainJsonError aliased to Value (V8 stack shape opaque) |
| N13 | Type stub for skipped `backup` plugin (`RxBackupState` type) | `src/plugins/backup/types_stub.rs` | T3 | done | main | wave-036: BackupOptions + BackupMetaFileContent + RxBackupCollectionState + RxBackupWriteEvent + RxBackupState placeholder (no in-process backup loop in CTOX MVP) |
| N14 | Type stub for skipped `pipeline` plugin (`RxPipeline*` types) | `src/plugins/pipeline/types_stub.rs` | T3 | done | main | wave-036: RxPipelineHandler closure + RxPipelineOptions + CheckpointDocData + RxPipeline placeholder (no pipeline runtime in CTOX MVP) |
| N15 | `event-reduce` conservative Rust subset | `src/event_reduce.rs` | T2 | done | main | wave-131: calculate_patched_find_results now applies Rust-native insert/update/remove actions for unpaginated find queries and re-sorts the cached result set with the existing Mango sort comparator; paginated and ambiguous cases still fall back to storage for correctness. 3 event-reduce unit tests. wave-098: calculate_new_results now avoids full storage re-execution for provably irrelevant change events where neither previous nor current document data matches the query; relevant/ambiguous changes still fall back to full query for correctness. wave-036: initial fallback always returned run_full_query_again=true |
| **N-OOC** | Parquet blob-sync side-channel (content-addressed, second WebRTC DataChannel) | **outside rxdb-rs** — CTOX glue code | — | — | — | not part of this crate; documents only carry hash refs |

## External Rust crate mapping (V2)

Mapping from upstream NPM deps to chosen Rust crates. CTOX already pulls in most of these.

| NPM dep | Rust crate | Notes |
|---|---|---|
| `rxjs` (peerDep) | `tokio` + `tokio-stream` + `futures` + `async-trait` | wrapped by N3 (`rxjs_compat`) |
| `mingo` (mango engine) | own minimal port | full mingo is ~3-5k LOC; we port only what `prepareQuery`/`normalizeMangoQuery` actually use |
| `custom-idle-queue` | own mini-port (N8) | ~50 LOC |
| `oblivious-set` | own mini-port (N8) | ring-buffer set |
| `array-push-at-sort-position` | own mini-port (N8) | binary-search insert |
| `simple-peer` | replaced by `webrtc` (webrtc-rs) | N5 |
| SQLite | `rusqlite` | CTOX-standard; sync API wrapped in `spawn_blocking` |
| JSON | `serde` + `serde_json` | standard |
| hash | `sha2` | replaces `js-sha256` |
| errors | `thiserror` (typed) + `anyhow` (CTOX-glue side) | standard |
| sync primitives | `parking_lot` (sync), `tokio::sync` (async) | rule in PORT_STYLE.md |
| concurrent map | `dashmap` | for caches (doc-cache, query-cache) |
| atomic shared state | `arc-swap` | for `overwritable` Rust idiom (N4) |
| WebSocket signaling | `tokio-tungstenite` | if WebRTC signaling uses WS |
| Skipped upstream deps | — | `ajv`, `dexie`, `broadcast-channel`, `firebase`, `mongodb`, `nats`, `crypto-js`, `event-reduce-js`, `graphql*`, `is-my-json-valid`, `z-schema`, `jsonschema-key-compression`, `ws`, `isomorphic-ws`, `reconnecting-websocket`, `js-base64`, `p2pcf`, `webtorrent`, all `@types/*` |

## phase-0

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/plugins/utils/utils-regex.ts` | 75 | T3 | `src/plugins/utils/utils_regex.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-rxdb-version.ts` | 111 | T3 | `src/plugins/utils/utils_rxdb_version.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-global.ts` | 152 | T3 | `src/plugins/utils/utils_global.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-number.ts` | 258 | T3 | `src/plugins/utils/utils_number.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-map.ts` | 672 | T3 | `src/plugins/utils/utils_map.rs` | done | main | foundation utilities |
| `src/plugins/utils/index.ts` | 719 | T3 | `src/plugins/utils/mod.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-time.ts` | 1214 | T3 | `src/plugins/utils/utils_time.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-revision.ts` | 1482 | T3 | `src/plugins/utils/utils_revision.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-object-deep-equal.ts` | 1505 | T3 | `src/plugins/utils/utils_object_deep_equal.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-error.ts` | 1665 | T3 | `src/plugins/utils/utils_error.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-hash.ts` | 1713 | T3 | `src/plugins/utils/utils_hash.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-other.ts` | 1720 | T3 | `src/plugins/utils/utils_other.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-string.ts` | 2127 | T3 | `src/plugins/utils/utils_string.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-document.ts` | 3387 | T3 | `src/plugins/utils/utils_document.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-promise.ts` | 3597 | T3 | `src/plugins/utils/utils_promise.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-array.ts` | 5070 | T3 | `src/plugins/utils/utils_array.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-object.ts` | 7323 | T3 | `src/plugins/utils/utils_object.rs` | done | main | foundation utilities |
| `src/plugins/utils/utils-object-dot-prop.ts` | 9509 | T3 | `src/plugins/utils/utils_object_dot_prop.rs` | done | main | foundation utilities |
| `src/overwritable.ts` | 1483 | T1 | `src/overwritable.rs` | done | main | overridable defaults |
| `src/plugin.ts` | 2798 | T1 | `src/plugin.rs` | done | main | plugin system |
| `src/hooks.ts` | 3377 | T1 | `src/hooks.rs` | done | main | hooks |
| `src/rx-error.ts` | 4553 | T1 | `src/rx_error.rs` | done | main | error type |

## phase-1

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/rx-query-mingo.ts` | 1607 | T3 | `src/rx_query_mingo.rs` | done | main | re-export shim over `util::mango` (per-Query context replaces upstream global registry) |
| `src/rx-schema.ts` | 7123 | T3 | `src/rx_schema.rs` | done | main | T1 deviations: overwriteGetterForCaching → OnceLock, getDocumentPrototype omitted (no Rust prototypes) |
| `src/rx-schema-helper.ts` | 10648 | T3 | `src/rx_schema_helper.rs` | done | main | wave-141: stale deferred comment removed; fillObjectWithDefaults is represented by `rx_collection_helper::fill_object_data_before_insert`, where Rust also applies insert-envelope fields and composite primary values. Existing schema helper surface covers pseudo schema, primary key helpers, index helpers, normalization, final fields, and default checkpoint schema |
| `src/query-planner.ts` | 11984 | T3 | `src/query_planner.rs` | done | main | get_query_plan + is_selector_satisfied_by_index + INDEX_MIN/MAX sentinels |
| `src/custom-index.ts` | 12134 | T3 | `src/custom_index.rs` | done | main | indexable-string monad + bounds string helpers; ParsedLengths struct |

## phase-2

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/rx-storage-multiinstance.ts` | 6244 | T2 | `src/rx_storage_multiinstance.rs` | done | main | single-process stub; BROADCAST_CHANNEL_BY_TOKEN registry kept for shape but no-op when multi_instance=false (always in CTOX) |
| `src/incremental-write.ts` | 8439 | T2 | `src/incremental_write.rs` | done | main | IncrementalWriteQueue with tokio::sync::{Mutex, oneshot}; Promise/resolve/reject → oneshot channel; modifier as Box<dyn FnOnce -> BoxFuture> |
| `src/plugins/storage-memory/memory-indexes.ts` | 1151 | T1 | `src/plugins/storage_memory/memory_indexes.rs` | done | main | add_indexes_to_internals_state + get_memory_index_name |
| `src/plugins/storage-memory/index.ts` | 1653 | T1 | `src/plugins/storage_memory/index_mod.rs` | done | main | renamed to index_mod.rs (Rust reserved-name avoidance); get_rx_storage_memory + create_storage_instance |
| `src/plugins/storage-memory/binary-search-bounds.ts` | 2947 | T1 | `src/plugins/storage_memory/binary_search_bounds.rs` | done | main | bound_ge/gt/lt/le/eq with Ordering-based Compare; 3 unit tests green |
| `src/plugins/storage-memory/memory-types.ts` | 3045 | T1 | `src/plugins/storage_memory/memory_types.rs` | done | main | MemoryStorageInternals + ByIndex + DocWithIndexString; SharedMemoryStorageInternals = Arc<Mutex<...>>; RxAttachmentData/WriteData added to types/storage.rs |
| `src/plugins/storage-memory/memory-helper.ts` | 4855 | T1 | `src/plugins/storage_memory/memory_helper.rs` | done | main | get_memory_collection_key, ensure_not_removed, attachment_map_key, put_write_row_to_state hotpath, remove_doc_from_state, compare_docs_with_index. `array-push-at-sort-position` inlined as `Vec::binary_search_by` + `Vec::insert` |
| `src/doc-cache.ts` | 10730 | T1 | `src/doc_cache.rs` | done | main | wave-077: get_cached_rx_documents now refreshes the per-id latest document data whenever a newer persisted/cache-mapped version is observed, keeping primary-key query shortcuts and same-handle reads current after write paths. wave-040: Rust-native Weak<T>/Arc<T> cache over document handles, keyed by primary + revision-height + `_meta.lwt`; explicit stale weak-entry pruning replaces JS FinalizationRegistry, change-event latest-data updates wired through RxStream<Vec<RxStorageChangeEvent>>, map_documents_data_to_cache_docs helper exported. 3 unit tests |
| `src/plugins/storage-memory/rx-storage-instance-memory.ts` | 16609 | T1 | `src/plugins/storage_memory/rx_storage_instance_memory.rs` | done | main | wave-141: module docs updated from the early skeleton wording to the completed in-memory storage surface. wave-065: get_changed_documents_since now uses a native in-memory `(lwt,id)` scan instead of the generic Mango fallback, aligning Memory with SQLite for checkpoint pagination and unblocking conformance coverage. Existing: full RxStorageInstance trait impl: bulk_write + find_documents_by_id + query + count + cleanup + change_stream + remove + close + get_changed_documents_since. query uses index-bound BSearch + queryMatcher fallback + manual resort + skip/limit; deserializes prepared_query JSON. T1 deviations: synchronous persistence (no requestIdleCallback); OPEN_MEMORY_INSTANCES test-hook omitted |
| `src/rx-storage-helper.ts` | 37030 | T1 | `src/rx_storage_helper.rs` | done | main | wave-141: removed stale in-file deferred note for `getWrappedStorageInstance`; the wrapper has been live since wave-043. wave-043: get_wrapped_storage_instance completed with RxDatabase.locked_run serialization, storage-instance tracking, revision/_meta.lwt write normalization, preStorageWrite hook bridge, deleted-document reinsert retry, and delegated query/count/find/getAttachmentData/getChangedDocumentsSince/cleanup/remove/close. Existing helper surface now covers upstream exports: get_single_document, write_single, observe_single, stack_checkpoints, throw_if_is_storage_write_error, categorize_bulk_write_rows(+hooks), attachment helpers, flat_clone_doc_with_meta, ensure_rx_storage_instance_params_are_correct, has_encryption, get_changed_documents_since, get_written_documents_from_bulk_write_response, random_delay_storage. 2 wrapper tests added; crate test count 62 |
| `src/plugin-helpers.ts` | 12609 | T2 | `src/plugin_helpers.rs` | done | main | wave-038: wrap_rx_storage_instance + WrappedRxStorageInstance (full RxStorageInstance trait impl with per-doc modify_to_storage/modify_from_storage closures applied across bulk_write+find+query+changed_since+change_stream, plus error path projection on write_row.{previous,document}+document_in_db, plus RxBehaviorSubject<u64>-backed processing-changes drain before write resolves), wrapped_validate_storage_factory + ValidationStorageFactory trait (caches ValidatorFunction by validator_key+schema_json, returns 422 errors with validation_errors populated). 2 e2e unit tests against memory storage (round-trip + 422 rejection) |

## phase-3

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/replication-protocol/default-conflict-handler.ts` | 1231 | T2 | `src/replication_protocol/default_conflict_handler.rs` | done | main | DefaultConflictHandler trait impl: is_equal via deep_equal, resolve returns real_master_state |
| `src/replication-protocol/conflicts.ts` | 2179 | T2 | `src/replication_protocol/conflicts.rs` | done | main | resolve_conflict_error; depends on RxStorageInstanceReplicationState + RxConflictHandlerInput (added to types/replication.rs) |
| `src/replication-protocol/helper.ts` | 2699 | T2 | `src/replication_protocol/helper.rs` | done | main | wave-112: shared remote revision-height marker helper added for upstream/downstream (`_rev` height equals `_meta[identifier]`), including integer/float JSON marker coverage. wave-042: doc_state_to_write_doc + write_doc_to_doc_state complete; strip_attachments_data_from_meta_write_rows now strips inline attachment payloads from meta docData only when state.has_attachments; get_underlying_persistent_storage walks RxStorageInstance::underlying_persistent_storage chain. 4 unit tests; crate test count 140 |
| `src/plugins/leader-election/index.ts` | 2937 | T2 | `src/plugins/leader_election/mod.rs` | done | main | single-process stub: is_leader always true, wait_for_leadership resolves immediately |
| `src/replication-protocol/checkpoint.ts` | 5506 | T2 | `src/replication_protocol/checkpoint.rs` | done | main | get_last_checkpoint_doc + set_checkpoint (with 409 conflict retry loop) + get_checkpoint_key (async hash); serialized via state.checkpoint_queue tokio::sync::Mutex |
| `src/plugins/replication/replication-helper.ts` | 3606 | T1 | `src/plugins/replication/replication_helper.rs` | done | main | wave-054: full portable helper surface complete — default_modifier, swap_default_deleted_to_deleted_field, handle_pulled_documents(collection, deleted_field, docs), schema-based handle_pulled_documents_with_schema, await_retry, and prevent_hibernate_browser_tab. Browser-only online-event short-circuit and synthetic mousemove hibernation workaround intentionally collapse to sleep/no-op for CTOX server-side runtime. 2 helper tests cover deleted-field swap + composite primary fill. |
| `src/replication-protocol/meta-instance.ts` | 5669 | T1 | `src/replication_protocol/meta_instance.rs` | done | main | META_INSTANCE_SCHEMA_TITLE const + get_rx_replication_meta_instance_schema (Composite PrimaryKey id from itemId+isCheckpoint) + get_assumed_master_state + get_meta_write_row |
| `src/replication-protocol/index.ts` | 12372 | T1 | `src/replication_protocol/index_mod.rs` | done | main | wave-141: stale deferred protocol-gap note removed now that upstream/downstream rows are complete. wave-133: module documentation corrected from the old skeleton-port note to the current functional protocol state: replicate_rx_storage_instance starts upstream/downstream tasks, first-sync/checkpoint queue helpers are live, and rx_storage_instance_to_replication_handler uses storage-native changed-documents plus conflict-aware master writes. Existing: All 6 exports ported: await_first_in_sync (tokio::select! over BehaviorSubjects), await_in_sync (lock all queues), await_idle (double-drain), cancel, replicate_rx_storage_instance, rx_storage_instance_to_replication_handler |
| `src/replication-protocol/downstream.ts` | 21692 | T1 | `src/replication_protocol/downstream.rs` | done | main | wave-141: module docs updated from wip/substantial-port wording to functional CTOX-core wording. wave-138: downstream protocol row marked complete for CTOX core; RxDB's JS `addNewTask`/`openTasks`/`nonPersistedFromMaster` Promise mechanics are represented by the Rust stream buffer drain, `stream_queue.down` mutex serialization, and primary-key dedupe inside `persist_from_master`. wave-137: ongoing downstream drains immediately buffered master stream document batches and persists them in one `persist_from_master` call with stacked checkpoints; regression holds downstream behind `events.active.up`, queues two master events, then proves one persist writes both fork docs and advances to the newest checkpoint. Existing: initial checkpoint write, paginated master_changes_since, typed `RxReplicationMasterChange` with `"RESYNC"` handling, resync cutoffs, upstream-active wait, conflict-aware insert / already-equal skip / fast-forward / local-conflict skip paths, remote revision-height marker handling, resolved-conflict handoff via `stream_queue.up`, attachment-stripped meta writes, processed-down events, checkpoint writes, and cancel handling. Crate test count 158 incl. 29 conformance tests |
| `src/replication-protocol/upstream.ts` | 21852 | T1 | `src/replication_protocol/upstream.rs` | done | main | wave-141: module docs updated from wip/substantial-port wording to functional CTOX-core wording. wave-138: upstream protocol row marked complete for CTOX core; RxDB's JS `openTasks` Promise-chain batching is represented by Rust stream buffering plus `stream_queue.up` mutex serialization, with recursive late-arrival behavior covered by draining queued stream items after each awaited persist. Existing: initial checkpoint write, storage-native get_changed_documents_since initial sync, conflict retry/resolution loop, assumed-master tracking, skip-if-already-replicated including remote revision-height markers, downstream-origin loop-avoidance checkpointing, wait_before_persist placement, strict push_batch_size master-write chunking, buffered ongoing change-event batching, `RxReplicationMasterChange::Resync` listener, attachment-stripped meta writes, processed-up/conflict events, checkpoint writes, and cancel handling. Crate test count 158 incl. 29 conformance tests |
| `src/plugins/replication/index.ts` | 25090 | T1 | `src/plugins/replication/index_mod.rs` | done | main | wave-141: module docs updated from wip/functional wording and stale protocol-openTasks note; remote RESYNC/event injection is part of the completed public replication API. wave-135: public Rust counterparts `re_sync()` and `emit_event()` now feed an internal remote_events subject that is merged with the optional pull stream_factory, matching upstream remoteEvents$ semantics; regression covers remote RESYNC delivery through ClosureReplicationHandler. Replication index is complete for CTOX core; protocol batching is complete on upstream/downstream rows. wave-134: pull stream_factory and ClosureReplicationHandler now pass through typed `RxReplicationMasterChange::Resync` items while preserving the upstream `"RESYNC"` JSON wire shape; this unlocks protocol-level RESYNC handling for WebRTC/browser streams. wave-132: start() now serializes the full async startup path with a TokioMutex start_lock, matching upstream startQueue semantics so parallel start calls wait until internal_state/meta-storage registration is complete; deterministic regression blocks the first startup hash and proves the second start does not return early. Crate test count 156 incl. 29 conformance tests. wave-129: pull/push handler failures now emit upstream-style RC_PULL/RC_PUSH envelopes on the replication error stream with direction, checkpoint/pushRows, and error_to_plain_json details before retrying; regressions cover retry success plus envelope shape. wave-128: await_in_sync now mirrors upstream by draining database idle work and await_rx_storage_replication_in_sync twice after first-sync; regression proves the second drain catches queue work that arrives while the first drain is waiting. wave-116: pull stream modifier failures now emit on the replication error relay and drop the stream batch instead of emitting an empty document batch with the original checkpoint, preventing accidental checkpoint advancement after transformation errors; regression covers no batch plus error emission. wave-107: paused replication state now suppresses pull stream items plus master_changes_since/master_write handler calls, and start() after pause clears the public/internal paused flags. Regressions cover paused pull, paused push, and resume-unpause state. wave-103: non-live replications now await first sync + in-sync and then auto-cancel, matching upstream one-shot lifecycle; regression covers stopped state and connected meta-storage cleanup after start(). wave-102: replicate_rx_collection now returns Result and rejects configurations with neither pull nor push using UT3, matching upstream's constructor guard; WebRTC call sites now propagate the Result and the meta-storage lifecycle regression uses an explicit empty pull direction. wave-070: rx_storage_instance_to_replication_handler.master_changes_since now delegates to storage-native get_changed_documents_since instead of forcing the generic Mango fallback; conformance covers seeded pull/checkpoint behavior across Memory and SQLite. wave-061: RxReplicationState now exposes pause/is_paused/is_stopped/is_stopped_or_paused/await_initial_replication/await_in_sync and the registration cleanup test covers these status paths. wave-054: ClosureReplicationHandler applies pull/push modifiers, maps custom deleted fields, retries handler errors, transforms pull.stream_factory batches, and wires internal processed/active events to public received/sent/active subjects. wave-053: start registers the replication meta-instance in the internal store and cancel removes it. Existing: ReplicationOptions/PullOptions/PushOptions/PullHandlerResult types, replicate_rx_collection constructor + auto_start, meta-instance creation, replicate_rx_storage_instance. Browser-visibility/preventHibernate remain CTOX server-side no-ops |

## phase-4

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/plugins/replication-webrtc/webrtc-helper.ts` | 1498 | T1 | `src/plugins/replication_webrtc/webrtc_helper.rs` | done | main | is_master_in_webrtc_replication (hash-based deterministic master picking), send_message_and_await_answer (subscribes to response stream, sends frame, awaits matching (peer, request_id)) |
| `src/plugins/replication-webrtc/webrtc-types.ts` | 2977 | T1 | `src/plugins/replication_webrtc/webrtc_types.rs` | done | main | WebRTCMessage, WebRTCResponse, WebRTCWireFrame enum (collapses TS Message\|Response union), PeerWithMessage<P>, PeerWithResponse<P>, WebRTCConnectionHandler async-trait with associated Peer type. SyncOptionsWebRTC/RxWebRTCReplicationState are exported from index_mod.rs; webrtc-types.rs remains the transport type layer |
| `src/plugins/replication-webrtc/signaling-server.ts` | 5704 | T1 | `src/plugins/replication_webrtc/signaling_protocol.rs` | done | main | CTOX is signaling **client**, not server — skipped server-side WebSocketServer/peerById/peersByRoom routing. Protocol wire-types extracted into signaling_protocol.rs (ServerToClient/ClientToServer enums); wave-047 corrected browser-compatible `signal` frame shape with nested `data` + `room` and explicit camelCase fields; N6 SignalingClient consumes them |
| `src/plugins/replication-webrtc/connection-handler-simple-peer.ts` | 10999 | T1 | `N5 WebRTCRsConnectionHandler` | done | main | wave-140: row marked complete as an intentional Rust-native replacement, not a missing file port. `simple-peer` is a JS-native browser adapter; CTOX uses webrtc-rs via N5 (`connection_handler_rs.rs`) while the browser side keeps the upstream JS bundle and still executes simple-peer there. wave-039: original skip decision recorded this architecture split |
| `src/plugins/replication-webrtc/index.ts` | 11079 | T1 | `src/plugins/replication_webrtc/index_mod.rs` | done | main | wave-142: replicate_web_rtc_rs now joins the exact caller-provided topic, matching upstream JS `replicateWebRTC()`/simple-peer signaling room behavior so browser and daemon peers rendezvous in the same room. wave-141: module docs updated from wip wording and stale master-path-only function docs to the current both-path WebRTC replication surface. wave-134: fork-side masterChangeStream$ decoding now accepts both document/checkpoint batches and the browser-compatible `"RESYNC"` sentinel via `RxReplicationMasterChange`, forwarding it into the Rust replication protocol. wave-052: user-facing WebRTC replication API completed — SyncOptionsWebRTC, SyncOptionsWebRTCRs, RxWebRTCReplicationState alias, replicate_web_rtc_with_options, and replicate_web_rtc_rs(signaling_url/topic) that creates SignalingClient + WebRTCRsConnectionHandler. Fork replication identifier now includes topic (`collection||topic||peerToken`) and pull/push batch/retry settings are configurable. wave-035: both master/fork paths wired with token handshake, masterChangesSince/masterWrite tunneling, masterChangeStream$ relay, per-peer RxReplicationState cleanup. Browser/daemon E2E belongs to the CTOX integration layer, not the rxdb-rs file-port matrix. |

## phase-5

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/event-reduce.ts` | 5718 | T2 | `src/event_reduce.rs` | done | main | wave-131: conservative Rust event-reduce now patches unpaginated find result sets for insert/update/remove actions and keeps full-query fallback for paginated/ambiguous cases. wave-098: irrelevant events can be proven no-op without re-query. wave-036: initial fallback always returned run_full_query_again=true (drops event-reduce-js dep) |
| `src/rx-change-event.ts` | 4539 | T1 | `src/rx_change_event.rs` | done | main | get_document_data_of_rx_change_event, rx_change_event_to_event_reduce_change_event (event-reduce-js JSON shape preserved despite N15 stub), flatten_events (dedup), rx_change_event_bulk_to_rx_change_events. EVENT_BULK_CACHE WeakMap omitted (perf-opt). New types: RxChangeEvent, RxChangeEventBulk |
| `src/change-event-buffer.ts` | 5084 | T1 | `src/change_event_buffer.rs` | done | main | T1 redesign: takes RxStream<EventBulk> instead of RxCollection (phase-6); WeakMap counter replaced by oldest_counter+index; RxJS Subscription → tokio JoinHandle.abort(); IDLE-task batching collapsed to sync mutex on event-arrival |

## phase-6

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/rx-query-single-result.ts` | 3580 | T3 | `src/rx_query_single_result.rs` | done | main | wave-085: RxQuerySingleResult now materializes cached RxDocument handles through DocumentCache, adds documents()/docs_map(), and still keeps JSON docs_data()/docs_data_map() accessors for CTOX call sites. wave-039: primary_path/docs_data/count/time + docs_data()/docs_data_map()/doc_count() accessors. 3 unit tests. Deferred: full reactive getValue stream parity lives in rx-query.rs |
| `src/index.ts` | 1039 | T2 | `src/prelude.rs` | done | main | wave-038: prelude module (`use ctox_rxdb::prelude::*`) re-exporting every ported user-facing module. T2 deviation: skips rx-document/rx-query/query-cache/doc-cache pending phase-6 |
| `src/rx-collection-helper.ts` | 6390 | T2 | `src/rx_collection_helper.rs` | done | main | wave-038: fill_object_data_before_insert (defaults + composite primary + _meta/_deleted/_attachments/_rev envelope), create_rx_collection_storage_instance (sets multi_instance + delegates), remove_collection_storages (full impl — fetches meta docs, dedupes by `{name}||{version}`, creates+removes each storage instance, soft-deletes meta docs via flat_clone_doc_with_meta+create_revision, fires postRemoveRxCollection hook), ensure_rx_collection_is_not_closed (COL21). 2 unit tests. T1: takes explicit storage/internal-store/token/name args since RxDatabase is stub |
| `src/rx-query-helper.ts` | 10036 | T2 | `src/rx_query_helper.rs` | done | main | wave-055: run_query_update_function ported for the current Rust query result shape (`serde_json::Value`): findOne preserves single/null, find and findByIds map results return updated arrays like upstream Promise.all. Existing: normalize_mango_query, get_sort_comparator, get_query_matcher, prepare_query; mingoSortComparator inlined as value_compare. 1 query-update regression test; crate test count 85. Future refinement: narrow update_fn to RxDocument handles after RxQuery materializes document instances in every result mode. |
| `src/rx-document-prototype-merge.ts` | 3779 | T1 | `src/rx_document_prototype_merge.rs` | done | main | T1 deviation: skipped. JS prototype manipulation (schemaProto + ormProto + basePrototype merge via defineProperty) has no Rust analog. ORM methods become user-defined impl blocks; createNewRxDocument folds into rx_collection.rs when ported |
| `src/query-cache.ts` | 4369 | T1 | `src/query_cache.rs` | done | main | wave-095: collection-bound cache replacement trigger now schedules the upstream-style policy after query-cache access, marks evicted RxQueryBase handles as uncached, and has a regression for old unexecuted query eviction. wave-071: RxCollection now owns a query cache and create_rx_query tunnels through it, so identical op/query/other triples reuse the same RxQueryBase Arc; regression covers cache hit and distinct miss. wave-039: full standalone cache port — QueryCache (HashMap by string_rep), QueryCacheable trait, get_by_query+uncache_rx_query+count_rx_query_subscribers, default_cache_replacement_policy_monad. |
| `src/rx-database-internal-store.ts` | 12046 | T1 | `src/rx_database_internal_store.rs` | done | main | wave-037: full port — INTERNAL_STORE_SCHEMA (build via fill_with_default_settings), 4 context constants, get_primary_key_of_internal_document, get_all_collection_documents (normalize_mango_query+prepare_query path), ensure_storage_token_document_exists with full conflict-path validation (DM5 major-skew + DB1 password-hash mismatch), is_database_state_version_compatible_with_database_code (v15→v16 carveout), add_connected_storage_to_collection + remove_connected_storage_from_collection with retry-on-conflict loops, _collection_name_primary. 10 unit tests including end-to-end ensure_storage_token_doc against memory storage (insert + conflict-path + DM5 reject). T1 deviation: helpers take explicit `internal_store: &Arc<dyn RxStorageInstance>` argument since full RxDatabase/RxCollection are stubs. Browser-only `sharding` config omitted. RxDatabase stub extended with internal_store/password/rxdb_version. Added types/internal_store.rs (InternalStoreDocType + StorageTokenData + CollectionDocData + ConnectedStorage) |
| `src/rx-document.ts` | 17383 | T1 | `src/rx_document.rs` | done | main | wave-126: Rust-native $$ parity added via double_dollar, deleted_double_dollar, and get_double_dollar(path), each backed by RxBehaviorSubject/reactive_from_stream with duplicate initial stream emissions skipped; regressions cover document, field, and deleted signal updates. wave-101: deleted_stream now derives from document_stream like upstream deleted$, so it shares revision-level deduplication, latest-cache start behavior, and the same document-handle mapping path as `$`. wave-100: document_stream now suppresses duplicate emissions by revision and get_stream(path) suppresses unchanged field values, matching upstream distinctUntilChanged behavior; save_data now refreshes the same Arc handle from persisted storage-normalized data after writes so chained patch/save calls keep current `_rev`/`_meta.lwt`. Regression covers unchanged-field suppression and same-handle chained patch. wave-086: document_stream() and get_stream(path) added as Rust-native counterparts for RxDocument `$` and `get$`, starting with the current cached document/value and tracking later storage change events; regression covers field stream update delivery. wave-084: save_data and IncrementalWriteQueue-backed incremental_modify/incremental_patch now run collection pre/post save hooks around persisted document writes; regressions cover direct save pre-hook mutation, queue pre-hook mutation, and post-hook delivery. wave-075: Rust-native document change_events() and deleted_stream() added; deleted_stream starts with current deletion state and tracks later DELETE events for the same document. wave-066: populate array refs are now covered by a database-level regression that preserves input id order. wave-063: dev-mode `get$` path validation now throws DOC4 for schema-unknown paths via a typed schema path helper; covered by a collection-backed document test. wave-062: populate(path) now resolves simple `ref` and array `ref` fields through the database collection registry using find_one/find_by_ids, preserving Rust's JSON-returning interim API and DOC5/DOC6/DOC7 error paths. wave-058: save_data/incremental_modify now replace the current handle's document data and clear property_cache after successful writes, so get()/deleted()/to_json() observe the persisted state on the same Arc handle. Existing: RxDocument struct + constructor/create_with_constructor/is_rx_document, primary/revision/deleted/get/to_json/to_mutable_json/plugin-missing methods, modify/patch/save/remove paths, before_document_update_write, collection save hooks, get_document_property. Crate test count 146 incl. 29 conformance tests. |
| `src/rx-query.ts` | 25120 | T1 | `src/rx_query.rs` | done | main | wave-131: unpaginated find queries now use Rust-native event-reduce patching for matching insert/update/remove events, updating cached docs without storage re-execution and preserving Mango sort order; paginated/ambiguous cases conservatively re-query. Module docs updated from first-pass to complete CTOX-core port. wave-126: Rust-native query $$ added as double_dollar(), returning an RxBehaviorSubject<Option<RxQueryExecResult>> that starts as upstream's undefined-equivalent and follows result_stream(); regression covers initial and changed query results. wave-099: count queries now reject storage count results with mode=slow unless the database was created with allow_slow_count, matching upstream QU14 behavior; event_reduce=false disables the find/findOne EventReduce shortcut. wave-098: ensure_equal now uses the conservative event-reduce path for find/findOne, skipping storage re-execution when all missed change events are irrelevant to the query; regression covers a findOne-by-id query ignoring an unrelated insert. wave-095: RxQueryBase now carries an atomic uncached flag used by collection query-cache replacement; regression covers evicting stale unexecuted cached queries. wave-089: ensure_equal now awaits collection await_before_reads callbacks before every read, matching upstream awaitBeforeReads placement before the in-sync shortcut; regression covers callback execution without extra storage re-query. wave-087: result_stream() added as Rust-native counterpart to RxQuery `$`, emitting an initial document-handle result and then changed results after collection storage events; regression covers initial + changed find results. wave-085: exec_rx_documents exposes RxDB-like document-handle query results (find -> Vec<Arc<RxDocument>>, findOne -> Option<Arc<RxDocument>>, findByIds -> HashMap) while preserving existing JSON exec(); regression proves handles come from the DocumentCache and are reused. wave-083: plugin-backed query methods now exist as Rust stubs: update plus query-builder where_/sort/skip/limit return PLUGIN_MISSING until the corresponding plugin is ported; regression covers all stubs. wave-082: incremental_remove added and wired to the persistent query remove path; regression covers findOne tombstone persistence and subsequent null query result. wave-077: count queries now apply the upstream ChangeEventBuffer incremental count path and primary-key find/findOne queries use find_documents_by_id instead of a full Mango storage query; regressions cover no storage re-count, shortcut call routing, deleted filtering, and requested id order. wave-076: `ensure_equal` tracks the ChangeEventBuffer counter and skips storage re-execution while the cached result is already in sync; collection-owned query mutations invalidate cached queries so immediate follow-up reads do not race the async change stream. wave-071: create_rx_query now tunnels through the collection query cache and reuses identical query instances. wave-068: query-level remove/patch/incremental_patch/modify/incremental_modify now persist through collection bulk_remove_by_ids/bulk_upsert while preserving find/findOne/findByIds result shapes; regression covers patch persistence and query remove tombstones. wave-045: first functional non-reactive RxQueryBase port added with RxQueryOp, get_default_query, create_rx_query hook bridge, is_find_one_by_id_query, to_string_key, query_matcher/does_document_data_match, get_prepared_query with `_deleted=false` + prePrepareQuery, query_collection, exec_over_database, ensure_equal, exec projection for find/findOne/findByIds/count. 21 Query tests; crate test count 151 incl. 29 conformance tests. |
| `src/rx-database.ts` | 27854 | T1 | `src/rx_database.rs` | done | main | wave-130: database port marked complete for CTOX core; module docs now describe the implemented Rust lifecycle and keep json-dump/state/backup/leader-election/migration enumeration as explicit plugin-missing boundaries instead of database gaps. wave-099: RxDatabaseCreator/RxDatabase now carry upstream event_reduce and allow_slow_count options; RxDatabase::new_with_query_options added for Rust tests/manual construction. wave-097: remove_collection_doc(name, schema) soft-deletes the internal collection metadata document with cloned meta + fresh revision; regression covers successful metadata tombstone and SNH on a second removal. wave-096: create_rx_database now supports ignore_duplicate and close_duplicates via a Weak open-instance registry; close removes only the matching instance, and the duplicate/reopen regression now covers ignore and forced-close behavior. wave-094: startup_errors queue and ensure_no_startup_errors() added; regression covers returning the first queued startup error. wave-093: plugin-backed database methods export_json/import_json/add_state/backup/leader_elector/migration_states now exist and return PLUGIN_MISSING; regression covers all stubs. wave-092: db_count() global lifecycle counter and is_first_time_instantiated() storage-token check added; create database regression covers first-instantiation detection and count increment. wave-091: collection removal regression now covers RxCollection on_remove callback delivery along with database registry and internal-store cleanup. wave-074: event_bulks() now merges current collection EventBulk streams into RxChangeEventBulk values with collection_name; regression covers database-level event emission after insert. wave-057: open database name registry added; create_rx_database rejects duplicate open names with DB8 and close releases the name. Added request_idle_promise/request_idle_promise_no_queue server-side bridges and duplicate/reopen regression test. wave-050: collection-level removal participates in DB registry cleanup via RxCollection::remove. wave-049: remove_rx_database opens internal store, discovers collection docs, removes known collection storages via remove_collection_storages, removes internal store, returns names. Existing: RxDatabaseCreator/RxCollectionCreator, create_rx_database internal store + storage-token doc, add_collections metadata + wrapped collection storage + registry, collection()/collection_names(), close(). 9 DB lifecycle/event tests; crate test count 149 incl. 29 conformance tests. |
| `src/rx-collection.ts` | 38000 | T1 | `src/rx_collection.rs` | done | main | wave-127: collection port marked complete for CTOX core; module docs now reflect the Rust-native stream surface instead of the old partial-stub status, and migration-schema remains an explicit plugin-missing boundary rather than a collection gap. wave-095: query cache replacement trigger added with per-collection running guard, tokio-yield scheduling, synchronous fallback outside a runtime, and typed RxQueryBase eviction. wave-091: on_remove callback registry added and remove() drains callbacks after close, mirroring upstream onRemove lifecycle. wave-090: migrationNeeded/getMigrationState/startMigration/migratePromise surface added and returns PLUGIN_MISSING for the skipped migration-schema plugin; regression covers all four methods. wave-089: await_before_reads registry added with add/remove helpers and async drain API for migration/pipeline read blocking. wave-088: checkpoint_stream() added as Rust-native counterpart to checkpoint$, mapping EventBulk checkpoints from the collection change stream; regression covers checkpoint delivery after insert. wave-085: bulk_upsert update writes now refresh returned cached RxDocument handles with persisted data, matching the remove/save handle-refresh behavior and preserving query mutation projections after RxQuerySingleResult materializes documents. wave-081: bulk_remove_by_ids now runs pre/post remove hooks around tombstone writes, passing the current RxDocument instance and preserving pre-hook mutations in the deleted write; returned document handles are refreshed with persisted tombstone data even when the cache reuses an existing Arc. Regression covers hook mutation, post delivery, and query remove tombstone shape. wave-080: Rust-native collection hook registry added with add_hook/has_hooks/run_hooks and bulk_insert now runs pre/post insert hooks; regression covers pre mutation plus post hook instance delivery. wave-079: find/find_one now run prePrepareRxQuery before create_rx_query and re-parse hook-mutated queryObj into typed MangoQuery; regression covers hook mutation through execution. wave-078: query factories now return RxResult, guard closed collections, and find_one mirrors upstream by rejecting caller-provided limits with QU6 while forcing limit=1; regressions cover prepared limit and closed-collection errors. wave-073: typed change event streams added as Rust APIs change_events/insert_events/update_events/remove_events, flattening EventBulk and filtering by operation; regression covers insert/update/delete emission. wave-072: public event_bulks() exposes the collection storage change stream for Rust callers. wave-069: incremental_upsert(json) now fills schema defaults, serializes same-document runs through a Rust async lock, inserts missing docs and updates existing docs through RxDocument::incremental_modify. wave-062: cleanup(minimum_deleted_time) added as collection-level storage delegation with closed-collection guard. wave-056: `new_with_schema` now creates a ChangeEventBuffer from `storage_instance.change_stream()` and closes it with the collection. wave-050: remove() removes collection storages via internal-store metadata, closes callbacks, and drops the DB registry entry. wave-047: write surface expanded and stabilized — insert, insert_if_not_exists, bulk_insert, bulk_remove_by_ids, bulk_upsert, upsert. Existing: RxSchema, DocumentCache<RxDocument>, IncrementalWriteQueue, query factories find/find_one/find_by_ids/count, collection hook registry used by insert/save/remove paths. 14 collection tests; crate test count 146 incl. 29 conformance tests. |

## phase-7

| Upstream | Bytes | Tier | Rust target | Status | Owner | Notes |
|---|---:|:---:|---|---|---|---|
| `src/plugins/test-utils/revisions.ts` | 288 | T2 | `tests/conformance/revisions.rs` | done | main | wave-117: EXAMPLE_REVISION_1..4 constants ported with upstream golden assertion in the conformance integration binary |
| `src/plugins/test-utils/port-manager.ts` | 749 | T2 | `tests/conformance/port-manager.rs` | done | main | wave-118: next_port() ported with serialized search over 0.0.0.0:18669..65535 and conformance assertion for monotonic bindable ports |
| `src/plugins/test-utils/index.ts` | 777 | T2 | `tests/conformance/index.rs` | done | main | wave-119: Rust barrel re-exports the currently ported test utils (`revisions`, `port_manager`) and asserts the public surface |
| `src/plugins/test-utils/test-util.ts` | 1676 | T2 | `tests/conformance/test-util.rs` | done | main | wave-120: repeat_test and ensure_json_states_equal utilities ported for Rust conformance tests; barrel re-export updated and covered |
| `src/plugins/test-utils/replication.ts` | 3195 | T2 | `tests/conformance/replication.rs` | done | main | wave-122: get_pull_handler/get_pull_stream/get_push_handler wrappers ported over the Rust RxReplicationHandler trait, plus ensure_equal_state/clean_doc_to_compare parity for `_meta`/`_rev`-agnostic replicated-state assertions |
| `src/plugins/test-utils/config.ts` | 3988 | T2 | `tests/conformance/config.rs` | done | main | wave-121: Rust conformance config module ports runtime detection, env/default-storage lookup, init guard, set/get config, encrypted-storage passthrough, storage exclusion helper, and password provider/random fallback semantics; config tests serialize global mutations to match upstream's singleton state |
| `src/plugins/test-utils/schema-objects.ts` | 14880 | T2 | `tests/conformance/schema-objects.rs` | done | main | wave-123: Fixture factories ported as serde_json::Value generators with Rust partial merging; covers TEST_DATA_CHARSET, randomStringWithSpecialChars, human/simple/nested/deep/encrypted/compound/ref/timestamp/average/point/composite/ownership data helpers |
| `src/plugins/test-utils/humans-collection.ts` | 16186 | T2 | `tests/conformance/humans-collection.rs` | done | main | wave-125: Collection factory helpers ported against real RxDatabase/RxCollection: create, create_by_schema, attachments/no-compression/age/nested/deep/multi-instance/primary/timestamp/migration/related/id-age/ownership helpers with seed-count and special-field conformance coverage |
| `src/plugins/test-utils/schemas.ts` | 34776 | T2 | `tests/conformance/schemas.rs` | done | main | wave-124: All exported test schema fixtures ported as serde_json::Value factories, including human variants, nested/encrypted/compound/ref/composite/timestamp/index schemas, randomized average_schema titles, and enable_key_compression clone behavior |

## skip

| Upstream | Bytes | Reason |
|---|---:|---|
| `src/plugins/vector/helper.ts` | 0 | empty/stub upstream |
| `src/plugins/vector/types.ts` | 31 | empty/stub upstream |
| `src/plugins/vector/index.ts` | 71 | empty/stub upstream |
| `src/plugins/replication-websocket/index.ts` | 116 | V1 trim — WebSocket replication transport; we use WebRTC; signaling lives in replication-webrtc/signaling-server |
| `src/plugins/utils/utils-rxdb-version.template.ts` | 117 | MVP trim — build-time template; replaced by a Rust const (see "New code" N10) |
| `src/plugins/electron/index.ts` | 126 | electron-specific |
| `src/plugins/electron/electron-helper.ts` | 129 | electron-specific |
| `src/plugins/storage-dexie/index.ts` | 156 | browser-only (Dexie stays JS-side) |
| `src/plugins/storage-mongodb/index.ts` | 164 | out-of-scope backend |
| `src/plugins/storage-remote/index.ts` | 201 | out-of-scope multi-process scheme |
| `src/plugins/storage-foundationdb/foundationdb-helpers.ts` | 217 | out-of-scope backend |
| `src/plugins/cleanup/cleanup-helper.ts` | 346 | MVP trim — replaced by SQL-level tombstone GC (see "New code") |
| `src/plugins/replication-nats/nats-helper.ts` | 355 | SaaS-specific replication |
| `src/plugins/pipeline/index.ts` | 418 | V1 trim — type-only import from rx-collection; type stub provided (see "New code" N14) |
| `src/plugins/storage-denokv/denokv-types.ts` | 503 | out-of-scope backend |
| `src/plugins/update/mingo-updater.ts` | 611 | MVP trim — mango update DSL; CTOX writes full documents |
| `src/plugins/storage-denokv/denokv-helper.ts` | 645 | out-of-scope backend |
| `src/plugins/pipeline/types.ts` | 651 | V1 trim — type-only import from rx-collection; type stub provided (see "New code" N14) |
| `src/plugins/replication-supabase/types.ts` | 710 | SaaS-specific replication |
| `src/plugins/storage-remote/storage-remote-helpers.ts` | 770 | out-of-scope multi-process scheme |
| `src/plugins/replication-appwrite/appwrite-types.ts` | 795 | SaaS-specific replication |
| `src/plugins/state/types.ts` | 798 | defer |
| `src/plugins/validate-is-my-json-valid/index.ts` | 833 | pick one validator; ajv also deferred |
| `src/plugins/storage-mongodb/mongodb-types.ts` | 918 | out-of-scope backend |
| `src/plugins/replication-couchdb/couchdb-types.ts` | 929 | SaaS-specific replication |
| `src/plugins/storage-localstorage/localstorage-mock.ts` | 934 | browser-only |
| `src/plugins/state/index.ts` | 1011 | defer |
| `src/plugins/query-builder/mquery/mquery-utils.ts` | 1013 | MVP trim — chained query DSL; no user-facing queries on CTOX side |
| `src/plugins/replication-websocket/websocket-types.ts` | 1029 | V1 trim — WebSocket replication transport; we use WebRTC; signaling lives in replication-webrtc/signaling-server |
| `src/plugins/migration-schema/migration-types.ts` | 1042 | V1 trim — type-only import from rx-database/rx-collection; type stub provided (see "New code" N12) |
| `src/plugins/replication-nats/nats-types.ts` | 1068 | SaaS-specific replication |
| `src/plugins/replication-appwrite/appwrite-helpers.ts` | 1108 | SaaS-specific replication |
| `src/plugins/storage-remote-websocket/types.ts` | 1142 | out-of-scope multi-process scheme |
| `src/plugins/vector/vector-distance.ts` | 1178 | empty/stub upstream |
| `src/plugins/utils/utils-premium.ts` | 1238 | MVP trim — license checks; utils/index.ts barrel re-export omitted, constant-false replacement in foundation |
| `src/plugins/storage-denokv/index.ts` | 1269 | out-of-scope backend |
| `src/plugins/utils/utils-base64.ts` | 1277 | MVP trim — via utils-blob for attachments; utils/index.ts barrel re-export omitted |
| `src/plugins/storage-mongodb/rx-storage-mongodb.ts` | 1310 | out-of-scope backend |
| `src/plugins/cleanup/index.ts` | 1442 | MVP trim — replaced by SQL-level tombstone GC (see "New code") |
| `src/plugins/replication-mongodb/mongodb-types.ts` | 1461 | SaaS-specific replication |
| `src/plugins/replication-graphql/helper.ts` | 1474 | GraphQL replication |
| `src/plugins/validate-ajv/index.ts` | 1490 | MVP trim — browser-side validates pre-push |
| `src/plugins/dev-mode/check-migration-strategies.ts` | 1519 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/update/index.ts` | 1519 | MVP trim — mango update DSL; CTOX writes full documents |
| `src/plugins/validate-z-schema/index.ts` | 1539 | pick one validator; ajv also deferred |
| `src/plugins/storage-localstorage/index.ts` | 1544 | browser-only |
| `src/plugins/electron/rx-storage-ipc-main.ts` | 1570 | electron-specific |
| `src/plugins/replication-graphql/graphql-websocket.ts` | 1602 | GraphQL replication |
| `src/plugins/replication-supabase/helper.ts` | 1688 | SaaS-specific replication |
| `src/plugins/dev-mode/check-orm.ts` | 1695 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/electron/rx-storage-ipc-renderer.ts` | 1816 | electron-specific |
| `src/plugins/utils/utils-blob.ts` | 1826 | MVP trim — attachments helper only; utils/index.ts barrel ported without this re-export |
| `src/plugins/storage-foundationdb/index.ts` | 1841 | out-of-scope backend |
| `src/plugins/replication-mongodb/mongodb-helper.ts` | 1921 | SaaS-specific replication |
| `src/plugins/storage-dexie/rx-storage-dexie.ts` | 2111 | browser-only (Dexie stays JS-side) |
| `src/plugins/dev-mode/entity-properties.ts` | 2115 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/migration-schema/index.ts` | 2183 | V1 trim — type-only import from rx-database/rx-collection; type stub provided (see "New code" N12) |
| `src/plugins/state/helpers.ts` | 2263 | defer |
| `src/plugins/storage-foundationdb/foundationdb-types.ts` | 2268 | out-of-scope backend |
| `src/plugins/replication-firestore/firestore-helper.ts` | 2308 | SaaS-specific replication |
| `src/plugins/replication-firestore/firestore-types.ts` | 2329 | SaaS-specific replication |
| `src/plugins/flutter/index.ts` | 2347 | flutter-specific |
| `src/plugins/query-builder/index.ts` | 2468 | MVP trim — chained query DSL; no user-facing queries on CTOX side |
| `src/plugins/replication-couchdb/couchdb-helper.ts` | 2511 | SaaS-specific replication |
| `src/plugins/replication-webrtc/connection-handler-p2pcf.ts` | 2536 | V1 trim — alternative connection handler (p2pcf lib); we use simple-peer port → webrtc-rs |
| `src/plugins/storage-sqlite/index.ts` | 2544 | RxDB Premium trial stub — we build our own SQLite backend |
| `src/plugins/attachments/attachments-utils.ts` | 2702 | parquet stored as external content-addressed file; type stub provided (see "New code" N11) |
| `src/plugins/storage-remote/message-channel-cache.ts` | 2761 | out-of-scope multi-process scheme |
| `src/plugins/local-documents/index.ts` | 2794 | defer |
| `src/plugins/dev-mode/unallowed-properties.ts` | 2833 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/cleanup/cleanup-state.ts` | 3092 | MVP trim — replaced by SQL-level tombstone GC (see "New code") |
| `src/plugins/backup/file-util.ts` | 3133 | V1 trim — type-only import from rx-database; type stub provided (see "New code" N13) |
| `src/plugins/local-documents/local-documents-helper.ts` | 3286 | defer |
| `src/plugins/pipeline/flagged-functions.ts` | 3394 | V1 trim — type-only import from rx-collection; type stub provided (see "New code" N14) |
| `src/plugins/dev-mode/dev-mode-tracking.ts` | 3513 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/storage-mongodb/mongodb-helper.ts` | 3542 | out-of-scope backend |
| `src/plugins/storage-remote/storage-remote-types.ts` | 3557 | out-of-scope multi-process scheme |
| `src/plugins/json-dump/index.ts` | 3701 | MVP trim — export/import not on sync path |
| `src/plugins/storage-remote-websocket/index.ts` | 3902 | out-of-scope multi-process scheme |
| `src/plugins/dev-mode/check-document.ts` | 4146 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/local-documents/local-documents.ts` | 4224 | defer |
| `src/plugins/attachments-compression/index.ts` | 4228 | parquet self-compressed; rxdb attachments not used |
| `src/plugins/storage-sqlite/sqlite-types.ts` | 4282 | RxDB Premium trial stub — we build our own SQLite backend |
| `src/plugins/replication-websocket/websocket-server.ts` | 4510 | V1 trim — WebSocket replication transport; we use WebRTC; signaling lives in replication-webrtc/signaling-server |
| `src/plugins/storage-denokv/denokv-query.ts` | 4523 | out-of-scope backend |
| `src/plugins/cleanup/cleanup.ts` | 4758 | MVP trim — replaced by SQL-level tombstone GC (see "New code") |
| `src/plugins/migration-schema/migration-helpers.ts` | 5033 | V1 trim — type-only import from rx-database/rx-collection; type stub provided (see "New code" N12) |
| `src/plugins/replication-webrtc/connection-handler-webtorrent.ts` | 5229 | V1 trim — alternative connection handler (webtorrent); we use simple-peer port → webrtc-rs |
| `src/plugins/storage-foundationdb/foundationdb-query.ts` | 5780 | out-of-scope backend |
| `src/plugins/replication-mongodb/mongodb-checkpoint.ts` | 6360 | SaaS-specific replication |
| `src/plugins/dev-mode/index.ts` | 6466 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/replication-websocket/websocket-client.ts` | 6524 | V1 trim — WebSocket replication transport; we use WebRTC; signaling lives in replication-webrtc/signaling-server |
| `src/plugins/replication-graphql/query-builder-from-rx-schema.ts` | 6745 | GraphQL replication |
| `src/plugins/dev-mode/check-query.ts` | 6778 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/storage-dexie/dexie-query.ts` | 7391 | browser-only (Dexie stays JS-side) |
| `src/plugins/storage-sqlite/sqlite-helpers.ts` | 7687 | RxDB Premium trial stub — we build our own SQLite backend |
| `src/plugins/encryption-crypto-js/index.ts` | 7747 | defer |
| `src/plugins/migration-storage/index.ts` | 8075 | MVP trim — defer |
| `src/plugins/key-compression/index.ts` | 8105 | defer |
| `src/plugins/replication-graphql/index.ts` | 8196 | GraphQL replication |
| `src/plugins/attachments/index.ts` | 8451 | parquet stored as external content-addressed file; type stub provided (see "New code" N11) |
| `src/plugins/storage-remote/rx-storage-remote.ts` | 9452 | out-of-scope multi-process scheme |
| `src/plugins/backup/index.ts` | 9496 | V1 trim — type-only import from rx-database; type stub provided (see "New code" N13) |
| `src/plugins/storage-dexie/dexie-helper.ts` | 9564 | browser-only (Dexie stays JS-side) |
| `src/plugins/pipeline/rx-pipeline.ts` | 9997 | V1 trim — type-only import from rx-collection; type stub provided (see "New code" N14) |
| `src/plugins/storage-remote/remote.ts` | 10687 | out-of-scope multi-process scheme |
| `src/plugins/replication-supabase/index.ts` | 10735 | SaaS-specific replication |
| `src/plugins/replication-appwrite/index.ts` | 11090 | SaaS-specific replication |
| `src/plugins/replication-nats/index.ts` | 11186 | SaaS-specific replication |
| `src/plugins/replication-graphql/graphql-schema-from-rx-schema.ts` | 11281 | GraphQL replication |
| `src/plugins/replication-mongodb/index.ts` | 12060 | SaaS-specific replication |
| `src/plugins/state/rx-state.ts` | 12136 | defer |
| `src/plugins/local-documents/rx-local-document.ts` | 12773 | defer |
| `src/plugins/storage-dexie/rx-storage-instance-dexie.ts` | 12953 | browser-only (Dexie stays JS-side) |
| `src/plugins/replication-couchdb/index.ts` | 13622 | SaaS-specific replication |
| `src/plugins/storage-sqlite/sqlite-storage-instance.ts` | 15647 | RxDB Premium trial stub — we build our own SQLite backend |
| `src/plugins/replication-firestore/index.ts` | 15661 | SaaS-specific replication |
| `src/plugins/storage-denokv/rx-storage-instance-denokv.ts` | 15821 | out-of-scope backend |
| `src/plugins/storage-foundationdb/rx-storage-instance-foundationdb.ts` | 16429 | out-of-scope backend |
| `src/plugins/query-builder/mquery/nosql-query-builder.ts` | 16890 | MVP trim — chained query DSL; no user-facing queries on CTOX side |
| `src/plugins/crdt/index.ts` | 17401 | advanced — defer |
| `src/plugins/storage-mongodb/rx-storage-instance-mongodb.ts` | 17551 | out-of-scope backend |
| `src/plugins/dev-mode/check-schema.ts` | 18662 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/dev-mode/error-messages.ts` | 20088 | MVP trim — runtime debug checks, not needed in production |
| `src/plugins/storage-localstorage/rx-storage-instance-localstorage.ts` | 21015 | browser-only |
| `src/plugins/migration-schema/rx-migration-state.ts` | 21720 | V1 trim — type-only import from rx-database/rx-collection; type stub provided (see "New code" N12) |
| `src/plugins/storage-sqlite/sqlite-basics-helpers.ts` | 21819 | RxDB Premium trial stub — we build our own SQLite backend |

---

## Update protocol for subagents

1. Subagents may only claim rows with `Tier = T2` or `Tier = T3`. T1 rows are reserved for the main agent.
2. Before claiming a row, verify `Status` is `pending`.
3. Atomic claim: change row's `Status` → `claimed` and `Owner` → `<agent-id>` in a single `Edit` operation, then commit.
4. On completion: `Status` → `done`. Add the Git SHA of the porting commit in `Notes` if helpful.
5. If a dependency is missing, set `Status` → `pending` again, leave a note, and stop. Do not invent missing modules.
6. Never edit `Upstream`, `Bytes`, `Tier`, or `Rust target` columns — those are derived from the pinned upstream commit and the agreed taxonomy.

## Revision protocol (main agent only)

- This `PORTING.md` is the single live master. Subagents edit it in place during a wave.
- At every wave boundary or scope revision, the main agent snapshots into `revisions/PORTING.wave-NNN-<slug>.md` (immutable).
- A wave = one parallel subagent batch (or one sequential foundation step) that ends with `cargo build` green. After commit, snapshot before kicking off the next wave.
- Snapshot naming: `wave-000-baseline.md`, `wave-001-scope-trimmed.md`, `wave-002-v1-applied.md`, `wave-003-phase0-foundation-t1.md`, …
