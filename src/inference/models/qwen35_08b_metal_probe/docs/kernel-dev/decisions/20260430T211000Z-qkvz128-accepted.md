# Kernel Decision Record - QKV/Z MMA128 Accepted

## Decision

```text
date: 2026-04-30 21:10 CEST
experiment: docs/kernel-dev/experiments/20260430T170513Z-prefill-gap-forensics.md
decision: accepted
accepted_env: CTOX_QWEN35_DELTA_PROJECT_QKVZ_MMA128=1
rejected_env: n/a
```

## Summary

```text
one_sentence:
  Promote DeltaNet QKV/Z MMA128 because it is hidden-dump bitexact against the
  accepted QKV/Z64 path and improves p4096/p16384 integrated Delta18+FFN time.
```

## Learning Capture

```text
hypothesis:
  Larger QKV/Z token grouping can reduce repeated weight streaming enough to
  beat the added register pressure for the integrated DeltaNet+FFN path.

actual_result:
  QKV/Z MMA128 is bitexact at p4096 and faster at 512, 4096, and 16384 tokens.

failure_mode:
  n/a

root_cause:
  inferred - repeated QKV/Z weight streaming was still large enough that the
  broader tile wins in the integrated stack.

do_not_repeat:
  Do not reject larger tiles from isolated register-pressure intuition; require
  integrated p95 and hidden-dump evidence.

retry_only_if:
  n/a
```

## Evidence

```text
model: Qwen3.5-0.8B text-only DeltaNet+FFN stack
metalpack: /tmp/ctox_qwen35_08b_real_fp16.metalpack
tokens/context: 512, 4096, 16384 prefill token sweeps
iterations: 2-3
warmup: 1
baseline_command: tools/run_accepted_profile.sh target/release/bench_metalpack_prefill_delta3_ffn_superblock /tmp/ctox_qwen35_08b_real_fp16.metalpack 0 4096 3 1 18
candidate_command: CTOX_QWEN35_DELTA_PROJECT_QKVZ_MMA128=1 tools/run_accepted_profile.sh target/release/bench_metalpack_prefill_delta3_ffn_superblock /tmp/ctox_qwen35_08b_real_fp16.metalpack 0 4096 3 1 18
forensics_command: target/release/memory_forensics /tmp/ctox_qwen35_08b_real_fp16.metalpack 4096 1 81.6
forensics_record: /tmp/ctox_qwen35_memory_forensics_qkvz128.txt
search_based: yes
autotune_record: /tmp/ctox_qwen35_autotune_scan_extended_4096.txt
```

## Performance

```text
baseline_median_s: 1.363063958
baseline_p95_s: 1.363883125
baseline_tok_s: 3004.99

candidate_median_s: 1.325504959
candidate_p95_s: 1.326096916
candidate_tok_s: 3090.90

median_delta_percent: -2.76
p95_delta_percent: -2.77
```

Token sweep:

```text
512:
  baseline_median_s: 0.175855875
  candidate_median_s: 0.174649959

4096:
  baseline_median_s: 1.363780708
  candidate_median_s: 1.326008083

16384:
  baseline_median_s: 5.484693833
  candidate_median_s: 5.283299125
```

## Correctness

```text
checksum: unchanged -0.910950
hidden_mean_abs_error: 0.000000000
hidden_rms_error: 0.000000000
hidden_max_abs_error: 0.000000000
checksum_delta: 0.000000000
greedy_tokens: n/a
logits_check: n/a
```

## Memory / Cache Interpretation

```text
modeled_bytes_baseline: 51.57 GiB delta18+ffn before QKV/Z128 model update
modeled_bytes_candidate: 42.58 GiB delta18+ffn after QKV/Z128 model update
weight_stream_delta: qkvz groups drop from 64@64 to 32@128 at p4096
scratch_delta: none expected
tail_underfill: 0.0% at tested token counts
dram_equivalent_bytes: 100.82 GiB inferred at p4096 after promotion
cache_miss_claim:
  inferred-only
```

## Why This Decision Is Safe

```text
correctness_gate:
  p4096 final hidden dump mismatch_count=0, max_abs_error=0, checksum_delta=0

integrated_path_gate:
  full Delta18+FFN stack improves at p4096/p3 and at p512/p4096/p16384 sweep

token_sweep_gate:
  passed 512, 4096, 16384

reference_comparison:
  not a model-output reference change; this is a bitexact kernel tile change
```

## Follow-Up

```text
next_experiment:
  scan+norm recurrence math and project/register-pressure forensics

cleanup:
  keep QKV/Z64 as autotune fallback candidate

docs_to_update:
  RESEARCH_LOG.md, KERNEL_DEV_HANDBOOK.md, accepted_profile.env

handbook_update_required: yes
```
