// Compatibility marker for installed CTOX release-root detection.
//
// The production binary is declared in Cargo.toml as src/core/main.rs. Older
// installed wrappers still validate a CTOX root by checking for src/main.rs, so
// this file intentionally remains present in release archives.
